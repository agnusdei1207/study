---
sidebar:
  order: 37
  label: "037. 유사도 측정: 코사인•자카드•유클리드 (Similarity Measures)"
  badge:
    text: "기출 · 50%"
    variant: note
title: "유사도 측정: 코사인•자카드•유클리드 (Similarity Measures)"
date: "2026-09-07T14:00:00+09:00"
tags:
  - "notes-basic-theory"
weight: 37
extra:
  question_no: "037"
  source_status: "기출"
  source_history: "128회"
  priority: 50
  priority_note: "거리•유사도 척도 선택의 높은 재사용성"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **순위 역전(Rank Inversion)**: 데이터 특성과 맞지 않는 거리 척도 적용 시 실제 가장 유사한 객체가 하위 순위로 밀려나는 검색 왜곡 현상.

</details>

- 정의/개념: 다차원 공간 내 데이터 객체 간의 **사잇각 방향**, **집합 중첩**, **직선거리**를 수식화하여 근접도를 정량화하는 계산 척도
- 배경/필요성: 데이터의 표현 특성을 고려하지 않고 단일 거리 척도를 일률 적용할 경우 발생하는 **최근접 이웃 순위 왜곡 및 의미적 불일치 한계**

#### 한줄 요약

- 세 척도는 각각 크기·빈도·절대 좌표 가운데 하나를 일부러 버리는 선택이므로, 무엇을 무시해도 되는지가 척도 선택의 기준이 된다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **L2 정규화(L2 Normalization)**: 벡터의 크기(Norm)를 1로 맞추어($\|A\|=1$) 유클리드 거리의 제곱과 코사인 유사도 간의 수학적 등가성($d^2 = 2 - 2\cos\theta$)을 형성하는 전처리.
- **방향적 일치도(Directional Alignment)**: 벡터의 절대 크기(길이)와 무관하게 공간 상에서 가리키는 사잇각의 유사성 정도.
- **희소 이진 데이터(Sparse Binary Data)**: 대부분의 원소가 0이고 특정 속성의 존재 유무(1)만 표시되는 고차원 데이터.

</details>

- 코사인 유사도는 크기 편향을 배제한 **방향적 일치도** 평가
- 자카드 유사도는 **희소 이진 데이터** 공통 원소 비율에 특화
- 유클리드 거리는 **물리적 절대 크기와 좌표 차이**를 직접 반영

#### 한줄 요약

- 척도별 기하학적 정의에 따라 근접 순위가 달라지므로 데이터의 특성에 부합하는 척도를 선택

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **Top-K 후보 선정기(Top-K Candidate Selector)**: 유사도 점수를 내림차순(또는 거리 오름차순) 정렬하여 최상위 $K$개 항목을 실시간 추출하는 인덱싱 엔진.

</details>

```text
[유사도 측정 및 검색 아키텍처]
  │
  ├─ [임베딩 및 전처리부] (표현 변환 및 정규화)
  │     ├─ 특징 변환기 (Dense 임베딩 / 희소 이진 집합 변환)
  │     └─ L2 정규화기 (크기 편향 제거 ‖A‖ = 1 정규화)
  │
  ├─ [척도 연산 엔진] (유사도/거리 계산)
  │     ├─ 방향 일치도 연산 (코사인 유사도 cos θ 계산)
  │     ├─ 집합 중첩도 연산 (자카드 계수 |A∩B|/|A∪B| 계산)
  │     └─ 공간 거리 연산 (유클리드 직선 거리 ‖A-B‖ 계산)
  │
  └─ [랭킹 및 필터링부] (최근접 이웃 추출)
        ├─ 임계치 필터링 (최소 유사도 조건 만족 검증)
        └─ Top-K 선정기 (최상위 K개 결과 실시간 정렬 반환)
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 특징 변환기 | 원시 데이터를 밀집 임베딩(Dense), 희소 벡터, 집합 형태로 변환 |
| 정규화기 | 척도 계산 전 **L2 단위 벡터 정규화 및 표준화** 수행 |
| 척도 연산 엔진 | 도메인에 부합하는 **코사인·자카드·유클리드 점수 연산** |
| Top-K 선정기 | 산출된 점수를 기준으로 **최적 유사 후보군 필터링 및 랭킹** |

#### 한줄 요약

- 변환기가 질의와 후보를 동일 표현으로 맞추고, 척도 함수가 점수를 매겨 상위 후보를 추출

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **벡터 검색(Vector Search, ANN)**: 수백만 개 고차원 벡터 중에서 코사인/유클리드 거리 기준으로 가장 가까운 이웃을 HNSW, ScaNN 등의 알고리즘으로 초고속 탐색하는 기법.

</details>

```text
[유사도 측정·검색 진행 흐름] (진행 ①→⑤, 데이터 표현 속성으로 갈림, Top-K 결과 리스트 반환)
  │
  ├─ [데이터 표현 속성 판별] (① 고차원 Dense 임베딩 / 이진 집합 / 연속 좌표 식별)
  │
  ├─ [방향 일치도 연산] (② Dense 임베딩 → 코사인 cos(θ) = A·B / (‖A‖·‖B‖))
  │
  ├─ [집합 중첩도 연산] (③ 클릭·구매 이진 집합 → 자카드 J(A, B) = |A∩B| / |A∪B|)
  │
  ├─ [공간 거리 연산] (④ 물리 센서 좌표·연속 수치 → 유클리드 직선거리 d(A, B) = √Σ(a_i − b_i)²)
  │
  └─ [Top-K 선정기] (⑤ 점수 내림차순(거리는 오름차순) 정렬, 벡터 검색(ANN)은 HNSW·ScaNN 인덱스 탐색)
```

분기 결과: 데이터가 고차원 의미 임베딩이면 코사인 유사도로 분기하고, 집합형 카테고리 데이터이면 자카드 유사도로 분기하며, 물리적 공간 연속 좌표이면 유클리드 거리로 분기하여 계산한다.

#### 한줄 요약

- 데이터 속성에 따라 코사인, 자카드, 유클리드 척도로 분기 연산하고 Top-K 순위로 정렬

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **코사인 유사도(Cosine Similarity)**: 벡터 간 사잇각의 코사인 값을 이용하여 방향의 일치도를 측정하는 척도.
- **자카드 유사도(Jaccard Similarity)**: 두 집합의 교집합 크기를 합집합 크기로 나누어 중복 비율을 측정하는 척도.
- **유클리드 거리(Euclidean Distance)**: 다차원 공간에서 두 점 사이의 최단 직선거리를 측정하는 척도.

</details>

| 유사도 척도 | 코사인 유사도 (Cosine) | 자카드 유사도 (Jaccard) | 유클리드 거리 (Euclidean) |
|:---|:---|:---|:---|
| 적용 기준 | 텍스트 임베딩 검색 및 추천 | 사용자 구매 이력 및 태그 집합 | 물리적 좌표 및 연속형 수치 거리 |
| 핵심 특징 | **벡터 크기 무관 사잇각(방향)** 기반 | **집합 간 교집합/합집합 비율** 기반 | **다차원 공간의 최단 직선거리** 기반 |
| 한계 | 영벡터 시 분모 0($\text{NaN}$) 오류 | 단어 출현 빈도수 정보 소실 | **차원 증가 시 거리 집중 및 스케일 민감** |

#### 한줄 요약

- 임베딩 방향은 코사인, 집합 겹침은 자카드, 좌표 공간의 직선거리는 유클리드를 적용

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **영벡터 예외(Zero Vector Exception)**: 모든 원소가 0인 벡터($\|A\|=0$) 인입 시 코사인 유사도 분모가 0이 되어 `NaN`이 발생하는 결함.
- **계층형 탐색 그래프(HNSW, Hierarchical Navigable Small World)**: 고차원 벡터 공간에서 다계층 그래프 구조를 통해 초고속 근사 최근접 이웃(ANN)을 탐색하는 알고리즘.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 영벡터 인입 시 코사인 유사도 분모 0($\text{NaN}$) 연산 오류 발생 | 최소 엡실론($\epsilon=10^{-9}$) 가산 또는 사전 제로 벡터 검증 | 연산 크래시 방지 및 기본값 반환 |
| 유클리드 거리 측정 시 **단위가 큰 특정 변수의 거리 지배** | 사전 `StandardScaler` 또는 `MinMaxScaler` 정규화 | 피처별 공정한 거리 가중치 반영 |
| 자카드 유사도에서 **단어 출현 빈도수 정보 손실** | **가중 자카드(Weighted Jaccard)** 척도 적용 | 빈도 가중치가 반영된 정밀 유사도 산출 |
| 고차원 벡터 간의 **브루트포스 거리 연산 병목** | **HNSW / IVFPQ 기반 근사 최근접 탐색(ANN)** 구축 | 검색 지연시간 $O(N) \to O(\log N)$ 단축 |

#### 한줄 요약

- 영벡터 입력은 분모 0 방어로 처리하고, 스케일 차이는 표준화로 거리 왜곡을 제거하며, 빈도 정보는 가중 자카드로 보존하고, 대규모 검색은 HNSW로 가속한다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **검색 증강 생성(Retrieval-Augmented Generation, RAG)**: 질의와 의미적으로 유사한 지식 문서 청크를 고차원 벡터 공간에서 검색하여 거대 언어 모델의 프롬프트 문맥으로 주입하는 차세대 생성 AI 아키텍처.
- **프로덕트 양자화(Product Quantization, PQ)**: 고차원 벡터를 여러 저차원 부분 공간으로 분할하고 각 부분 공간의 중심점 인덱스로 치환하여 메모리 점유를 대폭 압축하는 벡터 인덱싱 기법.
- **재순위화 모델(Cross-Encoder Re-ranker)**: 1차 벡터 유사도 검색으로 선별된 후보 문서들을 질의와 직접 결합 입력하여 세밀한 토큰 간 상호작용 어텐션으로 최종 검색 순위를 재산출하는 심층 모델.

</details>

- 전통적 전자상거래 추천을 넘어, 현대 검색 증강 생성(RAG) 파이프라인, 파운데이션 모델의 외부 지식 주입을 위한 벡터 데이터베이스 및 프로덕트 양자화(PQ) 기반 초저지연 시맨틱 검색 엔진으로 확장 진화 추세
- 실무에서는 단위 벡터(L2 Norm=1) 전처리를 통해 코사인 유사도를 하드웨어 친화적 내적(Dot Product) 연산으로 극대화해야 하며, 고속 1차 벡터 검색과 고정밀 재순위화(Re-ranker) 모델을 계층화하는 아키텍처적 절충 필요

#### 한줄 요약

- RAG 및 벡터 검색 엔진의 핵심인 유사도 척도 원리를 확립하고, L2 정규화 내적 가속과 리랭커 계층화를 통해 검색 정밀도와 지연시간을 최적화하는 아키텍처 결단이 필수적이다.
