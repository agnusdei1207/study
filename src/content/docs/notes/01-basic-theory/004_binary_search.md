---
sidebar:
  order: 4
  label: "004. 이진 탐색 (Binary Search)"
  badge:
    text: "미출 · 30%"
    variant: note
title: "이진 탐색 (Binary Search)"
date: "2026-09-07T14:00:00+09:00"
tags:
  - "notes-basic-theory"
weight: 4
extra:
  question_no: "004"
  source_status: "미출"
  source_history: ""
  priority: 30
  priority_note: "탐색 절차와 복잡도 계산의 대표 기초"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **이진 탐색(Binary Search)**: 정렬된 배열에서 중앙 키와 비교하여 후보 탐색 공간을 절반씩 분할 축소하는 $O(\log n)$ 탐색 알고리즘.
- **선형 탐색(Linear Search)**: 배열의 처음부터 끝까지 순차적 비교를 통해 목표 데이터를 검색하는 $O(n)$ 탐색 방식.
- **중앙 키(Mid Key)**: 현재 탐색 구간의 중심 위치에 해당하는 원소 값.

</details>

- 정의/개념: 정렬 배열에서 **중앙 키** 비교로 후보 범위를 절반씩 줄이는 **이진 탐색** 기법
- 배경/필요성: 대규모 정렬 데이터에서 순차 탐색($O(n)$)의 비교 횟수가 원소 수에 비례해 누적되는 조회 지연

#### 한줄 요약

- 정렬 배열의 중앙값과 목표값을 비교해 후보 범위를 절반씩 줄인다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **정렬 불변식(Sorted Invariant)**: 탐색 과정 전체에서 배열의 정렬 상태가 항상 유지되는 전제 조건.
- **로그 시간(Logarithmic Time, $O(\log n)$)**: 입력이 배로 늘어도 연산 단계가 1단계만 증가하는 시간 차수.
- **하한(Lower Bound)**: 목표값 이상인 첫 번째 원소의 인덱스 위치.
- **상한(Upper Bound)**: 목표값을 초과하는 첫 번째 원소의 인덱스 위치.

</details>

![이진 탐색과 선형 탐색의 비교 횟수 증가율](/study/diagrams/binary-search-growth.svg)

> 입력 규모 증가 시 선형 탐색보다 비교 횟수가 완만하게 늘어나는 로그 시간 증가 양상

- **정렬 불변식** 전제에서 후보 구간을 절반씩 축소
- 데이터 규모 대비 시간 소모가 완만히 늘어나는 **로그 시간($O(\log n)$)** 성능 보장
- **하한·상한** 변형으로 중복 키 범위 및 조건부 위치 조회 확장

#### 한줄 요약

- 입력이 두 배로 늘어도 비교 단계는 한 번만 증가하는 로그 시간 탐색이다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **임의 접근(Random Access)**: 인덱스를 활용해 배열의 임의 요소에 $O(1)$ 상수 시간에 즉시 접근하는 특성.
- **후보 구간(Candidate Range)**: 목표 키가 존재할 수 있는 유효 인덱스 범위.
- **탐색 방향(Search Direction)**: 대소 비교 결과에 따라 선택하는 다음 분할 구간.
- **키 순서(Key Order)**: 배열 내 원소들이 오름차순 또는 내림차순으로 정렬되어 유지되는 논리적 순서.
- **비교 함수(Comparator)**: 목표 키와 중앙 키를 대조해 대소·동등 관계를 판정하고 다음 탐색 방향을 정하는 함수.

</details>

```text
[이진 탐색 상태 체계]
  │
  ├─ [정렬 배열] (키 순서 유지·O(1) 임의 접근)
  ├─ [후보 구간] (low·high 인덱스 경계 관리)
  └─ [비교 판정부] (목표 키 대소 비교 및 분기)
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 정렬 배열 | **키 순서** 유지 및 **임의 접근** 제공 |
| **후보 구간** | 양 끝 인덱스로 유효 탐색 범위 관리 |
| **비교 함수** | 목표 키와 중앙 키 비교로 **탐색 방향** 결정 |

#### 한줄 요약

- 정렬 배열·후보 경계·비교 규칙이 함께 다음 탐색 구간을 결정한다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **중앙 인덱스(Mid Index)**: 현재 탐색 후보 구간의 중심 위치.
- **대소 비교(Comparison)**: 목표 키와 현재 중앙 키를 대조하여 일치 여부와 탐색 방향을 결정하는 연산.
- **정수 오버플로(Integer Overflow)**: $low+high$ 합산 시 자료형 최대 범위를 초과하는 현상.
- **후보 경계(Candidate Boundary)**: 탐색 과정에서 갱신되는 $low$, $high$ 인덱스값.

</details>

```text
[이진 탐색 반복 흐름] (비교 ①→④ 반복, 키 일치 시 위치 반환·구간 소진 시 미발견 반환)
  │
  ├─ [정렬 배열] (① 임의 접근 전제의 정렬 상태로 탐색 대상 유지)
  │
  ├─ [중앙 인덱스] (② low + (high - low) / 2 식 산출로 정수 오버플로 회피)
  │
  ├─ [대소 비교] (③ 목표 키와 중앙 키 대조로 탐색 방향 결정)
  │
  └─ [후보 경계] (④ 비교 결과 따라 한쪽 경계 이동·후보 절반 축소 후 ② 재진입)
```

분기 결과: **대소 비교**에서 목표 키와 중앙 키가 일치하면 위치를 즉시 반환하고 불일치하면 **후보 경계**를 한쪽으로 옮겨 반복하며, 매 반복 후보가 절반으로 줄어 비교 횟수가 $\log_2 n$에 묶이는 대신 배열이 정렬돼 있고 임의 접근이 가능해야 한다

#### 한줄 요약

- 중앙값 비교 결과에 따라 한쪽 경계를 옮기며 후보 구간이 빌 때까지 반복한다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **해시 조회(Hash Lookup)**: 해시 함수를 통해 키를 주소로 직접 변환하여 평균 $O(1)$ 시간에 탐색하는 기법.
- **해시값(Hash Value)**: 해시 함수에 키를 입력하여 도출되는 고정 길이의 정수 주소값.
- **해시 충돌(Hash Collision)**: 서로 다른 키가 동일한 해시값을 가져 같은 버킷 주소에 배정되는 현상.
- **범위 조회(Range Query)**: 특정 키 범위를 연속 검색하는 탐색 유형.
- **정렬 유지 비용(Sorting Maintenance Cost)**: 삽입 및 수정 발생 시 배열의 정렬 상태를 보존하는 부하.

</details>

| 탐색 방식 | 이진 탐색 (Binary Search) | 선형 탐색 (Linear Search) | 해시 조회 (Hash Lookup) |
|:---|:---|:---|:---|
| 적용 기준 | 정렬된 배열 및 **범위 조회** 요구 시 | 미정렬 소규모 데이터 단순 탐색 시 | 정확한 단일 키 점조회(**해시 조회**) 시 |
| 핵심 특징 | **이진 탐색** 기반 매 단계 후보 절반 제거 | **선형 탐색** 기반 처음부터 순차 비교 | **해시값** 계산으로 버킷에 $O(1)$ 직접 접근 |
| 한계 | 데이터 삽입 시 **정렬 유지 비용** 발생 | 데이터 증가에 비례하여 비교 비용 급증 | **해시 충돌** 및 범위 조회 지원 불가 |

#### 한줄 요약

- 정렬 범위 조회는 이진 탐색, 작은 미정렬 집합은 선형 탐색, 점조회는 해시를 선택한다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **반열린 구간(Half-Open Interval, $[low, high)$)**: 시작 인덱스는 포함하고 종료 인덱스는 미포함하는 인덱스 설계.
- **배치 정렬(Batch Sorting)**: 무작위 삽입 건을 지연 적재 후 일괄 정렬하여 갱신 비용을 최소화하는 기법.
- **균형 탐색 트리(Balanced Search Tree)**: 동적 삽입·삭제 상황에서도 $O(\log n)$ 탐색 높이를 보장하는 트리 구조.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 잦은 삽입의 **정렬 유지 비용** 조회 이득을 초과 | 갱신 누적 후 **배치 정렬** 또는 **균형 탐색 트리** 적용 | 갱신 빈도에 맞춰 총 처리비용 절감 |
| 중복 키의 **하한**•**상한** 계약이 불명확해 결과 불일치 | **반열린 구간** 및 하한•상한 계약 고정 | 중복 구간의 반환 범위 일관성 확보 |
| 큰 중앙 인덱스 합산으로 **정수 오버플로** 발생 | $low+(high-low)/2$ 방식 적용 | 유효 **중앙 인덱스** 산정 |
| 탐색•정렬의 비교 기준 불일치로 누락•오탐 | 정렬과 탐색에 동일 **비교 함수** 적용 | 이진 탐색의 **정렬 불변식** 보장 |

#### 한줄 요약

- 삽입이 잦으면 배치 정렬이나 균형 트리로 전환하고, 중복 키 범위는 반열린 구간 계약으로 고정하며, 중앙 인덱스 합산 오버플로를 방지해야 한다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **매개변수 탐색(Parametric Search)**: 최적화 문제의 해를 이진 탐색의 판정 함수로 반복 검증하여 최적값을 좁혀 가는 알고리즘 설계 기법.
- **B-Tree**: 디스크 I/O를 최소화하기 위해 하나의 노드에 여러 키를 저장하고 자식 수를 넓힌 균형 다진 탐색 트리.
- **LSM-Tree(Log-Structured Merge-Tree)**: 쓰기를 메모리 버퍼에 순차 적재한 뒤 정렬된 런(Run) 단위로 디스크에 병합 기록하는 쓰기 최적화 인덱스 구조.

</details>

- 정렬된 정적 집합의 $O(\log n)$ 표준 조회 알고리즘이자 매개변수 탐색(Parametric Search)의 판정 함수로 확립되었으며, 동적 갱신 환경에서는 B-Tree·LSM-Tree 인덱스 내부의 페이지 검색 엔진으로 내재화 추세
- 정적 배열에는 이진 탐색을 직접 적용하되, 삽입·삭제가 빈번한 워크로드에서는 균형 탐색 트리와 병행하는 인덱스 설계 필요

#### 한줄 요약

- 범위·경계 조회가 필요하면 이진 탐색을 사용하고, 단일 키 점조회만 반복된다면 해시 조회가 더 빠르다.
