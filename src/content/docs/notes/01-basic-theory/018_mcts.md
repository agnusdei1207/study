---
sidebar:
  order: 18
  label: "018. 몬테카를로 트리 탐색 MCTS (Monte Carlo Tree Search)"
  badge:
    text: "기출 · 50%"
    variant: note
title: "몬테카를로 트리 탐색 MCTS (Monte Carlo Tree Search)"
date: "2026-09-07T14:00:00+09:00"
tags:
  - "notes-basic-theory"
weight: 18
extra:
  question_no: "018"
  source_status: "기출"
  source_history: "135회"
  priority: 50
  priority_note: "최근 기출, 선택•확장•롤아웃•역전파 절차가 핵심"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **트리 적용 상한 신뢰구간(Upper Confidence Bound applied to Trees, UCT)**: 가치 활용과 불확실성 탐험의 균형을 유지하며 최선의 자식 노드를 선별하는 확률적 의사결정 수식.
- **정적 평가 함수(Static Evaluation Function)**: 게임의 현재 국면 점수를 수작업 휴리스틱 수식으로 정량화하여 판정하는 알고리즘.

</details>

- 정의/개념: **UCT**로 선택·확장·롤아웃·역전파를 반복하는 트리 탐색 기법
- 배경/필요성: 초거대 분기 계수를 갖는 복합 게임 환경에서 **정적 평가 함수** 설계의 난제와 미니맥스 탐색의 지평선 효과(Horizon Effect) 한계

#### 한줄 요약

- 손으로 만든 평가 함수를 표본 통계로 대체한 거래이므로, 롤아웃이 편향되면 그 편향이 그대로 최종 착수에 실린다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **통계적 가치 산정(Statistical Value Estimation)**: 정적 평가 함수 대신 무작위 모의 대국의 평균 보상값으로 상태 노드의 승률 가치를 추정하는 기법.
- **탐색 예산(Search Budget)**: 한 턴의 의사결정에 허용된 연산 시간(ms) 또는 시뮬레이션 반복 횟수(Iterations).

</details>

![자식 방문 수에 따른 UCT 탐험 보너스](/study/diagrams/uct-exploration-bonus.svg)

> 파란 탐험 보너스는 적게 방문한 자식에서 크고 방문 수가 늘수록 감소하는, 부모 방문 수 100을 가정한 UCT 공식값

- 롤아웃 시뮬레이션의 평균 보상으로 상태 가치를 추정하는 **통계적 가치 산정**
- **UCT 수식**을 통해 활용과 탐색 간 최적 균형 유지
- 제한된 **탐색 예산** 내에서 최다 방문 노드를 선택하는 견고한 결정 체계

#### 한줄 요약

- UCT로 평균 보상이 높은 경로의 활용과 저방문 경로의 탐색을 조절한다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **트리 정책(Tree Policy)**: 이미 생성된 탐색 트리 내부에서 UCT 수식으로 리프 노드까지 이동 및 확장하는 정책.
- **기본 정책(Default / Rollout Policy)**: 탐색 트리 외부에서 게임 종료 시점까지 빠르게 수를 두는 경량 무작위 시뮬레이션 정책.

</details>

```text
[MCTS 탐색 엔진]
  │
  ├─ [트리 정책부] (트리 내부 탐색 및 확장)
  │     ├─ 선택기 (UCT 수식 기반 하향 최적 노드 선택)
  │     └─ 확장기 (미방문 상태를 새 자식 노드로 생성)
  │
  ├─ [시뮬레이션 및 평가부] (모의 대국 및 보상 산출)
  │     ├─ 시뮬레이터 (기본 정책 기반 롤아웃 무작위 대국)
  │     └─ 역전파기 (획득 보상값 상위 노드 연쇄 전파)
  │
  └─ [노드 통계 저장소] (탐색 상태 관리)
        ├─ 방문 통계기 (노드별 방문 횟수 n_i 누적)
        └─ 가치 보관기 (누적 보상 Q_i 및 평균 승률 관리)
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 선택기 | **UCT** 수식으로 트리 내부 최적 경로 하향 이동 |
| 확장기 | 미방문 유효 행동을 새 **자식 노드**로 등록 |
| 시뮬레이터 | 종료 상태까지 빠른 **모의 진행** 수행 |
| 역전파기 | 획득 보상을 루트까지 **통계 갱신($N, Q$)** 전파 |
| 노드 통계 저장소 | 노드별 **방문 횟수·누적 보상** 보관 |

#### 한줄 요약

- 트리 정책이 UCT로 경로를 선택하고, 롤아웃 보상과 방문 횟수를 노드 통계에 누적한다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **견고한 행동 선택(Robust Child Selection)**: 탐색 완료 후 평균 보상 대신 총 방문 횟수가 가장 높은 루트 자식 노드를 최종 착수로 결정하는 방식.

</details>

```text
[MCTS 탐색 반복 진행 흐름] (진행 ①→⑤, ①→④ 반복, 예산 소진 시 최종 착수 반환)
  │
  ├─ [선택기] (① UCT 수식 최대 자식 추적으로 리프 노드까지 하향 이동)
  │
  ├─ [확장기] (② 종료 아닌 리프에서 미포함 합법 행동 신규 자식 노드 생성·등록)
  │
  ├─ [시뮬레이터] (③ 기본 정책으로 게임 종료까지 모의 대국 완주·승패 보상 획득)
  │
  ├─ [역전파기] (④ 보상 R을 선택 경로 전체 역방향 전달, n_i+1·Q_i+R 갱신)
  │
  └─ [견고한 행동 선택] (⑤ 예산 소진 시 최다 방문(max N) 자식을 최종 착수 반환)
```

분기 결과: 할당된 탐색 시간 또는 시뮬레이션 반복 횟수가 남아 있으면 루트부터 UCT 선택 과정을 반복하고, 예산이 소진되면 평균 보상 노이즈가 제거된 최다 방문 자식 노드를 최종 착수로 반환한다.

#### 한줄 요약

- 역전파가 통계를 갱신하는 순간에만 트리가 비대칭으로 자라므로, 예산을 늘리면 폭이 아니라 유망 경로의 깊이가 먼저 깊어진다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **몬테카를로 트리 탐색(Monte Carlo Tree Search, MCTS)**: 평가 함수 없이 롤아웃 기반 통계적 샘플링으로 비대칭 트리를 확장하는 탐색 기법.
- **미니맥스 알고리즘(Minimax Algorithm)**: 상대방이 최선의 수를 둔다는 가정하에 평가 함수를 기반으로 최소-최대 역전파를 수행하는 결정론적 게임 탐색 기법.
- **지평선 효과(Horizon Effect)**: 탐색 깊이의 한계 너머에 존재하는 치명적인 위협이나 보상을 보지 못해 잘못된 판단을 내리는 현상.

</details>

| 게임 탐색 기법 | MCTS (Monte Carlo Tree Search) | 미니맥스 (Minimax + Alpha-Beta) |
|:---|:---|:---|
| 적용 기준 | **분기 계수($b > 100$)** 가 크고 평가 함수 정의 곤란 시 | 분기 계수가 작고 **정확한 정적 평가 함수** 존재 시 |
| 핵심 특징 | **UCT + 롤아웃 통계** 기반 유망 경로 비대칭 탐색 | 상대 최선 대응 가정한 **Min-Max 값 전파 및 가지치기** |
| 한계 | 롤아웃 정책의 품질에 따른 **롤아웃 편향** | 제한된 탐색 깊이 밖의 위협을 놓치는 **지평선 효과** |

#### 한줄 요약

- 분기 계수가 크고 롤아웃이 가능하면 MCTS, 평가 함수가 정확하면 미니맥스를 선택한다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **가상 손실(Virtual Loss)**: 병렬 MCTS에서 다수의 워커 스레드가 동일한 유망 경로에만 몰리는 현상을 막기 위해 선택 중인 노드의 방문 수에 일시적 페널티를 부여하는 동시성 제어 기법.
- **알파고 방식(AlphaGo Style - PUCT)**: 무작위 롤아웃을 심층 신경망의 가치 네트워크와 정책 네트워크로 대체하여 롤아웃 편향을 극복한 하이브리드 MCTS.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| **롤아웃 편향**으로 특정 행동의 보상 추정 왜곡 | 딥러닝 **가치 네트워크** 평가 결합(PUCT) | 롤아웃 편향 및 분산 대폭 완화 |
| 부적절한 **탐색 상수($C$)** 로 과도한 방황 또는 조기 고착 | 문제 도메인 보상 범위에 맞춘 상수 튜닝($C = \sqrt{2}$) | **탐색**과 활용 균형 |
| 저방문 노드의 높은 분산으로 인한 우연적 고보상 이상치 선택 위험 | 최종 선택 시 평균 보상이 아닌 **최다 방문 횟수($\max N$)** 채택 | 이상치 노이즈에 대한 **견고성** 확보 |
| 병렬 탐색 시 워커들이 동일 노드에 집중되어 중복 탐색 | **가상 손실** 및 원자적 CAS 통계 갱신 | 멀티스레드 병렬 탐색의 **경로 다양성** 극대화 |

#### 한줄 요약

- 롤아웃 편향은 가치 신경망(PUCT)으로 완화하고, 병렬 탐색 시에는 가상 손실로 경로 중복을 억제하며, 최종 수는 평균 보상이 아닌 최다 방문 횟수로 결정한다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **대형 언어 모델(Large Language Model, LLM)**: 방대한 텍스트 코퍼스를 학습하여 자연어 생성 및 다단계 순차 추론 과제를 수행하는 초거대 인공신경망.
- **생각의 트리(Tree of Thoughts, ToT)**: 문제를 여러 사고 단위 노드로 분기하여 탐색하고 백트래킹 및 MCTS 평가를 적용하는 다단계 언어 모델 추론 프레임워크.
- **다항 상한 신뢰구간 트리(Polynomial Upper Confidence Trees, PUCT)**: 심층 신경망의 사전 확률(Policy)을 UCB 탐색 보너스에 결합하여 편향을 줄이고 탐색을 안내하는 MCTS 확장 기법.
- **자가 개선(Self-Improvement)**: 탐색 트리에서 자체 생성된 궤적과 검증 피드백을 강화학습 데이터로 활용하여 모델 성능을 지속 고도화하는 학습 구조.

</details>

- 최근 MCTS는 게임 AI를 넘어 대형 언어 모델(LLM)의 추론 시점 트리 탐색(Tree of Thoughts, ToT)과 자가 개선(Self-Improvement) 및 로봇 자율 모션 플래닝의 핵심 순차 의사결정 코어로 진화 추세.
- 실무 시스템 구현 시에는 무작위 롤아웃 편향 극복을 위해 PUCT 신경망 평가를 채택하고, 멀티스레드 병렬 탐색 시 가상 손실(Virtual Loss)을 적용하여 경로 편중을 방지하는 동시성 통제 아키텍처 확보 필요.

#### 한줄 요약

- MCTS는 신경망 결합 PUCT 및 ToT 추론 탐색으로 진화하고 있으며, 실무에서는 롤아웃 편향 제거와 가상 손실 기반 병렬성 확보가 핵심이다.
