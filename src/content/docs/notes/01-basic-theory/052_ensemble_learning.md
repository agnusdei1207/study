---
sidebar:
  order: 52
  label: "052. 앙상블 학습 — 배깅·부스팅·스태킹 (Ensemble Learning)"
  badge:
    text: "미출 · 50%"
    variant: note
title: "앙상블 학습 — 배깅·부스팅·스태킹 (Ensemble Learning)"
date: "2026-09-07T14:00:00+09:00"
tags:
  - "notes-basic-theory"
weight: 52
extra:
  question_no: "052"
  source_status: "기출"
  source_history: ""
  priority: 50
  priority_note: "편향-분산 감소 원리와 결합 전략"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **앙상블 학습(Ensemble Learning)**: 여러 개의 기본 모델(약학습기)을 결합하여 단일 모델보다 우수한 예측 성능을 달성하는 기계학습 기법.
- **약학습기(Weak Learner)**: 무작위 추측보다 약간 더 나은 성능을 보이는 단순한 모델.
- **편향-분산 트레이드오프(Bias-Variance Tradeoff)**: 모델의 단순성으로 인한 체계적 오차(편향)와 훈련 데이터 민감성으로 인한 오차(분산) 사이의 상충 관계.

</details>

- 정의/개념: 여러 개의 단일 약학습기를 결합하여 **편향과 분산을 동시에 통제**함으로써 단일 모델 대비 높은 예측력과 일반화 성능을 얻는 머신러닝 학습 기법
- 배경/필요성: 단일 모델(Single Model)이 내포하는 고편향 및 고분산 한계와 표본 변동에 따른 **예측 불안정성 및 일반화 오차 발생 한계**

#### 한줄 요약

- 오차가 서로 다를 때만 상쇄가 일어나므로 모델 간 다양성이 곧 이득의 원천이며, 그 대가로 해석 가능성과 추론 비용을 내준다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **부트스트랩(Bootstrap)**: 훈련 데이터셋에서 복원 추출을 통해 원본과 동일한 크기의 다중 표본 집합을 생성하는 기법.
- **잔차(Residual)**: 실제 관측값과 현재 모델의 예측값 사이의 차이($y - \hat{y}$).

</details>

- 다양한 약학습기 오차의 **상호 보완을 통한 일반화 성능 향상**
- 독립 병렬 훈련(배깅)을 통한 **분산(Variance) 감소 및 과적합 억제**
- 오차 기반 순차 훈련(부스팅)을 통한 **편향(Bias) 집중 감소 및 정확도 극대화**

#### 한줄 요약

- 배깅은 분산 감소, 부스팅은 편향 감소, 스태킹은 이종 모델 메타 결합으로 성능을 최적화

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **메타 학습기(Meta Learner)**: 스태킹 앙상블에서 1단계 기본 모델들의 예측값들을 입력 특징으로 받아 최종 예측을 수행하는 모델.
- **소프트 보팅(Soft Voting)**: 각 분류기의 클래스 예측 확률값들을 평균하여 가장 높은 확률의 클래스를 선택하는 집계 방식.

</details>

```text
[앙상블 학습 체계]
  │
  ├─ [데이터 표본화] (다양성 확보)
  │     ├─ 부트스트랩 복원추출 (배깅 데이터셋 생성)
  │     └─ 오차 잔차 피드백 (부스팅 가중치 조정)
  │
  ├─ [기본 학습기군] (개별 모델 훈련)
  │     ├─ 동종 약학습기 병렬 학습 (Random Forest)
  │     ├─ 동종 약학습기 순차 학습 (GBDT, AdaBoost)
  │     └─ 이종 다양한 기본 학습기 (Stacking 1단계)
  │
  └─ [예측 결합 및 메타 학습] (집계 및 최적화)
        ├─ 보팅 및 가중 평균 (하드/소프트 보팅)
        └─ 2단계 메타 학습기 (OOF 예측 기반 학습)
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 부트스트랩 생성기 | 원본 데이터 복원 추출을 통한 **다양성 확보 데이터셋 분할** |
| 기본 약학습기군 | 병렬 또는 순차로 훈련되어 **개별 예측값 도출** |
| 가중 결합기 | 부스팅 및 배깅의 개별 모델 예측을 **가중합 또는 보팅 집계** |
| 메타 학습기 | 스태킹 구조에서 1단계 모델 예측값들을 기반으로 **최종 최적화** |

#### 한줄 요약

- 데이터 분할, 개별 모델 훈련, 가중 결합 또는 메타 학습을 거쳐 단일 결론을 도출

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **OOF 예측(Out-of-Fold Prediction)**: 스태킹 학습 시 타깃 누수를 방지하기 위해 K-Fold 교차 검증의 검증 폴드 예측값만으로 메타 데이터셋을 구성하는 기법.

</details>

```text
[앙상블 전략 분기·집계 진행 흐름] (진행 ①→⑤, 오차 특성 3갈래 분기, 앙상블 예측 집계 후 결과 반환)
  │
  ├─ [앙상블 전략 선택] (① 학습 오차 편향·분산 진단으로 파이프라인 분기)
  │
  ├─ [부트스트랩 복원추출] (② 분산 감소 → N개 부트스트랩 표본 동종 트리 병렬 훈련, 다수결/평균 결합)
  │
  ├─ [오차 잔차 피드백] (③ 편향 감소 → 이전 약학습기 잔차(Residual) 줄이도록 순차 훈련·가중 결합)
  │
  ├─ [2단계 메타 학습기] (④ 이종 결합 → K-Fold OOF 예측값 메타 피처 생성 후 메타 모델 학습)
  │
  └─ [앙상블 예측 집계] (⑤ 보팅/가중 평균/메타 학습기 추론으로 일반화 성능 극대화)
```

분기 결과: 모델 오차 원인이 과적합(고분산)이면 배깅으로 분기하고, 과소적합(고편향)이면 부스팅으로 분기하며, 이종 모델 간 상호보완이 목적이면 스태킹으로 분기하여 학습을 집행한다.

#### 한줄 요약

- 목표 오차에 따라 배깅, 부스팅, 스태킹으로 분기하여 개별 모델을 학습시키고 결과를 통합

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **배깅(Bagging)**: 부트스트랩 샘플링으로 여러 모델을 독립 병렬 학습시켜 분산을 줄이는 기법.
- **부스팅(Boosting)**: 이전 모델의 오차 잔차에 가중치를 두어 순차적으로 학습시켜 편향을 줄이는 기법.
- **스태킹(Stacking)**: 서로 다른 유형의 기본 모델 예측값을 메타 모델의 입력으로 활용해 결합하는 기법.

</details>

| 앙상블 기법 | 배깅 (Bagging) | 부스팅 (Boosting) | 스태킹 (Stacking) |
|:---|:---|:---|:---|
| 적용 기준 | 모델의 **높은 분산(과적합) 완화** 및 병렬 연산 요구 시 | 모델의 **높은 편향(과소적합) 해소** 및 최고 정확도 추구 시 | **이종 모델(Heterogeneous)** 장점 융합 요구 시 |
| 핵심 특징 | **부트스트랩 병렬 훈련** 및 단순 다수결/평균 결합 | **오차 샘플 순차 가중 훈련** 및 가중 선형 결합 | **1단계 예측값 기반 메타 모델 입력** 학습 |
| 한계 | 편향 감소 효과 제한적 | 이상치 노이즈에 **과적합 위험** 및 직렬 연산 지연 | **타깃 누수 위험** 및 파이프라인 복잡도 급증 |

#### 한줄 요약

- 분산 감소는 배깅, 정밀 편향 축소는 부스팅, 이종 모델 융합은 스태킹을 선택

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **타깃 누수(Target Leakage)**: 훈련 데이터 외의 미래 정보나 정답 정보가 모델 특징으로 유입되어 훈련 성능만 비정상적으로 높아지는 현상.
- **지식 증류(Knowledge Distillation)**: 거대한 앙상블 모델(교사)의 지식을 가볍고 빠른 단일 모델(학생)로 압축 전달하는 기법.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 부스팅 알고리즘 적용 시 노이즈 및 이상치 **과적합 취약** | **조기 종료(Early Stopping) 및 학습률(Shrinkage) 축소** | 과적합 억제 및 일반화 안정성 확보 |
| 스태킹 앙상블 훈련 시 발생하는 **타깃 누수 및 과적합** | **K-Fold 교차 검증 기반 OOF(Out-of-Fold) 메타셋 생성** | 1단계 오버피팅 전파 차단 및 신뢰도 확보 |
| 다중 모델 결합으로 인한 실시간 **추론 지연 시간(Latency) 증가** | **지식 증류(Knowledge Distillation) 기반 단일 모델 압축** | 실시간 서비스 SLA 충족 및 리소스 절감 |

#### 한줄 요약

- 부스팅 과적합은 조기 종료로 막고, 스태킹 누수는 OOF로 차단하며, 추론 지연은 지식 증류로 최적화

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **그래디언트 부스팅 결정 트리(Gradient Boosted Decision Tree, GBDT)**: 이전 단계 트리들의 잔차(Gradient)를 순차적으로 학습하여 손실 함수를 최소화하는 고성능 부스팅 앙상블 프레임워크.
- **서비스 수준 협약(Service Level Agreement, SLA)**: 시스템의 응답 지연 시간(Latency)이나 가용성 등 실시간 서비스 운영을 위해 보장해야 하는 성능 평가지표.

</details>

- **하이브리드 앙상블 진화 추세**: 정형 데이터 및 산업 이상 거래 탐지에서 그래디언트 부스팅 결정 트리(GBDT)와 딥러닝 임베딩을 결합하는 복합 하이브리드 아키텍처로 지속 진화.
- **추론 지연 극복 및 다양성 절충 통찰**: 모델 수 증가에 따른 연산 복잡도와 서비스 수준 협약(SLA) 위배 위험을 제어하기 위해 모형 간 다양성을 담보하고 지식 증류(Knowledge Distillation)로 압축 배포하는 아키텍처적 결단 필요.

#### 한줄 요약

- 앙상블 학습은 편향과 분산의 상충을 극복하는 정형 데이터 표준이며, 복잡도 폭증에 대응하는 지식 증류와 모형 다양성 확보 통찰이 운영의 핵심이다.
