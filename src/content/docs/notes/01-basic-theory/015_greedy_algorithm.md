---
sidebar:
  order: 15
  label: "015. 탐욕 알고리즘 (Greedy Algorithm)"
  badge:
    text: "기출 · 30%"
    variant: note
title: "탐욕 알고리즘 (Greedy Algorithm)"
date: "2026-09-07T14:00:00+09:00"
tags:
  - "notes-basic-theory"
weight: 15
extra:
  question_no: "015"
  source_status: "기출"
  source_history: "122회"
  priority: 30
  priority_note: "탐욕 선택 조건과 반례 제시형에 적합"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **국소 최적 선택(Local Optimum Choice)**: 미래의 단계나 전역 결과를 고려하지 않고 현재 시점에서 당장 비용이 가장 적거나 이익이 큰 후보를 즉시 확정하는 결정.
- **상태 공간 폭발(State Space Explosion)**: 문제 규모가 커짐에 따라 탐색해야 하는 경우의 수(순열/조합)가 기하급수적으로 폭증하여 연산이 불가능해지는 현상.

</details>

- 정의/개념: 단계마다 **국소 최적 선택**을 확정해 해를 구성하는 알고리즘
- 배경/필요성: 전역 상태 공간 완전 탐색 시 발생하는 지수적 조합 폭발($O(2^n)$)과 동적 계획법의 상태 테이블 메모리 점유로 인한 대규모 최적화 문제 해결의 연산 비용 한계

#### 한줄 요약

- 되돌림을 포기해 탐색 비용을 단일 경로로 낮추는 거래이므로, 그 포기가 정당하다는 증명이 없으면 속도 대신 최적성을 잃는다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **탐욕 선택 속성(Greedy Choice Property)**: 앞선 국소적 최적 선택이 이후의 선택에 영향을 주더라도, 이 선택을 포함하는 전역 최적해(Global Optimum)가 반드시 존재함을 보장하는 수학적 성질.
- **최적 부분 구조(Optimal Substructure)**: 부분 문제의 최적해들을 결합하여 전체 문제의 최적해를 구성할 수 있는 성질.
- **교환 논증(Exchange Argument)**: 임의의 최적해가 주어졌을 때, 해당 해의 원소를 탐욕적 선택으로 교체해도 해의 질(Quality)이 저하되지 않음을 증명하는 수학적 기법.

</details>

- 선택한 결정을 번복하지 않는 **비되돌림 단방향 탐색** 체계
- **탐욕 선택 속성** 및 **최적 부분 구조** 동시 성립 시 전역 최적해 보장
- 단일 경로만 추적하여 상태 캐시 없는 최소의 **메모리 오버헤드**

#### 한줄 요약

- 탐욕 선택 속성과 최적 부분 구조가 성립할 때 전역 최적해가 보장된다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **선택 함수(Selection Function)**: 목적 함수 기준으로 현재 가장 유망한 후보를 선별하는 함수.
- **타당성 검사 함수(Feasibility Function)**: 선택된 후보가 문제의 제약 조건을 위반하지 않는지 검증하는 함수.
- **해 판정 함수(Solution Function)**: 현재까지 수집된 부분 해가 문제의 최종 완성 조건을 만족했는지 검사하는 함수.

</details>

```text
[탐욕 알고리즘 제어기]
  │
  ├─ [후보 관리부] (후보군 관리 및 우선순위 제어)
  │     ├─ 후보 집합 (미선택 후보군 보관 및 정렬)
  │     └─ 선택 함수 (목적 함수 기준 국소 최선 후보 결정)
  │
  └─ [검증 및 판정부] (제약 위반 검사 및 해 완성 판정)
        ├─ 타당성 검사 함수 (제약 조건 위반 여부 검증)
        └─ 해 판정 함수 (최종 해 완성 여부 판단)
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 후보 집합 | 미선택 후보군 보관 및 정렬 |
| **선택 함수** | 목적 함수 기준 국소 최선 후보 결정 |
| 타당성 검사 함수 | 제약 조건 위반 여부 검사 및 필터링 |
| **해 판정 함수** | 최종 해 완성 여부 판정 및 반환 |

#### 한줄 요약

- 선택 함수, 타당성 검사 함수, 해 판정 함수가 연계되어 부분 해의 제약 충족 여부를 판단하고 확정한다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **되돌림 없는 선택(Irrevocable Step)**: 한 번 결정된 부분 해 요소를 큐/배열에서 번복하지 않고 즉시 해 집합에 고정하는 동작.

</details>

```text
[탐욕 선택 진행 흐름] (진행 ①→⑤, ②→⑤ 반복, 해 완성 시 최종 전역 최적해 반환)
  │
  ├─ [후보 집합] (① 목적 함수 기준 사전 정렬·우선순위 큐 적재)
  │
  ├─ [선택 함수] (② 국소 최적 후보 1개 추출)
  │
  ├─ [타당성 검사 함수] (③ 제약 위반 검증, 위반 시 영구 폐기·차순위 탐색)
  │
  ├─ [되돌림 없는 선택] (④ 통과 후보 해 집합 즉시 병합·유효 자원 차감)
  │
  └─ [해 판정 함수] (⑤ 완성 판정, 미완성 시 ② 복귀 반복)
```

분기 결과: 국소 최적 후보가 제약 조건을 위반하면 영구 폐기 후 다음 최적 후보를 탐색하고, 타당성을 충족하면 해 집합에 확정 추가한 뒤 전체 문제의 완성 조건을 검사하여 최종 해를 반환하거나 탐욕 선택을 반복한다.

#### 한줄 요약

- 국소 최선 선택, 타당성 판정, 되돌림 없는 확정을 해가 완성될 때까지 반복한다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **탐욕 알고리즘(Greedy Algorithm)**: 각 단계에서 되돌림 없이 국소 최적해만을 선택하여 빠른 시간에 해를 확정하는 방식.
- **동적 계획법(Dynamic Programming)**: 모든 하위 부분 문제 상태를 캐싱하여 최적 부분 구조를 상향식으로 결합하는 방식.
- **완전 탐색(Brute Force)**: 상태 공간 트리의 모든 노드를 전수 조사하여 전역 최적해를 찾는 방식.
- **반례(Counterexample)**: 탐욕적 선택이 전역 최적해를 산출하지 못함을 입증하는 구체적인 입력 사례.

</details>

| 알고리즘 기법 | 탐욕 알고리즘 (Greedy) | 동적 계획법 (DP) | 완전 탐색 (Brute Force) |
|:---|:---|:---|:---|
| 적용 기준 | **탐욕 선택 속성** 및 최적 부분 구조 성립 시 | 중복 부분 문제 및 **점화식** 수립 가능 시 | 입력 규모가 극소하여 전수 조사가 가능할 때 |
| 핵심 특징 | **되돌림 없는 단일 선택** 기반 빠른 해 확정 | **DP 캐시 테이블** 기반 하위 해 재사용 | 상태 공간 트리의 모든 노드 전수 검사 |
| 한계 | **반례** 존재 시 오답 발생 | 상태 차원 증가 시 메모리·시간 오버헤드 | 입력 증가에 따른 **조합 폭발($O(2^n)$)** |

#### 한줄 요약

- 탐욕 선택 속성과 교환 논증으로 전역 최적성을 입증하거나, 반례 발견 시 DP 또는 백트래킹으로 전환한다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **매트로이드 이론(Matroid Theory)**: 탐욕 알고리즘이 항상 전역 최적해를 보장함을 수학적으로 증명하는 대수적 독립 구조 이론.
- **반례 검증(Counterexample Verification)**: 배낭 문제(0/1 Knapsack) 등에서 탐욕 선택이 실패하는 최소 반례를 구성하여 알고리즘 타당성을 검증하는 작업.
- **동률 해소 규칙(Tie-breaking Rule)**: 동일한 우선순위를 갖는 복수의 후보 중 하나를 결정론적으로 선택하는 일관된 기준.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| **탐욕 선택 속성** 미성립으로 국소 최선이 전역 오답 생성 | **교환 논증** 및 최소 반례로 최적성 검증 | 문제의 **전역 최적성** 수학적 보장 |
| 숨은 제약조건 누락에 따른 실행 불가능한 무효 해 생성 | 모든 제약 조건을 **타당성 검사 함수**에 엄격히 반영 | 매 선택 후 **실행 가능 해** 유지 |
| 동률 후보 처리 불명확으로 실행별 결과 변동 | 결정적 **동률 해소 규칙** 적용 | 동일 입력에서 재현 가능한 일관된 결과 |
| 입력 범위 확대 시 검증하지 않은 경계에서 반례 발생 | 소규모 입력 대상 **DP/완전 탐색 결과와 교차 검증** | 적용 범위의 반례 사전 식별 |

#### 한줄 요약

- 교환 논증으로 탐욕 선택의 최적성을 먼저 증명하고, 동률 후보에는 결정적 규칙을 두며, 소규모 입력에서 DP 결과와 교차 검증해 반례를 사전에 잡아야 한다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **탐욕 알고리즘 패러다임(Greedy Algorithm Paradigm)**: 각 단계마다 되돌림 없는 국소적 최선 선택을 누적하여 전역 최적해를 구성하는 계산 설계 모델.
- **다항식 비결정론적 난해(Non-deterministic Polynomial-time Hard, NP-Hard)**: 다항 시간 내에 정확한 최적해를 구하기 어려운 계산 복잡도 난제 집합.
- **근사 알고리즘(Approximation Algorithm)**: NP-Hard 문제에 대해 다항 시간 내에 이론적으로 유계된 근사 비율 내의 준최적해를 도출하는 알고리즘.
- **온라인 스트리밍 알고리즘(Online Streaming Algorithm)**: 전체 입력을 사전에 알 수 없는 상태에서 연속 유입되는 데이터 스트림에 대해 실시간 탐욕 결정을 수행하는 기법.

</details>

- 탐욕 기법은 허프만 압축 및 신장 트리의 표준으로 안착되었으며, **NP-난해(NP-Hard)** 최적화 환경에서는 다항 시간 준최적해를 보증하는 **근사 알고리즘** 및 **온라인 스트리밍 알고리즘**으로 진화 추세.
- 교환 논증과 매트로이드 이론으로 탐욕 속성을 엄밀히 검증하고 반례 영역에서는 동적 계획법이나 분기한정법으로 즉시 전환하는 엔지니어링 결단 필요.

#### 한줄 요약

- 근사 알고리즘과 온라인 스트리밍 제어로 확장되는 탐욕 기법에서 수학적 정당성 검증과 반례 기반 패러다임 전환 통찰 확보 필요.
