---
sidebar:
  order: 55
  label: "055. 객체 탐지: YOLO·R-CNN (Object Detection)"
  badge:
    text: "기출 · 50%"
    variant: note
title: "객체 탐지: YOLO·R-CNN (Object Detection)"
date: "2026-09-07T14:00:00+09:00"
tags:
  - "notes-basic-theory"
weight: 55
extra:
  question_no: "055"
  source_status: "기출"
  source_history: "126회"
  priority: 50
  priority_note: "1-Stage vs 2-Stage 구조 비교"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **객체 탐지(Object Detection)**: 이미지 내에 존재하는 모든 대상 객체들의 위치(바운딩 박스)와 클래스를 동시에 예측하는 컴퓨터 비전 기술.
- **바운딩 박스(Bounding Box, BBox)**: 객체의 위치와 크기를 감싸는 직사각형 좌표($x, y, w, h$).
- **IoU(Intersection over Union)**: 실제 정답 영역과 예측 영역의 합집합 면적 대비 교집합 면적의 비율($\text{교집합}/\text{합집합}$).

</details>

- 정의/개념: 이미지 내 존재하는 복수 객체의 **위치(바운딩 박스)와 클래스(카테고리)를 동시에 예측**하는 딥러닝 비전 핵심 기술
- 배경/필요성: 영상 분류(Classification)의 공간 위치 식별 불가 한계와 슬라이딩 윈도우 방식의 **극심한 연산 병목 및 실시간 다중 객체 검출 한계**

#### 한줄 요약

- 분류에 위치 회귀를 더한 다중 과업이라, 두 과업을 나눠 풀지 한 번에 풀지의 선택이 곧 정확도와 속도의 균형점이 된다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **2-Stage Detector**: 영역 제안(Region Proposal)과 영역 분류(Classification)를 두 단계로 나누어 순차 처리하는 구조.
- **1-Stage Detector**: 영역 제안 없이 전체 이미지를 단일 신경망에 통과시켜 위치와 클래스를 한 번에 회귀 예측하는 구조.
- **평균 정밀도의 평균(Mean Average Precision, mAP)**: 클래스별 정밀도-재현율 곡선의 면적을 평균해 검출 성능을 측정하는 지표.
- **초당 프레임(Frames Per Second, FPS)**: 검출기가 1초에 처리하는 영상 프레임 수로 실시간성을 측정하는 지표.

</details>

- **위치 인식(Localization) 및 분류(Classification)**에 대한 다중 손실(Multi-task Loss) 동시 최적화
- 2-Stage Detector: 영역 제안과 정밀 분류의 분리를 통한 **높은 검출 정확도(mAP) 달성**
- 1-Stage Detector: 단일 회귀 파이프라인 설계를 통한 **실시간 고속 추론(FPS) 구현**

#### 한줄 요약

- 2-Stage는 높은 정확도, 1-Stage는 실시간 속도에 특화되어 상호 발전

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **백본(Backbone)**: 입력 이미지에서 다중 스케일 특징 맵을 추출하는 기본 합성곱 신경망(ResNet, Darknet 등).
- **영역 제안 네트워크(Region Proposal Network, RPN)**: 특징 맵 상에서 객체가 존재할 만한 후보 영역 바운딩 박스를 추천하는 모듈.
- **NMS(Non-Maximum Suppression)**: 겹치는 여러 개의 바운딩 박스 중 신뢰도 점수가 가장 높은 박스만 남기고 중복을 억제하는 후처리 알고리즘.

</details>

```text
[객체 탐지 (Object Detection) 구조]
  │
  ├─ [특징 추출 및 융합] (Backbone & Neck)
  │     ├─ 백본 네트워크 (ResNet, CSPDarknet 특징 맵 추출)
  │     └─ 넥 모듈 / FPN (멀티스케일 계층적 특징 융합)
  │
  ├─ [검출 헤드] (Detection Head)
  │     ├─ 2-Stage RPN (후보 영역 제안 및 정밀 분류)
  │     └─ 1-Stage Grid Head (바운딩 박스/클래스 직접 회귀)
  │
  └─ [후처리 및 확정] (Post-processing)
        └─ NMS 엔진 (IoU 기반 중복 경계상자 제거)
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 백본(Backbone) | 원본 이미지로부터 저차원~고차원 **특징 맵(Feature Map) 추출** |
| 넥(Neck / FPN) | 다양한 해상도의 특징 맵을 융합하여 **멀티스케일 표현력 강화** |
| RPN / Grid Head | 객체 존재 후보 영역 제안(2-Stage) 또는 **그리드별 직접 예측(1-Stage)** |
| NMS 후처리기 | 중복 예측된 바운딩 박스들을 제거하여 **최종 단일 박스 선별** |

#### 한줄 요약

- 백본의 특징 추출, 넥의 특징 융합, 헤드의 위치/클래스 예측, NMS 중복 제거로 구성

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **FPN(Feature Pyramid Network)**: 상위 계층의 고수준 의미 정보와 하위 계층의 고해상도 공간 정보를 결합하여 소형 객체 검출력을 높이는 구조.

</details>

```text
[객체 탐지 추론 진행 흐름] (진행 ①→⑤, 신뢰도 임계값으로 갈림, NMS 후 최종 탐지 결과 반환)
  │
  ├─ [백본 계층적 특징 맵 추출] (① ResNet·CSPDarknet 합성곱, stride 8·16·32 다단계 특징 맵 생성)
  │
  ├─ [멀티스케일 FPN 특징 융합] (② 하향식(Top-down) 경로·횡단 연결(Lateral Connection)로 P3·P4·P5 피라미드 생성)
  │
  ├─ [검출 헤드 좌표·클래스 예측] (③ 그리드 셀·앵커 단위 (x, y, w, h) 오프셋·Objectness·클래스 확률 동시 회귀)
  │
  ├─ [신뢰도 임계값 필터링] (④ 임계치(예: 0.25) 미만 배경 노이즈 즉시 폐기, 이상 유효 후보 박스 보존)
  │
  └─ [비최대 억제(NMS)] (⑤ 최고 점수 박스와 IoU > 0.5 중복 박스 제거, 객체당 단일 경계상자 확정)
```

분기 결과: 각 후보 바운딩 박스의 신뢰도 점수가 임계치 미만이면 배경으로 분류하여 폐기하고, 임계치 이상인 유효 박스들에 한하여 NMS를 수행해 중복을 억제한 최종 박스로 분기한다.

#### 한줄 요약

- 특징 맵을 추출하고 FPN으로 융합한 뒤 위치와 클래스를 동시 예측하고 NMS로 중복을 제거

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **트랜스포머 검출기(DETR)**: 앵커와 NMS 없이 셀프 어텐션과 이분 매칭 손실로 객체를 직접 집합 예측하는 모델.

</details>

| 객체 탐지 구조 | 2-Stage (Faster R-CNN) | 1-Stage (YOLO) | Transformer 기반 (DETR) |
|:---|:---|:---|:---|
| 적용 기준 | **정밀 불량 검사·의료 영상** 등 최고 정확도 우선 시 | **자율주행·CCTV 관제** 등 실시간 고속 처리 요구 시 | **엔드투엔드(End-to-End) 학습** 및 후처리 제거 선호 시 |
| 핵심 특징 | RPN 후보 제안과 RoI 분류의 **2단계 순차 처리** | 그리드 분할 기반 **단일 단계 동시 회귀 예측** | 셀프 어텐션 기반 **전역 문맥 파악 및 세트 예측** |
| 한계 | 연산 복잡도로 인한 **실시간 추론 불가(낮은 FPS)** | 소형 객체 밀집 시 **검출 정확도 상대적 저하** | 대규모 데이터 요구 및 **긴 훈련 수렴 시간** |

#### 한줄 요약

- 정밀 판독은 2-Stage, 실시간 관제는 1-Stage, 후처리 없는 완전 종단 학습은 DETR을 선택

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **초점 손실(Focal Loss)**: 쉬운 예제(배경)의 손실 가중치를 낮추고 어려운 예제(객체)에 집중하도록 교차 엔트로피를 수정한 손실 함수.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 동일 객체에 대한 **다중 중복 바운딩 박스 오탐** | **IoU 임계값 기반 비최대 억제(NMS) 및 Soft-NMS 적용** | 중복 박스 제거 및 최적 단일 박스 확정 |
| 원거리 **소형 객체(Small Object) 검출 실패** | **특징 피라미드(FPN) 구축 및 다중 스케일 훈련** | 해상도 유지 및 소형 객체 검출률 향상 |
| 배경과 객체 간 극단적인 **클래스 불균형(Class Imbalance)** | **초점 손실(Focal Loss) 도입 및 OHEM 적용** | 쉬운 배경 손실 억제 및 검출기 학습 안정화 |

#### 한줄 요약

- 중복 박스는 NMS로 병합하고, 소형 객체는 FPN으로 포착하며, 클래스 불균형은 초점 손실로 완화

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **비최대 억제(Non-Maximum Suppression, NMS)**: 겹치는 다수의 바운딩 박스 중 신뢰도 점수가 가장 높은 박스만 남기고 중복을 제거하는 후처리 알고리즘.
- **실시간 탐지 트랜스포머(Real-Time Detection Transformer, RT-DETR)**: 트랜스포머 기반의 엔드투엔드 세트 예측 구조를 채택하여 NMS 병목을 제거하고 실시간 처리 속도를 달성한 비전 아키텍처.

</details>

- **NMS 배제 및 실시간 트랜스포머 진화 추세**: 합성곱 기반의 앵커 박스 및 비최대 억제(NMS) 후처리 병목을 극복하기 위해 실시간 탐지 트랜스포머(RT-DETR) 등 종단 간 세트 예측 모델로의 패러다임 전환.
- **추론 지연시간과 검출 정밀도 절충 통찰**: 엣지 디바이스의 초당 프레임 수(FPS) 요구량과 소형 객체 오탐 위험을 분석하여, 초고속 YOLO 계열과 고정밀 다단계 검출기를 선별 연계하는 엔지니어링 거버넌스 확립 필요.

#### 한줄 요약

- 객체 탐지는 합성곱 앵커 기반에서 NMS-free 트랜스포머로 발전하고 있으며, 현장 하드웨어의 FPS 제약과 소형 객체 정밀도 간의 아키텍처적 절충이 핵심이다.
