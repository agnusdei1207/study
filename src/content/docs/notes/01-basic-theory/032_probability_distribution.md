---
sidebar:
  order: 32
  label: "032. 확률 분포: 정규•이항•포아송 (Probability Distribution)"
  badge:
    text: "미출 · 30%"
    variant: note
title: "확률 분포: 정규•이항•포아송 (Probability Distribution)"
date: "2026-09-07T14:00:00+09:00"
tags:
  - "notes-basic-theory"
weight: 32
extra:
  question_no: "032"
  source_status: "기출"
  source_history: ""
  priority: 30
  priority_note: "분포 조건과 모수는 통계 모델의 기본"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **확률 분포(Probability Distribution)**: 확률 변수(Random Variable)가 취할 수 있는 모든 값과 그 값들이 발생할 확률적 가중치를 수학적으로 정의한 함수 관계.
- **확률 질량 함수(PMF, Probability Mass Function)**: 이산 확률 변수의 개별 값 $x$에 대해 $P(X=x)$의 확률을 직접 할당하는 함수.
- **확률 밀도 함수(PDF, Probability Density Function)**: 연속 확률 변수의 특정 구간 $[a, b]$ 내에 존재할 확률을 적분($\int_a^b f(x)dx$)으로 나타내는 함수.

</details>

- 정의/개념: 이산/연속 확률 변수의 발생 가능한 수치 공간에 확률 질량(PMF) 또는 밀도(PDF)를 배정하여 불확실성을 체계화하는 **확률 모델링 기초 체계**
- 배경/필요성: 불확실성을 내포한 관측 데이터에서 적합한 확률 분포를 가정하지 못할 경우 발생하는 **통계적 추정 왜곡과 신뢰구간 산정 오류 한계**

#### 한줄 요약

- 관측 전체를 모수 몇 개로 줄이는 압축이므로, 분포족을 잘못 고르면 데이터가 아니라 가정이 결론을 만든다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **중심극한정리(CLT)**: 서로 독립이고 동일한 분포(IID)를 따르는 확률 변수들의 합이나 평균은 표본 크기 $N$이 커질수록 원래 분포에 무관하게 정규 분포에 수렴한다는 정리.
- **포아송 극한 정리(Poisson Limit Theorem)**: 이항 분포에서 $n \to \infty, p \to 0$이고 $np = \lambda$로 일정할 때 이항 분포가 포아송 분포로 근사 수렴한다는 성질.

</details>

![이항분포와 포아송분포의 사건 횟수별 확률 비교 차트](/study/diagrams/binomial-poisson-comparison.svg)

> 지정 모수의 계산값에서 사건 2회의 확률은 이항분포($n=4,p=0.5$) 0.375, 포아송분포($\lambda=2$) 0.271로 중심 높이의 차이

- 이산형(이항, 포아송)과 연속형(정규, 지수)으로 구분되는 **확률 변수 도메인 분기**
- 전 구간 확률의 총합(또는 적분값)이 반드시 1이 되는 **정규화 조건($\sum P(x) = 1, \int f(x)dx = 1$)** 준수
- 기댓값($E[X]$)과 분산($\text{Var}(X)$) 모수를 통한 **분포의 중심 위치와 산포도 정량화**

#### 한줄 요약

- 이산 변수는 PMF의 이산 합, 연속 변수는 PDF 곡선의 적분 면적으로 확률을 산출

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **CDF(Cumulative Distribution Function, 누적 분포 함수)**: 확률 변수가 $x$ 이하의 값을 가질 누적 확률 ($F(x) = P(X \le x)$).

</details>

```text
[확률 분포 모델링 프레임워크]
  │
  ├─ [사상 기초 계층] (사건 및 변수 정의)
  │     ├─ 표본 공간 Ω (발생 가능한 기저 사건 집합)
  │     └─ 확률 변수 X (사건을 실수 수치로 사상 X: Ω → ℝ)
  │
  ├─ [확률 배정 함수] (밀도 및 누적 확률 부여)
  │     ├─ 분포 함수 (이산 PMF / 연속 PDF 확률 밀도 배정)
  │     └─ 누적 분포 함수 CDF (F(x) 구간 누적 확률 산출)
  │
  └─ [분포 모수 집합] (통계적 특성치 확정)
        ├─ 중심 위치 모수 (평균 μ, 기댓값 E[X])
        └─ 형태/산포 모수 (분산 σ², 시행수 n, 발생률 λ)
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 표본 공간 (Ω) | 시행에서 발생 가능한 모든 기저 사건 집합 정의 |
| 확률 변수 (X) | 표본 공간의 사건을 **실수 수치값으로 사상(Mapping)** |
| 분포 함수 (PMF/PDF) | 값 또는 구간별 **발생 확률 및 확률 밀도 부여** |
| 분포 모수 ($\mu, \sigma^2, \lambda$) | 분포의 **중심 위치와 형태(퍼짐 정도) 결정** |
| 누적 분포 함수 (CDF) | 구간 확률 계산($P(a \le X \le b) = F(b) - F(a)$) 제공 |

#### 한줄 요약

- 표본 공간을 확률 변수로 사상하고, 분포 모수를 통해 PMF/PDF 및 CDF를 결정

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **과산포(Overdispersion)**: 포아송 분포의 가정인 평균과 분산의 일치($\lambda = \text{Var}(X)$)를 벗어나 실제 관측 데이터의 분산이 평균보다 훨씬 크게 나타나는 현상.

</details>

```text
[확률 분포 선택 진행 흐름] (진행 ①→④, 데이터 유형·시행 조건으로 갈림, 적합도 검정 후 모형 채택)
  │
  ├─ [값의 유형 판별] (① 연속형 측정값과 정수형 이산 카운트 데이터 식별)
  │
  ├─ [독립 오차 합산 검증] (② 연속형 → 중심극한정리에 따라 정규 분포 N(μ, σ²) 모형화)
  │
  ├─ [고정 시행 횟수 판별] (③ 고정 n회 독립 베르누이 시행 → 이항 분포 B(n, p), 단위 구간 발생률 λ 고정 → 포아송 분포 Poisson(λ))
  │
  └─ [적합도 검정] (④ Q-Q Plot·카이제곱 적합도 검정·잔차 분석으로 가정 유효성 실증, 포아송 평균=분산 위반 과산포 확인)
```

분기 결과: 데이터가 연속형이면 오차 합산성에 따라 정규분포를 선택하고, 이산형 카운트이면 고정 시행 n 여부에 따라 이항분포 또는 단위구간 포아송분포로 분기하여 채택한다.

#### 한줄 요약

- 데이터 생성 과정과 시행 조건을 분석하여 정규•이항•포아송 후보를 선정하고 적합도를 검증

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **정규 분포(Normal Distribution)**: 평균을 중심으로 좌우 대칭인 종 모양의 연속 확률 분포.
- **이항 분포(Binomial Distribution)**: $n$번의 독립적인 베르누이 시행 중 성공 횟수를 나타내는 이산 확률 분포.
- **포아송 분포(Poisson Distribution)**: 단위 시간이나 공간에서 드물게 발생하는 사건의 발생 건수를 나타내는 이산 확률 분포.

</details>

| 확률 분포 | 정규 분포 (Normal) | 이항 분포 (Binomial) | 포아송 분포 (Poisson) |
|:---|:---|:---|:---|
| 적용 기준 | 센서 잡음, 오차 분석, 주가 수익률 | 동전 던지기, A/B 테스트 전환율 | 서버 초당 요청 수, 불량 발생 건수 |
| 변수 유형 | **연속형 (Continuous)** | **이산형 (Discrete)** | **이산형 (Discrete)** |
| 확률 함수 | $f(x) = \frac{1}{\sigma\sqrt{2\pi}} e^{-\frac{(x-\mu)^2}{2\sigma^2}}$ | $P(X=k) = \binom{n}{k} p^k (1-p)^{n-k}$ | $P(X=k) = \frac{\lambda^k e^{-\lambda}}{k!}$ |
| 핵심 모수 | 평균($\mu$), 표준편차($\sigma$) | 시행 수($n$), 성공률($p$) | 단위 시간 평균 발생률($\lambda$) |
| 한계 | 두꺼운 꼬리(Fat-Tail) 극단값 과소평가 | 시행 횟수 $n$이 미정일 때 적용 불가 | 평균과 분산 불일치 시 **과산포 오류** |

#### 한줄 요약

- 연속 오차 합은 정규, 고정 시행 성공 수는 이항, 단위 시간당 사건 발생은 포아송 분포를 적용

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **Q-Q Plot(Quantile-Quantile Plot)**: 수집된 데이터의 분위수와 이론적 정규분포 분위수를 1:1 산점도로 대조하여 선형성을 통해 정규성을 시각적으로 검증하는 기법.
- **음이항 분포(Negative Binomial)**: 포아송 분포에서 과산포(분산 > 평균)가 발생할 때 분산 매개변수를 추가하여 과산포를 흡수하는 대체 분포.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 정규분포 가정 시 두꺼운 꼬리 특성에 따른 극단값 과소평가 | **Q-Q Plot** 검증 및 스튜던트-t/파레토 분포 대체 | 극단값 및 이상치 리스크 반영 |
| 포아송 모델에서 트래픽 버스트로 인한 **과산포(Overdispersion)** | **음이항 분포(Negative Binomial)** 모델 전환 | 분산 팽창 수용 및 정확한 대기열 모델링 |
| 표본 크기 부족으로 인한 **이항분포 정규근사 오차** | $np \ge 5, n(1-p) \ge 5$ 조건 확인 및 정확 검정 | 소표본 이항 검정 정합성 확보 |
| 시계열에 따른 **분포 모수 표류(Parameter Drift)** | 슬라이딩 윈도우 기반 **이동 창 모수 갱신(Online MLE)** | 환경 변화에 따른 적응적 모형 유지 |

#### 한줄 요약

- Q-Q Plot으로 정규성을 검증하고, 과산포 데이터에는 음이항 분포를 적용하며, 정규 근사 조건을 확인한 뒤 이동창 모수 갱신으로 비정상성에 대응한다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **최대 우도 추정(Maximum Likelihood Estimation, MLE)**: 관측된 표본 데이터의 결합 우도 함수를 최대화하는 모수를 도출하는 표준 통계적 최적화 기법.
- **확산 모델(Diffusion Model, DM)**: 데이터에 가우시안 정규 분포 노이즈를 점진적으로 주입한 후 이를 역방향으로 복원 제거하는 확률 미분 과정을 학습하는 심층 생성 모델.
- **대기행렬 이론(Queueing Theory)**: 포아송 도착 분포와 서비스 시간 분포를 기반으로 대기열 길이 및 처리 지연시간을 수리적으로 모델링하는 시스템 공학 이론.

</details>

- 전통적 최대 우도 추정(MLE) 및 대기행렬 이론(Queueing Theory) 모델링을 넘어, 현대 확산 모델(DM)의 가우시안 노이즈 스케줄링 및 거대 언어 모델의 확률론적 토큰 디코딩 엔진으로 확장 진화 추세
- 실무에서는 기계적 정규성 가정을 지양하고 두꺼운 꼬리 및 과산포 발생 시 음이항 분포 등 대안 분포로 즉각 전환해야 하며, 비정상 환경에서 모수 표류를 실시간 모니터링하는 통계적 거버넌스 확보 필요

#### 한줄 요약

- 확산 모델 노이즈 스케줄링과 생성형 AI 샘플링의 기저로 확장되는 확률 분포 메커니즘을 규명하고, 과산포와 극단치 왜곡을 제어하는 통계적 거버넌스 확립이 필수적이다.
