---
sidebar:
  order: 9
  label: "009. 교착상태 조건•예방•회피•탐지•복구"
  badge:
    text: "기출 · 85%"
    variant: note
title: "교착상태 조건•예방•회피•탐지•복구 (Deadlock)"
date: "2026-09-07T09:55:00+09:00"
tags: [notes-software]
weight: 9
extra:
  question_no: "009"
  source_status: "기출"
  source_history: "131회, 132회, 134회"
  priority: 85
  priority_note: "131•132•134회 반복, 자원 할당 통제 핵심"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **교착상태(Deadlock)**: 2개 이상의 프로세스가 서로 상대방이 차지하고 있는 자원을 무한정 기다리며 실행이 영구 중단되는 현상.
- **코프만 4대 조건**: 교착상태에 필요한 상호 배제·점유 대기·비선점·환형 대기 조건.

</details>

- 정의/개념: 복수 프로세스가 서로의 자원을 무한 대기하여 시스템이 멈추는 현상과 이를 통제하는 **교착상태 4대 제어 기법**
- 배경/필요성: 다중 프로세스가 한정된 비공유 자원을 상호 경쟁 점유할 때 발생하는 **영구 대기 상태와 시스템 중단(Hang) 위험**

#### 한줄 요약
- 코프만 4조건을 무력화하거나 탐지 후 롤백하여 교착상태를 체계적으로 제어한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **상호 배제(Mutual Exclusion)**: 한 번에 프로세스 하나만 자원을 독점 사용하는 성질.
- **점유 대기(Hold & Wait)**: 자원을 쥔 채로 다른 자원을 추가 요구하며 대기하는 성질.
- **비선점(No Preemption)**: 다른 프로세스가 사용 중인 자원을 강제로 뺏을 수 없는 성질.
- **환형 대기(Circular Wait)**: 대기 프로세스 간에 원형 순환 고리(Cycle)가 형성되는 성질.

</details>

- **코프만 4대 조건**(상호 배제·점유 대기·비선점·환형 대기)이 동시 성립할 때만 발생
- 단일 인스턴스 자원은 **RAG 사이클**로 교착상태 판정
- 예방(Prevention), 회피(Avoidance), 탐지(Detection), 복구(Recovery) 4단계 방어 전략

#### 한줄 요약
- 예방은 자원 사용의 자유를, 회피는 요청마다의 검사 시간을, 탐지는 이미 진행한 작업을 각각 대가로 내주므로 세 접근은 비용을 지불하는 시점만 다르다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **대기 그래프(Wait-for Graph)**: 자원 할당 그래프에서 자원 노드를 생략하고 프로세스 간 직접 대기 관계만 표현하여 사이클을 빠르게 검출하는 그래프.

</details>

```text
[교착상태 제어 체계]
  │
  ├─ [예방 모듈] (코프만 4대 조건 사전 무력화)
  │     └─ [락 오더링] (환형 대기 원천 차단)
  │
  ├─ [회피 모듈] (동적 안전 상태 판정)
  │     └─ [은행가 알고리즘] (안전 순서열 검증)
  │
  ├─ [탐지 모듈] (주기적 순환 대기 검출)
  │     └─ [대기 그래프] (Wait-for Graph 사이클 탐색)
  │
  └─ [복구 모듈] (교착 해소 및 시스템 정상화)
        └─ [희생자 선정] (최소 비용 기반 선점·롤백)
```

선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 예방 모듈 | **락 오더링**으로 환형 대기 조건 제거 |
| 회피 모듈 | **은행가 알고리즘**으로 안전 상태 검증 |
| 탐지 모듈 | **대기 그래프(Wait-for Graph)** 상의 순환 사이클을 주기적 탐색 |
| 복구 모듈 | 비용 기준 희생자 선정과 롤백 수행 |

#### 한줄 요약
- 예방·회피·탐지·복구는 같은 조건을 서로 다른 시점에서 끊는 수단이므로, 어느 단계에 개입하느냐가 곧 지불할 비용의 종류를 정한다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **은행가 알고리즘(Banker's Algorithm)**: 자원 요청 시 할당 후에도 모든 프로세스가 정상 종료 가능한 안전 순서열(Safe Sequence)이 존재하는지 검증하는 회피 기법.

</details>

```text
프로세스의 자원 할당 요청 인입
        │
   설계 단에서 전역 락 순서(Lock Ordering) 예방이 적용되었는가?
   ┌────┴─────┐
  예           아니오
   │             │
환형 대기 배제로  최대 자원 요구량을 사전에 파악할 수 있는가?
즉시 안전 할당    ┌────┴─────┐
   │             예           아니오 (사전 예측 불가)
   │             │             │
   │        은행가 알고리즘    일단 자원 할당 실행
   │        안전 상태 검증     │
   │        ┌────┴─────┐      주기적으로 대기 그래프(Wait-for Graph)
   │       안전        불안전    사이클 탐지 수행
   │        │          │       │
   │     자원 할당   요청 대기  교착 사이클 발견 시
   │        │        (회피)    희생자 선정 후 자원 회수 및 롤백
   └────┬───┴──────────┴───────┘
        │
   프로세스 실행 정상 완료
```

#### 한줄 요약
- 예방은 진입 자체를 막아 처리량을 깎고 회피는 요청마다 검사 비용을 치르며 탐지는 이미 멈춘 뒤 롤백 비용을 지불하므로, 교착 빈도가 낮을수록 뒤쪽 수단이 유리해진다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **타조 알고리즘**: 교착상태 대응 비용이 발생 위험보다 크면 별도 처리를 생략하는 방식.

</details>

| 제어 방식 | 예방 (Prevention) | 회피 (Avoidance) | 탐지 및 복구 (Detection) | 타조 알고리즘 (Ostrich) |
|:---|:---|:---|:---|:---|
| 자원 할당 시점 | 조건 제한 하에 즉시 할당 | **안전 상태 검증 후 할당** | 즉시 할당 후 사후 검사 | 무조건 즉시 할당 |
| 오버헤드 | **자원 낭비 및 처리량 저하** | 요청별 안전성 검사 | 탐지와 롤백 비용 | 별도 대응 비용 없음 |
| 사전 지식 | 없음 | **프로세스별 최대 요구량** | 없음 | 없음 |
| 주요 적용처 | 임베디드 락 설계, OS 내부 | 특수 목적 배치 시스템 | RDBMS 트랜잭션 엔진 | 범용 운영체제 (Linux, Windows) |

#### 한줄 요약
- 일반 락은 락 오더링 예방, RDBMS는 탐지 및 롤백, 범용 OS는 타조 알고리즘을 실용적으로 적용한다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **락 오더링(Lock Ordering)**: 시스템 내 모든 뮤텍스에 고유 ID 번호를 부여하고 오름차순으로만 락을 획득하도록 강제하는 예방 기법.
- **tryLock 타임아웃**: 락을 무한정 기다리지 않고 지정 시간 경과 시 실패를 반환하고 보유 락을 모두 해제하는 점유 대기 방지 기법.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 다중 뮤텍스 획득 순서 불일치로 **환형 대기** 발생 | 전역 **락 오더링(Lock Ordering)** 규칙 엄격 적용 | 환형 대기 조건 원천 차단 및 데드락 예방 |
| 락 획득 실패 후 무한 대기로 인한 **점유 대기** 지속 | **`tryLock()` 타임아웃** 적용 및 실패 시 보유 락 전체 해제 | 점유 대기 조건 해소 및 자원 즉시 양보 |
| RDBMS 트랜잭션 간 교착상태 발생 | 트랜잭션 타임아웃 및 **대기 그래프 사이클 탐지 롤백** | 교착 트랜잭션 자동 해소 및 데이터 일관성 유지 |
| 분산 환경에서 네트워크 단절 시 분산 락 고착 | 분산 락(Redlock) **TTL(Time-To-Live) 임대 만료** 설정 | 노드 다운 시에도 락 자동 해제 및 시스템 복구 |

#### 한줄 요약
- 교착 대응은 예방·회피·탐지 중 무엇을 택하든 처리량이나 관리 비용을 지불하므로, 락 순서 규약처럼 값싼 예방을 기본으로 깔고 분산 락은 TTL·타임아웃으로 회복 가능성만 남긴다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **분산 락(Distributed Lock)**: 다중 노드 분산 환경에서 Redis나 ZooKeeper를 활용해 임대 시간(TTL) 기반으로 공유 자원의 독점을 보장하는 동기화 메커니즘.
- **관계형 데이터베이스 관리 시스템(Relational Database Management System, RDBMS)**: 트랜잭션 ACID 원칙을 준수하며 동시성 제어 및 데드락 탐지·롤백 엔진을 제공하는 데이터베이스 관리 시스템.

</details>

- 마이크로서비스 및 클라우드 분산 환경으로 확장되면서 단일 OS 락을 넘어 네트워크 파티션과 시계 불일치를 고려한 **분산 락** 메커니즘으로 진화 추세
- 애플리케이션 계층의 엄격한 **락 오더링**과 타임아웃 예방을 선행하고, **RDBMS** 영역의 사이클 탐지 및 최소 비용 롤백 복구를 결합하는 계층별 방어 아키텍처 결단 필요

#### 한줄 요약
- 코드 레벨 락 오더링 예방과 DB 엔진 탐지·롤백, 분산 TTL 락을 계층별로 결합하여 데드락 위험을 통제해야 한다.
