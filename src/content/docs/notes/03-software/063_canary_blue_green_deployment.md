---
sidebar:
  order: 63
  label: "063. 카나리 배포•블루-그린 배포"
  badge:
    text: "기출 · 70%"
    variant: note
title: "카나리 배포•블루-그린 배포 (Canary Blue-Green Deployment)"
date: "2026-09-07T10:00:00+09:00"
tags:
  - "notes-software"
weight: 63
extra:
  question_no: "063"
  source_status: "기출"
  source_history: "132회, 138회"
  priority: 70
  priority_note: "132•138회 반복, 점진 배포 전략 핵심"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **카나리 배포(Canary Deployment)**: 광산의 카나리아 새처럼, 신규 버전을 전체의 1~5% 소수 트래픽에만 먼저 노출하여 실환경 반응을 검증한 후 점진 확대하는 배포 기법.
- **블루-그린 배포(Blue-Green Deployment)**: 기존 운영 환경(Blue)과 동일한 신규 환경(Green)을 병렬 구축해두고 로드밸런서 라우팅을 한 번에 스위칭하는 무중단 배포 기법.

</details>

- 정의/개념: **점진 노출(Canary)** 또는 **이중 환경 전환(Blue-Green)** 전략
- 배경/필요성: 인플레이스 교체 배포가 유발하는 **서비스 다운타임, 복구 지연 및 전면 장애 전파(Blast Radius) 위험 한계**

#### 한줄 요약
- 카나리는 장애 범위를 제한하고 블루-그린은 환경 전환 롤백을 지원한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **Blast Radius(장애 파급 반경)**: 배포 실패 시 장애의 영향을 받게 되는 사용자 또는 시스템의 물리적/논리적 범위.
- **Router Switching**: DNS 또는 L7 인그레스(Ingress) 라우팅 테이블 변경을 통해 트래픽 대상을 1초 미만으로 전환하는 기법.

</details>

- 카나리: **장애 파급 반경**을 초기 트래픽 비율로 제한
- 블루-그린: 구버전(Blue)과 신버전(Green) 병렬 운용 후 **라우터 스위칭을 통한 즉시 전환**
- 결함 감지 시 라우팅 규칙 원복으로 **빠른 롤백** 지원

#### 한줄 요약
- 카나리는 점진 노출로 위험을 최소화하고, 블루-그린은 병렬 환경으로 즉각 전환/롤백을 지원한다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **Argo Rollouts**: Kubernetes 환경에서 NGINX/Envoy와 연계하여 가중치 기반 트래픽 분할(Canary)과 메트릭 분석을 수행하는 컨트롤러.

</details>

```text
[무중단 배포 제어 체계]
  │
  ├─ [L7 트래픽 라우터] (가중치 기반 분할·즉시 전환)
  │
  ├─ [카나리 배포 환경] (소수 트래픽 점진 노출·지표 수집)
  │
  ├─ [블루-그린 배포 환경]
  │     ├─ [Blue 환경] (기존 활성 서비스·롤백 대기)
  │     └─ [Green 환경] (신규 완전 격리 환경·스위칭)
  │
  └─ [공통 데이터 계층] (DB 스키마 하위호환·Redis 세션)
```
- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| L7 트래픽 라우터 | **가중치 분할·엔드포인트 전환** 수행 |
| Blue 환경 | 기존 안정 버전 서비스와 롤백 대기 |
| Canary 환경 | 제한된 트래픽으로 **신버전 지표** 수집 |
| Green 환경 | 전환 전 신버전 검증과 대체 서비스 |
| 공통 데이터 계층 | 세션 외장화와 호환 DB 스키마 제공 |

#### 한줄 요약
- 라우터가 트래픽 비율을 쥐고 있기 때문에 롤백이 재배포가 아닌 가중치 변경이 되며, 관측 시스템이 그 가중치를 바꿀 근거를 공급한다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **ACA(Automated Canary Analysis)**: 프로메테우스 메트릭을 분석하여 카나리 Pod의 에러율이 기준치 이하일 때만 다음 가중치로 자동 승격시키는 엔진.

</details>

```text
[무중단 배포 워크플로우] (진행 ①→⑦, 전략별 판정 후 전량 노출 또는 롤백으로 종료)
  │
  ├─ [배포 전략 선택] (① 인프라 비용 여유·검증 범위에 따라 카나리 또는 블루-그린 택일)
  │
  ├─ [카나리 경로]
  │     ├─ [초기 트래픽 할당] (② 1~5% 가중치로 신버전 노출)
  │     ├─ [관찰 구간 분석] (③ 프로메테우스 에러율·지연시간 수집)
  │     ├─ [SLO 가드레일 판정] (④ 서비스별 SLO 충족 여부 판정)
  │     ├─ [가중치 점진 확대] (④ 충족 시 승격 반복 후 전량 노출, 구버전 종료)
  │     └─ [카나리 차단·기존 버전 유지] (④ 미충족 시 트래픽 차단)
  │
  └─ [블루-그린 경로]
        ├─ [Green 병렬 구축·스모크 테스트] (⑤ Blue 운영 중 v2.0 인프라 검증)
        ├─ [L7 라우터 스위칭] (⑥ 서비스 대상을 Green으로 1초 미만 전환)
        ├─ [이상 감지 판정] (⑦ 신버전 장애 여부 판정)
        ├─ [Blue 복원] (⑦ 장애 시 라우팅 규칙 원복)
        └─ [Blue 자원 회수] (⑦ 정상 시 구버전 인스턴스 Scale-in)
```

분기 결과: 카나리는 장애 파급 반경을 1~5%로 좁히는 대신 단계 승격에 검증 시간을, 블루-그린은 병렬 인프라 2배 비용을 치르고 즉시 전환을 얻으므로, 복구 속도와 인프라 비용 중 무엇이 더 비싼지에 따라 전략이 갈린다

#### 한줄 요약
- 카나리는 소수 사용자를 노출시켜 자원을 아끼고 블루-그린은 자원을 두 배로 사용하여 노출을 없애므로, 무엇을 대가로 낼지가 두 경로의 유일한 차이다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **Rolling vs Blue-Green vs Canary**: 순차 교체(Rolling), 이중 환경 일괄 스위칭(Blue-Green), 점진 가중치 노출(Canary) 3대 무중단 배포 비교.

</details>

| 비교 항목 | 롤링 배포 (Rolling Update) | 블루-그린 배포 (Blue-Green) | 카나리 배포 (Canary) |
|:---|:---|:---|:---|
| 구현 난이도 | **가장 단순 (K8s 기본 지원)** | 중간 (이중 인프라 필요) | 높음 (L7 트래픽 제어 및 ACA 필요) |
| 추가 인프라 비용 | 없음 (최소 오버헤드) | **2배 인프라 비용 발생** | 소폭 추가 (카나리 인스턴스) |
| 장애 파급 반경 | 전체 사용자에 점진 노출 | 전체 사용자에 일괄 노출 | **1~5% 소수 사용자에게만 국소화** |
| 롤백 방식 | 역순 롤링 | **Blue로 라우팅 복원** | **카나리 가중치 회수** |

#### 한줄 요약
- 비용 최소화는 롤링, 빠른 스위칭은 블루-그린, 리스크 극소화는 카나리를 선택한다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **Expand-Contract 패턴**: 배포 중 신/구버전이 DB를 동시 조회할 때 발생하는 스키마 불일치를 해결하기 위해 컬럼 추가 $\to$ 배포 $\to$ 구컬럼 삭제로 진행하는 기법.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 신·구버전의 DB 스키마 불일치 | **Expand-Contract 마이그레이션** | 전환 중 스키마 호환성 유지 |
| 블루-그린 배포 시 2배 인프라 비용 발생 | 전환 완료 즉시 **구버전(Blue) 인스턴스 자동 정지(Scale-in)** | 클라우드 인프라 비용 낭비 차단 |
| 인스턴스 간 세션 불일치 | 세션을 **외부 저장소**로 분리 | 버전 전환 중 세션 연속성 지원 |
| 카나리 분석 지표의 모호성 | **Prometheus + Argo Rollouts AnalysisTemplate** 지표 정량화 | 무인 자동 승격 및 자동 롤백 실현 |

#### 한줄 요약
- 블루그린은 자원을 두 배로 사용해 즉시 롤백을 사고 카나리는 일부 사용자 노출을 대가로 자원을 아끼므로, 롤백 속도와 인프라 비용 중 무엇이 더 비싼지로 방식을 고른다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **자동화된 카나리 분석(Automated Canary Analysis, ACA)**: 신규 버전과 기존 버전의 성능 지표와 오류율을 통계적으로 자동 비교하여 릴리즈 승격 여부를 판정하는 기법.
- **내부 개발자 플랫폼(Internal Developer Platform, IDP)**: 배포 파이프라인과 인프라 구성을 셀프서비스 API 및 골든 패스로 표준화하여 제공하는 플랫폼 엔지니어링 환경.

</details>

- **GitOps 및 플랫폼 엔지니어링 IDP 결합 진화**: 블루-그린과 카나리 배포는 Argo Rollouts 및 서비스 메시(Service Mesh) 기반 **자동화된 카나리 분석(ACA)**과 연계되어, **내부 개발자 플랫폼(IDP)**의 골든 패스 파이프라인에서 무인 점진 승격과 즉각적 자동 롤백을 실현하는 방향으로 발전 추세.
- **비용·속도 트레이드오프 통제와 DB 역호환성 결단**: 즉각적 복구가 필요한 핵심 결제 도메인은 블루-그린을, 대규모 트래픽 분산 검증은 카나리를 채택하되, 무중단 배포의 본질적 병목인 상태 저장소 정합성을 담보하기 위해 점진적 스키마 확장·축소 거버넌스 확보 필요.

#### 한줄 요약
- 무중단 배포는 IDP와 ACA 기반 자동화로 진화하고 있으며, 네트워크 전환 이전에 데이터베이스 역호환성과 도메인별 복구 트레이드오프 기준 수립이 필수적이다.
