---
sidebar:
  order: 5
  label: "005. 멀티레벨 피드백 큐 MLFQ"
  badge:
    text: "기출 · 50%"
    variant: note
title: "멀티레벨 피드백 큐 MLFQ (Multilevel Feedback Queue)"
date: "2026-09-07T09:55:00+09:00"
tags: [notes-software]
weight: 5
extra:
  question_no: "005"
  source_status: "기출"
  source_history: "138회"
  priority: 50
  priority_note: "138회 기출, 피드백 큐•기아 방지 설계"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **다단계 피드백 큐(Multi-Level Feedback Queue, MLFQ)**: 작업의 CPU 사용 패턴을 학습하여 I/O 바운드 작업은 상위 큐로, CPU 바운드 작업은 하위 큐로 자동 분류하는 적응형 스케줄링.
- **우선순위 부스트(Priority Boost)**: 주기마다 프로세스를 최상위 큐로 옮겨 기아를 완화하는 기제.

</details>

- 정의/개념: 실행 이력을 피드백하여 타임 퀀텀 소진 시 강등(Demotion)과 주기적 **우선순위 부스트**를 수행하는 적응형 **MLFQ** 스케줄링
- 배경/필요성: SJF류는 미래 CPU 버스트를 미리 알아야 하고 정적 우선순위 큐는 한 번 분류된 작업을 되돌리지 못해 하위 작업이 굶으므로, 실행 이력을 관찰해 큐를 옮기는 피드백 계층을 스케줄러에 두어 사전 정보 없이 대화형과 배치를 가르는 방식이 필요

#### 한줄 요약
- I/O 바운드 작업은 상위 큐에서 빠른 응답을, CPU 바운드 작업은 하위 큐에서 긴 타임 슬라이스를 부여한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **강등(Demotion)**: 현재 큐에서 할당된 타임 퀀텀을 모두 소진한 프로세스를 한 단계 낮은 우선순위 큐로 강등시키는 규칙.
- **게임 방어(Gaming Defense)**: 프로세스가 타임 퀀텀 만료 직전 의도적으로 짧은 I/O를 발생시켜 상위 큐를 독점하는 어뷰징을 차단하는 기법.

</details>

- 짧은 I/O 바운드 프로세스를 상위 큐에 유지해 응답시간 단축
- 타임 슬라이스 소진 시 하위 큐로 **강등(Demotion)** 시켜 CPU 집약 작업의 선점 남용 방지
- 주기적 **우선순위 부스트(Priority Boost)** 및 누적 시간 계측을 통한 **게임 방어** 달성

#### 한줄 요약
- 큐 이동은 작업 성격을 미리 알아야 할 부담을 없애는 대신 매 퀀텀마다 판정 비용을 치르며, 주기적 부스트는 그 판정이 누적한 편향을 되돌리는 보정이다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **타임 퀀텀 계층(Time Quantum Hierarchy)**: 상위 큐일수록 짧은 타임 퀀텀(10ms)과 높은 우선순위, 하위 큐일수록 긴 타임 퀀텀(80ms)을 부여하는 계층 구조.

</details>

```text
[MLFQ 적응형 스케줄러]
  │
  ├─ [다단계 큐 계층] (우선순위 역순 퀀텀 구조)
  │     ├─ [Q0 상위 큐] (짧은 퀀텀·신규/대화형)
  │     ├─ [Q1 중간 큐] (중간 퀀텀·혼합형)
  │     └─ [Q2 하위 큐] (긴 퀀텀·CPU 집약형)
  │
  ├─ [강등 제어기] (누적 퀀텀 소진 시 하위 강등)
  │
  └─ [부스트 타이머] (주기적 Q0 일괄 승격·기아 방지)
```

선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 최상위 큐(Q0) | 신규 작업과 대화형 I/O 작업 수용 |
| 중간 계층 큐(Q1) | 대화형·연산 혼합 작업의 순환 처리 |
| 최하위 큐(Q2) | **타임 퀀텀 계층**의 긴 퀀텀으로 CPU 집약 작업 처리 |
| 강등 제어기 | 누적 CPU 시간이 퀀텀을 넘으면 강등 |
| 부스트 타이머 | 주기마다 Q0으로 옮겨 기아 현상 완화 |

#### 한줄 요약
- 큐 사이의 강등·승격이 알 수 없는 실행 시간을 대신하는 근사 장치이므로, 큐 개수와 퀀텀 배분이 곧 스케줄러가 가정하는 작업 분포가 된다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **누적 시간 계측**: 프로세스가 단일 큐에 머무는 동안 사용한 총 CPU 시간을 합산하여 타임 퀀텀 초과 여부를 정밀 판정하는 방법.

</details>

```text
[MLFQ 적응형 스케줄링] (진행 ①→④, ③ 분기는 강등·큐 유지로 갈림)
  │
  ├─ [신규 배치] (① 신규 프로세스 도착 시 최상위 큐 Q0에 배치)
  │
  ├─ [디스패치] (② 스케줄러가 Q0에서 Round-Robin 방식으로 디스패치)
  │
  ├─ [강등 분기] (③ 현재 큐에서 타임 퀀텀을 모두 소진 시 누적 시간 계측으로 판정하여 한 단계 하위 큐로 강등(Demotion))
  │
  ├─ [큐 유지 분기] (③ 타임 퀀텀 내 I/O 발생 시 I/O 완료 후 현재 큐 수준 유지로 대화형 우선순위 보존)
  │
  └─ [부스트 승격] (④ 부스트 주기 S 도달 시 모든 프로세스를 최상위 Q0으로 일괄 승격하여 기아 방지, ① 배치 단계로 재진입)
```

분기 결과: 퀀텀을 모두 소진한 경로는 **누적 시간 계측**으로 강등되어 다음 큐에서 더 긴 퀀텀을 받고, I/O로 반납한 경로는 현재 큐를 유지해 짧은 퀀텀의 빠른 응답을 지킨다.

#### 한줄 요약
- 퀀텀 소진으로 강등되는 경로와 I/O로 큐를 유지하는 경로가 대화형과 배치를 가르는 실질적 판정이며, 주기적 승격은 그 판정이 굳어져 기아가 되는 것을 되돌린다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **다단계 큐(Multi-Level Queue, MLQ)**: 큐 간의 이동이 불가능하고 프로세스 유형별로 큐가 영구 고정되는 정적 스케줄링.

</details>

| 스케줄링 기법 | MLFQ (다단계 피드백 큐) | MLQ (정적 다단계 큐) | CFS (완전 공정 스케줄러) |
|:---|:---|:---|:---|
| 큐 간 이동 | 실행 이력 기반 동적 이동 | **MLQ**는 이동 불가 (정적 고정) | 가상 실행시간(vruntime) 순 정렬 |
| 작업 사전 지식 | 사전 정보 불필요 (자가 학습) | 사전 분류 필수 (시스템/대화형/배치) | 사전 정보 불필요 |
| 기아 방지책 | **주기적 우선순위 부스트** | 큐별 타임 슬라이스 고정 분배 | vruntime 지연 프로세스 우선 배정 |
| 한계점 | 부스트 주기 및 큐 파라미터 튜닝 난해 | 프로세스 성격 변화 시 대처 불가 | 실시간 마감 기한 보장 한계 |

#### 한줄 요약
- 미분류 혼합 워크로드에는 MLFQ가, 정적 시스템 분리에는 MLQ가, 범용 Linux에는 CFS가 적합하다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **스케줄러 게이밍(Scheduler Gaming)**: 프로세스가 타임 퀀텀 만료 직전 의도적으로 짧은 I/O를 호출하여 CPU를 반납함으로써 상위 큐에서 내려오지 않는 꼼수.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 의도적 99% CPU 점유 후 I/O 호출로 상위 큐 독점(**게이밍**) | 큐 단위 **누적 시간 계측** 기반 강등 | 스케줄러 어뷰징 차단 및 CPU 공정 배분 |
| 하위 큐의 배치 작업 기아 현상 | 워크로드별 **우선순위 부스트 주기** 설정 | 장기 대기 작업의 실행 기회 제공 |
| 큐와 퀀텀 설정 오류에 따른 성능 왜곡 | 워크로드 I/O 비율 기반 **타임 퀀텀 계층** 조정 | 응답시간과 배치 처리량 균형 조정 |
| 고빈도 문맥 전환에 따른 캐시 오염 | 하위 큐에 FCFS·긴 퀀텀 적용 | 문맥 전환과 캐시 오염 감소 |

#### 한줄 요약
- 부스트 주기를 짧게 잡으면 기아는 사라지지만 CPU 집약 작업의 처리량을 깎으므로, 응답성과 처리량 중 어느 쪽 손실이 더 비싼지에 따라 부스트 주기와 큐별 퀀텀을 맞춘다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **누적 시간 계측(Time Slice Accounting)**: 프로세스가 타임 퀀텀 만료 전 I/O를 호출하더라도 해당 큐에서 소비한 누적 CPU 시간을 합산 기록하여 어뷰징을 방지하는 기법.
- **적응형 스케줄러(Adaptive Scheduler)**: 작업의 실행 이력과 자원 소모 패턴을 실시간 관찰하여 큐 우선순위와 퀀텀을 동적으로 재조정하는 스케줄링 메커니즘.

</details>

- 대화형 응답성과 배치 처리량을 사전 정보 없이 조율하는 **적응형 스케줄러**의 표준 모델로 발전
- 최신 하이퍼바이저 vCPU 스케줄링의 부하 분산 기제로 확장
- 퀀텀 어뷰징을 차단하는 **누적 시간 계측**의 철저한 적용과 워크로드 특성에 부합하는 부스트 주기 최적화로 기아 방지와 캐시 보존을 동시 달성하는 엔지니어링 결단 필요

#### 한줄 요약
- 누적 시간 계측과 부스트 주기를 정밀 조정하여 대화형 응답성과 배치 처리량의 균형을 유지하는 적응형 스케줄링 설계가 핵심이다.
