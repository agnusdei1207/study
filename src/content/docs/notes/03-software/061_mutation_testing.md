---
sidebar:
  order: 61
  label: "061. 뮤테이션 테스트"
  badge:
    text: "미출 · 50%"
    variant: note
title: "뮤테이션 테스트 (Mutation Testing)"
date: "2026-09-07T10:00:00+09:00"
tags:
  - "notes-software"
weight: 61
extra:
  question_no: "061"
  source_status: "기출"
  source_history: ""
  priority: 50
  priority_note: "테스트 스위트의 결함 탐지력 정량 측정"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **뮤테이션 테스트(Mutation Testing)**: 소스 코드에 인위적으로 작은 결함(변이체)을 주입한 후, 기존 단위 테스트가 이를 실패로 잡아내는지 검증하는 기법.
- **변이 점수(Mutation Score)**: 생성된 전체 유효 변이체 중 테스트가 성공적으로 사멸(Killed)시킨 변이체의 백분율(%).

</details>

- 정의/개념: 소스 코드에 변이 연산자를 적용해 인위적 결함을 주입하고 단위 테스트의 결함 탐지력을 평가하는 **결함 기반(Fault-based) 테스트 검증 기법**
- 배경/필요성: 기존 코드 커버리지가 단순 실행 여부만 측정하여 **단언문(Assertion)이 부실한 테스트를 오판하는 커버리지 착시 한계**

#### 한줄 요약
- 원본 코드에 고의적 결함을 주입하여 단위 테스트 스위트의 실질적 결함 탐지 능력을 정량 측정한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **Killed vs Survived**: 결함이 주입된 코드를 테스트가 실패로 잡아내면 사멸(Killed), 테스트가 모두 통과해버리면 생존(Survived - 테스트 부실).
- **Equivalent Mutant(등가 변이체)**: 문법은 바뀌었으나 실행 결과는 원본과 동일하여 어떤 테스트로도 사멸시킬 수 없는 변이체.

</details>

- **결함 탐지 능력(Fault Detection Capability)** 을 직접 측정하여 테스트 케이스의 질적 수준 평가
- 실행 결과를 **사멸(Killed), 생존(Survived), 등가 변이체(Equivalent)** 로 명확히 분류
- **변이 점수 산출 공식**: $\text{Mutation Score} = \frac{\text{Killed Mutants}}{\text{Total Mutants} - \text{Equivalent Mutants}} \times 100$

#### 한줄 요약
- 변이 점수는 커버리지가 보지 못하는 검출력을 드러내는 대신 변이체 수만큼 시험을 반복 실행하므로, 정보의 질을 연산 시간으로 사들인다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **변이 연산자(Mutation Operator)**: `+`를 `-`로 변경(산술), `>`를 `<=`로 변경(관계), 조건문 반전 등 정형화된 결함 주입 규칙.

</details>

```text
[뮤테이션 테스트 체계]
  │
  ├─ [원본 소스 코드] (기준 대상 프로그램)
  │
  ├─ [변이 연산자 엔진] (산술·조건 반전 등 고의 결함 주입)
  │
  ├─ [변이체 집합] (생성된 Mutants 풀)
  │
  ├─ [단위 테스트 스위트] (Killed / Survived 판정 실행)
  │
  └─ [변이 리포터] (PITest·변이 점수 산출 및 리포팅)
```
- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 변이 연산자 엔진 | 코드에 **표준 결함 패턴** 주입 |
| 변이체 집합 | 연산자별 결함 포함 코드 보관 |
| 단위 테스트 스위트 | 변이체를 실행해 **단언 실패** 검증 |
| 변이 리포터 | **변이 점수**와 생존 위치 제공 |

#### 한줄 요약
- 변이 연산자가 결함을 인위적으로 만들어 시험의 반응을 관측하므로, 측정 대상이 코드가 아니라 시험 자체라는 점이 커버리지와의 근본 차이다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **PITest**: Java 바이트코드를 메모리상에서 실시간 변조하여 고속으로 뮤테이션 테스트를 수행하는 프레임워크.

</details>

```text
[뮤테이션 테스트 수행 흐름] (진행 ①→⑥, 변이체 실행 결과가 사멸·생존을 갈림)
  │
  ├─ [기준 확인] (① 원본 프로그램의 기존 단위 테스트 통과 확인)
  │
  ├─ [변이체 생성] (② PITest가 바이트코드 조작으로 N개의 변이체(Mutant) 생성)
  │
  ├─ [격리 실행] (③ 각 변이체마다 연관된 단위 테스트 격리 실행)
  │
  ├─ [결과 판정] (④ 단위 테스트가 실패(AssertionError)·타임아웃 발생 시 사멸(Killed), 미발생 시 생존(Survived))
  │
  ├─ [생존 보강] (⑤ 생존 갈래는 테스트 단언문 부실로 판단, 누락된 Assert 단언문 추가 보강)
  │
  └─ [점수 판정] (⑥ 사멸·생존 집계로 변이 점수(Mutation Score) 리포트 출력 및 CI 품질 게이트 판정)
```

분기 결과: 변이체 실행 결과에서 갈리므로, 사멸 갈래는 결함 탐지력을 입증해 고신뢰성 로직의 검증 비용을 줄이는 반면, 생존 갈래는 단언문 보강 재실행 비용과 등가 변이체 여부의 사람 검토 비용을 추가로 치른다

#### 한줄 요약
- 생존한 변이체는 시험이 그 결함을 못 잡는다는 증거이지만 등가 변이체일 수도 있으므로, 판정 이후의 사람 검토가 이 기법의 실제 비용이 된다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **코드 커버리지 vs 뮤테이션 점수**: 라인이 실행되었는가(양적)와 결함을 잡아낼 수 있는가(질적)의 비교.

</details>

| 비교 항목 | 코드 커버리지 (Code Coverage) | 뮤테이션 점수 (Mutation Score) |
|:---|:---|:---|
| 평가 관점 | 코드가 얼마나 많이 실행되었는가? (**양적 평가**) | 결함을 얼마나 정밀하게 잡아내는가? (**질적 평가**) |
| 단언문 검증 | 단언 없이도 실행 라인은 집계 | **단언이 약하면 생존 변이체 증가** |
| 실행 시간 | 테스트 1회 실행 | **변이체별 테스트 반복 실행** |
| 주 적용 영역 | 전사 CI 빌드 파이프라인 기본 게이트 | **결제, 금융, 원전, 의료 등 고신뢰성 핵심 로직** |

#### 한줄 요약
- 코드 커버리지가 실행 여부의 양적 척도라면, 뮤테이션 점수는 단언문 정밀도의 질적 척도다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **등가 변이체 문제**: `if (a == b)`를 `if (b == a)`로 바꿔도 의미가 같아 절대 실패시킬 수 없는 튜링 완전 언어의 한계.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 대량 변이체 실행에 따른 CI 시간 증가 | 변경 코드 중심 **증분 변이 검사** | 실행 대상 축소와 CI 시간 절감 |
| 사멸시킬 수 없는 **등가 변이체(Equivalent Mutant)** | 정적 분석 연계 및 `@Suppress` 어노테이션 수동 등록 | 점수 왜곡 방지 및 분석 피로도 제거 |
| Getter·Setter·DTO 검증의 낮은 효용 | **도메인 핵심 로직** 중심 적용 | 검사 비용 대비 결함 탐지력 향상 |
| 비종료 변이체에 따른 테스트 정지 | 기준 실행시간 기반 **타임아웃** 설정 | 빌드 파이프라인 정지 방지 |

#### 한줄 요약
- 변이 테스팅은 시험의 실제 검출력을 알려 주는 대신 전 변이체 실행이라는 큰 연산 비용을 요구하므로, 변경분 증분 스캔과 핵심 도메인 선별로 측정 범위를 줄여야 파이프라인에 들어간다.

## Ⅶ. 결론

- 소프트웨어 테스트 스위트의 품질을 질적으로 보증하는 **최상위 결함 기반(Fault-based) 테스트 검증 방법론**으로 확립.
- 실무 환경에서는 높은 연산 오버헤드를 제어하기 위해 전체 코드가 아닌 금융·결제·보안 등 핵심 비즈니스 도메인에 우선 적용하고, CI 단계에서는 PR 변경분에 대한 **증분 뮤테이션 검사(PITest 연동)** 와 등가 변이체 필터링을 결합하여 실효성을 확보.

#### 한줄 요약
- 핵심 로직에는 변이 검사를 적용하고 변경 범위부터 증분 실행한다.
