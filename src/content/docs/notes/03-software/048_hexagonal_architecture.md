---
sidebar:
  order: 48
  label: "048. 헥사고날 아키텍처: 포트•어댑터"
  badge:
    text: "미출 · 50%"
    variant: note
title: "헥사고날 아키텍처: 포트•어댑터 (Hexagonal Architecture)"
date: "2026-09-07T10:00:00+09:00"
tags:
  - "notes-software"
weight: 48
extra:
  question_no: "048"
  source_status: "기출"
  source_history: ""
  priority: 50
  priority_note: "포트•어댑터는 도메인 의존성 격리 핵심"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **헥사고날 아키텍처(Ports & Adapters)**: 앨리스터 코번(Alistair Cockburn)이 제안한, 순수 비즈니스 로직을 중심에 두고 외부 DB/웹을 포트와 어댑터로 격리하는 아키텍처.
- **포트(Port) & 어댑터(Adapter)**: 도메인이 정의한 인터페이스 규격(포트)과 외부 기술(JPA, REST)을 포트에 맞게 구현한 변환기(어댑터).

</details>

- 정의/개념: 핵심 비즈니스 로직을 중심에 두고 외부 프레임워크 및 DB와의 결합을 **포트(Port)**와 **어댑터(Adapter)** 인터페이스로 격리하는 아키텍처
- 배경/필요성: DB 중심 계층형 아키텍처는 스키마·프레임워크 변경이 상위 계층까지 전파되고 단위 테스트마다 DB·컨테이너 기동 비용을 치르므로, 도메인이 정의한 포트 안쪽으로 의존 방향을 역전시켜 인프라를 어댑터로 교체 가능하게 격리한 **헥사고날 아키텍처(Ports & Adapters)**를 도입

#### 한줄 요약
- 헥사고날은 계층을 위아래가 아니라 안팎으로 나누므로, DB도 UI와 같은 '바깥'이 되어 영속성 기술이 설계 중심에서 밀려난다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **인바운드 포트(Inbound Port) vs 아웃바운드 포트(Outbound Port)**: 외부에서 도메인을 호출하는 진입 인터페이스(Inbound/Driver)와 도메인이 외부 DB/메시징을 호출하는 출력 인터페이스(Outbound/Driven).
- **DIP(의존역전)**: 어댑터가 도메인 포트를 바라보게 하여 모든 의존성 화살표가 도메인 코어 안쪽을 향하도록 역전시키는 원칙.

</details>

- **인바운드 포트(Inbound Port)**와 **아웃바운드 포트(Outbound Port)** 분리를 통한 도메인 코어의 순수성 보장
- 어댑터가 도메인 포트를 구현하도록 강제하는 **의존성 역전 원칙(DIP)** 전면 적용
- 외부 DB나 웹 프레임워크 없이도 Mock 객체를 주입하여 순수 POJO 초고속 단위 테스트 가능

#### 한줄 요약
- 포트 기반 역전은 도메인을 인프라에서 떼어내는 대신 매핑 코드와 계층 수를 늘리므로, 도메인 규칙이 얇은 영역에서는 비용만 남는다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **POJO(Plain Old Java Object)**: 특정 프레임워크(Spring, JPA) 어노테이션이나 상속에 종속되지 않은 순수한 자바 객체.
- **SPI(Service Provider Interface)**: 도메인이 필요한 인프라 기능(저장·발송)을 인터페이스로 먼저 선언하고 구현은 바깥 어댑터가 끼워 넣도록 뒤집은 아웃바운드 포트의 계약 형태.

</details>

```text
[헥사고날 아키텍처 체계]
  │
  ├─ [인바운드 영역: Driving]
  │     ├─ [인바운드 어댑터] (REST / Kafka Consumer)
  │     └─ [인바운드 포트] (UseCase 인터페이스)
  │
  ├─ [도메인 코어: Core]
  │     └─ [순수 비즈니스 로직] (POJO Entity·Domain Service)
  │
  └─ [아웃바운드 영역: Driven]
        ├─ [아웃바운드 포트] (Repository SPI 규격)
        └─ [아웃바운드 어댑터] (JPA / Kafka Producer)
```
- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 인바운드 어댑터 | 외부 요청의 커맨드 변환·포트 호출 |
| 인바운드 포트 | 외부에 노출할 유스케이스 정의 |
| 도메인 코어 | 순수 **POJO**로 비즈니스 규칙 실행 |
| 아웃바운드 포트 | 인프라 기능의 **SPI** 정의 |
| 아웃바운드 어댑터 | JPA·Kafka 등 인프라 구현 |

#### 한줄 요약
- 포트는 도메인이 요구하는 계약이고 어댑터는 그 계약의 구현이므로, 의존이 항상 안쪽을 향해 인프라 교체가 도메인 코어에 닿지 않는다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **도메인 모델 매핑(Domain Model Mapping)**: 외부 DTO와 JPA Entity를 순수 도메인 객체로 상호 변환하여 계층 간 오염을 차단하는 기법.

</details>

```text
외부 클라이언트의 HTTP 주문 생성 요청 인입
        │
   인바운드 어댑터(REST Controller): 요청 DTO 검증 및 CreateOrderCommand 생성
        │
   인바운드 포트(OrderUseCase) 인터페이스 호출
        │
   도메인 서비스가 비즈니스 규칙(재고 확인, 가격 계산)을 순수 POJO로 수행
        │
   도메인이 아웃바운드 포트(OrderRepositoryPort SPI) 인터페이스 호출
        │
   아웃바운드 어댑터(JPA Adapter): 도메인 객체를 JPA Entity로 매핑 후 DB 커밋
```

#### 한줄 요약
- 도메인 연산은 어댑터의 **도메인 모델 매핑**을 거치지 않고는 외부에 닿지 못하므로, 인프라 교체가 어댑터 교체로 끝나고 도메인 코어는 다시 검증할 필요가 없다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **클린 아키텍처(Clean Architecture)**: 로버트 마틴이 제안한, 엔티티→유스케이스→인터페이스 어댑터→프레임워크의 동심원 계층에서 의존성이 항상 안쪽만 향하게 하는 의존성 규칙 기반 아키텍처.

</details>

| 비교 항목 | 계층형 아키텍처 (Layered) | 헥사고날 아키텍처 (Hexagonal) | 클린 아키텍처 (Clean) |
|:---|:---|:---|:---|
| 의존성 방향 | 상위 $\to$ 하위 (DB 중심 종속) | **외부 $\to$ 내부 도메인 코어 (DIP)** | 외부 $\to$ 내부 엔티티 (의존성 규칙) |
| 기술 결합도 | JPA/RDBMS에 비즈니스 로직 결합 | **프레임워크/DB 완전 격리 (POJO)** | 프레임워크/DB 완전 격리 |
| 테스트 용이성 | Spring/DB 기동 필요 (느린 테스트) | Mock 주입 순수 단위 테스트 (수 ms) | Mock 주입 순수 단위 테스트 |
| 코드 복잡도 | 낮음 (초기 개발 빠름) | 포트/어댑터/매퍼로 다소 증가 | UseCase/Presenter 세분화로 높음 |

#### 한줄 요약
- 단순 CRUD는 계층형, 복잡한 비즈니스 규칙과 장기 유지보수는 헥사고날/**클린 아키텍처**가 적합하다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **MapStruct**: 컴파일 타임에 DTO $\leftrightarrow$ Domain Model $\leftrightarrow$ JPA Entity 간의 변환 코드를 자동 생성해주는 매핑 라이브러리.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| JPA `@Entity` 어노테이션으로 인한 도메인 모델 오염 | 순수 도메인 엔티티와 JPA 영속성 엔티티 물리 분리 | DB 테이블 변경이 비즈니스 로직에 영향 0화 |
| 계층 간 객체 매핑(DTO/Entity)으로 보일러플레이트 급증 | **MapStruct** 컴파일 타임 매퍼 라이브러리 표준화 | 매핑 코드 수작업 오버헤드 90% 제거 |
| 단순 CRUD 도메인에 헥사고날 적용 시 오버엔지니어링 | 도메인 복잡도에 따른 하이브리드 적용 (핵심만 헥사고날) | 개발 생산성과 유지보수성의 최적 절충 |
| 포트 인터페이스 명명 규칙 혼선 | `Inbound/UseCase`, `Outbound/Port` 명명 표준화 | 개발팀 내 아키텍처 일관성 확립 |

#### 한줄 요약
- 포트-어댑터 분리는 도메인 독립성과 시험 용이성을 매핑 코드와 클래스 증가로 사므로, MapStruct로 매핑 비용을 낮추고 도메인 규칙이 얇은 영역은 적용 대상에서 제외한다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **모듈러 모놀리스(Modular Monolith)**: 단일 배포 아티팩트 형태를 유지하면서도 헥사고날 원칙을 적용하여 패키지 간 경계와 데이터 의존성을 엄격히 격리한 아키텍처.
- **아키텍처 규칙 검증(Architecture Fitness Checking)**: CI 파이프라인에서 ArchUnit 등의 도구를 통해 도메인 코어가 어댑터를 직접 참조하는 아키텍처 침식 결함을 빌드 시점에 자동 차단하는 메커니즘.
- **유스케이스 주도 설계(UseCase-Driven Design)**: 사용자의 행위와 의도를 담은 인바운드 유스케이스를 우선 정의하여 도메인 모델과 입출력 포트를 구조화하는 설계 접근법.

</details>

- **차세대 발전 전망**: 분산 마이크로서비스 및 **모듈러 모놀리스(Modular Monolith)** 환경에서 특정 클라우드 프레임워크 락인(Lock-in)을 방어하고, 기술 교체 주기를 비즈니스 로직과 분리하는 핵심 아키텍처 패턴으로 지속 확산 추세.
- **실무 공학적 통찰**: 단순 조작 위주의 CRUD 도메인에 대한 무조건적 적용 시 매핑 비용만 누적되는 오버엔지니어링을 경계하고, 고유 업무 규칙이 밀집된 핵심 서브도메인에 선별 도입하며 **아키텍처 규칙 검증**을 통해 도메인 순수성을 통제하는 공학적 결단 필요.

#### 한줄 요약
- 헥사고날 아키텍처는 포트와 어댑터로 도메인을 외부 인프라로부터 격리하되, 핵심 도메인 중심의 선별 적용과 ArchUnit 기반 아키텍처 검증이 성공의 열쇠다.
