---
sidebar:
  order: 8
  label: "008. 스레드 스케줄링•스레드 풀"
  badge:
    text: "미출 · 50%"
    variant: note
title: "스레드 스케줄링•스레드 풀 (Thread Scheduling•Thread Pool)"
date: "2026-09-07T09:55:00+09:00"
tags: [notes-software]
weight: 8
extra:
  question_no: "008"
  source_status: "기출"
  source_history: ""
  priority: 50
  priority_note: "스레드 풀은 생성 비용•대기열 절충 설계"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **스레드 풀(Thread Pool)**: 작업마다 스레드를 매번 생성·파괴하지 않고 일정 수의 워커 스레드를 미리 생성하여 큐 기반으로 작업을 재사용하는 자원 관리 기법.
- **작업 큐(Work Queue)**: 워커 스레드가 모두 사용 중일 때 유입된 태스크를 메모리에 대기시키는 유한 버퍼 큐(BlockingQueue).

</details>

- 정의/개념: 스레드 생성 및 소멸 오버헤드를 방지하고 자원 한도 내에서 작업을 재사용·스케줄링하는 **스레드 풀(Thread Pool)** 아키텍처
- 배경/필요성: 요청마다 OS 스레드를 생성하면 커널 스택 할당·소멸 비용이 매 요청 반복되고 동시 요청 수만큼 스레드가 늘어 OOM에 이르므로, 미리 만든 워커와 유한 큐를 요청과 실행 사이에 두어 생성 비용을 없애고 동시성 상한을 고정하는 계층이 필요

#### 한줄 요약
- 워커 스레드를 사전 할당하고 유한 큐와 거절 정책을 통해 동시성 자원을 통제한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **Core/Max Pool Size**: 상시 유지하는 기본 활성 워커 스레드 수(Core)와 피크 부하 시 동적 확장 가능한 최대 스레드 수(Max).
- **역압력(Backpressure)**: 작업 큐 포화 시 생산자(Producer)의 작업 인입 속도를 강제로 억제하여 시스템을 보호하는 흐름 제어 메커니즘.

</details>

- 스레드 사전 생성·재사용으로 생성·소멸 비용 절감
- **Core/Max Pool Size** 및 유한 **작업 큐**(Bounded Queue) 설정을 통한 메모리 자원 한도 통제
- 큐 포화 시 포화 거절 정책(RejectedExecutionHandler)을 통한 상위 계층 **역압력(Backpressure)** 전파

#### 한줄 요약
- 풀은 생성 비용과 동시성 상한을 함께 통제하지만 그 대가로 요청이 큐에서 기다리게 되므로, 얻은 안정성은 지연으로 지불된다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **워커 스레드(Worker Thread)**: 작업 큐에서 태스크를 인출하여 실행하고 완료 후 대기 상태로 복귀하는 실행 주체.
- **포화 거절 정책(RejectedExecutionHandler)**: 작업 큐와 최대 스레드가 모두 찼을 때 신규 유입 작업을 처리하는 정책(Abort, CallerRuns 등).

</details>

```text
[스레드 풀 실행 체계]
  │
  ├─ [작업 제출자] (Client 작업 제출)
  │
  ├─ [스레드 풀 실행기] (ThreadPoolExecutor)
  │     ├─ [유한 작업 큐] (BlockingQueue 대기열)
  │     ├─ [워커 스레드] (Core/Max 워커 풀)
  │     └─ [포화 거절 처리기] (RejectedExecutionHandler)
  │
  └─ [동시성 자원 관리] (Keep-Alive 수명 제어)
```

선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 스레드 풀 실행기 | 태스크 수용과 워커 수·유휴 시간 관리 |
| 유한 작업 큐 | **워커 스레드**가 모두 바쁠 때 태스크를 BlockingQueue에 적재 |
| 워커 스레드 | 큐에서 태스크를 인출해 로직 실행 |
| 포화 거절 처리기 | 큐와 최대 워커 포화 시 **거절 정책** 수행 |

#### 한줄 요약
- 유한 큐와 포화 정책이 풀의 실질적인 안전장치이므로, 워커 수보다 큐 길이와 거절 방식이 과부하 시 시스템의 거동을 결정한다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **CallerRunsPolicy**: 스레드 풀 포화 시 작업을 제출한 호출자 스레드가 직접 태스크를 실행하도록 유도하여 신규 유입을 늦추는 정책.

</details>

```text
신규 작업 제출 (execute/submit)
        │
   현재 활성 스레드 수 < Core Pool Size 인가?
   ┌────┴─────┐
  예           아니오
   │             │
신규 코어 워커    유한 작업 큐(Work Queue)에 여유 공간이 있는가?
스레드 즉시 생성   ┌────┴─────┐
후 작업 실행      예           아니오
   │             │             │
   │        작업 큐에 적재     현재 활성 스레드 수 < Max Pool Size 인가?
   │        후 워커 대기       ┌────┴─────┐
   │             │            예           아니오
   │             │             │             │
   │             │        신규 임시 워커   포화 거절 정책 실행
   │             │        생성 후 실행   (CallerRunsPolicy 등)
   └────┬────────┴─────────────┴─────────────┘
        │
   작업 완료 후 유휴 시간(Keep-Alive) 초과 시 임시 스레드 소멸
```

분기 결과: 코어 스레드가 남으면 생성 비용만 치르고 즉시 실행하며, 큐에 여유가 있으면 대기 지연을, 큐가 차면 임시 워커 생성 비용을, 그마저 상한이면 **CallerRunsPolicy** 같은 거절 정책이 호출자에게 비용을 되돌린다.

#### 한줄 요약
- 코어 스레드·큐·최대 스레드·거절은 순서를 가진 방어선이므로, 큐를 무한정 키우면 확장과 거절 단계가 영영 발동하지 않아 지연이 메모리로 옮겨 쌓인다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **포크-조인 풀(Fork-Join Pool, ForkJoinPool)**: 대규모 작업을 재귀 분할(Fork)하고 유휴 스레드가 타 스레드의 작업 덱에서 작업을 가져오는 작업 훔치기 기반 병렬 풀.
- **작업 훔치기(Work-Stealing)**: 자신의 작업 덱이 빈 유휴 워커 스레드가 다른 워커의 작업 덱 꼬리에서 작업을 훔쳐와 실행하는 부하 분산 기법.

</details>

| 스레드 풀 유형 | 고정 스레드 풀 (Fixed) | 캐시 스레드 풀 (Cached) | 포크-조인 풀 (ForkJoinPool) |
|:---|:---|:---|:---|
| 스레드 수 관리 | **Core/Max Pool Size**를 같게 고정 | 필요 시 무제한 생성 (60초 유휴 회수) | **포크-조인 풀**의 CPU 코어 수 비례 풀 유지 |
| 작업 큐 특성 | 유한/무한 LinkedBlockingQueue | 동기 큐 (SynchronousQueue) | 덱(Deque) 기반 **작업 훔치기** |
| 주 적용처 | 일반 웹 서버, 트랜잭션 처리 WAS | 짧고 독립적인 단기 비동기 작업 | CPU 집약 병렬 연산 (Parallel Stream) |
| 한계점 | 무한 큐 사용 시 메모리 OOM 위험 | 트래픽 폭증 시 스레드 폭증으로 다운 | 블로킹 I/O 작업 시 스레드 기아 발생 |

#### 한줄 요약
- 일반 백엔드는 FixedThreadPool, 단기 태스크는 Cached, 대규모 분할 정복은 ForkJoinPool을 선택한다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **벌크헤드(Bulkhead)**: 작업 유형별로 독립 스레드 풀을 두어 한 유형의 포화·블로킹이 다른 유형의 워커를 잠식하지 못하게 격리하는 패턴.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 무제한 큐의 메모리 누적으로 OOM 발생 | 유한 **작업 큐** 크기 제한 | 큐 메모리 상한 통제와 장애 위험 감소 |
| 포화 상태에서 AbortPolicy 사용 시 요청 유실 | 거절 정책을 **CallerRunsPolicy** 로 전환 | 호출자 스레드 실행을 통한 유입 속도 자연 억제(**역압력**) |
| CPU 바운드 연산과 블로킹 I/O 작업 혼용으로 풀 고갈 | CPU 집약용 풀과 I/O 블로킹용 풀을 **벌크헤드**로 분리 | I/O 지연으로 인한 CPU 연산 스레드 블로킹 차단 |
| 스레드 풀 크기 산정 미흡 | 대기·연산 시간 비율로 워커 수 부하 시험 | 처리량과 문맥 전환 비용의 균형 확인 |

#### 한줄 요약
- 풀 크기를 키우면 대기는 줄지만 문맥 전환과 메모리를 더 쓰므로, 유한 큐와 역압력으로 과부하를 호출자에게 되돌리고 CPU 바운드와 I/O 바운드를 별도 풀로 나눠 서로의 포화를 옮기지 않는다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **유한 큐(Bounded Queue)**: 큐에 적재 가능한 작업의 최대 개수를 엄격히 제한하여 OOM 장애를 방지하는 대기 큐 구조.
- **가상 스레드(Virtual Thread)**: OS 플랫폼 스레드 풀링 모델을 탈피하여 요청당 경량 스레드를 할당하는 차세대 JVM 동시성 기제.

</details>

- 기존 플랫폼 스레드 풀링 방식에서 경량 **가상 스레드** 및 비동기 이벤트 런타임과 결합하여 I/O 블로킹 비용을 원천 제거하는 방향으로 진화 추세
- CPU 바운드와 I/O 바운드 작업의 엄격한 **벌크헤드** 격리와 **유한 큐** 기반 역압력 설계를 통해 트래픽 폭증 시 시스템 붕괴를 방어하는 아키텍처적 결단 필요

#### 한줄 요약
- I/O 블로킹을 해소하는 가상 스레드 진화와 함께 유한 큐 기반 역압력 및 벌크헤드 격리를 결합해야 시스템 복원력을 담보할 수 있다.
