---
sidebar:
  order: 10
  label: "010. MCP Tool (모델 컨텍스트 프로토콜 도구)"
  badge:
    text: "기출 · 30%"
    variant: note
title: "MCP Tool (모델 컨텍스트 프로토콜 도구)"
date: "2026-09-15T09:55:00+09:00"
tags:
  - "notes-latest_tech"
weight: 10
extra:
  question_no: "010"
  source_status: "기출"
  source_history: "138회"
  priority: 30
  priority_note: "도구 노출은 MCP 실행 구조의 핵심"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **모델 컨텍스트 프로토콜(Model Context Protocol, MCP)**: 호스트와 서버의 기능•컨텍스트 교환을 표준화한 프로토콜로 정의된다.
- **모델 컨텍스트 프로토콜 도구(Model Context Protocol Tool, MCP Tool)**: 구조화된 인자로 외부 시스템의 행동을 실행하는 MCP 기능.
- **대규모 언어 모델(Large Language Model, LLM)**: 문맥과 도구 명세를 바탕으로 호출할 도구와 인자를 생성하는 모델로 정의된다.

</details>

- 정의: MCP 서버가 외부 행동을 공개하는 **MCP Tool**이다.
- 배경/필요성: AI 에이전트가 외부 시스템의 상태를 변경하거나(파일 쓰기, DB 갱신, API 요청), 복잡한 연산을 외부 프로세스에 위임할 때, 개별 시스템마다 이종의 함수 시그니처와 호출 규격을 사용하면 모델의 호출 정확도가 급감하고 간접 프롬프트 주입(Prompt Injection)을 통한 비인가 명령 실행 위험이 통제되지 않는 심각한 보안 결함이 발생함에 따라, 외부 실행 기능을 JSON Schema 기반의 표준화된 도구 명세(tools/list)와 실행 엔드포인트(tools/call)로 정의한 MCP Tool 메커니즘을 구축하여 **모델의 자율적 도구 발견 및 타입 안전(Type-safe) 인자 호출 보장, 호스트-서버 간 이중 인자 검증 및 비가역 액션에 대한 인간 개입(HITL) 통제 확립**을 달성할 필요

#### 한줄 요약
- **LLM**이 부르는 도구는 자원과 달리 부작용을 남기므로 명세 준수만으로는 부족하고, 호출 편의를 줄이더라도 승인과 인자 검증을 앞에 두는 편이 안전하다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **자바스크립트 객체 표기법 스키마(JavaScript Object Notation Schema, JSON Schema)**: 도구 입력•출력 구조와 자료형•필수 필드를 정의하는 규격.
- **부작용(Side Effect)**: 도구 실행으로 외부 시스템의 데이터나 상태가 바뀌는 현상을 뜻하며, 복잡한 문제 해결과 동적 환경 적응에 필수적인 역할을 수행한다.
- **선택 축(Selection Axis)**: 목표•문맥에 맞는 도구와 인자를 모델이 제안하는 기능 영역으로 정의된다.
- **계약 축(Contract Axis)**: tools/list•tools/call과 JSON Schema로 호출 계약을 표준화하는 기능 영역으로 정의된다.
- **통제 축(Control Axis)**: 호스트 동의와 서버 재검증으로 부작용을 제한하는 기능 영역.

</details>

- **선택 축**: 목표•문맥별 도구•인자 제안
- **계약 축**: tools/list•tools/call•JSON Schema 표준화
- **통제 축**: 호스트 동의•서버 재검증으로 부작용 통제

#### 한줄 요약
- **JSON Schema•통제 축** 기반 인자•고위험 행동 제한

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **백엔드 도구(Backend Tool)**: MCP 서버가 검증 후 실제 외부 시스템에서 실행하는 기능이다.
- **모델 컨텍스트 프로토콜 서버(Model Context Protocol Server, MCP Server)**: 도구 명세를 제공하고 검증된 백엔드 행동을 실행하는 구성요소.

</details>

```text
[MCP Tool 실행 아키텍처] ── [에이전틱 실행 및 통제 인터페이스]
├── [도구 명세 계층 (Specification)]
│   ├── tools/list 인터페이스
│   ├── 도구 이름 및 상세 설명
│   └── JSON Schema 기반 입력 인자 스키마
├── [호출 검증 및 통제 (Control)]
│   ├── 호스트 레벨 HITL 승인 통제
│   ├── 서버 레벨 입력 스키마 재검증
│   └── 권한·스코프 및 부작용(Side Effect) 제어
└── [실행 및 결과 반환 (Execution)]
    ├── tools/call 요청 디스패치
    ├── 격리된 백엔드 기능 실행
    └── 구조화된 결과(Content/isError) 반환
```

- 선들의 의미: 실선(──)과 가지선(├──, └──)은 MCP Tool 아키텍처 계층과 세부 제어 요소 간의 계층적 포함 관계를 나타냄

| 구성요소 | 책임 |
|:---|:---|
| 도구 명세 | 도구 이름, 설명 및 JSON Schema 기반 입력 매개변수 선언 |
| 입력 검증기 | 호스트 및 서버 레벨 인자 타입, 필수 필드 및 범위 유효성 검증 |
| 정책 집행기 | 실행 권한 확인, 테넌트 격리 및 비가역 액션 대상 HITL 통제 |
| 실행기 | 검증 완료된 도구 요청을 백엔드 비즈니스 로직으로 디스패치 실행 |
| 결과 처리기 | 실행 결과 콘텐츠 및 오류 상태(isError)를 JSON-RPC 응답으로 정규화 |

#### 한줄 요약
- **정책 집행기•입력 검증기** 활용 거쳐 허용 도구 실행

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **인간 개입(Human-in-the-Loop, HITL)**: 고위험 호출을 사람이 검토•승인하는 통제로 정의된다.
- **tools/list•tools/call**: MCP 서버의 도구 명세를 조회하고 선택한 도구를 실행하는 표준 요청으로 정의된다.
- **모델 컨텍스트 프로토콜 호스트(Model Context Protocol Host, MCP Host)**: 사용자 동의와 정책에 따라 도구 호출을 중개하는 AI 애플리케이션.
- **도구 목록•입력 스키마 요청(Tool List Request)**: 사용 가능한 행동과 인자 계약을 탐색하는 단계.
- **선택 가능한 도구 명세 제공(Tool Spec Provision)**: 서버가 허용 기능의 이름•설명•스키마를 반환하는 단계로 정의된다.
- **승인된 도구명•구조화 인자 확정(Tool Confirmation)**: 모델 제안과 사람 승인 결과로 호출 내용을 정하는 단계.
- **tools/call 요청(tools/call Request)**: 도구명과 JSON 구조화 인자를 서버에 전송하는 단계를 뜻하며, 복잡한 문제 해결과 동적 환경 적응에 필수적인 역할을 수행한다.
- **권한•입력 검증 후 백엔드 실행(Backend Execution)**: 서버가 주체•인자•정책을 재검증해 허용 행동만 수행하는 단계이다.

</details>

```text
[도구 명세 조회] (① 호스트가 tools/list 요청으로 사용 가능한 도구 및 JSON 스키마 탐색)
       │
       ▼
[호출 제안/승인] (② 모델의 도구 호출 제안 접수 및 고위험 액션 대상 사용자 HITL 승인)
       │
       ▼
[tools/call 요청] (③ 확정된 도구명과 JSON 구조화 인자를 담아 tools/call 요청 전송)
       │
       ▼
[서버 인가/재검증] (④ 서버 정책 집행기가 인자 타입 및 백엔드 실행 권한을 독립적으로 재검증)
       │
       ▼
[백엔드 실행/반환] (⑤ 격리된 백엔드 기능 실행 후 구조화된 결과(Content/isError) 반환)
```

- 분기 결과: 스키마 및 권한 재검증 통과 시 백엔드 실행, 유효성 오류 또는 사용자 거절 시 에러 반환

#### 한줄 요약
- **도구•인자 승인** 후 서버 재검증•실행

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **Resource**: 애플리케이션이 제공하고 모델이 문맥으로 읽는 데이터를 뜻하며, 복잡한 문제 해결과 동적 환경 적응에 필수적인 역할을 수행한다.
- **Tool**: 모델이 구조화된 인자로 호출해 외부 행동을 수행하는 기능.
- **Prompt**: 사용자가 선택해 작업 메시지와 입력 틀을 만드는 기능.
- **모델 컨텍스트 프로토콜 리소스(Model Context Protocol Resource, MCP Resource)**: URI로 식별한 읽기 문맥을 제공하는 MCP 기능으로 정의된다.
- **통합 자원 식별자(Uniform Resource Identifier, URI)**: 리소스를 고유하게 가리키는 주소 표기로 정의된다.

</details>

| 구분 | Resource | Tool | Prompt |
|:---|:---|:---|:---|
| 적용 기준 | 읽기 전용 문맥 데이터 제공 | 상태 변경 및 외부 액션 실행 | 사용자 프롬프트 템플릿 제공 |
| 핵심 특징 | 애플리케이션 제어 및 URI 기반 식별 | 모델 주도 호출 및 JSON 스키마 제안 | 사용자 선택 기반 파라미터 완성 |
| 한계 | 외부 상태 변경 불가 | 부작용 발생 위험 및 이중 검증 필수 | 수동 선택 필요 및 자율 실행 한계 |

#### 한줄 요약
- 외부 행동은 **Tool**, 읽기 문맥은 **Resource**

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **프롬프트 주입(Prompt Injection)**: 비신뢰 콘텐츠의 악성 지시로 도구 호출을 유도하는 공격을 지칭한다.
- **최소권한(Least Privilege)**: 도구에 업무 수행에 필요한 최소 범위의 권한만 부여하는 원칙이다.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 과도한 호출과 행동 범위 확대 | 도구 허용 목록 지정 및 최소 권한 원칙 적용 | 모델의 행동 반경 제한 및 비인가 실행 차단 |
| 프롬프트 주입과 무단 상태 변경 | 비신뢰 데이터 격리 및 고위험 액션 HITL 승인 | 간접 프롬프트 주입을 통한 권한 상승 방지 |
| 대상·인자 오류와 잘못된 부작용 | 서버 측 독립적 스키마 유효성 및 업무 규칙 재검증 | 파라미터 변조 방지 및 시스템 오실행 방지 |

#### 한줄 요약
- 상태 변경은 **대상 검증•HITL** 승인 후 실행

## Ⅶ. 결론

- **기술 위상/발전**: MCP Tool은 AI 모델이 안전하고 표준화된 방식으로 외부 시스템을 조작하는 **에이전틱 실행 인터페이스 표준**
- **실무 적용/통제**: JSON Schema 기반 스키마 선언, **서버 측 독립적 2차 인자 검증**, 비가역 고위험 액션 대상 **HITL 승인 및 감사 로그** 적용 필수

#### 한줄 요약
- **행동 부작용•위험도** 대상 따라 최소 권한•HITL 결정

