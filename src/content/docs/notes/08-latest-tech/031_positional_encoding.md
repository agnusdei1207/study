---
sidebar:
  order: 31
  label: "031. Positional Encoding (위치 인코딩)"
  badge:
    text: "기출 · 50%"
    variant: note
title: "Positional Encoding (위치 인코딩)"
date: "2026-09-15T10:30:00+09:00"
tags:
  - "notes-latest_tech"
weight: 31
extra:
  question_no: "031"
  source_status: "기출"
  source_history: "135회"
  priority: 50
  priority_note: "순서 정보 주입이 병렬 처리의 전제"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **위치 인코딩(Positional Encoding, PE)**: 순열 불변성(Permutation Invariance)을 가진 어텐션 연산에 토큰의 순서와 거리 정보를 주입하는 기법.
- **순열 불변성(Permutation Invariance)**: 입력 원소의 순서 변경이 연산 결과에 영향을 주지 않는 특성.

</details>

- 정의/개념: 어텐션에 순서•거리 신호를 결합하는 **위치 인코딩**
- 배경/필요성: 트랜스포머의 셀프 어텐션(Self-Attention) 연산은 입력 시퀀스 내 토큰들의 순서가 바뀌어도 동일한 어텐션 가중치와 결과를 산출하는 본질적인 순열 불변성(Permutation Invariance)을 지니고 있어 단어의 어순과 문맥적 선후 관계를 전혀 인식하지 못하는 치명적 한계가 존재함에 따라, 입력 임베딩에 고유한 위치 좌표 및 주파수 신호를 가산하거나 쿼리·키 벡터에 회전 변환을 적용하는 위치 인코딩(Positional Encoding) 기법을 도입하여 **토큰 고유의 절대적 순서 및 상대적 거리 관계 주입, 사인/코사인 기반 정적 인코딩부터 RoPE(Rotary Position Embedding) 및 ALiBi 등 동적 회전/편향 구조 확립, 장문맥 외삽(Extrapolation) 및 위치 보간(Interpolation) 역량 확보**를 달성할 필요

#### 한줄 요약
- **순서•상대 거리 정보**를 값에 실어 보내는 방식은 어텐션 구조를 그대로 두고 순서를 되살리는 대신, 학습에서 본 적 없는 위치로 갈수록 외삽 성능이 떨어지는 한계를 남긴다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **절대 위치 인코딩(Absolute PE)**: 토큰 순번에 대응하는 값을 입력 임베딩에 가산.
- **상대 위치 인코딩(Relative PE)**: 토큰 간 거리/방향을 어텐션 점수 또는 Q, K 변환에 반영.
- **외삽(Extrapolation)**: 학습 문맥 길이 외부의 위치 정보를 추론 시 연장 적용.
- **쿼리/키(Query/Key, Q/K)**: 조회 대상 기준 벡터 및 매칭 식별 벡터.

</details>

![Observable Plot으로 그린 위치별 고주파•저주파 사인 인코딩 값](/study/diagrams/positional-encoding.svg)

> 빠른 주기는 인접 위치를 세밀하게 구분하고, 느린 주기는 넓은 위치 범위의 변화를 표현함.

- **순서 주입**: 입력 임베딩 또는 Q•K 회전에 결합
- **표현 구분**: 절대 위치와 상대 거리 방식
- **외삽 한계**: 학습 문맥 초과 시 위치 오차 발생

#### 한줄 요약
- **절대•상대 위치 표현** 기반 참조 관계 구분

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **회전 위치 임베딩(RoPE)**: Q, K 벡터를 위치 기반으로 회전시켜 내적에 상대적 거리를 반영.
- **위치 보간(Position Interpolation)**: 긴 위치 인덱스를 학습 범위 내로 비례 압축하여 표현 확장.
- **주입 계층(Injection Layer)**: 위치 벡터를 임베딩에 가산하거나 회전값을 Q, K에 적용하는 계층.

</details>

```text
[위치 인코딩(Positional Encoding) 아키텍처]
├── [위치 인덱스 생성기 (Position Index)]
│   ├── 절대 순서 인덱스 (1D 시퀀스 토큰 위치)
│   └── 상대 거리 행렬 (토큰 쌍 간격 (i - j))
├── [위치 표현기 (Position Representation)]
│   ├── 정적 삼각함수 인코딩 (Sinusoidal PE)
│   ├── 학습 기반 절대 임베딩 (Learned PE)
│   └── 복소 평면 회전 변환 (RoPE / ALiBi)
├── [주입 계층 (Injection Layer)]
│   ├── 임베딩 가산 방식 (Input Embedding + PE)
│   └── 쿼리·키 회전 변환 (RoPE Q/K 곱연산)
└── [길이 확장기 (Length Extender)]
    ├── 위치 보간 (Linear/Dynamic NTK Interpolation)
    └── 장문맥 외삽 스케일링 (Context Extension)
```

- 선들의 의미: 실선(──)과 가지선(├──, └──)은 위치 인코딩 시스템의 계층 분할 및 세부 모듈 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 위치 인덱스 생성기 | 토큰의 1D 시퀀스 절대 위치 순서 및 토큰 쌍 상대 거리 산출 |
| 위치 표현기 | 삼각함수, 학습 파라미터, 복소수 회전 각도로 위치 신호 변환 |
| 주입 계층 | 입력 임베딩에 직접 가산하거나 쿼리·키 벡터에 회전 연산 적용 |
| 길이 확장기 | 선형 보간 및 NTK 스케일링을 통해 사전학습 초과 장문맥 지원 |

#### 한줄 요약
- **위치 인덱스•표현•주입•확장** 계층 구성

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **위치 신호**: 고정 함수, 학습 벡터, 회전값 등을 활용한 순서 및 거리 표현값.
- **임베딩 생성**: 위치 정보를 제외한 토큰 내용의 벡터 표현 생성 절차.
- **위치 산출**: 절대 순서 및 토큰 쌍 상대 거리 계산.
- **주입(Injection)**: 위치 신호를 내용 표현 또는 Q, K 변환에 결합.

</details>

```text
[원시 토큰 시퀀스]
       │
       ▼
[토큰 임베딩 벡터 생성] (① 어휘 사전 매핑을 통한 의미 벡터 추출)
       │
       ▼
[위치 인덱스 및 주파수 산출] (② 시퀀스 위치 $i$ 기반 회전 각도 및 주파수 행렬 계산)
       │
       ▼
[위치 정보 주입] (③ 임베딩 직접 가산 또는 Q·K 벡터 복소 회전 변환(RoPE) 적용)
       │
       ▼
[위치 인식 어텐션 연산 수행] (④ 내적 시 상대 거리 정보가 자동 반영되는 문맥 계산)
```

- 분기 결과: 위치 신호가 결합된 텐서가 셀프 어텐션 소프트맥스 연산으로 전달되어 어순 인식 완료

#### 한줄 요약
- **위치 신호** 활용 임베딩 또는 Q•K에 주입

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **상대 위치 방식**: 토큰 간 거리 관계를 반영하여 장문 일반화에 유리.
- **절대 위치 방식**: 토큰별 위치값을 입력에 명시적으로 가산.

</details>

| 구분 | 상대 위치 인코딩 | 절대 위치 인코딩 |
|:---|:---|:---|
| 적용 기준 | 거리 관계 및 장문 일반화 | 고정 길이 및 명시적 순서 |
| 핵심 특징 | 어텐션 점수 및 Q, K에 거리 반영 | 입력 표현에 위치값 가산 |
| 한계 | 구현 및 캐시 변환 복잡성 | 학습 최대 길이 밖 외삽 취약 |

#### 한줄 요약
- 고정 길이는 **절대 위치**, 장문 거리는 **상대 위치**

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **위치 외삽 오차**: 학습 길이 밖에서 위치 신호 왜곡 및 해석 불정확 현상.
- **단거리 해상도**: 인접 토큰 간 위치 차이를 구분하는 정밀도.
- **위치 인덱스 일치**: 학습 및 추론 시 동일 토큰에 일관된 위치 규칙 적용.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 학습 길이 초과 시 위치 외삽 오차 | 위치 보간(PI) 및 NTK-aware RoPE 스케일링 적용 | 장문 추론 시 어텐션 집중 붕괴 완화 |
| 위치 확장에 따른 단거리 해상도 저하 | 주파수별 가중치를 달리하는 YaRN 기법 도입 | 단거리 문맥 정밀도 유지 및 장문 확장 양립 |
| 학습-추론 간 위치 인덱스 불일치 | KV 캐시 오프셋 기반 위치 인덱스 일치 단위 테스트 | 토큰 순서 역전 및 캐시 인덱싱 오류 원천 차단 |

#### 한줄 요약
- **길이별 평가•인덱스 일치 시험** 기반 위치 왜곡 방지

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **위치 표현 선택 기준**: 고정 길이에는 절대 위치를, 장문 외삽에는 RoPE와 위치 보간을 적용하는 설계 원칙.

</details>

- **기술 위상/발전**: 순열 불변의 어텐션 메커니즘에 기하학적 순서성과 거리 개념을 부여한 **트랜스포머의 필수 백본 기법**이며, 삼각함수 가산에서 RoPE 및 YaRN 중심의 동적 주파수 회전 방식으로 진화
- **실무 적용/통제**: 초장문맥 LLM 서빙 시 사전학습된 **RoPE 베이스 주파수 보정**과 KV Cache 위치 오프셋 무결성을 확보하여 장거리 문맥 해석의 정확성 유지

#### 한줄 요약
- **문맥 길이•외삽 요구** 대상 따라 위치 표현 결정

