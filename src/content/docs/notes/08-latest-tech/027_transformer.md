---
sidebar:
  order: 27
  label: "027. Transformer (트랜스포머)"
  badge:
    text: "기출 · 70%"
    variant: note
title: "Transformer (트랜스포머)"
date: "2026-09-15T10:30:00+09:00"
tags:
  - "notes-latest_tech"
weight: 27
extra:
  question_no: "027"
  source_status: "기출"
  source_history: "135회, 137회"
  priority: 70
  priority_note: "어텐션 기반 구조가 반복 출제되는 기반"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **트랜스포머(Transformer)**: 셀프 어텐션으로 토큰 관계를 직접 계산하고 피드포워드 계층으로 표현을 변환하는 신경망 구조로 정의된다.
- **순환 신경망(Recurrent Neural Network, RNN)**: 이전 은닉 상태를 다음 시점으로 전달해 시퀀스를 처리하는 구조이다.

</details>

- 정의/개념: 셀프 어텐션으로 토큰 관계를 계산하는 **트랜스포머**
- 배경/필요성: 순환 신경망(RNN, LSTM, GRU) 기반 시퀀스 모델링은 시점 $t$의 연산이 이전 시점 $t-1$의 은닉 상태에 종속되는 순차적(Sequential) 구조로 인해 GPU 병렬 학습이 불가능하고, 시퀀스 길이가 길어질수록 장기 의존성(Long-term Dependency) 손실 및 기울기 소실/폭발이 발생하는 근본적 한계가 존재함에 따라, 순환 구조를 전면 제거하고 입력 시퀀스 내 모든 토큰 쌍의 상호 연관성을 한 번에 행렬 연산으로 계산하는 어텐션(Attention) 메커니즘 전용 신경망인 트랜스포머(Transformer) 아키텍처를 도입하여 **셀프 어텐션(Self-Attention) 및 멀티 헤드 어텐션(MHA)을 통한 상수 시간($O(1)$) 장거리 문맥 참조 경로 확보, GPU 하드웨어에 최적화된 완전 병렬 학습(Full Parallelization) 구현, 현대 거대 언어 모델(LLM) 및 멀티모달 AI의 표준 백본 확립**을 달성할 필요

#### 한줄 요약
- **셀프 어텐션**은 순차 의존을 없애 병렬 학습과 상수 길이 경로를 얻는 대신 연산량이 시퀀스 길이의 제곱으로 늘므로, 속도의 이득을 문맥 길이 비용으로 되갚는 구조다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **셀프 어텐션(Self-Attention)**: 같은 시퀀스 안의 토큰들이 서로의 관련도를 계산하여 문맥 정보를 결합하는 연산이다.
- **크로스 어텐션(Cross-Attention)**: 한 시퀀스의 쿼리가 다른 시퀀스의 키와 밸류를 참조하여 정보를 연결하는 연산.
- **GPU(Graphics Processing Unit)**: 대규모 행렬곱을 병렬로 처리하여 트랜스포머 학습을 가속하는 연산 장치로 정의된다.

</details>

- **셀프 어텐션**: 동일 시퀀스의 문맥 관계 결합
- **크로스 어텐션**: 외부 시퀀스의 문맥 관계 결합
- **병렬 학습**: 순환 구조 제거와 GPU 행렬 연산
- **마스크 제어**: 미래•패딩 토큰 참조 제한
- **연산 복잡도**: 모든 토큰 쌍 계산의 O(N²) 비용

#### 한줄 요약
- 학습은 **GPU 병렬 처리**, 자기회귀 생성은 순차 처리

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **피드포워드 신경망(Feed-Forward Network, FFN)**: 어텐션을 거친 각 토큰 표현을 위치별로 비선형 변환하는 계층.
- **잔차 연결(Residual Connection)**: 계층 입력을 변환 결과에 더하여 원래 정보와 기울기 흐름을 보존하는 연결이다.
- **층 정규화(Layer Normalization)**: 각 토큰 표현의 특성 차원을 정규화하여 학습을 안정화하는 연산.

</details>

```text
[트랜스포머(Transformer) 아키텍처]
├── [입력 표현 계층 (Input Representation)]
│   ├── 토큰 임베딩 (Token Embedding)
│   └── 위치 인코딩 (Positional Encoding / RoPE)
├── [어텐션 서브블록 (Attention Sub-layer)]
│   ├── 멀티 헤드 셀프 어텐션 (MHA / GQA)
│   ├── 마스크드 어텐션 (인과적 마스킹)
│   └── 잔차 연결 및 층 정규화 (Add & Norm)
├── [피드포워드 서브블록 (FFN Sub-layer)]
│   ├── 2계층 선형 변환 및 비선형 활성화 (GELU/SwiGLU)
│   └── 잔차 연결 및 층 정규화 (Add & Norm)
└── [출력 계층 (Output Linear & Softmax)]
    ├── 선형 투영 (Linear Projection)
    └── 소프트맥스 확률분포 (Next Token Probabilities)
```

- 선들의 의미: 실선(──)과 가지선(├──, └──)은 트랜스포머 신경망의 계층 분할 및 서브블록 모듈 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 입력 표현 계층 | 토큰 임베딩과 위치 정보를 결합하여 초기 벡터 시퀀스 생성 |
| 어텐션 서브블록 | 다중 헤드 셀프 어텐션 기반 전역 문맥 상호작용 및 관계 추출 |
| 피드포워드 서브블록 | 각 토큰 위치별 비선형 특징 확장 및 잠재 지식 저장·변환 |
| 출력 계층 | 선형 투영 및 소프트맥스를 거쳐 다음 토큰 생성 확률 계산 |

#### 한줄 요약
- **어텐션•FFN•잔차•정규화** 블록 반복

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **쿼리•키•밸류(Query•Key•Value, Q•K•V)**: 검색•비교•전달 정보를 각각 나타내는 어텐션 벡터이다.
- **위치 인코딩(Positional Encoding)**: 병렬 처리되는 토큰 표현에 순서와 상대 위치 정보를 더하는 표현.
- **토큰•위치 표현 구성**: 토큰 임베딩과 위치 인코딩을 결합하는 절차이다.
- **Q•K•V 관계 가중치 계산**: 쿼리•키 유사도로 참조 가중치를 산출하는 절차이다.
- **어텐션 문맥 결합**: 계산된 가중치에 따라 밸류를 합산해 문맥 표현을 만드는 절차로 정의된다.
- **FFN 위치별 변환**: 각 토큰 표현을 비선형 변환하는 절차이다.
- **잔차•층 정규화**: 계층 입력을 더하고 특성 차원을 정규화해 정보와 기울기 흐름을 안정화하는 절차로 정의된다.

</details>

```text
[입력 시퀀스 및 토큰화]
       │
       ▼
[토큰 임베딩 및 위치 인코딩 결합] (① 토큰 의미 벡터와 순서 정보 결합 초기 표현 형성)
       │
       ▼
[Q·K·V 선형 투영 및 스케일드 닷 프로덕트] (② 토큰 쌍 유사도 행렬 계산 및 소프트맥스 가중치 산출)
       │
       ▼
[멀티 헤드 어텐션 문맥 집약] (③ 다중 관점 어텐션 가중합으로 문맥 벡터 합성)
       │
       ▼
[잔차 연결 및 층 정규화 (Add & Norm)] (④ 입력 보존 및 특성 차원 정규화로 학습 안정화)
       │
       ▼
[위치별 피드포워드 신경망 (FFN)] (⑤ 비선형 활성화 적용 고차원 잠재 특징 변환)
       │
       ▼
[출력 선형 투영 및 소프트맥스] (⑥ 어휘 사전 전역 다음 토큰 확률분포 생성)
```

- 분기 결과: 학습 시 완전 병렬 행렬 연산으로 역전파 수행, 추론 시 인과적 마스크 및 KV 캐시를 통한 토큰 순차 생성 완료

#### 한줄 요약
- **Q•K•V 어텐션•FFN** 기반 문맥 표현 갱신

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **BERT(Bidirectional Encoder Representations from Transformers)**: 양방향 트랜스포머 인코더를 사전학습하여 입력 문맥 표현을 만드는 모델.

</details>

- **트랜스포머**•**RNN** 병렬 관계와 순차 상태로 구분
- **BERT** 기반 인코더로 양방향 문맥 표현 학습

| 구분 | 트랜스포머 | RNN 계열 |
|:---|:---|:---|
| 적용 기준 | 긴 문맥의 병렬 학습•확장 | 짧은 스트림의 순차 상태 처리 |
| 핵심 특징 | 어텐션으로 토큰 관계 직접 계산 | 은닉 상태로 정보 순차 전달 |
| 한계 | 문맥 길이의 O(N²) 비용 | 병렬화 제한•장거리 정보 소실 |

#### 한줄 요약
- 순차 상태는 **RNN**, 관계 병렬 계산은 **트랜스포머**

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **희소 어텐션(Sparse Attention)**: 일부 토큰 관계만 계산해 장문 연산량을 줄이는 방식이다.
- **키-값 캐시(Key-Value Cache, KV Cache)**: 자기회귀 생성 중 이전 토큰의 어텐션 키와 값을 저장하여 반복 계산을 줄이는 기법.
- **추측 디코딩(Speculative Decoding)**: 작은 모델이 제안한 여러 토큰을 큰 모델이 한 번에 검증하여 생성 지연을 줄이는 방식.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 긴 문맥의 O(N²) 메모리•연산량 | 청크•윈도•희소 어텐션 및 FlashAttention 적용 | 연산 복잡도 완화 및 GPU 메모리 점유 최적화 |
| 위치 표현의 학습 길이 외 일반화 한계 | 상대적 위치 인코딩 및 RoPE(Rotary Position) 도입 | 학습 길이 초과 장문 시퀀스 순서 왜곡 방지 |
| 자기회귀 디코딩 순차 지연 | PagedAttention 기반 KV Cache 및 추측 디코딩 연계 | 토큰당 추론 지연 단축 및 서빙 처리량 증대 |
| 마스크 오류로 인한 정보 누출 | 인과적 어텐션 마스크 단위 단위 테스트 및 검증 | 학습-추론 조건 일치 및 미래 토큰 참조 차단 |

#### 한줄 요약
- **희소 어텐션•KV Cache** 기반 장문•생성 비용 절감

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **시퀀스 구조 선택 기준**: 긴 문맥의 병렬 관계 계산에는 트랜스포머를, 짧은 스트림의 순차 상태 유지에는 RNN을 적용하는 기준.

</details>

- **기술 위상/발전**: 자연어 처리를 넘어 컴퓨터 비전(ViT), 음성, 멀티모달 전 영역을 단일 구조로 통합한 **현대 AI의 표준 백본**이며, MoE 및 선형 어텐션 구조와 융합되어 진화 중
- **실무 적용/통제**: 시퀀스 길이 $N$에 따른 $O(N^2)$ 메모리 병목을 극복하기 위해 FlashAttention, RoPE, **KV 캐시 최적화** 기법을 필수 결합하여 추론 효율성 극대화 달성

#### 한줄 요약
- **문맥 길이•병렬성•O(N²) 비용** 대상 따라 구조 결정

