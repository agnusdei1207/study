---
sidebar:
  order: 33
  label: "033. Context Window (컨텍스트 윈도우)"
  badge:
    text: "기출 · 50%"
    variant: note
title: "Context Window (컨텍스트 윈도우)"
date: "2026-09-15T10:30:00+09:00"
tags:
  - "notes-latest_tech"
weight: 33
extra:
  question_no: "033"
  source_status: "기출"
  source_history: "138회"
  priority: 50
  priority_note: "장문 처리 한계와 비용 설계가 중요"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **컨텍스트 윈도우(Context Window)**: 모델이 한 번의 추론에서 참조할 수 있는 입력과 출력 토큰의 최대 범위.
- **토큰 예산(Token Budget)**: 컨텍스트 윈도우 안에서 지시•대화•근거 문서•생성 결과에 배분할 수 있는 토큰 수를 의미한다.

</details>

- 정의/개념: 한 번의 추론에서 입력•출력이 공유하는 **토큰 한도**
- 배경/필요성: 거대 언어 모델(LLM)이 단일 추론 세션에서 입력(Prompt)과 출력(Generation)으로 처리할 수 있는 최대 토큰 한도가 제한되어 있을 뿐 아니라, 문맥 길이가 증가함에 따라 $O(N^2)$의 연산 복잡도와 시퀀스 길이에 비례하는 KV Cache 메모리 점유 및 첫 토큰 생성 지연(TTFT: Time to First Token)이 급격히 증가하는 물리적 한계가 발생함에 따라, 한정된 토큰 용량 내에서 시스템 프롬프트, 대화 이력, RAG 검색 근거 및 출력 공간을 엄밀히 분할 관리하는 컨텍스트 윈도우(Context Window) 예산 최적화 아키텍처를 도입하여 **입력과 출력 간의 토큰 예산(Token Budget) 분할 및 선제적 출력 버퍼 예약, 핵심 문맥 선별 및 중복 제거를 통한 인출 정확도 보존, 프롬프트 캐싱(Prompt Caching)과 GQA 기반의 추론 메모리 및 서빙 지연 최적화**를 달성할 필요

#### 한줄 요약
- 입력과 출력이 하나의 **토큰 예산**을 나눠 쓰므로 근거를 더 넣을수록 답변 여지가 줄어, 문맥 설계는 무엇을 담을지가 아니라 무엇을 덜어낼지의 문제가 된다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **어텐션(Attention)**: 입력 토큰 사이의 관련도를 계산하여 현재 처리에 필요한 문맥 정보를 결합하는 연산.
- **첫 토큰 지연(Time to First Token, TTFT)**: 요청 전송부터 모델의 첫 출력 토큰을 받기까지 걸리는 시간.
- **키-값 캐시(Key-Value Cache, KV Cache)**: 이전 토큰의 키(Key, K)•밸류(Value, V) 연산 결과를 저장하여 후속 생성에서 재사용하는 메모리이다.

</details>

![Observable Plot으로 그린 컨텍스트 길이별 어텐션 연산량과 KV 캐시 증가 개념도](/study/diagrams/context-resource-growth.svg)

> 파란 전체 어텐션 선은 토큰 길이의 제곱으로, 붉은 KV 캐시 선은 길이에 비례해 증가하며 두 계열은 절대량이 아닌 증가율 비교다.

- **토큰 예산 공유**: 지시•대화•검색•출력의 한도 경쟁
- **프리필 비용**: 입력 길이에 따른 어텐션•TTFT 증가
- **KV Cache 비용**: 생성 길이에 비례한 메모리 증가

#### 한줄 요약
- 문맥 확대 시 **어텐션•KV Cache 비용** 증가

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **프리필(Prefill)**: 입력 토큰 전체의 어텐션을 병렬 계산하여 생성에 사용할 K•V 상태를 만드는 단계로 정의된다.
- **문맥 선택기**: 토큰 예산과 관련도에 따라 지시•근거를 포함•압축•제외하는 구성요소로 정의된다.
- **출력 예약**: 최대 길이에서 예상 답변 토큰 수를 먼저 확보한 뒤 남은 범위를 입력에 할당하는 방식이다.

</details>

```text
[컨텍스트 윈도우 관리 아키텍처]
├── [길이 한도 정책 (Context Limit Policy)]
│   ├── 총 토큰 한도 정의 (Max Context Length)
│   └── 입력 및 출력 비율 설정
├── [토큰 예산 관리자 (Token Budget Allocator)]
│   ├── 출력 버퍼 선제 예약 (Output Reservation)
│   └── 세그먼트별 토큰 쿼터 할당
├── [문맥 선택기 (Context Selector & Pruner)]
│   ├── 시스템 프롬프트 및 최근 대화 고정
│   └── RAG 검색 근거 압축 및 슬라이딩
└── [추론 런타임 엔진 (Inference Engine)]
    ├── 프리필 어텐션 연산 (Prefill Phase)
    └── KV Cache 메모리 할당 및 재사용
```

- 선들의 의미: 실선(──)과 가지선(├──, └──)은 컨텍스트 윈도우 관리 아키텍처의 계층 분할 및 세부 모듈 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 길이 한도 정책 | 모델 아키텍처 및 GPU 메모리 기반 총 토큰 한도 정의 |
| 토큰 예산 관리자 | 예상 답변을 위한 출력 버퍼 선제 확보 및 세그먼트별 예산 배분 |
| 문맥 선택기 | 중요도 및 관련도 기반 지시·대화·근거의 포함·압축·제외 결정 |
| 추론 런타임 엔진 | 입력 토큰 프리필 병렬 연산 및 KV Cache 메모리 동적 할당 |

#### 한줄 요약
- **예산•문맥 선택•프리필•KV Cache** 계층 구성

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **토큰량 산정**: 입력 문자열을 모델 토크나이저로 변환하여 지시•근거•대화가 차지할 실제 토큰 수를 계산하는 과정.
- **문맥 배치**: 지시 우선순위와 근거 관련도를 고려하여 선택한 내용을 컨텍스트 안에 정렬하는 과정이다.
- **질의•근거 토큰량 산정**: 모든 입력을 공통 토큰 단위로 환산해 총 사용량을 계산하는 절차로 정의된다.
- **출력 예약•입력 예산 할당**: 예상 출력 공간을 먼저 확보하고 남은 토큰 예산을 입력에 배분하는 절차.
- **핵심 문맥 선택•배치**: 지시 우선순위와 근거 관련도에 따라 문맥을 압축하고 정렬하는 절차.
- **어텐션 계산•KV Cache 생성**: 입력을 프리필하고 후속 생성에 재사용할 키•밸류 상태를 저장하는 절차이다.
- **잔여 예산 내 응답 생성**: 남은 출력 토큰 한도 안에서 종료 신호까지 결과를 생성하는 절차.

</details>

```text
[질의 및 참조 문서 입력]
       │
       ▼
[토큰량 산정 및 출력 버퍼 선제 예약] (① 지시·대화·문서 토큰 수 측정 및 답변용 출력 공간 확보)
       │
       ▼
[문맥 압축 및 우선순위 배치] (② 관련도 순위화, 중복 제거, 핵심 지시 양끝 배치)
       │
       ▼
[프리필 어텐션 및 KV 캐시 생성] (③ 프롬프트 캐싱 확인 후 입력 시퀀스 병렬 연산 수행)
       │
       ▼
[잔여 예산 내 자기회귀 토큰 생성] (④ 할당된 최대 출력 토큰 범위 내에서 단계별 디코딩)
```

- 분기 결과: 생성 완료 토큰 시퀀스가 사용자 애플리케이션으로 반환되고 KV 캐시 메모리 반환 또는 다음 턴 캐싱 수행

#### 한줄 요약
- **출력 예약•문맥 선택•KV Cache** 후 응답 생성

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **롱 컨텍스트(Long Context)**: 넓은 윈도우에 원문을 직접 배치하여 문서 전체의 상호 관계를 처리하는 방식.
- **검색 증강 생성(Retrieval-Augmented Generation, RAG)**: 질의와 관련된 외부 문서 조각만 검색하여 모델 입력에 근거로 제공하는 방식.
- **혼합 방식**: 핵심 원문은 직접 넣고 최신•세부 근거는 검색하여 전역 문맥과 선택 근거를 결합하는 방식이다.

</details>

- 전체 상호 참조 범위와 외부 근거 선택 여부에 따른 구분

| 구분 | 롱 컨텍스트 | RAG | 혼합 방식 |
|:---|:---|:---|:---|
| 적용 기준 | 문서 전체 상호 참조 | 대규모 지식 일부 근거 탐색 | 전역 문맥•최신 근거 동시 요구 |
| 핵심 특징 | 원문을 넓은 윈도우에 직접 배치 | 질의 관련 조각 검색 후 주입 | 핵심 원문과 검색 근거 선별 결합 |
| 한계 | 프리필•KV 캐시 비용과 인출 저하 | 검색 누락•문서 조각 단절 | 예산 배분•중복 제거 복잡성 |

#### 한줄 요약
- 전체 상호 참조는 **롱 컨텍스트**, 일부 근거는 **RAG**

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **그룹 쿼리 어텐션(Grouped-Query Attention, GQA)**: 여러 쿼리 헤드가 K•V 헤드를 그룹별로 공유하여 캐시 사용량을 줄이는 구조이다.
- **프롬프트 캐싱(Prompt Caching)**: 여러 요청에서 동일한 프롬프트 접두사의 프리필 결과를 재사용하는 기법이다.
- **중간 위치 인출 저하**: 긴 문맥의 가운데에 놓인 중요한 정보가 시작이나 끝의 정보보다 덜 활용되는 현상.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 장문 입력 시 프리필 지연(TTFT) 급증 | 문맥 선택기 기반 청킹·압축 및 프롬프트 캐싱 도입 | 프리필 계산량 절감 및 첫 토큰 응답 속도 향상 |
| 시퀀스 길이에 비례한 KV 캐시 메모리 폭증 | GQA 모델 채택 및 FP8/INT4 KV 캐시 양자화 적용 | GPU 메모리 절감 및 동시 세션 서빙 용량 증대 |
| 긴 문맥 중간부 정보 인출 누락(Lost in the Middle) | 핵심 지시사항의 프롬프트 전단/후단 재배치 | 장문맥 내 핵심 정보 회수율 및 응답 정확도 개선 |
| 입력 과다로 인한 응답 중도 절단(Truncation) | 예상 생성량 기준 선제적 출력 버퍼 예약 정책 수립 | 답변 완결성 보장 및 모델 출력 잘림 방지 |

#### 한줄 요약
- **문맥 선별•출력 예약•캐싱** 기반 비용 통제

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **문맥 처리 방식 선택 기준**: 전체 상호 참조에는 롱 컨텍스트를, 일부 근거에는 RAG를, 전역 문맥과 최신 근거 결합에는 혼합 방식을 적용하는 기준.

</details>

- **기술 위상/발전**: 모델의 인지 용량과 서빙 하드웨어 리소스를 결정짓는 **엔터프라이즈 AI 시스템의 핵심 물리적 제약이자 최적화 경계**이며, 백만 토큰급 초장문 모델과 RAG의 하이브리드 결합으로 진화
- **실무 적용/통제**: 중간 위치 정보 손실 방지를 위한 양끝 배치, **프롬프트 캐싱**을 통한 TTFT 단축, 출력 토큰 선제 예약 정책을 필수로 결합하여 서빙 안정성 확보

#### 한줄 요약
- **상호 참조 범위•비용** 대상 따라 문맥 처리 방식 결정

