---
sidebar:
  order: 24
  label: "024. Large Language Model (대규모 언어 모델, LLM)"
  badge:
    text: "기출 · 85%"
    variant: note
title: "Large Language Model (대규모 언어 모델, LLM)"
date: "2026-09-15T10:30:00+09:00"
tags:
  - "notes-latest_tech"
weight: 24
extra:
  question_no: "024"
  source_status: "기출"
  source_history: "135회, 137회, 138회"
  priority: 85
  priority_note: "생성형 AI 전반을 연결하는 기반 주제"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **대규모 언어 모델(Large Language Model, LLM)**: 대규모 말뭉치에서 토큰 간 관계를 학습하여 여러 언어 과업을 이해하고 생성하는 모델이다.
- **말뭉치(Corpus)**: 언어 패턴과 지식을 학습하도록 수집•정제한 대규모 텍스트 집합이다.

</details>

- 정의/개념: 대규모 말뭉치의 토큰 관계를 사전학습한 **LLM**
- 배경/필요성: 자연어 처리(NLP) 분야에서 과업별(분류, 번역, 요약 등)로 독립된 전용 모델을 개별 구축하던 기존 방식은 막대한 라벨링 데이터 구축 비용과 모델 간 지식 전이의 불가능이라는 근본적 한계가 존재함에 따라, 트랜스포머(Transformer) 아키텍처를 기반으로 웹 스케일의 대규모 텍스트 말뭉치에서 자기지도 학습(Self-supervised Learning)을 통해 범용 언어 지식과 추론 패턴을 사전학습한 대규모 언어 모델(LLM: Large Language Model)을 도입하여 **단일 모델 기반의 제로샷(Zero-shot)/퓨샷(Few-shot) 문맥 학습(In-context Learning) 역량 확보, 파인튜닝 및 프롬프트 엔지니어링을 통한 범용 과업 적응성 극대화, 자연어 기반 AI 에이전트 및 생성형 지능의 핵심 파운데이션 확립**을 달성할 필요

#### 한줄 요약

- LLM은 사전학습 한 번으로 과업별 학습 비용을 없애는 대신 지식이 학습 시점에 고정되므로, 최신성이 필요한 영역은 재학습이 아니라 검색·도구로 바깥에서 메우는 편이 싸다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **자기지도 사전학습(Self-supervised Pre-training)**: 텍스트 자체에서 예측 목표를 만들어 별도 정답 표기 없이 언어 표현을 학습하는 방식이다.
- **문맥 학습(In-context Learning)**: 모델 매개변수를 바꾸지 않고 입력의 지시와 예시로 과업을 수행하는 능력이다.
- **확률적 생성**: 다음 토큰 확률분포에서 출력을 선택해 결과가 달라질 수 있는 생성 방식이다.

</details>

- 별도 정답 없이 언어 패턴을 배우는 **자기지도 사전학습**
- 매개변수 변경 없이 지시·예시로 적응하는 **문맥 학습**
- 출력 변동과 사실성·편향 한계의 **확률적 생성**

#### 한줄 요약

- 다음 토큰 예측은 **최신성•사실성•공정성 미보장**

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **어텐션(Attention)**: 토큰 간 관련도에 따라 다른 토큰의 정보를 가중 결합하여 문맥 표현을 만드는 연산이다.
- **피드포워드 신경망(Feed-Forward Network, FFN)**: 어텐션을 거친 각 토큰 표현을 위치별로 비선형 변환하는 신경망.
- **토크나이저(Tokenizer)**: 입력 문자열을 모델 어휘의 토큰 식별자 열로 변환하는 구성요소이다.

</details>

```text
[대규모 언어 모델(LLM) 아키텍처]
├── [토크나이저 (Tokenizer)]
│   ├── BPE/WordPiece 서브워드 분절
│   └── 텍스트 문자열 ↔ 토큰 ID 매핑
├── [임베딩 계층 (Embedding Layer)]
│   ├── 토큰 임베딩 (Token Representation)
│   └── 위치 임베딩 (Positional Encoding / RoPE)
├── [트랜스포머 블록 (Transformer Blocks × N)]
│   ├── 다중 헤드 어텐션 (Multi-Head Attention)
│   ├── 정규화 (LayerNorm / RMSNorm)
│   └── 피드포워드 신경망 (FFN / SwiGLU)
└── [출력 계층 (Output Layer)]
    ├── 선형 투영 (Linear Projection / Unembedding)
    └── 소프트맥스 (다음 토큰 조건부 확률분포)
```

- 선들의 의미: 실선(──)과 가지선(├──, └──)은 최상위 대규모 언어 모델 아키텍처에서 토크나이저, 임베딩 계층, 트랜스포머 블록, 출력 계층으로 이어지는 계층 구조를 나타냄

| 구성요소 | 책임 |
|:---|:---|
| 토크나이저 | 문자열 분절 및 토큰 식별자(Token ID) 변환 |
| 임베딩 계층 | 토큰 및 위치 정보를 연속 벡터 공간으로 투영 |
| 트랜스포머 블록 | 다중 헤드 어텐션 및 FFN 기반 심층 문맥 표현 갱신 |
| 출력 계층 | 어휘 사전문맥 선형 변환 및 다음 토큰 확률 산출 |

#### 한줄 요약

- **토크나이저•임베딩•트랜스포머•출력 계층** 구성

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **키-값 캐시(Key-Value Cache, KV Cache)**: 자기회귀 생성 중 이전 토큰의 어텐션 키와 값을 저장하여 중복 계산을 줄이는 저장소로 정의된다.
- **자기회귀 생성(Autoregressive Generation)**: 지금까지의 입력과 생성 토큰을 조건으로 다음 토큰을 하나씩 예측하는 방식.

</details>

```text
[프롬프트 입력 및 서브워드 토큰화] (① 텍스트 문자열을 BPE/WordPiece 알고리즘으로 분절하여 토큰 ID 변환)
│
▼
[임베딩 투영 및 위치 인코딩] (② 토큰 벡터와 RoPE 등 위치 정보를 결합해 연속 다차원 공간에 매핑)
│
▼
[KV 캐시 조회 및 트랜스포머 연산] (③ 이전 토큰의 어텐션 KV Cache를 로드하고 다중 헤드 어텐션 및 FFN 계층 통과)
│
▼
[로짓 산출 및 다음 토큰 샘플링] (④ 소프트맥스 확률분포 기반으로 온도(Temperature) 및 Top-p 적용 다음 토큰 선택)
│
▼
[KV 캐시 갱신 및 종료 판정] (⑤ 생성된 토큰의 KV 값을 캐시에 추가하고 EOS 토큰 또는 최대 길이에 도달할 때까지 반복)
```

- 분기 결과: 정상 종료(EOS) 도달 시 최종 완성 텍스트 디토큰화 반환, 최대 토큰 초과 시 길이 제한 플래그와 함께 부분 텍스트 반환

#### 한줄 요약

- **KV Cache•자기회귀 생성** 기반 다음 토큰 반복 예측

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **디코더형 모델**: 앞선 토큰을 조건으로 다음 토큰을 생성하여 개방형 생성에 적합한 구조.
- **인코더형 모델**: 양방향 문맥을 반영해 분류•검색에 적합한 구조이다.
- **인코더-디코더형 모델**: 입력 표현을 조건으로 디코더가 출력 열을 생성하는 구조이다.

</details>

- 모델 아키텍처 유형별 비교

| 모델 구조 | 디코더형 | 인코더형 | 인코더-디코더형 |
|:---|:---|:---|:---|
| 적용 기준 | 개방형 생성 중심 | 입력 이해 중심 | 입력·출력 변환 중심 |
| 핵심 특징 | 자기회귀 생성 | 양방향 문맥 표현 학습 | 입력 인코딩 후 조건부 생성 |
| 한계 | 환각·긴 생성 비용 | 직접 생성 능력 제한 | 구조·추론 비용 증가 |

#### 한줄 요약

- 생성은 **디코더형**, 이해는 인코더형, 변환은 인코더-디코더형

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **검색 증강 생성(Retrieval-Augmented Generation, RAG)**: 외부에서 검색한 최신•권한별 근거를 모델 문맥에 넣어 답변을 생성하는 방식.
- **환각(Hallucination)**: 근거나 확인 가능한 사실과 다른 내용을 그럴듯하게 생성하는 현상이다.
- **문맥 압축**: 긴 입력에서 목표에 필요한 정보만 요약•선별하여 토큰과 추론 비용을 줄이는 기법.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 학습 이후 지식의 최신성 부족 | RAG 기반 권한별 검색 근거 | 근거성 및 최신성 보완 |
| 확률적 생성에 따른 환각 | 근거 검증 및 구조화 출력 강제 | 사실 오류 반영 차단 |
| 긴 문맥과 생성의 추론 비용 | 문맥 압축, KV Cache, SLM 라우팅 | 지연 및 토큰 컴퓨팅 절감 |
| 민감 데이터의 모델 유출 | 입력 최소화, 접근통제, 출력 검사 | 비밀정보 노출 방지 |

#### 한줄 요약

- **RAG** 기반 권한이 있는 근거만 문맥에 넣어 최신성 보완

## Ⅶ. 결론

- **기술 위상/발전**: 자기지도 사전학습과 문맥 내 학습을 통해 현대 인공지능 패러다임을 범용 인지 및 자율 에이전트로 대전환시킨 핵심 파운데이션으로 안착
- **실무 적용/통제**: RAG 및 도구 호출 연계를 통한 지식 최신성 확보, KV Cache 관리 및 SLM 라우팅 기반 추론 비용 최적화 필수

#### 한줄 요약

- **최신성•오류 피해•비용** 대상 따라 보완 방식을 결정
