---
sidebar:
  order: 5
  label: "005. 서브네팅•CIDR"
  badge:
    text: "미출 · 30%"
    variant: note
title: "서브네팅•CIDR (Subnetting CIDR)"
date: "2026-09-07T14:00:00+09:00"
tags:
  - "notes-network"
weight: 5
extra:
  question_no: "5"
  source_status: "미출"
  source_history: ""
  priority: 30
  priority_note: "설명•계산형: CIDR 주소 설계의 필수 기반"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **Subnetting (서브네팅)**: 단일 네트워크 주소 공간을 호스트 비트를 빌려 복수의 더 작은 서브넷(Subnet)으로 분할하는 기술.
- **CIDR (Classless Inter-Domain Routing)**: 기존의 클래스(Class A/B/C) 개념을 폐지하고, 슬래시 표기법(`/N`)을 사용하여 필요한 크기만큼 유연하게 주소를 할당하고 집약(Supernetting)하는 라우팅 기술.

</details>

- 정의/개념: IP 주소 공간의 낭비를 최소화하고 라우팅 테이블 크기를 최적화하기 위해 **네트워크를 유연하게 분할(Subnetting)하고 공통 프리픽스로 집약(CIDR)하는 주소 관리 기술**
- 배경/필요성: 과거 고정된 클래스풀(Classful: Class A/B/C) 주소 체계로 인한 심각한 IP 주소 낭비(예: 300대 필요 시 Class B 65,534개 할당)와 전 세계 인터넷 백본 라우터의 BGP 라우팅 테이블 엔트리 폭증 문제를 해결하기 위해, 비트 단위의 가변 길이 서브넷 마스크(VLSM)로 필요한 크기만큼 주소를 분할하는 서브네팅과 연속된 서브넷 블록을 상위 프리픽스로 집약(Supernetting)하는 CIDR(Classless Inter-Domain Routing) 체계를 도입하여 **IP 주소 자원의 활용 효율 극대화, 브로드캐스트 도메인 격리 및 인터넷 코어 라우팅 테이블 축약**을 달성할 필요

#### 한줄 요약
- VLSM 기반의 미세 주소 분할과 CIDR 프리픽스 집약을 통해 주소 효율과 라우팅 확장성을 확보한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **VLSM (Variable Length Subnet Mask)**: 동일 네트워크 내에서 서브넷마다 서로 다른 길이의 서브넷 마스크를 적용하여 호스트 요구량에 최적화하는 기법.
- **Supernetting (경로 집약 / Route Aggregation)**: 연속된 복수의 서브넷 경로를 공통 상위 프리픽스를 가진 단일 경로로 축약하여 광고하는 기법.

</details>

- 클래스 경계 없이 임의의 비트 단위로 주소를 할당하는 **클래스리스(Classless) 주소 지정**
- 부서별 호스트 규모에 맞추어 마스크 길이를 가변 적용하는 **가변 길이 서브넷 마스크(VLSM)**
- 연속된 서브넷 블록을 단일 상위 경로로 묶어 라우터 메모리를 절감하는 **경로 집약(Supernetting)**

#### 한줄 요약
- 클래스리스 할당, VLSM 가변 분할, Supernetting 경로 집약을 통해 네트워크를 최적화한다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **서브넷 주소 계산 구조**: $N$비트 네트워크 프리픽스 + $H$비트 호스트 비트 ($N + H = 32$). 유효 호스트 수는 $2^H - 2$ (네트워크 ID와 브로드캐스트 주소 제외).

</details>

```text
[서브네팅 및 CIDR 주소 체계]
  │
  ├─ [네트워크 프리픽스 (Prefix)] (라우팅 및 망 식별)
  │     ├─ [네트워크 주소] (호스트 비트 전부 0, Subnet ID)
  │     └─ [서브넷 마스크 / CIDR 표기] (/N 비트 길이 지정)
  │
  ├─ [호스트 필드 (Host Field)] (단말 노드 식별)
  │     ├─ [유효 호스트 할당 범위] (1 ~ 2^H - 2)
  │     └─ [브로드캐스트 주소] (호스트 비트 전부 1)
  │
  └─ [주소 집약 및 분할 기법] (최적화)
        ├─ [VLSM] (부서별 크기에 맞춘 가변 길이 분할)
        └─ [Supernetting] (연속된 블록의 상위 CIDR 축약 집약)
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 네트워크 프리픽스 | 목적지 서브넷 식별과 LPM 기준 제공 |
| 호스트 비트 | 서브넷 내부 단말 식별 |
| 유효 호스트 범위 | 실제 호스트에 할당 가능한 주소 공간 제공 |
| 경로 집약 | 연속 하위 경로를 상위 경로로 축약하여 BGP 테이블 최적화 |

#### 한줄 요약
- 서브넷 마스크가 32비트 주소의 어디를 자르는지가 유효 호스트 범위를 결정하고, 경로 집약은 상위 프리픽스만 남겨 연속된 하위 경로 엔트리를 라우팅 테이블에서 걷어낸다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **VLSM 설계 5단계**: 부서별 필요 호스트 산정 $\to$ 수요 내림차순 정렬 $\to$ 서브넷 마스크 할당 $\to$ 연속 주소 부여 $\to$ CIDR 집약 광고.

</details>

```text
[VLSM 서브네팅 설계 흐름] (진행 ①→⑤, 진입, ⑤ CIDR 집약 광고로 설계 완료)
  │
  ├─ [부서별 호스트 수요 목록] (① 서브넷별 요구 호스트 수를 유효 호스트 2^H-2 기준으로 집계)
  │
  ├─ [수요 내림차순 정렬] (② 큰 블록부터 배치할 순서를 확정해 잔여 공간을 연속으로 유지)
  │
  ├─ [서브넷 마스크·블록 크기] (③ 요구량을 만족하는 최소 프리픽스를 할당)
  │
  ├─ [연속 주소 블록] (④ 블록 경계에 맞춰 주소를 순차 부여)
  │
  └─ [CIDR 집약 경로] (⑤ 공통 상위 프리픽스로 하위 경로를 축약 광고)
```

분기 결과: **VLSM 설계 5단계** 중 ② 내림차순 정렬을 지켜 큰 블록부터 경계에 배치하면 잔여 주소가 연속으로 남아 ⑤ 집약이 성립하지, 순서를 어기면 주소는 아껴도 공통 프리픽스가 만들어지지 않아 경로 축약 이득이 사라진다.

#### 한줄 요약
- 큰 수요부터 경계에 맞춰 배치해야 잔여 주소가 연속으로 남아 마지막 집약이 성립하므로, 정렬 순서를 어기면 주소는 아껴도 경로 수는 줄지 않는다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **Subnetting vs Supernetting (CIDR Aggregation)**: 큰 네트워크를 작게 분할하는 서브네팅과 작은 서브넷들을 크게 통합하는 슈퍼네팅.

</details>

| 비교 항목 | 서브네팅 (Subnetting) | CIDR 경로 집약 (Supernetting) |
|:---|:---|:---|
| 작업 방향 | 대형 네트워크를 소형 서브넷으로 분할 | 소형 서브넷을 상위 경로로 집약 |
| 프리픽스 변화 | 프리픽스 길이 증가 | 프리픽스 길이 감소 |
| 적용 위치 | 사내망·데이터센터·VPC | 백본·BGP 경로 광고 |
| 목적 | 주소 효율과 브로드캐스트 격리 | 라우팅 테이블 축소 |
| 전제 조건 | 호스트 수와 블록 경계 일치 | 주소 블록의 연속성과 경계 일치 |

#### 한줄 요약
- 사내 망에서는 서브네팅으로 세분화하고, 코어 라우팅 광고 시에는 슈퍼네팅으로 경로를 축약한다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **Broadcast Domain Isolation (브로드캐스트 도메인 격리)**: 과도한 L2 브로드캐스트 트래픽이 전체 네트워크 장비의 CPU를 고갈시키는 브로드캐스트 스톰을 방지하기 위해 서브넷을 분할하는 조치.
- **IPAM (IP Address Management)**: 전사 주소 블록·서브넷·할당 상태를 단일 데이터베이스로 관리하고 DNS/DHCP와 연동해, 중복 없이 연속 블록을 배정하고 잔여 공간을 추적하는 주소 관리 도구.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 단일 서브넷 크기 과다로 인한 브로드캐스트 스톰 발생 | **브로드캐스트 도메인 격리**를 위해 /24(254개) 이하로 세분화 서브네팅 | L2 트래픽 오버헤드 격리 및 스위치 보호 |
| 주소 여유 없이 설계하여 향후 조직 확장 시 IP 고갈 | 향후 30~50% 확장을 고려한 용량 산정(Capacity Planning) 반영 | 대규모 네트워크 주소 재설계 방지 |
| 비연속적 IP 할당으로 인한 CIDR 경로 집약 실패 및 라우터 과부하 | **IPAM**으로 조직별 주소를 연속 블록(Contiguous)으로 배치 | BGP 라우팅 테이블 축약 성공률 100% 달성 |
| 클라우드 VPC 서브넷 간 IP 중복으로 인한 VPN/피어링 연결 불가 | 전사 하이브리드 클라우드 단일 **IPAM** 거버넌스 구축 | 서브넷 IP 충돌 원천 방지 |

#### 한줄 요약
- 서브넷을 잘게 나누면 브로드캐스트는 격리되지만 경로 수와 관리 지점이 늘어나므로, 연속 블록 배치와 IPAM 같은 중앙 관리 비용을 치러 집약 가능성을 지켜야 분할의 이득이 라우팅 부담으로 되돌아오지 않는다.

## Ⅶ. 결론

- 온프레미스 기업망부터 퍼블릭 클라우드(AWS VPC, Azure VNet, GCP VPC) 가상 네트워크 설계의 가장 기본적이면서도 핵심적인 **주소 아키텍처 및 라우팅 최적화 표준 엔지니어링 기술**로 자리잡음.
- 실무 설계 시에는 **IPAM(IP 주소 관리) 도구를 통한 연속된 블록 배치로 Supernetting 집약률 극대화**, **브로드캐스트 스톰 방지를 위한 /24 이하 서브넷 격리**, **클라우드-온프레미스 하이브리드 연동 시 CIDR 충돌을 방지하는 전사 주소 거버넌스 수립**을 결합하여 확장성 높은 네트워크를 완성.

#### 한줄 요약
- 서브네팅과 CIDR은 주소 공간의 미세 분할과 경로 집약을 통해 IP 자원 활용률을 극대화하고 라우팅 부하를 절감하는 핵심 네트워크 설계 기술이다.
