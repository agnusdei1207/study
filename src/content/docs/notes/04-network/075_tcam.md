---
sidebar:
  order: 75
  label: "075. TCAM: 삼진 검색 메모리"
  badge:
    text: "기출 · 30%"
    variant: note
title: "고속 패킷 검색 하드웨어 : TCAM (Ternary Content Addressable Memory)"
date: "2026-09-07T14:00:00+09:00"
tags:
  - "notes-network"
weight: 75
extra:
  question_no: "75"
  source_status: "기출"
  source_history: "132회"
  priority: 30
  priority_note: "0/1/X 삼진 마스킹, 1-Clock 전병렬 검색, 우선순위 인코더 및 SRAM 연동 구조"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **TCAM (Ternary Content Addressable Memory)**: 데이터 키(Key)를 입력하면 0, 1, X(Don't Care) 삼진 마스크로 단 1클럭 내에 주소(Index)를 찾는 연상 메모리.
- **Ternary States (삼진 상태)**: 이진수 '0'과 '1' 외에 와일드카드 '무관(Don't Care, X)' 비트를 추가 지원하여 비트 단위 마스킹을 수행하는 특성.

</details>

- 정의/개념: 0·1·X 규칙을 **1클럭 전병렬 매칭**하는 검색 메모리
- 배경/필요성: 초고속 백본 라우터와 고성능 스위치에서 IP 패킷을 라인 레이트(100Gbps~800Gbps)로 포워딩하기 위해 가변 길이 서브넷 마스크(LPM: Longest Prefix Match)와 복합 5-Tuple ACL 보안 규칙을 순차 검색하거나 소프트웨어 트리(Trie) 기반으로 탐색하는 방식은 메모리 다중 접근 지연으로 인한 패킷 병목과 처리량 급락을 초래하는 한계를 극복하기 위해, 0과 1 외에 '무관(Don't Care, X)' 비트 마스킹을 지원하여 수십만 개의 규칙을 단 1클럭 사이클 내에 전병렬(Fully-Parallel)로 동시 비교하는 **TCAM**(Ternary Content Addressable Memory) 하드웨어 아키텍처를 도입하여 규칙 개수와 무관한 $O(1)$ 초저지연 라우팅 룩업, 복합 ACL 정책의 고속 하드웨어 집행 및 기가비트/테라비트급 스위칭 라인 레이트를 달성할 필요

#### 한줄 요약
- 삼진 마스킹과 전병렬 검색 구조를 통해 유선 속도(Line-Rate) 패킷 처리를 달성해야 한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **LPM (Longest Prefix Match)**: 목적지 IP 주소와 가장 길게 일치하는 서브넷 마스크를 가진 라우팅 엔트리를 최우선 선택하는 IP 포워딩 알고리즘.
- **Priority Encoder (우선순위 인코더)**: 복수의 엔트리가 동시에 매칭되었을 때 가장 상위(Lowest Index)의 우선순위 인덱스를 단일 출력하는 하드웨어 회로.

</details>

- 1클럭 전병렬 검색: 모든 엔트리의 일치 여부 동시 검출
- **LPM**·와일드카드: 가변 프리픽스와 ACL 마스크 지원
- **우선순위 인코더**: 동시 일치 중 최상위 엔트리 선택

#### 한줄 요약
- 전병렬 검색, 와일드카드 마스킹, 우선순위 인코더를 통해 신속하게 최적 규칙을 선별해야 한다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **TCAM Cell (16T)**: 데이터 비트와 마스크 비트를 저장하는 2개의 SRAM 셀 및 비교 트랜지스터(통상 16T)로 구성된 최소 물리 단위.
- **Associated Action RAM**: TCAM이 출력한 매칭 인덱스를 입력받아 실제 포워딩 액션(출력 포트, VLAN 등)을 인출하는 고속 SRAM.

</details>

```text
[TCAM 아키텍처]
  │
  ├─ [입력 제어] ── Input Control
  │     └─ [검색 키 생성기] (헤더를 파싱하여 검색 키 벡터 생성)
  ├─ [병렬 비교] ── Parallel Comparison
  │     └─ [TCAM 어레이] (삼진 규칙(0, 1, X) 저장 및 16T 셀 기반 매치 라인 출력)
  ├─ [우선순위 판정] ── Priority Arbitration
  │     └─ [우선순위 인코더] (복수 일치 발생 시 최우선 순위 인덱스 선별)
  └─ [출력 계층] ── Output Layer
        └─ [동작 SRAM] (인덱스에 대응하는 포워딩 동작 및 출력 포트 정보 반환)
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 검색 키 생성기 | 헤더를 파싱하여 검색 키 벡터 생성 |
| TCAM 어레이 | 삼진 규칙(0, 1, X) 저장 및 16T 셀 기반 매치 라인 출력 |
| 우선순위 인코더 | 복수 일치 발생 시 최우선 순위 인덱스 선별 |
| 동작 SRAM | 인덱스에 대응하는 포워딩 동작 및 출력 포트 정보 반환 |

#### 한줄 요약
- TCAM 어레이는 일치 여부 판정만 맡고 실제 동작 값은 SRAM이 보관하므로, 비싼 삼진 셀은 검색 폭에만 쓰이고 결과 저장은 값싼 메모리가 대신한다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **Match Line (매치 라인)**: 엔트리의 모든 비트가 입력 키와 일치할 때 전압이 High로 유지되는 하드웨어 감지 신호선.

</details>

```text
[TCAM 패킷 검색 흐름] (진행 ①→⑤, 인입에서 진입, ①→② 전병렬 비교, ③ 매치 판정, ④ 인덱스 선택, ⑤ 액션 조회)
  │
  ├─ [검색 키 생성기] (① 패킷 헤더를 파싱해 검색 키 벡터 생성)
  │
  ├─ [TCAM 셀 어레이] (② 삼진 규칙(0·1·X) 전 엔트리와 1클럭 병렬 비교)
  │
  ├─ [**Match Line** 감지부] (③ 엔트리 전 비트 일치 시 매치 라인 High 유지)
  │
  ├─ [우선순위 인코더] (④ 복수 일치 중 최우선 순위 인덱스 선별)
  │
  └─ [동작(Action) SRAM] (⑤ 인덱스로 포워딩·차단 액션 및 출력 포트 인출)
```

분기 결과: ③에서 무일치·복수 일치·단일 일치가 갈라져 무일치는 기본 경로로 떨어지고 복수 일치는 ④ 우선순위 인코더가 1건으로 좁히며, 규칙 수가 늘어날 때 늘어나는 대가는 조회 지연이 아니라 **전병렬 검색** 구동 전력과 셀 면적이다.

#### 한줄 요약
- 여러 규칙이 동시에 적중해도 우선순위 인코더가 1건으로 좁히므로, 규칙 수가 늘어날 때 늘어나는 대가는 조회 지연이 아니라 전력과 셀 면적이다.

## Ⅴ. 종류 및 비교


| 비교 항목 | 삼진 메모리 (TCAM) | 2진 내용 주소화 메모리 (CAM) | 정적 램 (SRAM / DRAM) |
|:---|:---|:---|:---|
| 저장/비교 상태 | **0, 1, X** | **0, 1 이진 일치** | 0, 1 데이터 저장 |
| 검색 복잡도 | **$O(1)$ 전병렬 검색** | **$O(1)$ 전병렬 검색** | $O(\log N)$ 트리 탐색 |
| 비트 셀 면적 | **대형, 약 16T** | 중형, 약 10T | **소형, 6T** |
| 소비 전력 및 발열 | **매우 높음** | 높음 | **낮음** |
| 주요 적용 분야 | **LPM·ACL·QoS** | **L2 MAC 테이블** | 일반 메모리·Action RAM |

#### 한줄 요약
- TCAM은 LPM 및 ACL, CAM은 MAC 주소 검색, SRAM은 포워딩 액션 저장에 최적화된다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **Range Expansion (범위 확장)**: L4 포트 범위(예: 1024~65535)를 비트 마스크로 변환할 때 다수의 엔트리로 분할 적재되어 용량이 낭비되는 현상.
- **SDM (Switch Database Management)**: 스위치의 사용 목적(라우팅 중심 vs ACL 보안 중심)에 따라 물리 TCAM 뱅크 비율을 동적으로 재분배하는 설정.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| L4 포트 범위(Range) 설정 시 비트 마스킹 분할로 인한 **TCAM 엔트리 폭증** | L4 포트 범위 전용 연산기(Range Matching Unit) 및 그룹화 | 엔트리 소모량 80% 절감 및 TCAM 메모리 보존 |
| 광범위한 서브넷 규칙이 구체적 규칙을 가려 정책이 무력화되는 섀도잉(Shadowing) | ACL 컴파일러를 통한 규칙 선별 정렬(Top-Down 정밀도 배치) | 보안 정책 누락 방지 및 정확한 화이트리스트 집행 |
| 라우팅 테이블(FIB) 또는 ACL 급증으로 인한 **TCAM 메모리 고갈** | **SDM** (Switch Database Management) 템플릿 최적화 적용 | 환경별(Routing vs ACL) 최적 뱅크 파티셔닝 |
| 전병렬 구동으로 인한 스위치 ASIC 전력 소모 및 발열 심화 | 뱅크 분할 블록 활성화(Block-Select Gating) 저전력 설계 | 비활성 메모리 뱅크 전력 차단으로 전력 50% 절감 |

#### 한줄 요약
- Range 매칭 최적화, 정밀도 순서 정렬, SDM 템플릿 최적화, 뱅크 게이팅을 통해 메모리와 전력 효율을 확보해야 한다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **FIB (Forwarding Information Base)**: 제어 평면의 라우팅 테이블(RIB)을 기반으로 데이터 평면 TCAM에 고속 룩업용으로 컴파일된 포워딩 정보 베이스.
- **P4 (Programming Protocol-Independent Packet Processors)**: 네트워크 데이터 평면의 패킷 파싱 및 매칭/동작 파이프라인을 소프트웨어로 직접 프로그래밍하는 표준 언어.

</details>

- L3 IP 라우팅(FIB), L4 ACL 보안 필터링, QoS 분류 및 OpenFlow/SDN 플로우 테이블 검색을 위한 현대 고성능 스위치/라우터 ASIC 데이터 평면(Data Plane)의 핵심 하드웨어 룩업 엔진으로 확고히 정립되었다.
- 고비용·고발열·셀 면적 한계를 극복하기 위해 알고리즈믹 TCAM(Algorithmic Lookup), P4 프로그래머블 파이프라인 및 고속 SRAM 기반 해시 테이블과의 하이브리드 구조로 진화하는 가운데, 실무 네트워크 장비 설계 및 운영 시에는 **L4 포트 범위(Range) 설정 시 엔트리 폭증을 방지하는 전용 Range Matching Unit 적용**, **정책 무력화를 방지하는 ACL 정밀도 순서 정렬(Shadowing 방지)**, **역할에 따라 뱅크 비율을 재조정하는 SDM(Switch Database Management) 템플릿 튜닝**, **미사용 뱅크의 전력을 차단하는 블록 선택 게이팅(Block-Select Gating)**을 결합하여 안정적인 포워딩 성능과 에너지 효율을 확보해야 한다.

#### 한줄 요약
- TCAM과 SDM 템플릿을 유기적으로 조합하여 고속 검색 성능과 하드웨어 자원 효율의 균형을 확보해야 한다.
