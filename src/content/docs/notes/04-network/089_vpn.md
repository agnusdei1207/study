---
sidebar:
  order: 89
  label: "089. VPN: IPsec•SSL•WireGuard"
  badge:
    text: "기출 · 50%"
    variant: note
title: "가상 사설망 터널링 프로토콜 : IPsec, SSL, WireGuard (VPN)"
date: "2026-09-07T14:00:00+09:00"
tags:
  - "notes-network"
weight: 89
extra:
  question_no: "89"
  source_status: "기출"
  source_history: "137회"
  priority: 50
  priority_note: "L3 IPsec(AH/ESP/IKE), L4~L7 SSL VPN, 모던 L3 WireGuard(Noise Protocol) 및 분할 터널링(Split Tunneling)"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **VPN (Virtual Private Network)**: 공용 인터넷망 상에 암호화 가상 터널을 형성하여 사설 전용선처럼 안전하게 통신하는 기술.
- **Split Tunneling (분할 터널링)**: 사내 업무 트래픽만 VPN 터널로 보내고 일반 인터넷 트래픽은 로컬 게이트웨이로 직접 분기하는 라우팅 기법.

</details>

- 정의/개념: 공용망 위에 **터널링 캡슐화, 암호화(ESP), 상호 인증(IKE)을 적용하여 기밀성과 무결성이 보장된 가상 사설 통신로를 구축하는 기술**
- 배경/필요성: 본사-지사 간 또는 클라우드-온프레미스 간 물리적 전용선(Leased Line)을 직접 포설/임대하는 방식은 기하급수적인 통신 회선 비용과 구축 지연을 초래하고, 저렴한 공용 인터넷망을 그대로 사용할 경우 제3자에 의한 패킷 도청(Sniffing), 데이터 변조 및 중간자 공격(MITM)에 무방비로 노출되는 한계를 극복하기 위해, 공용망 상에서 터널링 캡슐화(Tunneling)와 강력한 암호화(ESP/AES-GCM) 및 상호 인증(IKEv2/TLS)을 적용하여 안전한 사설 통신로를 논리적으로 구현하는 VPN(Virtual Private Network) 기술을 도입하여 **전용선 대비 90% 이상의 비용 절감, 공용 인터넷 상에서의 기밀성·무결성 보장 및 재택·원격 근무자를 위한 안전한 사내망 접근성**을 달성할 필요

#### 한줄 요약
- 터널링과 암호화를 통해 공용 인터넷 상에서 안전하고 경제적인 가상 전용망을 구축해야 한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **PFS (Perfect Forward Secrecy, 전방향 안전성)**: 장기 개인키가 유출되더라도 과거 세션마다 독립적으로 생성된 임시 키들은 역산할 수 없도록 보장하는 암호학적 특성.
- **ESP (Encapsulating Security Payload)**: IPsec에서 데이터 암호화(AES-GCM), 무결성 검증, 안티 리플레이를 제공하는 프로토콜 (IP 프로토콜 50).

</details>

- **기밀성·무결성 보호**: 암호화와 인증 태그로 도청·변조 대응
- **계층별 적용 유연성**: Site-to-Site 백본망(L3 IPsec), 재택근무자 웹 포털(L7 SSL VPN), 저지연 모바일(L3 WireGuard) 지원
- **동적 키 교환 및 재키잉(Rekeying)**: IKEv2 및 Noise 프로토콜 기반 **주기적 세션 키 갱신을 통한 전방향 안전성(PFS) 보증**

#### 한줄 요약
- 기밀성 및 무결성 보장, 계층별 터널링 유연성, PFS 기반 주기적 재키잉을 체계적으로 제공해야 한다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **SA (Security Association)**: 암호 알고리즘, 세션 키, 모드(Transport/Tunnel), 수명을 정의한 종단 간 단방향 논리적 연결 계약.
- **IKEv2 (RFC 7296)**: Diffie-Hellman 키 교환을 통해 보안 연관(SA)을 자동 협상하고 수명주기를 관리하는 표준 프로토콜.

</details>

```text
[가상 사설망(VPN) 체계]
  │
  └─ [수신 종단] ── Receiver Endpoint
        ├─ [VPN 게이트웨이] (터널 종단과 내부망 접근 제어)
        ├─ [IKE·SA 엔진] (키 교환·인증·SA 수명 관리)
        ├─ [ESP 처리기] (암호화·무결성·재전송 방지 처리)
        └─ [분할 터널 라우터] (목적지별 터널·로컬 경로 선택)
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| **VPN 게이트웨이** | 터널 종단과 내부망 접근 제어 |
| **IKE·SA 엔진** | 키 교환·인증·SA 수명 관리 |
| **ESP 처리기** | 암호화·무결성·재전송 방지 처리 |
| **분할 터널 라우터** | 목적지별 터널·로컬 경로 선택 |

#### 한줄 요약
- IKE가 키 협상을, ESP가 실제 데이터 보호를 나눠 맡으므로 값비싼 공개키 연산은 세션 수립 시점에만 치르고 이후 전 구간은 대칭키로 처리된다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **IPsec Tunnel Mode**: 원본 IP 헤더 전체를 암호화하고 새로운 공용 IP 헤더(Outer IP)를 덧씌워 전송하는 망간 연동 표준 모드.

</details>

```text
[IPsec IKEv2 터널 수립·전송 흐름] (진행 ①→③ 왕복, 핸드셰이크에서 진입, Child SA 수립 후 ④→⑤ 데이터 평면 연속 처리)
  │
  ├─ [IKE_SA_INIT] (① DH 키 교환으로 보안 제어 채널 수립)
  │
  ├─ [IKE_AUTH] (② X.509 인증서·사전 공유 키로 상호 신원 인증, 유효 시 ③ 진행)
  │
  ├─ [Child SA 생성] (③ 데이터 전송용 ESP SA 생성, 대칭 암호화 키·SPI 바인딩)
  │
  ├─ [ESP 캡슐화 송출] (④ 원본 IP 패킷 전체 AES-256-GCM 암호화 후 Outer IP 부착 터널 전송)
  │
  └─ [게이트웨이 복호화 전달] (⑤ 외부 헤더 제거·ESP 복호화 후 내부 사설망 전달)
```

분기 결과: 데이터 전송용 Child SA의 재키잉 주기에서 갈리며, 주기를 짧게 하면 **PFS**에 따라 과거 캡처 세션 키의 후행 복호화 구간이 짧아지는 대신 DH 협상·**SA** 재수립 연산을 매 주기 지불하고, 주기를 늘리면 협상 부담은 줄지만 장기 동일 키 노출 위험과 세션 탈취 피해 범위가 커지므로, 갱신 빈도는 안전성 확보와 협상 성능의 절충점에서 정한다.

#### 한줄 요약
- Child SA 재키잉 주기에서 안전성과 성능이 갈리며, 자주 갱신할수록 키 노출 구간은 짧아지고 협상 연산 부담은 커진다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **IPsec (L3 망 연동)** vs **SSL VPN (L7 웹 접속)** vs **WireGuard (모던 초경량 L3)**.

</details>

| 비교 항목 | IPsec VPN (IETF RFC 4301) | SSL/TLS VPN (OpenVPN / AnyConnect) | WireGuard (최신 커널 기반) |
|:---|:---|:---|:---|
| 동작 범위 | IP 패킷 터널 | 웹·응용 또는 전용 클라이언트 | IP 패킷 터널 |
| 암호 체계 | IKEv2·ESP | TLS | Noise·ChaCha20-Poly1305 |
| 클라이언트 | OS 내장·전용 소프트웨어 | 브라우저·전용 소프트웨어 | WireGuard 에이전트 |
| NAT 통과 | **NAT-T** 사용 | TLS 전송 경로 활용 | UDP 엔드포인트 로밍 |
| 적용 영역 | 지사·VPC 사이 터널 | 사용자 원격 접속 | 경량 호스트·클러스터 터널 |

#### 한줄 요약
- IPsec은 본사-지사 백본망, SSL VPN은 재택근무 웹 접속, WireGuard는 고속 모바일 및 클라우드 연동에 쓰인다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **MSS Clamping**: VPN 암호화 오버헤드로 인한 패킷 단편화(Fragmentation)를 막기 위해 TCP SYN의 MSS 크기를 1360~1400B로 강제 축소 보정하는 설정.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| VPN 암호화 오버헤드로 인한 MTU 초과 시 패킷 단편화 및 성능 저하 | **`TCP MSS Clamping (1360~1400B)` 및 PMTU Discovery 적용** | 패킷 단편화 제거 및 대용량 파일 전송 처리율 40% 향상 |
| 재택 단말 분할 터널링 시 감염 PC를 통한 내부망 침해 횡적 확산 | **`엔드포인트 EDR/ZTNA 검증` 및 사내 접근 허용 `최소 라우팅(Least Route)`** | VPN 터널 경유 악성코드 전파 방지 및 제로 트러스트 달성 |
| 장시간 동일 암호화 키 사용으로 인한 수학적 키 유출 및 세션 탈취 | **`PFS (Perfect Forward Secrecy)` 활성화 및 주기적 재키잉(Rekeying)** | 세션 키 주기적 갱신 및 과거 캡처 패킷 사후 복호화 방지 |
| 이동 근무자 IP 변경 시 VPN 세션 단절 및 재인증 지연 | **`WireGuard Roaming` 또는 IKEv2 MOBIKE (RFC 4555) 활성화** | Wi-Fi/LTE 망 전환 시에도 무단절 세션 유지 |

#### 한줄 요약
- MSS Clamping으로 MTU 병목을 해소하고, ZTNA 검증으로 분할 터널을 통제하며, PFS 재키잉으로 전방향 안전성을 확보해야 한다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **SASE (Secure Access Service Edge)**: SD-WAN 네트워크 기능과 보안 서비스(ZTNA, CASB, FWaaS)를 단일 클라우드 플랫폼으로 통합 제공하는 모델.
- **EDR (Endpoint Detection and Response)**: 엔드포인트 단말의 행위를 지속적으로 모니터링하여 악성코드 침투와 비정상 행위를 탐지·대응하는 보안 솔루션.

</details>

- 글로벌 엔터프라이즈의 거점 간 안전한 백본망 구축과 원격지 직원의 재택근무를 지탱하는 **인터넷 보안 터널링의 가장 보편적이고 표준적인 통신 기술**로 정립되었다.
- 접속 후 내부 전체를 신뢰하던 레거시 VPN의 한계를 넘어 최소 권한 원칙의 제로 트러스트 네트워크 액세스(ZTNA) 및 SASE 아키텍처로 진화하는 가운데, 실무 VPN 인프라 구축 시에는 **터널 헤더 추가로 인한 패킷 단편화를 방지하는 TCP MSS Clamping(1360~1400B) 적용**, **과거 트래픽 복호화를 선제적으로 방지하는 전방향 안전성(PFS) 기반 IKEv2/Noise 주기적 재키잉(Rekeying)**, **재택 단말 감염 전파를 차단하는 엔드포인트 EDR 연동 및 최소 권한 분할 터널링(Split Tunneling)**을 결합하여 안정적인 가상 사설 통신 무결성을 확보해야 한다.

#### 한줄 요약
- VPN은 터널링과 암호화를 통해 공용망 위에서 가상 전용선을 제공하며, IPsec, SSL, WireGuard의 맞춤형 결합으로 보안 신뢰성을 확보해야 한다.
