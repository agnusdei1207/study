---
sidebar:
  order: 1
  label: "001. OSI 7계층 모델"
  badge:
    text: "기출 · 70%"
    variant: note
title: "OSI 7계층 모델 (OSI 7-Layer Model)"
date: "2026-09-07T14:00:00+09:00"
tags:
  - "notes-network"
weight: 1
extra:
  question_no: "001"
  source_status: "기출"
  source_history: "120회, 125회, 134회"
  priority: 70
  priority_note: "설명형: 120•134회 단답•서술, 계층 기능•PDU"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **OSI 7-Layer Model (개방형 시스템 상호연결 7계층)**: ISO(국제표준화기구)가 이기종 시스템 간 통신 호환성을 위해 네트워크 기능을 7개 계층으로 표준화한 참조 아키텍처.
- **PDU (Protocol Data Unit)**: 각 계층에서 프로토콜 처리를 위해 헤더와 페이로드를 결합한 표준 전송 단위 (Data, Segment, Packet, Frame, Bit).

</details>

- 정의/개념: 통신 기능을 7단계로 모듈화한 **OSI 참조 모델**
- 배경/필요성: 벤더별 독점 아키텍처(SNA, DECnet 등) 난립으로 인한 **이기종 시스템 간 상호 통신 불가, 장비 교체 시 프로토콜 변환 개발 비용 과다 및 장애 원인 규명 지연 한계**

#### 한줄 요약
- 계층별 독립 모듈화와 표준 PDU 캡슐화를 통해 이기종 네트워크 통신과 장애 격리를 보장한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **Encapsulation & Decapsulation (캡슐화 및 역캡슐화)**: 상위 계층 데이터에 제어 헤더를 덧붙여 하위로 전달(캡슐화)하고, 수신 측에서 하위 헤더를 벗겨내며 상위로 전달(역캡슐화)하는 과정.
- **Service Access Point (SAP)**: 인접 계층 간에 상호 서비스를 주고받기 위해 정의된 논리적 인터페이스 접점.

</details>

- 하위 계층 구현 변경이 상위 계층에 영향을 주지 않는 **계층별 독립성 및 모듈화**
- 송신 측 헤더 부착(캡슐화)과 수신 측 헤더 제거(역캡슐화)를 통한 **단계적 프로토콜 제어**
- 각 계층 간 명확한 인터페이스를 제공하는 **서비스 접근점(SAP) 기반 통신**

#### 한줄 요약
- 모듈화, 캡슐화/역캡슐화, 계층 간 표준 인터페이스를 통해 네트워크 복잡도를 분할 정복한다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **OSI 7계층 계층별 PDU**: L7~L5(Data/Message), L4(Segment), L3(Packet/Datagram), L2(Frame), L1(Bit).

</details>

```text
[OSI 7계층 참조 모델]
  │
  ├─ [상위 계층군 (Upper Layers)] (데이터 생성 및 표현)
  │     ├─ [L7 응용 계층] (사용자 인터페이스, HTTP/DNS, PDU: Data)
  │     ├─ [L6 표현 계층] (암복호화, 압축, 구문 변환, PDU: Data)
  │     └─ [L5 세션 계층] (세션 수립, 대화 동기화, PDU: Data)
  │
  └─ [하위 계층군 (Lower Layers)] (종단 및 물리 전송)
        ├─ [L4 전송 계층] (종단 신뢰성, 포트 다중화, PDU: Segment)
        ├─ [L3 네트워크 계층] (논리 주소 IP, 최적 경로 라우팅, PDU: Packet)
        ├─ [L2 데이터링크 계층] (물리 주소 MAC, 인접 노드 전송, PDU: Frame)
        └─ [L1 물리 계층] (전기/광 신호 비트 스트림 전송, PDU: Bit)
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| L7 응용 계층 | HTTP·SMTP·DNS 서비스 인터페이스 제공 및 최상위 인터페이스 |
| L6 표현 계층 | 암복호화·압축·포맷 변환 수행 및 구문 변환 |
| L5 세션 계층 | 통신 세션 수립·유지·동기화 관리 및 전이중·반이중 제어 |
| L4 전송 계층 | 포트 기반 종단 신뢰성·흐름·혼잡 제어 (TCP·UDP) |
| L3 네트워크 계층 | IP 기반 경로 설정 및 패킷 포워딩 (IP·라우터) |
| L2 데이터링크 계층 | MAC 기반 프레임 전송 및 CRC 오류 검출 (Ethernet·L2 스위치) |
| L1 물리 계층 | 비트를 전기·광 신호로 변환하여 물리 매체 송출 |

#### 한줄 요약
- 상위 3계층은 데이터의 의미를, 하위 4계층은 전달을 책임지므로 장비가 어느 계층 헤더까지 읽는지(스위치 L2·라우터 L3·로드밸런서 L4/L7)가 그 장비의 기능 경계를 결정한다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **캡슐화 5단계**: 응용 데이터 생성 $\to$ L4 TCP 헤더 부착 $\to$ L3 IP 헤더 부착 $\to$ L2 Ethernet 헤더/트레일러 부착 $\to$ L1 비트 전송.

</details>

```text
[캡슐화·역캡슐화 종단 간 흐름] (진행 ①→⑤, 송신 캡슐화에서 진입, 수신에서 역순 역캡슐화)
  │
  ├─ [응용 계층 L7~L5] (① 사용자 데이터 생성, 수신단에서 최종 데이터 인계)
  │
  ├─ [전송 계층 L4] (② TCP 헤더 부착으로 Segment 작성, 수신단에서 헤더 검증·제거)
  │
  ├─ [네트워크 계층 L3] (③ IP 헤더 부착으로 Packet 작성, 수신단에서 라우팅 판정)
  │
  ├─ [데이터링크 계층 L2] (④ MAC·FCS 부착으로 Frame 작성, 수신단에서 CRC 오류 검사)
  │
  └─ [물리 계층 L1] (⑤ 비트 신호로 변환해 매체 송출, 수신단에서 프레임 복원)
```

분기 결과: 송신 **캡슐화 5단계**는 한 경로로 진행하지만 수신 측 처리는 목적지 여부로 갈려, 종단 호스트는 5개 계층을 모두 벗겨 응용 데이터를 인계하고 중간 장비는 자기 계층(L2 스위치·L3 라우터)까지만 검사 후 재캡슐화하므로 홉당 처리 비용이 종단보다 낮다.

#### 한줄 요약
- 각 계층은 자기 헤더만 읽고 벗기므로 중간 장비가 어느 계층까지 역캡슐화하는지가 처리 비용을 결정하고, 스위치가 L2에서, 라우터가 L3에서 멈추는 이유가 된다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **OSI 7계층 vs TCP/IP 4계층**: 개념적 표준 참조 모델(OSI 7계층)과 인터넷 상용 구현 표준 프로토콜 스택(TCP/IP 4계층).

</details>

| 모델 | OSI 7계층 참조 모델 | TCP/IP 4계층 구현 모델 |
|:---|:---|:---|
| 핵심 성격 | 표준 네트워크 참조 모델 | 인터넷 구현 프로토콜 스택 |
| 계층 구성 | 7개 계층으로 세분화 | 4개 계층으로 통합 |
| 상위 계층 매핑 | 응용(L7), 표현(L6), 세션(L5) 분리 | 응용 계층(Application)으로 단일 통합 |
| 하위 계층 매핑 | 데이터링크(L2), 물리(L1) 분리 | 네트워크 인터페이스(Network Access)로 통합 |
| 실무 활용 | 아키텍처 설계와 계층별 장애 진단 | 인터넷 통신 소프트웨어 구현 |

#### 한줄 요약
- OSI는 계층별 책임과 장애 진단의 기준 모델이며, TCP/IP는 실제 인터넷의 상용 구현 스택이다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **Bottom-Up Troubleshooting (상향식 장애 분석)**: L1 물리 계층(링크 불빛, 케이블)부터 L2(MAC), L3(IP Ping), L4(Port Telnet), L7(HTTP) 순으로 점검하는 체계적 진단 기법.
- **MSS Clamping**: 터널 구간의 라우터가 통과하는 TCP SYN의 MSS 옵션 값을 터널 헤더 크기만큼 줄여 다시 써 넣어, 종단이 처음부터 작은 세그먼트를 보내게 해 경로 도중 IP 단편화를 막는 기법.
- **WAF (Web Application Firewall)**: HTTP 요청·응답의 URL·헤더·본문을 시그니처와 규칙으로 검사해 SQL 인젝션·XSS 같은 L7 공격을 차단하는 응용 계층 방화벽.
- **RSTP (Rapid Spanning Tree Protocol, IEEE 802.1w)**: 스위치 간 BPDU 교환으로 루프 경로의 포트를 차단하고, 링크 장애 시 Proposal/Agreement 핸드셰이크로 1초 내 대체 경로로 재수렴하는 L2 루프 방지 프로토콜.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 장애 원인 구간 불명확으로 MTTR 증가 | L1부터 L7까지 **상향식 진단** | 원인 국소화와 복구 시간 단축 |
| 터널 헤더로 MTU 초과·단편화 | 라우터 **MSS Clamping** 적용 | TCP 세그먼트 단편화와 드롭 방지 |
| 방화벽 통과 후 L7 공격 노출 | **WAF**로 페이로드 검사 | 응용 계층 침해 차단 |
| 스위치 루프로 브로드캐스트 스톰 | **RSTP** 활성화 | L2 루프 차단과 가용성 보장 |

#### 한줄 요약
- 장애 진단은 L1부터 올라가며 계층을 하나씩 배제하는 것이 가장 빠르고, 대책은 문제가 생긴 계층(L2 루프·L3 MTU·L7 페이로드)에 맞는 장치를 골라야 다른 계층에 부작용을 남기지 않는다.

## Ⅶ. 결론

- 현대 인터넷이 TCP/IP 4계층 프로토콜 스택으로 상용화되었음에도 불구하고 모든 네트워크 장비 설계와 트러블슈팅의 개념적 나침반 역할을 수행하는 **가장 근본적인 네트워크 아키텍처 참조 모델이자 장애 진단 표준 체계**로 자리잡음.
- 실무 운영 시에는 **L1 물리 케이블 확인부터 L7 WAF 페이로드 검증까지 이어지는 상향식(Bottom-Up) 진단 절차 준수**, **터널링 시 PMTUD/MSS Clamping을 통한 MTU 단편화 방지**, **L2 RSTP 루프 차단**을 결합하여 복잡한 분산 네트워크의 가용성과 안정성을 완성.

#### 한줄 요약
- OSI 7계층 모델은 7단계 모듈화와 캡슐화를 통해 이기종 통신을 보장하고 네트워크 설계와 장애 진단의 표준을 제공하는 핵심 참조 모델이다.
