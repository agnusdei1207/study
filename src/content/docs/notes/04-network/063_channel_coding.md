---
sidebar:
  order: 63
  label: "063. 채널 코딩"
  badge:
    text: "기출 · 30%"
    variant: note
title: "무선 전송 오류 제어 : 채널 부호화 (Channel Coding)"
date: "2026-09-07T14:00:00+09:00"
tags:
  - "notes-network"
weight: 63
extra:
  question_no: "63"
  source_status: "기출"
  source_history: "129회"
  priority: 30
  priority_note: "순방향 오류 정정(FEC), 해밍/리드-솔로몬/터보/LDPC 부호, 부호율(Code Rate) 및 인터리빙"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **Channel Coding (채널 부호화)**: 무선 채널의 잡음과 페이딩에 대처하기 위해 송신 데이터에 잉여 패리티 비트를 부가하는 신호 처리 기술.
- **FEC (Forward Error Correction)**: 수신 측에서 재전송 요청(ARQ) 없이 수신된 패리티 비트로 자체 연산하여 오류를 검출·정정하는 방식.

</details>

- 정의/개념: 송신 정보 비트($k$)에 패리티 비트($n-k$)를 결합하여 부호어를 생성하고 수신단에서 **FEC** 알고리즘으로 비트 오류를 자체 정정하는 물리 계층 오류 제어 기술
- 배경/필요성: 무선 전송 매체는 다중 경로 페이딩(Multipath Fading), 도플러 효과, 열잡음(AWGN) 및 간섭으로 인해 심각한 비트 왜곡과 버스트 오류(Burst Error)가 발생하며, 전통적인 오류 검출 및 재전송(ARQ) 방식에만 의존할 경우 왕복 지연 시간(RTT)이 누적되어 실시간 저지연 서비스(URLLC, 스트리밍)를 보장할 수 없는 한계를 극복하기 위해, 송신단에서 원래 정보 비트($k$)에 수학적 규칙에 따른 잉여 패리티 비트($n-k$)를 결합하여 전송하고 수신단에서 순방향 오류 정정(FEC: Forward Error Correction) 알고리즘으로 오류를 자체 정정하는 채널 부호화(**Channel Coding**)를 도입하여 재전송 없는 실시간 오류 복구, 부호화 이득(Coding Gain) 확보를 통한 송신 전력 절감 및 섀넌 채널 용량 한계에 근접하는 무선 전송 신뢰성을 달성할 필요

#### 한줄 요약
- 잉여 패리티 부가, 인터리빙, FEC 복호를 통해 재전송 없이 무선 전송 신뢰성을 확보해야 한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **Coding Gain (부호화 이득)**: 동일한 비트 오류율(BER)을 달성하기 위해 비부호화 시스템 대비 절감할 수 있는 신호 대 잡음비($E_b/N_0$)의 dB 차이.
- **Interleaving (인터리빙)**: 페이딩에 의해 연속적으로 발생하는 버스트 오류(Burst Error)를 랜덤 오류로 분산시키는 비트 재배열 기법.

</details>

- **순방향 오류 정정(FEC)**: 역방향 채널 피드백 없이 수신단 자체 연산만으로 오류를 복구하여 왕복 지연 제거
- **인터리빙**을 통한 버스트 오류 극복: 연속된 비트 손실을 시간/주파수 축으로 분산시켜 랜덤 오류로 변환
- 적응형 변조 및 부호화(AMC): 무선 채널 품질(CQI)에 따라 부호율($R=k/n$)을 동적으로 적응 조절

#### 한줄 요약
- FEC 자체 복원, 인터리빙을 통한 버스트 오류 분산, AMC 기반 동적 부호율 제어를 제공해야 한다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **Syndrome (신드롬)**: 수신 부호어 벡터와 패리티 검사 행렬($H^T$)을 곱하여 오류 발생 여부 및 오류 위치를 판별하는 지표 ($S=r \cdot H^T$).
- **Soft-Decision Decoding (연판정 복호)**: 0/1의 이진 판정 대신 로그 우도비(LLR) 확률값을 보존하여 2~3dB의 복호 이득을 얻는 기법.

</details>

```text
[채널 부호화 체계]
  │
  ├─ [송신 계층] ── Transmitter Layer
  │     ├─ [채널 부호기] (정보 비트에 패리티 부가)
  │     └─ [인터리버] (버스트 오류 방지를 위한 비트 재배열)
  ├─ [전송 매체] ── Wireless Channel
  │     └─ [무선 채널] (잡음·페이딩으로 신호 왜곡을 겪는 전송 매체)
  └─ [수신 계층] ── Receiver Layer
        ├─ [역인터리버] (수신 비트의 원래 순서 복원)
        └─ [채널 복호기] (LLR·신드롬 기반 오류 정정 및 복호)
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 채널 부호기 | 정보 비트에 **패리티 부가** |
| 인터리버 | 버스트 오류 방지를 위한 **비트 재배열** |
| 무선 채널 | 잡음·페이딩으로 신호 왜곡을 겪는 전송 매체 |
| 역인터리버 | 수신 비트의 **원래 순서 복원** |
| 채널 복호기 | LLR·**신드롬** 기반 **오류 정정 및 복호** |

#### 한줄 요약
- 인터리버와 역인터리버가 채널 앞뒤에 대칭으로 놓여 시간축에 흩뿌린 버스트 오류를 산발 오류로 바꾸므로, 복호기는 자신이 정정 가능한 형태만 마주한다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **HARQ (Hybrid ARQ)**: FEC의 오류 정정과 ARQ 재전송을 결합하여, 복호 실패 시 이전 신호를 버리지 않고 재전송 신호와 소프트 결합(Soft Combining)하는 기술.

</details>

```text
[채널 부호화·오류 정정 흐름] (진행 ①→⑤, 부호화에서 진입, ①→② 채널 통과, ③④ 복호, ⑤ CRC 판정 분기)
  │
  ├─ [채널 부호기·인터리버] (① 정보 비트($k$)에 패리티 부가·인터리빙 후 무선 송출)
  │
  ├─ [무선 채널] (② 페이딩·잡음으로 수신 신호 비트 왜곡)
  │
  ├─ [역인터리버·LLR 변환기] (③ 비트 순서 복원 후 실수 진폭을 LLR 확률값으로 변환)
  │
  ├─ [반복 복호기] (④ **신드롬** 판정과 반복 복호 알고리즘으로 왜곡 비트 복원)
  │
  └─ [CRC 검증기·HARQ 결합기] (⑤ 통과 시 상위 계층 전달, 실패 시 LLR 버퍼 보존 후 **HARQ** NACK로 재전송 패킷과 소프트 결합)
```

분기 결과: ⑤ CRC 판정이 갈림길로, FEC 자체 복원에 성공하면 복호 연산 비용만 치르고 실패하면 **HARQ**가 LLR을 버리지 않고 재전송과 결합해 RTT 왕복 비용을 추가 부담하는 대신 같은 채널 품질에서 수신 성공률을 끌어올린다.

#### 한줄 요약
- CRC 검증 결과에 따라 FEC 자체 복원과 **HARQ** 재전송으로 갈리며, 전자는 복호 연산만 치르는 반면 후자는 RTT 왕복 비용을 추가로 부담한다.

## Ⅴ. 종류 및 비교


| 부호 종류 | 핵심 수학적 원리 | 주요 오류 정정 특성 | 대표 적용 표준 |
|:---|:---|:---|:---|
| 해밍 부호 (Hamming) | 패리티 검사 행렬($H$), 거리 $d_{min}=3$ | 단일 비트 오류 정정(SEC), 2비트 검출(DED) | 서버 ECC RAM, L1 캐시 |
| 리드-솔로몬 (RS Code)| 갈루아 필드($GF(2^m)$) 비이진 다항식 | 연속된 심볼 버스트 오류(Burst Error) 정정에 탁월| 광디스크(CD/DVD), DVB-T |
| 터보 부호 (Turbo Code)| 합성곱 부호기 2개 병렬 결합 | 반복 연판정 복호로 섀넌 한계 0.5dB 근접 | 3G WCDMA, 4G LTE 데이터 |
| LDPC 부호 (Low-Density)| 희소 패리티 행렬, 팩터 그래프 | 병렬 고속 복호 가능, 대용량 고속 처리에 최적| 5G NR 데이터, Wi-Fi 6/7 |
| 폴라 부호 (Polar Code)| 채널 분극(Channel Polarization) | 이론상 섀넌 용량을 달성하는 증명된 부호 | 5G NR 제어 채널 (eMBB) |

#### 한줄 요약
- 해밍(단일 비트), RS(버스트 오류), 터보(4G 데이터), LDPC(5G 데이터), 폴라(5G 제어 채널)를 시스템 요구사항에 맞게 선택해야 한다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **Early Stopping (조기 중단)**: 반복 복호 도중 모든 패리티 검사 조건($H \cdot \hat{c}^T = 0$)이 만족되면 최대 반복 횟수를 채우지 않고 즉시 복호를 종료하는 저지연 기법.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 무선 채널 페이딩 심화 시 고정 부호율로 인한 BLER 급증 및 단절 | CQI 피드백 기반 적응형 변조 및 부호화(AMC) 동적 적용 | 채널 상태에 따른 부호율 최적화 및 10% 타겟 BLER 유지 |
| 복호기 반복 연산(Iteration) 과다로 인한 처리 지연 및 지터 발생 | 조기 중단 조건(**Early Stopping**) 및 하드웨어 LDPC 적용 | 불필요한 반복 복호 차단 및 복호 지연 단축 |
| 연판정 LLR 고정밀 연산으로 인한 단말기 모뎀 칩셋 발열 및 전력 소모 | 고정 소수점 양자화(Fixed-Point LLR) 및 전용 ASIC 가속기 | 모뎀 전력 소모 최소화 및 기가비트 라인 레이트 달성 |
| 초고속 이동 시 도플러 확산으로 인한 인터리빙 패턴 붕괴 | 2차원 시간-주파수 격자 인터리빙 및 DMRS 밀도 증가 | 고속 도플러 환경에서도 버스트 오류 분산 보증 |

#### 한줄 요약
- AMC 동적 부호율, Early Stopping 지연 단축, 고정소수점 ASIC 가속, 2D 인터리빙을 적용하여 전송 신뢰성을 확보해야 한다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **AMC (Adaptive Modulation and Coding)**: 채널 상태에 따라 변조 방식(QPSK~256QAM)과 채널 부호율을 실시간 변경하는 적응 제어 기술.
- **HARQ-IR (Incremental Redundancy)**: 재전송 시 매번 다른 패리티 비트를 순차 전송하여 결합 복호 성공률을 극대화하는 HARQ 방식.

</details>

- 3G 터보(Turbo) 부호에서 시작하여 5G/6G 이동통신 및 Wi-Fi 7의 초고속 데이터 채널용 LDPC(Low-Density Parity-Check) 부호와 제어 채널용 폴라(Polar) 부호에 이르기까지 무선 물리 계층의 전송 효율과 처리량을 결정짓는 핵심 디지털 신호 처리 표준으로 확립.
- 실무 무선 통신 시스템 구현 시에는 **채널 품질(CQI)에 따라 변조 및 부호율을 실시간 변경하는 AMC(Adaptive Modulation and Coding) 연동**, **복호 실패 시 수신 신호를 축적 결합하는 HARQ-IR(Incremental Redundancy)**, **복호 지연을 최소화하는 조기 중단(Early Stopping) 알고리즘**, **전용 ASIC/FPGA 하드웨어 가속기**를 결합하여 물리 계층 신뢰성을 확보해야 한다.

#### 한줄 요약
- 채널 코딩은 잉여 패리티와 반복 복호 알고리즘을 통해 열악한 무선 매체에서 전송 신뢰성을 보장하는 핵심 물리 계층 기술로 운용해야 한다.
