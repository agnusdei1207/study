---
sidebar:
  order: 83
  label: "083. 시간 민감 네트워킹 (TSN)"
  badge:
    text: "미출 · 50%"
    variant: note
title: "결정론적 이더넷 표준 : 시간 민감 네트워킹 (TSN)"
date: "2026-09-07T14:00:00+09:00"
tags:
  - "notes-network"
weight: 83
extra:
  question_no: "83"
  source_status: "미출"
  source_history: ""
  priority: 50
  priority_note: "IEEE 802.1AS 시간 동기화, IEEE 802.1Qbv TAS(GCL), IEEE 802.1Qbu 프레임 선점, IEEE 802.1CB FRER"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **TSN (Time-Sensitive Networking)**: 이더넷에 시간 동기·스케줄·신뢰성 기능을 더하는 IEEE 표준군.
- **Deterministic Latency**: 구성된 트래픽 조건에서 분석 가능한 지연 상한을 제공하는 특성.

</details>

- 정의/개념: 표준 이더넷 상에서 **초정밀 시간 동기화(802.1AS), 시간 스케줄링(802.1Qbv), 프레임 선점(802.1Qbu), 무손실 이중화(802.1CB)를 제공하는 결정론적 통신 기술군**
- 배경/필요성: 표준 이더넷(IEEE 802.3)은 최선형(Best-Effort) 전송 특성과 CSMA/CD 및 비동기 스위칭 구조로 인해 패킷 대기 큐(Queue)에서 예측 불가능한 지연(Latency)과 지터(Jitter)가 발생하여 마이크로초($\mu\text{s}$) 단위의 엄격한 데드라인을 요구하는 산업용 로봇 제어, 차량 자율주행, 항공 우주 통신에 직접 적용할 수 없는 한계를 극복하기 위해, IEEE 802.1 워킹그룹에서 초정밀 시간 동기화(802.1AS), 시간 인식 셰이퍼(802.1Qbv TAS), 프레임 선점(802.1Qbu), 무손실 이중화(802.1CB FRER) 표준군을 집대성한 시간 민감 네트워킹(TSN: Time-Sensitive Networking)을 도입하여 **표준 이더넷 인프라 상에서 마이크로초 미만 초저지연, 제로 지터 결정론성(Determinism) 및 0ms 무손실 고신뢰성**을 달성할 필요

#### 한줄 요약
- 802.1AS 정밀 동기화, 802.1Qbv GCL 스케줄링, 802.1Qbu 프레임 선점, 802.1CB 무손실 이중화를 제공한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **TAS (Time-Aware Shaper / 802.1Qbv)**: 전역 시간표(GCL)에 따라 8개 송출 큐의 게이트를 On/Off 제어하여 실시간 패킷을 무지연 전송하는 셰이퍼.
- **Frame Preemption (802.1Qbu / 802.3br)**: 긴 비실시간 프레임 전송 도중 긴급 실시간 프레임이 도착하면 전송을 일시 중단(선점)하고 우선 송출하는 기술.

</details>

- **마이크로초($\le 1\mu\text{s}$) 미만 초정밀 시간 동기화**: IEEE 802.1AS(gPTP)를 통해 **전체 네트워크 브리지와 종단의 시계를 나노초 단위 동기화**
- **시간 인식 셰이퍼(TAS) 기반 버퍼 제로 지연**: GCL 타임 슬롯에 맞춰 게이트를 개폐하여 **실시간 큐 패킷의 버퍼 대기 지연 원천 제거**
- **하드웨어 레벨 프레임 선점(Preemption)**: 긴 비실시간 프레임을 분할 전송하여 **긴급 패킷의 블로킹 지연 극소화**
- **프레임 복제 및 무손실 중복 제거(FRER)**: IEEE 802.1CB를 통해 **다중 경로 동시 전송으로 단선 시 0ms 무중단 전환**

#### 한줄 요약
- 결정론적 지연을 얻는 대가로 전 구간 시각 동기와 사전 스케줄 계산을 상시 유지해야 하며, 예약된 슬롯은 비어 있어도 다른 트래픽에 내주지 않는다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **CNC (Centralized Network Configurator)**: 토폴로지와 대역폭을 파악하여 최적 경로와 스위치별 GCL 시간표를 계산·배포하는 SDN 제어기 (802.1Qcc).
- **FRER (Frame Replication and Elimination for Reliability / 802.1CB)**: 프레임을 2개 이상의 독립 경로로 복제 송신하고 수신단에서 중복분을 제거하는 기술.

</details>

```text
[TSN 결정론적 네트워크]
  │
  ├─ [중앙 제어] ── Central Control
  │     └─ [CNC·CUC] (스트림 요구 수집과 경로·일정 구성)
  ├─ [동기화] ── Synchronization
  │     └─ [시간 동기화 엔진] (공통 시간 기준 배포)
  ├─ [트래픽 제어] ── Traffic Control
  │     └─ [TAS 스케줄러] (GCL에 따른 큐 게이트 제어)
  ├─ [대기시간 단축] ── Latency Reduction
  │     └─ [프레임 선점기] (긴 저우선 프레임의 블로킹 축소)
  └─ [무손실 신뢰성] ── Lossless Reliability
        └─ [FRER 복제기] (다중 경로 복제와 중복 제거)
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| **CNC·CUC** | 스트림 요구 수집과 경로·일정 구성 |
| **시간 동기화 엔진** | 공통 시간 기준 배포 |
| **TAS 스케줄러** | GCL에 따른 큐 게이트 제어 |
| **프레임 선점기** | 긴 저우선 프레임의 블로킹 축소 |
| **FRER 복제기** | 다중 경로 복제와 중복 제거 |

#### 한줄 요약
- CNC가 전체 브리지의 시간표를 중앙에서 계산해 주입하므로, 개별 브리지는 국소 판단 없이 계산된 게이트 개폐만 집행한다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **GCL (Gate Control List)**: 각 타임 인터벌마다 어떤 우선순위 큐의 게이트를 열고(Open) 닫을지(Close) 8비트 마스크로 정의한 시간표.

</details>

```text
[TSN 스트림 등록·결정론적 전송 흐름] (진행 ①→⑤, 요구 등록에서 진입, ①→② 전역 스케줄링, ③ **GCL** 주입, ④ 동기 송출, ⑤ 선점·중복제거 수신)
  │
  ├─ [Talker 요구 등록부] (① 주기·프레임 크기·최대 허용 지연을 CUC에 제출)
  │
  ├─ [CNC 전역 스케줄러] (② 전역 토폴로지 분석으로 충돌 없는 **GCL** 시간표 산출)
  │
  ├─ [브리지 GCL 주입부] (③ NETCONF/RESTCONF로 경로상 브리지 레지스터에 시간표 적재)
  │
  ├─ [802.1AS 동기 송출부] (④ 나노초 동기 시계에 맞춰 해당 타임 슬롯 게이트 개방 후 송신)
  │
  └─ [선점·FRER 수신단] (⑤ 저우선 프레임 분할 선점과 복제 경로 수신 후 중복 제거로 데드라인 내 도착)
```

분기 결과: ① 스트림 등록 가부에서 갈라져 수용된 스트림은 ② CNC가 산출한 **GCL** 전용 슬롯으로 데드라인을 보장받고 미등록 트래픽은 ⑤ 선점 대상 최선형으로 강등되며, 보장 쪽은 슬롯이 놀아도 타 트래픽에 개방하지 않는 대역폭 예약 비용을 치른다.

#### 한줄 요약
- 스트림 등록 가부에서 보장 대역 확보와 최선형 강등으로 갈리며, 보장은 미사용 시간 슬롯을 비워 두는 대가로만 성립한다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **TAS (802.1Qbv)** vs **Frame Preemption (802.1Qbu)** vs **ATS (802.1Qcr)**: 절대 시간표 게이트 제어, 하드웨어 선점 분할, 시계 동기화 없는 비동기 셰이핑.

</details>

| 비교 항목 | 시간 인식 셰이퍼 (TAS / 802.1Qbv) | 프레임 선점 (Preemption / 802.1Qbu) | 비동기 트래픽 셰이퍼 (ATS / 802.1Qcr) |
|:---|:---|:---|:---|
| 제어 방식 | **GCL 시간표** 기반 게이트 | 저우선 프레임 분할·선점 | 홉별 비동기 셰이핑 |
| 시간 동기 | 필요 | 단독 사용 시 불필요 | 불필요 |
| 지연 관리 | 예약 일정으로 상한 설계 | 직렬화 블로킹 축소 | 홉별 도착률 제어 |
| 오버헤드 | 가드 밴드·일정 관리 | 분할·재조립 | 큐별 상태 관리 |
| 적용 영역 | 주기적 실시간 스트림 | 긴 프레임 혼재 링크 | 비동기 혼합 트래픽 |

#### 한줄 요약
- TAS는 절대 시간표 기반 최고 정밀도 제어, 선점은 블로킹 방지, ATS는 시계 동기화 없는 비동기 제어에 최적화된다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **Guard Band (가드 밴드)**: TAS 환경에서 실시간 전용 타임 슬롯 시작 직전 비실시간 프레임 송출을 차단하여 윈도우 침범을 막는 안전 갭.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 비실시간 프레임 전송으로 인한 실시간 윈도우 침범 및 가드 밴드 대역폭 낭비 | **`IEEE 802.1Qbu 프레임 선점(Preemption)` 기술을 TAS와 결합** | 가드 밴드 크기를 1/10로 축소하고 실시간 지연 원천 차단 |
| 물리 링크/스위치 장애 시 패킷 유실 및 실시간성 붕괴 | **`IEEE 802.1CB FRER (프레임 복제 및 중복 제거)` 무손실 이중화** | 링크 단선 시 0ms 무중단 페일오버 및 제로 패킷 손실 |
| 대규모 브리지 경유 시 누적 지터로 인한 802.1AS 시간 오차 발생 | **`하드웨어 타임스탬핑 PHY 칩셋` 및 Transparent Clock(TC) 적용** | 100홉 통과 시에도 $1\mu\text{s}$ 이내 동기 정밀도 유지 |
| 다중 스트림 추가 시 실시간 GCL 시간표 재연산 병목 | **`계층적 분산 CNC 스케줄러` 및 증분 스케줄링(Incremental) 적용** | GCL 재연산 시간 단축 및 동적 스트림 추가 수용 |

#### 한줄 요약
- 프레임 선점으로 가드 밴드를 축소하고, FRER로 무중단 이중화를 달성하며, 하드웨어 타임스탬핑으로 동기 정밀도를 보장한다.

## Ⅶ. 결론

- 과거 독점적 산업용 필드버스(EtherCAT, PROFINET)와 차량용 CAN/FlexRay로 파편화되었던 실시간 제어망을 단일 표준 이더넷으로 통합하는 **차세대 스마트 제조(OPC UA over TSN), SDV 차량 백본 및 5G/6G 프론트홀의 핵심 결정론적 네트워크 인프라 표준**으로 확립.
- 실무 TSN 시스템 구축 시에는 **비실시간 프레임에 의한 윈도우 침범을 방지하고 가드 밴드 낭비를 제거하는 IEEE 802.1Qbu 프레임 선점 결합**, **물리 단선 시 0ms 무중단 전환을 보장하는 IEEE 802.1CB FRER 이중화**, **100홉 경유 시에도 1μs 이내 정밀도를 유지하는 하드웨어 타임스탬핑 PHY(802.1AS) 채택**, **중앙 집중형 CNC(802.1Qcc)를 통한 무충돌 GCL 시간표 자동 연산**을 결합하여 완벽한 결정론적 전송 품질을 완성.

#### 한줄 요약
- TSN은 802.1AS 시간 동기, 802.1Qbv 시간 스케줄링, 802.1Qbu 선점을 결합하여 표준 이더넷 위에서 결정론적 초저지연을 실현하는 차세대 네트워크 기술이다.
