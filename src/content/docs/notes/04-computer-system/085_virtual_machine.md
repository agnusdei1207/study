---
title: "가상머신(Virtual Machine)"
author: "Gemini 3.8 Flash"
date: "2026-09-20T00:44:00+09:00"
tags:
  - "notes-computer-system"
sidebar:
  badge:
    text: "기출 · 72%"
extra:
  model: "Gemini 3.8 Flash"
  source_status: "기출"
  source_history: "120회"
  priority: 72
  priority_note: "[출제(KPC):120]"
---

## 답안 골격
```text
[가상머신] ◀━━ 머리: Ⅶ 내 의견 (MicroVM 도입과 하이브리드 오케스트레이션을 통한 가상머신의 보안성과 컨테이너의 민첩성 융합)
 ┃
 ┣━ Ⅰ 개요 ───── 물리 하드웨어의 자원 독점과 OS 종속성 탈피 → 하이퍼바이저를 통해 단일 물리 서버 위에 소프트웨어로 에뮬레이션된 독립 가상 컴퓨터
 ┣━ Ⅱ 특징 ───── 완전한 커널 수준 격리 · 게스트 OS(Windows/Linux) 자유도 · 하드웨어 추상화(vCPU/vRAM) · 스냅샷 및 마이그레이션
 ┣━ Ⅲ 구조 ───── 물리 서버 + 하이퍼바이저(VMM) + 가상 하드웨어 계층 + 게스트 OS + 애플리케이션 바이너리
 ┣━ Ⅳ 흐름 ───── ① VM 생성 및 자원 할당 → ② 게스트 OS 부팅 → ③ 특권 명령 하이퍼바이저 트랩(VM-Exit) 중재 → ④ 물리 자원 매핑 실행
 ┣━ Ⅴ 비교 ───── 가상머신(VM) vs 컨테이너(Container)
 ┗━ Ⅵ 실무 ───── 게스트 OS 중복으로 인한 스토리지/메모리 오버헤드 / 느린 부팅 시간 / 가상화 이스케이프(Escape) 위험
```
- 필수 키워드: 가상머신(VM) · 하이퍼바이저 · 게스트 OS · 커널 수준 격리 · vCPU/vRAM · VM-Exit · 라이브 마이그레이션
- 배점 전략: 10점 = Ⅰ 개요 → Ⅲ 가상머신 vs 컨테이너 계층 아키텍처 비교도 → Ⅴ VM vs 컨테이너 8대 비교표 / 25점 = Ⅰ~Ⅶ 전개, Ⅳ 하드웨어 자원 가상화(CPU/Mem/IO) 원리 및 Ⅵ 현대 클라우드 MicroVM(Firecracker) 진화 방향
- 기출: 120회 2교시 4번 `가상머신(Virtual Machine)과 컨테이너(Container) 비교` 논술형 출제

## 한 줄 본질
- 하나의 서버에 1개의 OS만 띄우면 자원 활용률이 저조하고 이기종 OS 앱을 함께 띄울 수 없음 → 하이퍼바이저가 가상 하드웨어를 생성하고 그 위에 완전한 독립 커널을 가진 게스트 OS를 통째로 구동 → 완벽한 보안 격리 및 레거시 OS 호환성 확보 / 게스트 OS 구동에 따른 메모리 점유 및 수십 초의 부팅 지연 수반

## 핵심 그림
```text
[ 가상머신(VM) vs 컨테이너(Container) 계층 구조 및 격리 경계 비교 ]

(1) 가상머신 아키텍처 (완전 하드웨어 가상화)     (2) 컨테이너 아키텍처 (OS 커널 공유 가상화)
+─────────────────────────────────+             +─────────────────────────────────+
| App A           | App B         |             | App A           | App B         |
| (Libs / Bins)   | (Libs / Bins) |             | (Libs / Bins)   | (Libs / Bins) |
+─────────────────+───────────────+             +─────────────────+───────────────+
| 게스트 OS (A)   | 게스트 OS (B) |             |   컨테이너 엔진 (Docker/containerd) |
| (독립 리눅스)   | (독립 윈도우) |             +─────────────────────────────────+
+─────────────────+───────────────+             |   호스트 OS 커널 (Linux Kernel)  |
| 가상화 계층 (하이퍼바이저: KVM) |             |   - cgroups (자원 격리)          |
+─────────────────────────────────+             |   - namespaces (프로세스 격리)  |
| 물리 하드웨어 (CPU, RAM, DISK)  |             +─────────────────────────────────+
+─────────────────────────────────+             |   물리 하드웨어 (CPU, RAM, DISK) |
[ 격리 수준: 극상 (커널 침범 불가) ]            +─────────────────────────────────+
[ 오버헤드: GB 단위 용량, 수십초 부팅 ]         [ 격리 수준: 중간 (호스트 커널 취약점 공유) ]
                                                [ 오버헤드: MB 단위 용량, 밀리초 부팅 ]
```

## 핵심 용어
- 라이브 마이그레이션(Live Migration): 실행 중인 가상머신의 메모리 페이지와 CPU 상태를 네트워크를 통해 다른 물리 서버로 무중단(서비스 다운타임 수십 밀리초 이내) 이전하는 기술
- vCPU(Virtual CPU): 하이퍼바이저가 게스트 OS에 제공하는 가상 프로세서로, 물리 CPU 코어의 하이퍼스레딩 스레드에 1:1 또는 시분할 오버커밋(Overcommit) 매핑됨
- MicroVM: 대형 가상머신의 느린 부팅과 무거운 메모리를 극복하기 위해 레거시 하드웨어 에뮬레이션을 전면 제거하고 5ms 부팅을 구현한 초경량 가상머신(AWS Firecracker)

## 핵심 통찰
- 가상머신의 진정한 무기는 '보안 격리'임 → 컨테이너는 호스트 리눅스 커널을 공유하므로 커널 익스플로잇 취약점이 터지면 옆 컨테이너가 털리지만, 가상머신은 하드웨어 링(Ring) 레벨에서 완전히 분리되어 있어 멀티테넌트 퍼블릭 클라우드의 필수 불가결한 기본 단위임
- OS 중복의 비효율성을 안고 감 → 가상머신 100대를 띄우면 100개의 윈도우/리눅스 커널이 동일한 파일과 프로세스를 메모리에 중복 로드하여 기가바이트 단위의 RAM이 낭비됨
- 스냅샷과 롤백의 절대적 안정성 → 운영체제 전체 상태(디스크+RAM)를 단 1초 만에 스냅샷으로 박제할 수 있어, 위험한 시스템 업그레이드 실패 시 즉각 원상 복구할 수 있는 엔터프라이즈 신뢰성을 제공함

## 이웃 토픽과 구분
- 가상머신 vs 컨테이너: 가상머신 = 하드웨어 에뮬레이션, 독립 커널, GB 단위 크기, 완벽한 격리 / 컨테이너 = OS 커널 프로세스 격리(cgroups/namespace), 공유 커널, MB 단위 크기, 빠른 기동

## 문제·원인·대책
- 사례: 120회 기출 및 단일 물리 호스트 내 다수 VM 구동 중 특정 VM의 I/O 폭증으로 이웃 VM 성능 저하(Noisy Neighbor)
| 문제 | 원인 | 대책 | 효과 |
|---|---|---|---|
| 특정 VM의 CPU/디스크 점유로 동일 호스트 내 이웃 VM 응답 지연 | 하이퍼바이저 스케줄러의 자원 상한선 미설정(Overcommit 경합) | CPU 고정(CPU Pinning) 및 가상화 I/O 스케줄러(cgroups blkio) 한도 제한 | VM 간 자원 간섭 완벽 격리 및 일관된 성능 보장 |
| 수백 개 가상머신 스케일아웃 시 스토리지 용량 고갈 및 기동 지연 | VM마다 수십 GB의 OS 기본 이미지(ISO) 중복 설치 | 씬 프로비저닝(Thin) 및 연결 복제(Linked Clone / CoW) 기법 적용 | 스토리지 점유율 80% 절감 및 수초 내 VM 복제 배포 |

## 이렇게 출제된다
- 제120회 2교시 4번: "가상머신(Virtual Machine)과 컨테이너(Container)를 비교하여 설명하시오." → 요구 포인트: Ⅰ 가상머신과 컨테이너의 개념 정의 + Ⅲ 계층 구조도 및 가상화 경계 비교 + Ⅴ 자원 효율성, 기동 속도, 보안 격리성 등 다차원 비교표

## 내 의견
- [MicroVM과 카타 컨테이너를 결합한 가상머신-컨테이너 융합 인프라 표준화] 가상머신(보안 우수, 무거움)과 컨테이너(빠름, 격리 취약)의 이분법적 선택은 현대 클라우드에서 한계를 드러냄 → 나라면: 쿠버네티스 클러스터 런타임에 Kata Containers(MicroVM 기반)를 도입하여, 사용자는 컨테이너(Docker/K8s)의 빠른 CI/CD와 가벼운 배포 경험을 누리되 하부 실행은 독립된 가상머신 커널로 감싸는 '보안 샌드박스 컨테이너' 인프라 표준 구축

## 찾아볼 것
- 인텔 SGX 및 AMD SEV(Secure Encrypted Virtualization)를 활용한 가상머신 메모리 기밀 컴퓨팅
