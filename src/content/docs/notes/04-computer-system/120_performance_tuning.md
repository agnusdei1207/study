---
title: "성능 튜닝(Performance Tuning)"
author: "Gemini 3.8 Flash"
date: "2026-09-20T09:42:00+09:00"
tags:
  - "notes-computer-system"
sidebar:
  badge:
    text: "기출 · 58%"
extra:
  model: "Gemini 3.8 Flash"
  source_status: "기출"
  source_history: "81회, 95회"
  priority: 58
  priority_note: "이전(KPC):81,95"
---

## 답안 골격
```text
[성능 튜닝] ◀━━ 머리: Ⅶ 내 의견 (APM 풀스택 가시성 기반 병목 식별과 CI/CD 성능 회귀 자동화 테스트 파이프라인 정착)
 ┃
 ┣━ Ⅰ 개요 ───── 하드웨어 증설만으로 해결되지 않는 자원 경합 및 응답 지연 → 전 계층 과학적 계측 기반 성능 튜닝
 ┣━ Ⅱ 특징 ───── 파레토(80/20) 법칙 적용 · 베이스라인(기준선) 대비 비교 · 전 계층(End-to-End) 연계 최적화 · 병목 전이 방지
 ┣━ Ⅲ 구조 ───── 계측 도구(APM·OS·DBMS) + 튜닝 대상(OS 커널·WAS 스레드/GC·DB 인덱스/SQL·App 코드) + 검증 피드백 루프
 ┣━ Ⅳ 흐름 ───── ① 목표 성능 및 베이스라인 설정 → ② 병목 구간 식별 → ③ 원인 분석 및 튜닝안 수립 → ④ 테스트베드 검증 → ⑤ 운영 반영
 ┣━ Ⅴ 비교 ───── 하드웨어 증설(Scale-up) vs 인프라 파라미터 튜닝 vs 애플리케이션/SQL 아키텍처 튜닝
 ┗━ Ⅵ 실무 ───── 한 곳을 고치면 다른 곳이 막히는 병목 전이(Bottleneck Shifting) / 운영 서버 직접 파라미터 수정 / GC STW 지연
```
- 필수 키워드: 성능 튜닝 · 병목(Bottleneck) · 응답 시간(Response Time) · 처리량(TPS) · 베이스라인 · APM · SQL 튜닝 · GC 튜닝
- 배점 전략: 10점 = Ⅰ 개요 → Ⅲ 성능 튜닝 5단계 절차도 → Ⅴ 튜닝 계층별 비교표 / 25점 = Ⅰ~Ⅶ 전개, Ⅲ OS/WAS/DB 계층별 핵심 파라미터와 Ⅵ 병목 전이 트러블슈팅 및 성능 테스트 자동화
- 기출: 81회 1교시, 95회 2교시 `성능 튜닝의 개념, 절차 및 계층별 튜닝 기법` 출제

## 한 줄 본질
- 시스템 자원을 무작정 증설하지 않고 하드웨어, OS, WAS, 데이터베이스, 애플리케이션 전 계층의 응답 지연 원인을 계측하여 핵심 병목(Bottleneck)을 제거함으로써 최소 비용으로 처리량(TPS)을 높이고 응답 시간을 단축하는 엔지니어링 절차

## 핵심 그림
```text
[ 성능 튜닝 5단계 표준 절차 및 계층별 핵심 튜닝 영역 ]

  [ 1단계 ]        [ 2단계 ]         [ 3단계 ]         [ 4단계 ]         [ 5단계 ]
+───────────+    +───────────+    +───────────+    +───────────+    +───────────+
| 목표 수립 | -> | 병목 측정 | -> | 원인 분석 | -> | 튜닝 적용 | -> | 검증·반영 |
+───────────+    +───────────+    +───────────+    +───────────+    +───────────+
| TPS, 지연 |    | APM, OS   |    | CPU/메모리|    | 파라미터  |    | 베이스라인|
| 기준 설정 |    | DB 프로파일|   | I/O, 락경합|   | SQL 재작성|   | 대비 확인 |
+───────────+    +───────────+    +───────────+    +───────────+    +───────────+
                                        │
                                        ▼
+───────────────────────────────────────────────────────────────────────────────+
| 계층별 핵심 튜닝 영역 (Full-Stack Tuning Points)                              |
| - App: 비동기 논블로킹(I/O 분리), 캐싱(Redis), 알고리즘 복잡도 개선(O(N) 축소)|
| - WAS: 힙 메모리 크기(Heap Size), GC 알고리즘(G1/ZGC), 스레드/커넥션 풀(DBCP) |
| - DB : 인덱스 재설계, Slow SQL 튜닝, 힌트(Hint) 최적화, 락 경합 및 대기 해소 |
| - OS : 파일 디스크립터(ulimit), TCP 소켓 버퍼(sysctl), 스와핑(Swappiness) 차단|
+───────────────────────────────────────────────────────────────────────────────+
```

## 핵심 용어
- 병목(Bottleneck): 시스템 전체의 흐름을 제한하여 최대 처리량과 응답 속도를 저하시키는 가장 취약한 단일 구성요소 또는 구간
- 베이스라인(Baseline): 튜닝 작업 전 정상 운영 상태에서 시스템이 나타내는 기준 성능 측정값으로, 튜닝 효과의 성공 여부를 입증하는 비교 기준
- 병목 전이(Bottleneck Shifting): 한 계층(예: WAS 스레드 풀)을 튜닝하여 처리량을 늘렸을 때, 그 하위 계층(예: DB 커넥션 풀 또는 디스크 I/O)으로 병목이 이동하여 시스템이 더 크게 마비되는 현상

## 핵심 통찰
- 80/20 파레토 법칙의 지배 → 전체 시스템 성능 저하의 80%는 단 20%의 악성 SQL과 부적절한 인덱스 설계, 그리고 불필요한 반복 네트워크 호출에서 발생하므로, 인프라 파라미터를 건드리기 전에 데이터베이스 쿼리와 애플리케이션 코드를 먼저 정밀 분석해야 함
- 하드웨어 증설(Scale-up)은 근본 해법이 아님 → 락(Lock) 경합이나 소프트웨어 병목이 존재하는 상태에서 CPU를 2배로 늘려도 처리량은 10%도 증가하지 않으며, 오히려 코어 간 캐시 동기화 오버헤드로 인해 응답 지연이 악화될 수 있음
- 튜닝은 한 번에 '오직 하나의 변수'만 변경해야 함 → 여러 파라미터(힙 크기, 스레드 수, 커널 값)를 동시에 수정하면 어떤 변경이 성능 개선을 가져왔고 어떤 변경이 부작용을 일으켰는지 공학적으로 인과관계를 입증할 수 없음

## 이웃 토픽과 구분
- 부하 테스트(Load Testing) vs 성능 튜닝(Performance Tuning): 부하 테스트 = 임계치까지 가상 트래픽을 인가하여 시스템의 한계 용량과 취약점을 측정하는 진단 행위 / 성능 튜닝 = 측정된 취약점과 병목의 근본 원인을 찾아 설정을 수정하고 코드를 고치는 치료 행위

## 문제·원인·대책
- 적용 상황: 대규모 금융 거래 시스템 개통 전 성능 검증 및 튜닝
| 문제 | 원인 | 대책 | 효과 |
|---|---|---|---|
| WAS 스레드 풀을 200에서 1,000으로 늘린 후 오히려 시스템 전체 응답 불가 | 하위 DB 커넥션 풀 고갈 및 CPU의 과도한 문맥 교환(Context Switching) | 리틀의 법칙(Little's Law) 기반 스레드 풀 최적화 및 DB 커넥션 풀 1:1 정합 | 스레드 경합 제거 및 CPU 안정화 |
| 특정 대량 조회 화면 호출 시 JVM의 장시간 정지(Stop-The-World) 발생 | 수십만 건 객체 일괄 메모리 로드로 인한 Major GC 빈발 | 저지연 가비지 컬렉터(ZGC) 전환 및 페이지네이션(Limit/Offset) 강제 | GC 일시 정지 시간을 1ms 미만으로 단축 |
| 특정 테이블 UPDATE 시 동시 접속 트랜잭션 수백 개가 대기열에 걸려 타임아웃 | 인덱스 부재로 인한 테이블 전체 락(Table Lock) 유발 | 복합 인덱스 추가를 통한 로우 레벨 락(Row Lock) 전환 및 트랜잭션 범위 축소 | 락 대기 시간 제거 및 동시 처리량 증대 |

## 이렇게 출제된다
- 제81회 1교시: "소프트웨어 성능 튜닝의 개념 및 절차를 설명하시오." → 요구 포인트: Ⅰ 성능 튜닝 개념 + Ⅲ 5단계 절차도 + Ⅱ 원칙(파레토 법칙)
- 제95회 2교시: "웹 애플리케이션 시스템(WAS)과 데이터베이스(DB) 환경에서의 병목 현상 원인과 계층별 튜닝 방안을 설명하시오." → 요구 포인트: Ⅲ 계층별 병목 원인 도식 + Ⅴ WAS/DB 상세 튜닝 기법 비교표 + Ⅵ 병목 전이 방지책

## 내 의견
- [APM 기반 트랜잭션 추적 체계와 배포 파이프라인(CI/CD) 성능 게이트 자동화] 오픈 직전에 한 번 몰아서 수행하는 성능 튜닝은 기한에 쫓겨 땜질식 처방으로 끝나기 쉬움 → 나라면: 개발 단계부터 OpenTelemetry/APM 기반의 전 구간 분산 추적(Distributed Tracing)을 기본 탑재하고, Git PR 병합 시 k6 기반의 베이스라인 부하 테스트를 자동 실행하여 응답 지연이 10% 이상 증가한 코드는 머지를 자동 거부하는 '성능 회귀 방지 게이트'를 구축하여 성능 품질을 지속적으로 통제

## 찾아볼 것
- OpenTelemetry 표준 분산 추적 규격 및 eBPF 기반 무중단 커널 레벨 프로파일링
