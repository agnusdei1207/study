---
title: "실루엣 계수(Silhouette Coefficient)"
date: "2026-09-24T00:00:00+09:00"
tags:
  - "notes-latest-tech"
extra:
  model: "GPT-6"
---

## 지식 로드맵 내 현재 위치
IT 인공지능 → 비지도 학습 → 군집 타당성 평가 → 실루엣 계수

## 30초 인출
- **본질:** 실루엣 계수는 각 샘플이 자기 군집에는 가깝고 다른 군집에는 얼마나 떨어져 있는지 나타낸다.
- **메커니즘:** 군집 내부 평균거리 `a(i)`와 가장 가까운 다른 군집까지의 평균거리 `b(i)`를 비교한다.
- **해석:** 값은 −1~1 범위이며 높을수록 해당 분할에서 응집·분리가 양호하지만, 최적 군집 수를 자동 보장하지는 않는다.

<details><summary>핵심 용어</summary>

- **a(i):** 샘플 `i`와 같은 군집의 다른 샘플 간 평균 거리다.
- **b(i):** 샘플 `i`와 가장 가까운 다른 군집 샘플 사이의 평균 거리다.
- **실루엣 계수:** 두 거리의 상대적 차이로 각 샘플의 군집 적합도를 나타내는 값이다.
- **실루엣 다이어그램:** 샘플별 계수를 군집별로 시각화해 분포와 음수 값을 확인하는 그래프다.
</details>

---

## 1교시 예상문제 (10점)
> 실루엣 계수의 산출 원리와 값의 범위를 설명하고, 군집 수 선택 시 유의점을 서술하시오. (제135회 1교시 6번)

---

## 1교시 10점 답안
### 1. 정의·목적
정의: 실루엣 계수는 샘플의 군집 내부 응집도와 다른 군집과의 분리도를 함께 평가하는 지표다.
목적: 군집 배정과 군집 수 후보를 비교하는 데 활용한다.

### 2. 거리와 산출
```mermaid
flowchart TD
    I[샘플 i] -->|같은 군집 내 평균거리| A[a(i)]
    I -->|다른 군집별 평균거리 중 최솟값| B[b(i)]
    A -->|거리 비교| S[실루엣 s(i) = (b-a) / max(a,b)]
    B -->|거리 비교| S
```

### 3. 값 해석·활용
| 값 | 해석 |
|---|---|
| 1에 가까움 | 자기 군집에 가깝고 다른 군집과 분리 |
| 0 부근 | 군집 경계 부근일 가능성 |
| 음수 | 다른 군집에 더 가까울 수 있음 |

군집별 분포를 확인하고 여러 `k` 후보를 비교한다. 높은 평균값 하나만으로 최적 `k`를 단정하지 않는다.

**제언:** 평균과 군집별 분포를 함께 살펴 데이터 구조·업무 목적에 맞는 군집 수를 선택한다.

---

## 2~4교시 예상문제 (25점)
> 실루엣 계수의 정의·수식·해석을 설명하고, 군집 수 선택 및 대규모·비구형 데이터에서의 적용상 한계와 보완 방안을 제시하시오. (제135회 1교시 6번을 확장한 예상문제)

---

## 2~4교시 25점 답안
## Ⅰ. 개념과 평가 범위
실루엣 계수는 레이블 없는 군집 결과에서 각 샘플의 내부 응집도와 최근접 타 군집과의 분리도를 비교한다. 라벨 정답이 없어도 군집 결과를 요약할 수 있지만, 특정 형태의 군집을 선호할 수 있다.

| 평가 수준 | 관찰 대상 |
|---|---|
| 샘플 | 개별 계수의 크기와 부호 |
| 군집 | 계수 분포·크기·음수 비율 |
| 전체 | 샘플 계수의 평균과 군집 간 차이 |

## Ⅱ. 산출 수식과 의미
`a(i)`는 같은 군집의 다른 샘플과의 평균 거리, `b(i)`는 각 타 군집까지의 평균 거리 중 가장 작은 값이다.

```mermaid
flowchart TD
    I[샘플 i] -->|같은 군집 내 평균거리| A[a(i)]
    I -->|다른 군집별 평균거리 중 최솟값| B[b(i)]
    A -->|거리 비교| S[실루엣 s(i) = (b-a) / max(a,b)]
    B -->|거리 비교| S
```

`s(i) = (b(i) − a(i)) / max(a(i), b(i))`이며 −1에서 1 사이를 갖는다. 값이 클수록 해당 거리 기준에서 군집 배정이 양호하고, 음수이면 다른 군집에 더 가까울 수 있다.

## Ⅲ. 군집 수 비교와 한계
| 항목 | 적용 방법 | 유의점 |
|---|---|---|
| 군집 수 `k` | 같은 전처리·거리척도에서 후보별 평균 비교 | 평균만 최대로 만드는 것이 목표는 아님 |
| 분포 | 군집별 실루엣 다이어그램·음수 비율 점검 | 작은 군집과 이상치 영향을 살핌 |
| 거리척도 | 데이터 특성에 맞는 척도 선택 | 척도와 표준화에 결과가 달라짐 |
| 구조 적합성 | 업무 지식·시각화와 함께 평가 | 비볼록·밀도 기반 구조에서 낮은 점수 가능 |

고전적 평균 실루엣은 서로 다른 크기·밀도의 군집이나 비구형 구조를 충분히 반영하지 못할 수 있다. `k` 선택은 여러 내부 지표와 도메인 타당성을 함께 고려한다.

## Ⅳ. 기술사적 제언
| 문제 | 해결 방안 |
|---|---|
| 전체 평균이나 전 데이터 쌍 계산에 의존해 불균형 군집·대용량 데이터를 오판할 수 있음 | 군집별 분포를 점검하고 표본 기반 근사를 검토하며, 데이터 형태에 적합한 평가법과 현업 해석을 병행한다. |

## 출제 이력과 검증 출처
- 제135회 1교시 6번: “실루엣 계수(Silhouette Coefficient)”.
- [scikit-learn: Clustering performance evaluation](https://scikit-learn.org/stable/modules/clustering.html#silhouette-coefficient), 정의·해석 및 한계.
- [scikit-learn: silhouette_score API](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.silhouette_score.html), 평균 실루엣 계수와 표본 계산 옵션.
