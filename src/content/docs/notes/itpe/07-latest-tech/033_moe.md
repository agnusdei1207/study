---
title: "MoE(Mixture of Experts)"
author: "GPT-6"
date: "2026-09-24T00:00:00+09:00"
sidebar:
  order: 33
  label: "033. MoE"
  badge:
    text: "서브"
    variant: note
tags:
  - "notes-latest-tech"
extra:
  model: "GPT-6"
  keyword_grade: "서브"
---

## 지식 로드맵 내 현재 위치

지식 위치: 신경망 구조 → 조건부 희소 계산 → **전문가 혼합(MoE)**

## 30초 인출

- 본질: **전문가 혼합(Mixture of Experts, MoE)** 은 라우터가 입력별 전문가를 선택해 일부 하위망만 계산하는 신경망 구조
- 메커니즘: 토큰 표현 → 라우터 점수·전문가 선택 → 선택 전문가 처리 → 출력 가중 결합

<details>
<summary>핵심 용어</summary>

- **전문가 혼합(Mixture of Experts, MoE)** : 입력에 따라 하위 전문가 일부를 선택적으로 활성화하는 신경망 구조
- **전문가(Expert)** : 입력 일부를 처리하는 학습 가능한 하위 신경망
- **라우터(Router)** : 각 입력을 처리할 전문가를 점수화·할당하는 구성요소
- **희소 활성화(Sparse Activation)** : 전체 전문가 중 일부만 입력별로 계산하는 방식
- **부하 균형(Load Balancing)** : 입력이 일부 전문가에 집중되지 않도록 배분 상태를 조정하는 것
- **All-to-All 통신(All-to-All Communication)** : 분산 장치 사이에 입력 토큰을 전문가 담당 장치로 교환하는 통신 패턴
</details>

---
## 1교시 예상문제 (10점)

> MoE의 개념과 라우터 기반 희소 활성화 구조를 설명하시오. (예상·10점)

---
## 1교시 10점 답안

### Ⅰ. 개요

| 구분 | 핵심 |
|---|---|
| 정의 | **전문가 혼합(Mixture of Experts, MoE)** 은 라우터가 입력별 전문가를 선택해 일부 하위망만 계산하는 신경망 구조 |
| 목적 | 전체 모델 용량과 입력별 활성 계산량을 분리해 조건부 계산 제공 |

### Ⅱ. 토큰 라우팅 구조

```text
토큰 표현
   ↓
라우터 점수 산출
   ↓ 상위 전문가 선택
선택된 전문가 계산
   ↓
출력 결합
```

### Ⅲ. 구성요소

| 요소 | 역할 |
|---|---|
| 라우터 | 입력별 전문가 선택 |
| 전문가 | 할당된 입력의 표현 처리 |
| 결합 | 전문가 출력을 다음 계층으로 전달 |

제언: 토큰당 연산만 보지 않고 실제 부하·통신·메모리를 함께 평가하는 MoE 운영.

---
## 2~4교시 예상문제 (25점)

> MoE의 구성과 입력별 라우팅 원리를 설명하고, 품질·부하·통신·자원 측면의 적용 고려사항을 제시하시오. (예상·25점)

---
## 2~4교시 25점 답안

### Ⅰ. 개요

| 구분 | 핵심 |
|---|---|
| 정의 | **전문가 혼합(Mixture of Experts, MoE)** 은 라우터가 입력별 전문가를 선택해 일부 하위망만 계산하는 신경망 구조 |
| 목적 | 전체 모델 용량과 입력별 활성 계산량을 분리해 조건부 계산 제공 |

### Ⅱ. 토큰 라우팅 구조

```text
토큰 표현
   ↓
라우터 점수 산출
   ↓ 상위 전문가 선택
선택된 전문가 계산
   ↓
출력 결합
```

### Ⅲ. 논리 구성

| 구성 | 기능 |
|---|---|
| 라우터 | 토큰별 전문가 점수 계산·할당 |
| 전문가 묶음 | 할당된 토큰에 대한 하위망 계산 |
| 출력 결합 | 선택된 전문가 결과를 합성해 후속 계층으로 전달 |

### Ⅳ. 분산 운영 고려사항

| 관점 | 검토 내용 |
|---|---|
| 부하 균형 | 전문가별 토큰 집중·용량 제한·드롭 여부 |
| 통신 | 장치 간 토큰 교환과 All-to-All 지연 |
| 자원 | 전문가 배치·메모리·저장·오프로딩 비용 |
| 품질 | 라우팅 선택과 밀집 모델 기준선의 정확도·처리량 비교 |

희소 활성화는 매 입력에서 계산하는 전문가 수를 줄일 수 있으나 실제 지연·메모리 이점은 전문가 배치와 통신 구조에 따라 달라짐. 부하 균형 보조 손실 등은 널리 쓰이는 선택 기법이지 모든 MoE의 필수 구성은 아님.

### Ⅴ. 기술사적 제언

| 한계 | 해결 방안 |
|---|---|
| 총 매개변수나 토큰당 연산량만으로 운영상 이점을 판단하기 어려움 | 목표 장치·요청량에서 밀집 모델과 종단 간 처리량·지연·메모리를 같은 조건으로 측정한 뒤 채택 여부 결정 |

---
## 출제 이력과 검증 출처

- 제137회 1교시 7번: 트랜스포머(Transformer)와 MoE(Mixture of Experts)
- [Fedus et al., Switch Transformers](https://arxiv.org/abs/2101.03961): 희소 전문가 라우팅과 부하 균형 관련 설계 확인
