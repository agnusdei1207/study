---
title: "OFB"
author: "Gemini 3.8 Flash"
date: "2026-09-20T01:00:00+09:00"
tags:
  - "notes-security"
extra:
  model: "Gemini 3.8 Flash"

---

## 답안 골격
```text
[OFB(Output Feedback Mode)] ◀━━ 머리: Ⅶ 내 의견 (비트 플리핑 변조 취약점 극복 → 사전 연산 키스트림과 HMAC을 결합한 무결성 보장 동기식 스트림 암호화)
 ┃
 ┣━ Ⅰ 개요 ───── 블록 암호 엔진의 출력(Output) 자체를 다음 단계 암호화 엔진의 입력으로 피드백하여 평문과 무관한 의사 난수 키 스트림을 생성하고, 이를 평문과 XOR 연산하여 스트림 암호화하는 블록 암호 운용 모드
 ┣━ Ⅱ 특징 ───── 암호문 비트 오류는 대응 평문 비트에 국한 · 패딩 불필요 · 키스트림 사전 연산 가능 · 복호화에도 암호화 함수 사용. IV·동기 오류는 별도 영향
 ┣━ Ⅲ 구조 ───── 초기화 벡터(IV) + 대칭키 암호 엔진($E_K$) + 키 스트림 생성기($O_i$) + 평문/암호문 XOR 연산기
 ┣━ Ⅳ 흐름 ───── [키스트림 생성]: $O_0 = IV$, $O_i = E_K(O_{i-1})$ / [암호화]: $C_i = P_i \oplus O_i$ / [복호화]: 동일 키스트림 생성 후 $P_i = C_i \oplus O_i$
 ┣━ Ⅴ 비교 ───── CFB(암호문 $C_i$를 피드백, 1비트 에러 시 후속 블록 연쇄 파괴) vs OFB(엔진 출력 $O_i$를 피드백, 에러 전파 제로, 사전 연산 가능) vs CTR(카운터 암호화, 병렬 연산)
 ┗━ Ⅵ 실무 ───── 동일한 IV 재사용 시 키스트림 노출(Two-Time Pad 공격) / 비트 플리핑(Bit-flipping) 공격에 무방비(무결성 태그 미제공)
```
- 필수 키워드: OFB(Output Feedback) · 키 스트림(Key Stream) · 사전 연산(Pre-computation) · 오류 전파 없음 · 패딩 불필요 · 비트 플리핑 · $E_K$ 단일 엔진
- 출제 이력: 제127회 문항은 공식 Q-Net 원문 미수집으로 미확인. 아래 문항은 예상문제

## 한 줄 본질
- OFB는 블록 암호 출력의 반복 암호화로 키스트림을 만들고 평문·암호문과 XOR하는 방식. 암호문 한 비트 오류는 해당 평문 비트에 국한되지만 IV·동기 오류와 키스트림 재사용은 큰 위험

## 핵심 그림
```text
[OFB(Output Feedback) 운용 모드의 독립 키스트림 생성 및 암복호화 메커니즘]

[키스트림 생성 흐름 (평문/암호문과 무관하게 미리 계산 가능!)]
  [초기화 벡터 IV] ──> [암호 엔진 E_K] ──┬──> [키스트림 O1] ──> [암호 엔진 E_K] ──┬──> [키스트림 O2] ...
                                         │                                        │
                                (엔진 출력을 피드백!)                    (엔진 출력을 피드백!)

[실시간 암호화 및 복호화]
  [평문 P1] ──(XOR)──> [암호문 C1]         [암호문 C1] ──(XOR)──> [평문 P1]
                ▲                                          ▲
                │                                          │
          [키스트림 O1]                              [키스트림 O1]

* 암호문 C1의 한 비트 오류는 동기가 유지될 때 해당 평문 비트에만 영향. IV나 비트 정렬 오류는 별도 분석 필요.
```

## 핵심 용어
- 독립 키 스트림 (Independent Key Stream): 평문이나 암호문의 내용과 전혀 결합되지 않고 오직 초기화 벡터(IV)와 대칭키($K$)만으로 순수하게 생성되는 의사 난수 수열
- 비트 플리핑 공격 (Bit-flipping Attack): 암호문 $C_i$의 특정 비트를 0에서 1로 반전시키면 복호화된 평문 $P_i$의 동일한 비트 위치가 정확히 0에서 1로 반전되는 취약점을 악용하여 공격자가 데이터를 의도한 값으로 위변조하는 공격

## 핵심 통찰
- OFB는 암호문 비트 오류의 영향이 같은 위치의 평문 비트에 한정. IV 전송 오류나 비트 삽입·삭제로 동기가 어긋나는 경우에는 이후 복호 결과에도 영향
- 키스트림을 미리 계산할 수 있으나 OFB의 피드백 연산은 순차적이므로 병렬 생성 속도와 저장 비용을 구분해 평가
- 동일 키·IV를 재사용하면 $C_1 \oplus C_2 = P_1 \oplus P_2$가 되어 두 평문의 XOR가 노출. 이것만으로 두 원문이 항상 즉시 복원되는 것은 아님

## 이웃 토픽과 구분
- OFB vs CTR: OFB = 직전 출력을 순차 피드백하므로 키스트림 생성이 직렬(Serial) 방식 / CTR = 카운터 값을 독립 암호화하므로 키스트림 생성이 완전 병렬(Parallel) 방식 (현대에는 CTR이 우세)

## 문제·원인·대책
- 사례: 원격 제어 명령 패킷에 OFB 모드를 단독 적용했다가 공격자가 암호문 비트를 조작하여 '밸브 닫힘' 명령을 '밸브 열림'으로 변조한 사고
| 문제 | 원인 | 대책 | 효과 |
|---|---|---|---|
| 암호문 비트 플리핑을 통한 특정 제어 데이터 악의적 변조 | OFB가 기밀성만 제공하고 암호문 무결성(인증)을 검증하지 않음 | HMAC-SHA256 연계(Encrypt-then-MAC) 또는 AEAD(AES-GCM) 전환 | 암호문 1비트 변조 시에도 즉각 검증 실패로 패킷 폐기 |
| 고정 IV 재사용으로 인한 평문 간 XOR 노출 | 동일 키에서 IV 재사용 | 동일 키에 대해 고유한 IV 사용과 중복 방지 | 키스트림 재사용 위험 감소 |

## 1교시 예상문제 (10점)
- 블록 암호 OFB 모드의 키스트림 생성과 오류 전파 특성을 설명하시오.

## 1교시 10점 답안
- **정의:** 암호 함수의 출력을 다음 입력으로 피드백해 키스트림을 만들고 평문과 XOR하는 운용 모드.
- **식:** $O_0=IV$, $O_i=E_K(O_{i-1})$, $C_i=P_i\oplus O_i$; 복호화도 같은 키스트림으로 XOR.
- **특성:** 패딩 불필요, 사전 생성 가능, 암호문 비트 오류는 동기 유지 시 대응 평문 비트에 국한. 무결성은 제공하지 않음.

## 2~4교시 예상문제 (25점)
- OFB의 동작 원리와 CFB·CTR 비교, IV·무결성 위험의 대응을 설명하시오.

## 2~4교시 25점 답안
### Ⅰ. 키스트림
- IV를 암호화한 출력이 첫 키스트림이고 다음 암호 함수의 입력으로 반복 사용. 암·복호 모두 $E_K$ 활용.
### Ⅱ. 비교
| 모드 | 피드백·입력 | 암호문 비트 오류 | 사전 생성 |
|---|---|---|---|
| CFB | 앞선 암호문 | 후속 구간에도 영향 가능 | 불가 |
| OFB | 앞선 암호 함수 출력 | 해당 비트에 국한 | 가능하나 생성은 직렬 |
| CTR | 고유 카운터 블록 | 해당 비트에 국한 | 병렬 가능 |
### Ⅲ. 위험
- 동일 키·IV 재사용 시 키스트림 재사용, 암호문 비트 변조 시 평문 비트 변조, IV·동기 오류의 전파.
### Ⅳ. 대응
- 고유 IV 관리, Encrypt-then-MAC 또는 AEAD 채택, 메시지 순서·무결성 검증과 재동기화 절차.

## 이렇게 출제된다
- 예상문제: OFB의 키스트림 생성과 CFB·CTR 대비 특징을 설명하시오. 제127회 원문은 미확인

## 내 의견
- [신규 설계의 인증 암호 선택] OFB 단독 사용 시 무결성이 없으므로 신규 프로토콜에서는 환경의 지원 암호군·성능과 상호 운용 요구에 맞는 AEAD 방식을 검토

## 출처
- [NIST SP 800-38A](https://csrc.nist.gov/pubs/sp/800/38/a/final): OFB의 키스트림·IV·전송 오류 특성
