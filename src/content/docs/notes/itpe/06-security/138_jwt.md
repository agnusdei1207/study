---
title: "JWT(JSON Web Token)"
author: "Gemini 3.8 Flash"
date: "2026-09-20T01:00:00+09:00"
tags:
  - "notes-security"
extra:
  model: "Gemini 3.8 Flash"

---

## 답안 골격
```text
[JWT(JSON Web Token)] ◀━━ 머리: Ⅶ 내 의견 (자체 수용 토큰의 즉시 취소 불가 한계 극복 → 짧은 수명의 Access Token과 Redis 블랙리스트/Refresh Token 연계 아키텍처)
 ┃
 ┣━ Ⅰ 개요 ───── RFC 7519의 JSON 클레임 전달 형식. 서명된 JWS 또는 암호화된 JWE 형태로 사용하며 토큰 자체가 권한을 자동 보장하지 않음
 ┣━ Ⅱ 특징 ───── 클레임 기반 정보 전달 · 서명형 토큰의 무결성 검증 · 필요에 따른 분산 검증 · 발급·만료·폐기 정책 관리
 ┣━ Ⅲ 구조 ───── 서명형 Compact JWS의 3단 구조: Header + Payload(클레임) + Signature. 암호화형 JWE는 형식·보안 성질이 다름
 ┣━ Ⅳ 흐름 ───── 토큰 발급 → 클라이언트 전달 → 수신 서비스가 알고리즘·서명·발급자·대상·유효기간 검증 → 최신 권한·폐기 정책 반영 후 인가
 ┣━ Ⅴ 비교 ───── 서버 세션은 세션 상태 조회·즉시 폐기 관리 / 서명형 JWT는 로컬 검증이 가능하나 권한 변경·철회 반영에 별도 설계 필요
 ┗━ Ⅵ 실무 ───── `alg: none` 서명 우회 공격 / 페이로드 평문 노출(민감정보 포함 금지) / 토큰 탈취 시 만료 시간(exp)까지 무단 사용
```
- 필수 키워드: JWT(JSON Web Token) · Header.Payload.Signature · 자체 수용적(Self-contained) · 무상태(Stateless) · `alg: none` 공격 · Access/Refresh Token · JWS/JWE
- 출제 이력: 제125회 문항은 공식 Q-Net 원문 미수집으로 미확인. 아래 문항은 예상문제

## 한 줄 본질
- 수백만 사용자의 로그인 세션을 중앙 DB에 저장하고 매 요청마다 조회하느라 서버가 뻗는 세션 병목 → 사용자 권한을 JSON에 담고 암호학적 서명을 붙여 클라이언트에 들려보냄으로써 서버 조회 없이 검증 → 서버 무한 확장성 확보 / 토큰 탈취 시 즉각적인 원격 로그아웃 처리 난제

## 핵심 그림
```text
[JWT(JSON Web Token)의 3단 구조 및 무상태(Stateless) 검증 메커니즘]

[JWT 토큰 구조 (Base64URL 인코딩)]:  Header . Payload . Signature
┌───────────────────────────┬───────────────────────────┬───────────────────────────┐
│       1. Header (헤더)    │     2. Payload (내용)     │    3. Signature (서명)    │
│ {                         │ {                         │                           │
│   "alg": "HS256",         │   "sub": "user_1234",     │ HMACSHA256(               │
│   "typ": "JWT"            │   "role": "admin",        │   base64Url(Header) + "." │
│ }                         │   "exp": 1774000000       │   base64Url(Payload),     │
│                           │ }                         │   secret_key              │
│                           │                           │ )                         │
└───────────────────────────┴───────────────────────────┴───────────────────────────┘
                                           │
                                           ▼
[마이크로서비스(MSA) 환경에서의 분산 검증]
  [클라이언트] ──(Authorization: Bearer <JWT>)──> [마이크로서비스 A] ──> [로컬 서명 검증 성공: 인가!]
                                            └─> [마이크로서비스 B] ──> [중앙 DB 조회 전혀 없음!]
```

## 핵심 용어
- 자체 수용적(Self-contained): 필요한 클레임 일부를 토큰에 담아 로컬 검증이 가능한 설계. 발급자·대상·만료·서명 확인과 현재 권한·철회 상태 조회 필요성은 서비스 정책에 따라 결정
- `alg: none` 공격: 공격자가 JWT 헤더의 알고리즘 필드를 `"none"`으로 조작하고 서명(Signature) 부분을 아예 지워 보냈을 때, 검증 로직이 서명 검사를 생략하고 변조된 관리자 권한 페이로드를 그대로 통과시키는 고전적 취약점

## 핵심 통찰
- 페이로드(Payload)는 "암호화된 것이 아니라 단순 Base64로 인코딩된 것"임 → 누구나 디코딩 사이트(jwt.io)에 넣으면 1초 만에 평문으로 열람할 수 있으므로, 주민등록번호나 비밀번호 같은 기밀 데이터를 절대 페이로드에 넣으면 안 됨
- JWT의 가장 큰 장점(Stateless)은 동시에 가장 큰 저주임 → 사용자가 로그아웃 버튼을 누르거나 스마트폰을 분실해도, 이미 발급된 유효기간 24시간짜리 토큰은 만료 시간까지 전 세계 모든 서버에서 여전히 정상 작동함
- 실무에서는 위협 수준과 철회 요건에 맞춰 액세스 토큰 수명과 재발급 절차를 정하고, 리프레시 토큰의 저장·회전·탈취 대응을 설계

## 이웃 토픽과 구분
- JWS(JSON Web Signature) vs JWE(JSON Web Encryption): JWS = 내용을 누구나 볼 수 있지만 위변조를 막는 전자서명 토큰(일반 JWT) / JWE = 내용 자체를 대칭키로 암호화하여 기밀성을 보장하는 암호화 토큰

## 문제·원인·대책
- 가상 사례: 알고리즘 혼동 취약점이 있는 토큰 검증기에서 헤더의 알고리즘을 바꾼 위조 토큰 수용
| 문제 | 원인 | 대책 | 효과 |
|---|---|---|---|
| 알고리즘 혼동 공격(Algorithm Confusion)을 통한 서명 위조 | 서버 검증 라이브러리가 헤더의 `alg` 값을 무조건 신뢰하여 처리 | 서버 코드에서 허용 알고리즘(Whitelist: 오직 RS256만) 엄격 강제 | 비인가 알고리즘으로 변조된 위조 토큰 즉각 거부 |
| XSS 스크립트를 통한 브라우저 저장소의 JWT 탈취 | 스크립트가 읽을 수 있는 저장소에 토큰 보관 | `HttpOnly`·`Secure`·`SameSite` 쿠키와 CSRF 방어, XSS 예방 | 스크립트의 쿠키 값 직접 읽기 제한. 세션 악용 가능성은 별도 관리 |

## 1교시 예상문제 (10점)
- JWT의 개념과 서명형 JWT의 구조를 설명하시오.

## 1교시 10점 답안
- **정의:** RFC 7519의 JSON 클레임 전달 형식으로 서명형 JWS 또는 암호화형 JWE로 표현.
- **구조:** 일반적인 Compact JWS는 Base64url(Header).Base64url(Payload).Signature의 세 부분.
- **검증:** 허용 알고리즘·키와 서명, 발급자(iss)·대상(aud)·만료(exp)를 서비스 정책에 맞게 검사. Base64url 인코딩은 암호화가 아님.

## 2~4교시 예상문제 (25점)
- JWT 기반 인증·인가의 처리 절차와 보안 위험 및 대응을 설명하시오.

## 2~4교시 25점 답안
### Ⅰ. 토큰 역할
- 발급자가 주체·대상·시각 등의 클레임을 담아 전달. 토큰 검증과 실제 권한 인가는 별개 결정.
### Ⅱ. 처리 흐름
- 발급 → 보호된 채널로 전송 → 서명/암호 및 알고리즘·키 확인 → 필수 클레임 검증 → 권한·철회 정책 적용.
### Ⅲ. 주요 위험
- `alg` 혼동·무서명 토큰 수용, 토큰 탈취·재사용, 노출 가능한 JWS 페이로드, 장기 유효 토큰의 철회 지연.
### Ⅳ. 통제
- 알고리즘과 키의 고정·검증, 짧은 유효기간의 정책별 설정, 민감 클레임 최소화, 재발급 토큰 보호·회전과 철회 수단 설계.

## 이렇게 출제된다
- 예상문제: JWT의 서명형 구조와 클레임 검증·철회 방안을 설명하시오. 제125회 원문은 미확인

## 내 의견
- [철회 반영 설계] 짧은 유효기간, 철회 목록, 참조형 토큰의 조회 방식 중 위험과 지연 요건에 맞는 조합 선택. 블룸 필터 사용 시 거짓 양성 및 목록 동기화 실패 대응 필요

## 출처
- [RFC 7519, JSON Web Token](https://www.rfc-editor.org/rfc/rfc7519): JWT의 클레임과 JWS·JWE 표현
- [RFC 8725, JWT Best Current Practices](https://www.rfc-editor.org/rfc/rfc8725): 알고리즘·키·클레임 검증 지침
