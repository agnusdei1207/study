---
title: "CPU 스케줄링(CPU Scheduling)"
author: "Gemini 3.8 Flash"
date: "2026-09-20T00:30:00+09:00"
tags:
  - "notes-computer-system"
sidebar:
  badge:
    text: "기출 · 86%"
extra:
  model: "Gemini 3.8 Flash"
  source_status: "기출"
  source_history: "83회, 96회, 119회, 137회"
  priority: 86
  priority_note: "[출제:137] · 이전(KPC):83,96,119"
---

## 답안 골격
```text
[CPU 스케줄링] ◀━━ 머리: Ⅶ 내 의견 (워크로드 특성(CPU/IO 바운드) 분류와 CFS 가상 런타임 튜닝을 통한 공평성 확보)
 ┃
 ┣━ Ⅰ 개요 ───── 멀티프로그래밍 환경의 CPU 자원 경합 → 이용률 극대화 및 지연 최소화를 위한 디스패칭
 ┣━ Ⅱ 특징 ───── 선점(Preemptive) vs 비선점(Non-preemptive) · 공평성 · 기아 방지(Aging) · 오버헤드
 ┣━ Ⅲ 구조 ───── 준비 큐(Ready Queue) + 장기/중기/단기 스케줄러 + 디스패처(Dispatcher)
 ┣━ Ⅳ 흐름 ───── ① 프로세스 생성(Job) → ② 준비 큐 등록 → ③ 스케줄러 선택 및 디스패처 문맥 교환 → ④ CPU 실행 및 I/O 대기/완료
 ┣━ Ⅴ 비교 ───── FCFS vs SJF vs Round Robin vs SRTF vs 다단계 피드백 큐(MLFQ) vs CFS(리눅스)
 ┗━ Ⅵ 실무 ───── 기아 현상(Starvation) / 문맥 교환 오버헤드(잦은 타임 슬라이스) / 우선순위 역전
```
- 필수 키워드: 선점/비선점 · 준비 큐 · 디스패처 · 라운드 로빈(RR) · MLFQ · CFS(Completely Fair Scheduler) · 에이징(Aging)
- 배점 전략: 10점 = Ⅰ 개요 → Ⅲ 스케줄링 큐 구조도 → Ⅴ 대표 알고리즘 비교표 / 25점 = Ⅰ~Ⅶ 전개, Ⅳ 프로세스 상태 전이와 Ⅵ 리눅스 CFS 레드블랙트리 동작 원리
- 기출: 137회 3교시 `운영체제 스케줄링 기법`, 이전 기출 `스케줄링 알고리즘별 평균 대기시간 계산 및 비교`

## 한 줄 본질
- 단일/다중 CPU 코어를 둘러싼 다수 프로세스의 점유 경쟁 및 I/O 대기로 인한 CPU 유휴(Idle) 낭비 → 시스템 목표(처리량, 응답 시간, 반환 시간)에 맞춰 준비 큐의 프로세스 중 최적 대상을 선별해 CPU를 할당 → CPU 이용률 극대화 및 대화형 응답성 보장 / 잦은 선점으로 인한 문맥 교환 오버헤드 및 특정 작업의 기아 현상 발생

## 핵심 그림
```text
[ New ] ──(장기 스케줄러: 메모리 적재)──> [ Ready Queue (준비 큐) ]
                                                    │
                                           (단기 스케줄러 & 디스패처)
                                                    │
                                                    v
                     [ Terminated ] <── [ Running (CPU 실행) ]
                                          │            ▲
                      (I/O 요청: Blocked) │            │ (타임 퀀텀 만료: 선점)
                                          v            │
                                    [ Waiting Queue ] ─┘ (I/O 완료)
```

## 핵심 용어
- 디스패처(Dispatcher): CPU 스케줄러가 선택한 프로세스에게 실제로 CPU 제어권을 넘겨주는 커널 모듈로, 문맥 교환, 유저 모드 전환, PC 레지스터 복구를 수행
- 다단계 피드백 큐(MLFQ: Multi-Level Feedback Queue): 프로세스의 CPU 버스트 특성에 따라 우선순위가 다른 여러 큐 사이를 동적으로 이동시키는 적응형 스케줄링 기법
- CFS(Completely Fair Scheduler): 리눅스 표준 스케줄러로, 가상 런타임(vruntime)을 기반으로 레드블랙트리(Red-Black Tree)를 구성하여 가장 덜 실행된 프로세스에 CPU를 균등 배분

## 핵심 통찰
- 완벽한 만능 스케줄러는 존재하지 않으며 항상 트레이드오프임 → SJF(Shortest Job First)는 수학적으로 '최소 평균 대기 시간'을 보장하지만, 미래의 CPU 버스트 시간을 알 수 없어 실무 적용 불가
- 시분할 시스템의 핵심인 라운드 로빈(RR)에서 타임 슬라이스(Time Quantum) 크기 설정이 성능을 좌우 → 너무 크면 FCFS로 퇴보하여 호위 효과 발생, 너무 작으면 문맥 교환 오버헤드가 CPU 사이클을 잠식
- 현대 OS(Linux)는 고정 우선순위 대신 CPU를 덜 사용한 대화형(I/O 바운드) 프로세스에 우선순위 보너스를 주어 사용자 체감 응답성을 극대화하도록 설계됨

## 이웃 토픽과 구분
- 선점(Preemptive) vs 비선점(Non-preemptive): 선점 = 실행 중인 프로세스로부터 강제로 CPU를 빼앗을 수 있음(현대 OS 표준, 응답성 우수) / 비선점 = 프로세스가 스스로 CPU를 반납하거나 I/O를 만날 때까지 보장(문맥 교환 적음, 독점 위험)

## 문제·원인·대책
- 사례: 137회 기출 실시간 대화형 서비스와 대규모 배치 연산 동시 구동 시 응답 지연 및 기아 발생
| 문제 | 원인 | 대책 | 효과 |
|---|---|---|---|
| 우선순위가 낮은 백그라운드 배치 프로세스가 수 시간 동안 CPU를 할당받지 못함 | 정적 우선순위 스케줄링으로 인한 기아 현상(Starvation) | 에이징(Aging) 기법 적용 (대기 시간에 비례하여 우선순위 점진 승격) | 모든 프로세스의 유한 시간 내 실행 보장 |
| 컨테이너 CPU 제한(Limit) 도달 시 웹 응답 지연 10배 폭증 | 리눅스 CFS의 CPU 대역폭 제어(Quota/Period)에 의한 스로틀링(Throttling) | 타임 슬라이스(Period) 주기 단축 및 버스트(Burst) 할당 설정 활성화 | 순간 트래픽 폭주 시 불필요한 스로틀링 완화 |

## 이렇게 출제된다
- 제137회 3교시 1번: "운영체제 스케줄링 기법에 대한 각 내용을 설명하시오." → 요구 포인트: Ⅰ 스케줄링 목적 및 성능 척도 + Ⅱ 선점/비선점 분류 + Ⅴ 대표 알고리즘(RR, MLFQ, CFS) 동작 원리

## 내 의견
- [쿠버네티스 환경에서 CFS Quota 설정의 신중한 접근] 마이크로서비스 파드에 CPU Limit을 빡빡하게 설정하면 실제 노드 CPU가 50% 미만인데도 CFS에 의해 파드가 초당 수백 ms씩 강제 정지되는 스로틀링 참사 발생 → 나라면: CPU Request는 엄격히 설정하여 노드 스케줄링을 보장하되, CPU Limit은 제거하거나 매우 넉넉히 설정하고, HPA(오토스케일러)와 결합하여 스로틀링 없는 안정적 지연시간 유지
