---
title: "우선순위 역전(Priority Inversion)"
author: "Gemini 3.8 Flash"
date: "2026-09-20T00:40:00+09:00"
tags:
  - "notes-computer-system"
extra:
  model: "Gemini 3.8 Flash"

---

## 답안 골격
```text
[우선순위 역전] ◀━━ 머리: Ⅶ 내 의견 (실시간 시스템 설계 시 우선순위 상속(PIP) 프로토콜 기본 활성화 및 임계영역 최소화)
 ┃
 ┣━ Ⅰ 개요 ───── 공유 자원 락 경합 시 낮은 우선순위 태스크로 인해 높은 우선순위 태스크가 무기한 지연되는 이상현상
 ┣━ Ⅱ 특징 ───── RTOS 치명적 결함 · 데드라인 미준수 유발 · 중간 우선순위 태스크의 CPU 선점으로 악화
 ┣━ Ⅲ 구조 ───── 고우선순위 태스크(T_H) + 중우선순위 태스크(T_M) + 저우선순위 태스크(T_L) + 공유 뮤텍스(Mutex)
 ┣━ Ⅳ 흐름 ───── ① T_L 공유 자원 획득 → ② T_H 실행 시도 후 락 블록 → ③ 무관한 T_M이 T_L 선점 → ④ T_H 무기한 대기
 ┣━ Ⅴ 비교 ───── 우선순위 상속(PIP) vs 우선순위 올림(PCP)
 ┗━ Ⅵ 실무 ───── 화성 탐사선 패스파인더호 통신 두절 사건 / 연쇄 블로킹(Chained Blocking) / 교착상태(Deadlock)
```
- 필수 키워드: 우선순위 역전 · 실시간 스케줄링(RTOS) · 공유 자원 · 우선순위 상속(PIP) · 우선순위 천장(PCP) · 패스파인더호
- 기출: 126회 4교시 2번 `우선순위 역전(Priority Inversion)의 사례 기반 설명 및 해결 기법` 논술형 출제

## 한 줄 본질
- 가장 높은 우선순위 태스크가 가장 낮은 우선순위 태스크가 잡고 있는 락(Lock)을 기다리는 동안, 락과 무관한 중간 우선순위 태스크들이 CPU를 가로채 실행됨 → 고우선순위 태스크가 저우선순위 작업보다 늦게 처리되는 역전 발생 / 자원을 쥔 저우선순위 태스크의 우선순위를 일시 승격시키는 상속(PIP)으로 해결

## 핵심 그림
```text
[ 우선순위 역전 발생 과정 및 무기한 블로킹 타이밍도 ]

우선순위
   ^
High│                   (T_H 실행 시도)───[ 락 대기 블록! ]──────────────────> [락 획득 후 실행]
    │                        │                                                    ^
Mid │                        │       (T_M 등장: T_L 선점!)───[ T_M 긴 작업 수행 ]──│
    │                        │                 │                                  │
Low │ [T_L 락 획득]──────────┘                 └──[T_L 실행 중단/대기]────────────┴[T_L 락 반납]
    +─────────────────────────────────────────────────────────────────────────────> 시간(t)
    (결과: 락과 전혀 무관한 중간 태스크 T_M 때문에 최고 우선순위 T_H가 마비됨!)
```

## 핵심 용어
- 우선순위 상속 프로토콜(PIP, Priority Inheritance Protocol): 고우선순위 태스크가 대기하는 자원을 쥐고 있는 저우선순위 태스크의 우선순위를, 자원을 반납할 때까지 고우선순위 수준으로 일시 승격시키는 기법
- 우선순위 천장 프로토콜(PCP, Priority Ceiling Protocol): 각 공유 자원마다 해당 자원을 요청할 수 있는 태스크 중 가장 높은 우선순위를 미리 '천장'으로 지정하고, 자원 획득 시 즉시 천장 우선순위로 올려버려 연쇄 블로킹과 교착상태를 원천 방지하는 기법
- 패스파인더호(Mars Pathfinder): 1997년 화성 탐사선 착륙 후 통신 장비와 데이터 버스 태스크 간 우선순위 역전으로 시스템이 지속 재부팅된 역사적 사건

## 핵심 통찰
- 주범은 저우선순위가 아니라 '중간 우선순위(T_M)'임 → T_L이 락을 쥐고 있어도 T_H를 위해 빨리 끝나면 역전 시간이 짧지만, 락과 아무 상관없는 중간 태스크 수십 개가 끼어들어 T_L의 CPU를 뺏어버리면서 T_H가 무기한 굶어 죽는 것임
- 우선순위 상속(PIP)은 교착상태(Deadlock)까지는 못 막음 → 상속은 역전 기간을 줄여줄 뿐, 두 태스크가 서로 다른 자원을 쥐고 꼬이면 데드락이 발생하므로 완벽한 선제 차단을 위해서는 천장 프로토콜(PCP)이 필요함
- 범용 OS(Linux)도 실무에서 자유롭지 못함 → 뮤텍스 생성 시 `PTHREAD_PRIO_INHERIT` 속성을 명시하지 않으면 백그라운드 스레드에 묶인 메인 UI 스레드가 멈추는 프레임 드랍 현상이 빈발함

## 이웃 토픽과 구분
- 우선순위 역전 vs 기아 상태(Starvation): 우선순위 역전 = 우선순위가 높은 프로세스가 낮은 프로세스에게 밀려 대기하는 비정상 구조 / 기아 상태 = 우선순위가 낮은 프로세스가 높은 프로세스들에 밀려 실행 기회를 영구히 박탈당하는 현상

## 문제·원인·대책
- 사례: 126회 기출 및 1997년 화성 패스파인더 탐사선의 정보 버스 뮤텍스 경합으로 인한 실시간 워치독 리셋 장애
| 문제 | 원인 | 대책 | 효과 |
|---|---|---|---|
| 미션 크리티컬 실시간 제어 태스크의 데드라인 미준수 및 타임아웃 | 공유 센서 버스 락을 쥔 저우선순위 태스크를 중간 우선순위 통신 태스크가 선점 | RTOS 뮤텍스 옵션에 우선순위 상속(PIP) 플래그 활성화 | T_L이 T_H 우선순위로 즉시 승격되어 T_M 선점 차단 및 신속 반납 |
| 다중 자원 경합 환경에서 연쇄 블로킹 및 순환 대기 교착상태 | 자원별 우선순위 체계 없이 단순 상속만 반복 적용 | 우선순위 천장 프로토콜(PCP) 적용 및 세마포어 전수 뮤텍스화 | 최대 1개 태스크 블로킹 보장 및 상호 데드락 원천 차단 |

## 이렇게 출제된다
- 제126회 4교시 2번: "Real Time Scheduling이 갖는 문제 중 우선순위 역전(Priority Inversion)이 있다. Task 1, Task 2, Task 3 순으로 우선순위가 낮다고 할 때 우선순위 역전을 사례기반으로 설명하고,우선순위 역전을 해결하기 위한 기법..." → 요구 포인트: Ⅰ 우선순위 역전의 개념 + Ⅲ Task 1, 2, 3 기반 발생 시나리오 도식 + Ⅴ 해결 기법(PIP, PCP) 비교 및 화성 패스파인더호 사례

## 내 의견
- [실시간 뮤텍스 락 프리(Lock-Free) 구조 전환과 PIP 필수 적용 린트 강제] 우선순위 역전은 코드 상에서 눈으로 발견하기 극도로 어려운 동시성 버그임 → 나라면: RTOS 및 멀티스레드 기반 제어 소프트웨어 개발 시 공유 메모리 락을 지양하고 Lock-Free 링 버퍼나 메시지 큐(Actor 모델) 기반으로 설계를 전환하며, 불가피한 뮤텍스는 정적 코드 분석기를 통해 `PTHREAD_PRIO_INHERIT` 속성 누락 시 컴파일 에러를 발생시키도록 개발 환경을 표준화

## 찾아볼 것
- 리눅스 커널의 실시간 패치(PREEMPT_RT)에서 커널 스핀락을 rt_mutex(우선순위 상속 지원)로 대체하는 원리
