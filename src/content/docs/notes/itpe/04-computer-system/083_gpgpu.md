---
title: "GPGPU(General-Purpose computing on Graphics Processing Units)"
author: "Gemini 3.8 Flash"
date: "2026-09-24T21:00:00+09:00"
tags:
  - "notes-computer-system"
extra:
  model: "GPT-6"

---

## 지식 로드맵 내 현재 위치

컴퓨터 시스템 및 네트워크 → 핵심 개념 → GPGPU

## 30초 인출

- 본질: CPU는 직렬 제어와 캐시가 비대하여 행렬 연산과 대규모 병렬 계산에서 처리량 한계에 직면함
- 메커니즘: 수천 개의 경량 ALU 코어를 집적하고 SIMT(단일 명령 다중 스레드) 방식으로 병렬 연산 처리

## 핵심 용어

- SIMT(Single Instruction Multiple Threads): 32개의 스레드를 하나의 '워프(Warp)'로 묶어 동일한 단일 명령어를 서로 다른 데이터에 대해 병렬 실행하는 GPU 고유의 실행 모델
- 워프 발산(Warp Divergence): 워프 내 32개 스레드 간에 `if-else` 분기 조건이 갈릴 때, 조건에 맞지 않는 스레드는 비활성화된 채 직렬로 순차 실행되어 성능이 반토막 나는 현상
- 텐서 코어(Tensor Core): 딥러닝의 핵심인 $4 \times 4$ 행렬 곱셈-누적(Mixed Precision: FP16/BF16/FP8) 연산을 단 1클록 사이클에 끝내도록 특화된 하드웨어 ASIC 유닛

---

## 1교시 예상문제 (10점)

> GPGPU의 개념과 핵심 구조 또는 동작을 설명하시오. (예상)

---

## 1교시 10점 답안

### 정의·목적

- 정의: GPGPU은/는 수천 개의 경량 ALU 코어를 집적하고 SIMT(단일 명령 다중 스레드) 방식으로 병렬 연산 처리 방식이다.
- 목적: 딥러닝 및 과학 연산 수십~수백 배 가속에 기여한다.

### 핵심 구조와 작동

```text
[ GPGPU 스트리밍 멀티프로세서(SM) 내부 및 그리드 계층 구조 ]

+───────────────────────────────────────────────────────────────+
| GPU 디바이스 (Device Global Memory: HBM / GDDR)               |
|                                                               |
|  [ 스트리밍 멀티프로세서 (Streaming Multiprocessor: SM) x N ] |
|   +────────────────────────────────────────────────────────+  |
|   | 워프 스케줄러 (Warp Scheduler: 32개 스레드 동시 발행)  |  |
|   +────────────────────────────────────────────────────────+  |
|   | 공유 메모리 / L1 캐시 (Shared Memory: 128KB, 초고속)   |  |
|   +────────────────────────────────────────────────────────+  |
|   |  [FP32 코어] [FP32 코어] ... [Tensor Core (행렬 가속)] |  |
|   |  [INT32 코어][INT32 코어] ... [Special Function Unit]  |  |
|   +────────────────────────────────────────────────────────+  |
+───────────────────────────────┬───────────────────────────────+
                                │ PCIe Gen5 / NVLink 고속 버스
                                v
                [ 호스트 시스템 (Host CPU & DRAM) ]
```

---

## 2~4교시 예상문제 (25점)

> GPGPU의 구조와 동작을 설명하고, 주요 비교 또는 적용 시 문제와 대응책을 제시하시오. (예상)

---

## 2~4교시 25점 답안

### Ⅰ. 핵심 구조와 작동

```text
[ GPGPU 스트리밍 멀티프로세서(SM) 내부 및 그리드 계층 구조 ]

+───────────────────────────────────────────────────────────────+
| GPU 디바이스 (Device Global Memory: HBM / GDDR)               |
|                                                               |
|  [ 스트리밍 멀티프로세서 (Streaming Multiprocessor: SM) x N ] |
|   +────────────────────────────────────────────────────────+  |
|   | 워프 스케줄러 (Warp Scheduler: 32개 스레드 동시 발행)  |  |
|   +────────────────────────────────────────────────────────+  |
|   | 공유 메모리 / L1 캐시 (Shared Memory: 128KB, 초고속)   |  |
|   +────────────────────────────────────────────────────────+  |
|   |  [FP32 코어] [FP32 코어] ... [Tensor Core (행렬 가속)] |  |
|   |  [INT32 코어][INT32 코어] ... [Special Function Unit]  |  |
|   +────────────────────────────────────────────────────────+  |
+───────────────────────────────┬───────────────────────────────+
                                │ PCIe Gen5 / NVLink 고속 버스
                                v
                [ 호스트 시스템 (Host CPU & DRAM) ]
```

### Ⅱ. 핵심 특성

- GPGPU 혁명의 본질은 'CUDA라는 소프트웨어 생태계'임 → 과거에도 그래픽 칩으로 계산하려는 시도는 있었으나 픽셀 셰이더 문법으로 억지 변환해야 했음 → 엔비디아가 C/C++ 포인터 기반의 CUDA를 내놓으면서 모든 프로그래머가 GPU 병렬 연산에 뛰어들 수 있게 됨
- 호스트-디바이스 간 복사가 최대의 병목임 → 연산은 나노초 만에 끝나는데 PCIe 버스를 통해 CPU RAM에서 GPU VRAM으로 데이터를 실어 나르는 데 마이크로초가 걸림 → Unified Memory와 GPUDirect RDMA가 필수적인 이유임
- 캐시 미스를 '스레드 전환'으로 숨김 → CPU는 캐시 미스가 나면 파이프라인이 멈추지만, GPU는 메모리 응답을 기다리는 동안 워프 스케줄러가 0사이클 만에 다른 워프로 컨텍스트를 스위칭하여 연산기를 100% 굴림

### Ⅲ. 관련 개념과 구분

- GPGPU vs NPU: GPGPU = 범용 병렬 연산(시뮬레이션, 렌더링, 암호채굴, 딥러닝), 높은 전력, 소프트웨어 유연성 / NPU = 딥러닝 추론 전용, 고정된 매트릭스 연산기, 극도의 저전력 고효율

### Ⅳ. 적용 문제와 대응

- 사례: 120회 기출 및 딥러닝 커스텀 연산자(Custom CUDA Kernel) 구현 시 CPU 대비 성능이 2배밖에 안 나오는 성능 저하
| 문제 | 원인 | 대책 | 효과 |
|---|---|---|---|
| GPU 커널 내 조건 분기문(`if/else`)으로 인한 심각한 처리량 저하 | 동일 워프(32스레드) 내에서 서로 다른 분기 경로를 타며 워프 발산(Warp Divergence) 발생 | 분기문을 산술식(마스킹 연산)으로 치환하거나 스레드별 데이터 재정렬 | 32개 스레드 동시 연산(SIMT) 100% 복원 |
| 글로벌 메모리 접근 시 대역폭 포화 및 극심한 메모리 지연 | 인접 스레드가 연속 주소를 읽지 않고 불연속 산발 접근(비병합 접근) | 메모리 병합(Coalesced Access) 패턴 정렬 및 공유 메모리(Shared Memory) 캐싱 | 1회 메모리 트랜잭션으로 32스레드 데이터 일괄 인출 |

### Ⅴ. 기술사적 제언

| 문제 | 해결 방안 |
|---|---|
| 적용 환경에서 발생하는 핵심 제약 | 기존 대책을 적용하고 핵심 운영 지표를 확인해 개선한다. |

## 출제 이력과 검증 출처

- 제120회 1교시 12번: "CPU(Central Processing Unit)와 GPGPU(General-Purpose computing on Graphic Processing Unit)" → 요구 포인트: Ⅰ GPGPU의 등장 배경 및 개념 + Ⅲ SM 및 SIMT 병렬 연산 구조도 + Ⅴ CPU 대비 지연시간/처리량 철학 비교

## 찾아볼 것
- 엔비디아 호퍼/블랙웰 GPU의 TMA(Tensor Memory Accelerator) 비동기 전송 엔진 구조
