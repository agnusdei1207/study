---
title: "안드로이드(Android)"
author: "Gemini 3.8 Flash"
date: "2026-09-20T09:40:00+09:00"
tags:
  - "notes-computer-system"
sidebar:
  badge:
    text: "기출 · 60%"
extra:
  model: "Gemini 3.8 Flash"
  source_status: "기출"
  source_history: "95회, 96회"
  priority: 60
  priority_note: "이전(KPC):95,96"
---

## 답안 골격
```text
[안드로이드] ◀━━ 머리: Ⅶ 내 의견 (바인더 IPC 최적화와 ART 프로파일 기반 컴파일을 통한 모바일 앱 반응성 및 메모리 안정성 확보)
 ┃
 ┣━ Ⅰ 개요 ───── 모바일 칩셋의 하드웨어 파편화 및 제한된 자원 한계 극복 → 리눅스 커널과 HAL, 전용 런타임(ART) 기반 계층화 플랫폼
 ┣━ Ⅱ 특징 ───── 오픈소스(AOSP) · HAL 기반 드라이버 추상화 · 프로세스별 UID 샌드박스 격리 · 하이브리드 컴파일(AOT+JIT) · 바인더(Binder) IPC
 ┣━ Ⅲ 구조 ───── 리눅스 커널 + 하드웨어 추상화 계층(HAL) + 네이티브 라이브러리 & ART + 자바 API 프레임워크 + 시스템 애플리케이션
 ┣━ Ⅳ 흐름 ───── ① 앱 실행 인텐트 수신 → ② Zygote 프로세스 포크(Fork) → ③ 독립 ART 런타임 인스턴스 기동 → ④ 바인더 IPC 경유 시스템 서비스 호출
 ┣━ Ⅴ 비교 ───── 달빅 가상머신(Dalvik VM) vs 안드로이드 런타임(ART) vs 표준 Java 가상머신(JVM)
 ┗━ Ⅵ 실무 ───── 바인더 트랜잭션 버퍼(1MB) 초과 장애 / 메모리 누수로 인한 OOM 킬러 발동 / 백그라운드 배터리 과다 소모
```
- 필수 키워드: 안드로이드 아키텍처 · 리눅스 커널 · HAL(하드웨어 추상화 계층) · ART(Android Runtime) · Zygote · 바인더(Binder) IPC · 샌드박스
- 배점 전략: 10점 = Ⅰ 개요 → Ⅲ 5계층 소프트웨어 스택 아키텍처 도식 → Ⅴ Dalvik vs ART 비교표 / 25점 = Ⅰ~Ⅶ 전개, Ⅳ Zygote 포크 및 바인더 IPC 메커니즘과 Ⅵ 안드로이드 실무 메모리/배터리 최적화
- 기출: 95회 1교시, 96회 2교시 `안드로이드의 아키텍처 구조 및 가상머신(Dalvik/ART)` 출제

## 한 줄 본질
- 다양한 제조사의 이종 모바일 칩셋 위에서 애플리케이션을 안전하고 독립적으로 실행하기 위해 리눅스 커널 위에 하드웨어 추상화 계층(HAL)과 AOT/JIT 혼합 런타임(ART), 바인더 IPC를 모듈형으로 적층한 오픈소스 모바일 운영체제

## 핵심 그림
```text
[ 안드로이드 5계층 아키텍처 및 앱 실행(Zygote/Binder) 메커니즘 ]

+─────────────────────────────────────────────────────────────+
|  시스템 및 사용자 애플리케이션 (System & User Apps)          |
|  - 홈, 전화, 카메라, 브라우저, 서드파티 앱                  |
+─────────────────────────────────────────────────────────────+
                                ▲
                                │ (Java / Kotlin API 호출)
+─────────────────────────────────────────────────────────────+
|  자바 API 프레임워크 (Java API Framework)                   |
|  - Activity Manager, Window Manager, Content Provider       |
|  - Package Manager, Telephony Manager, View System          |
+─────────────────────────────────────────────────────────────+
                                ▲
                                │
+───────────────────────────────┼─────────────────────────────+
|  네이티브 C/C++ 라이브러리     │  안드로이드 런타임 (ART)     |
|  - WebKit, OpenGL ES, SQLite │  - 프로파일 기반 JIT + AOT  |
|  - Surface Manager, Media    │  - 압축형 가비지 컬렉터(GC) |
|  - IPC 바인더(Binder) 래퍼   │  - Zygote 포크 기반 독립 VM |
+───────────────────────────────┴─────────────────────────────+
                                ▲
                                │ (표준 하드웨어 인터페이스)
+─────────────────────────────────────────────────────────────+
|  하드웨어 추상화 계층 (HAL: Hardware Abstraction Layer)      |
|  - Audio, Camera, Bluetooth, Sensors, Gralloc                |
+─────────────────────────────────────────────────────────────+
                                ▲
                                │ (GPL 커널 인터페이스)
+─────────────────────────────────────────────────────────────+
|  리눅스 커널 (Linux Kernel)                                 |
|  - 프로세스 스케줄러, 메모리 관리, 드라이버(/dev/binder)    |
|  - Low Memory Killer (LMK), 전원 관리(Wakelock)             |
+─────────────────────────────────────────────────────────────+
```

## 핵심 용어
- Zygote: 시스템 부팅 시 공통 시스템 라이브러리와 프레임워크 클래스를 미리 메모리에 적재해 두고, 새 앱 기동 요청 시 `fork()`하여 메모리를 복사-공유(Copy-on-Write)하는 모체 프로세스
- 바인더(Binder) IPC: 리눅스 커널 드라이버를 통해 단 한 번의 메모리 복사(`mmap`)로 프로세스 간 대용량 데이터를 고속 전송하고 호출자의 UID/PID를 검증하는 안드로이드 핵심 통신 기제
- HAL(Hardware Abstraction Layer): 디바이스 드라이버를 사용자 공간(User Space)의 표준 라이브러리로 분리하여 하드웨어 제조사가 커널(GPL) 소스 코드를 공개하지 않고도 자체 IP를 보호할 수 있게 만든 계층

## 핵심 통찰
- 안드로이드의 보안 격리는 OS 수준의 'UID 기반 리눅스 샌드박스'임 → 가상머신 수준의 격리에 의존하지 않고, 앱마다 고유한 리눅스 사용자 ID(UID)를 부여하여 다른 앱의 파일 디렉터리와 메모리에 시스템 레벨에서 원천적으로 접근하지 못하게 차단함
- 런타임 컴파일의 진화(Dalvik JIT → ART AOT → 하이브리드) → 과거 Dalvik은 실행 시마다 JIT로 컴파일하여 버벅거렸고, 초기 ART는 앱 설치 시 100% AOT로 컴파일하여 설치 시간과 저장 공간이 폭증했음. 현행 안드로이드는 평상시 JIT로 구동하면서 자주 쓰이는 코드만 프로파일링하여 기기가 유휴 충전 중일 때 AOT로 백그라운드 최적화하는 절충안 달성
- 바인더(Binder) IPC의 한계 용량 1MB → 앱 간 데이터 전달(Intent) 시 메모리를 공유하지 못하고 바인더 버퍼를 거치므로, 고해상도 비트맵이나 대형 리스트를 Bundle에 넣어 넘기면 `TransactionTooLargeException`이 발생하며 앱이 강제 종료됨

## 이웃 토픽과 구분
- 표준 JVM vs 안드로이드 ART: 표준 JVM = 스택 기반(Stack-based) 구조로 바이트코드 명령어 수가 많음 / ART = 레지스터 기반(Register-based) DEX 코드를 실행하여 명령어 수를 줄이고 모바일 CPU 레지스터를 직접 활용하여 전력 소모를 절감

## 문제·원인·대책
- 적용 상황: 대규모 모바일 앱 서비스 개발 및 런타임 성능 안정화
| 문제 | 원인 | 대책 | 효과 |
|---|---|---|---|
| 화면 전환(Activity 전환) 시 Intent로 이미지 데이터 전달 중 크래시 발생 | 프로세스 공유 바인더 트랜잭션 버퍼 한도(1MB) 초과 | 객체 직접 전달을 중단하고 로컬 파일 캐시 경로(URI)만 전달 | TransactionTooLargeException 원천 방지 |
| 장시간 사용 시 가용 램 고갈로 백그라운드 서비스가 강제 종료(LMK 킬) | 정적 변수(Static)에 Activity 컨텍스트 참조 잔존으로 메모리 누수 발생 | LeakCanary 도입 및 ApplicationContext 참조 전환 | 액티비티 파괴 시 메모리 정상 회수 |
| 백그라운드 데이터 동기화 작업으로 인한 스마트폰 배터리 급방전 | 개별 앱의 무분별한 Wakelock 획득 및 주기적 CPU 깨움 | WorkManager 도입 및 Doze 모드(배터리 최적화) 정책 준수 | 백그라운드 전력 소비량 억제 |

## 이렇게 출제된다
- 제95회 1교시: "안드로이드(Android) 운영체제의 계층 구조와 특징을 설명하시오." → 요구 포인트: Ⅰ 안드로이드 개념 + Ⅲ 5계층 아키텍처 도식 + Ⅱ 계층별 핵심 역할
- 제96회 2교시: "안드로이드의 Dalvik 가상머신과 ART(Android Runtime)의 특징 및 컴파일 방식(JIT, AOT)을 비교 설명하시오." → 요구 포인트: Ⅲ Zygote 구동 방식 + Ⅴ Dalvik vs ART 비교표 + 컴파일러 진화 과정

## 내 의견
- [Jetpack 라이브러리 표준화와 코루틴 기반 비동기 파이프라인 정착] 안드로이드 프레임워크 API는 버전 파편화가 심하고 메인 스레드(UI 스레드) 블로킹에 매우 취약함 → 나라면: 프레임워크 직접 호출을 지양하고 AndroidX/Jetpack 아키텍처 컴포넌트를 필수 표준으로 지정하며, 네트워크 및 로컬 DB I/O는 코틀린 코루틴(Coroutines)의 `Dispatchers.IO` 풀로 완전 격리하여 프레임 드랍(Jank) 없는 60fps/120fps 부드러운 UI 렌더링 품질 보장

## 찾아볼 것
- 안드로이드 Treble 프로젝트의 벤더 인터페이스(VINTF) 및 Mainline 모듈식 OS 업데이트 아키텍처
