---
title: "세그먼테이션(Segmentation)"
author: "Gemini 3.8 Flash"
date: "2026-09-20T00:40:00+09:00"
tags:
  - "notes-computer-system"
sidebar:
  badge:
    text: "기출 · 76%"
extra:
  model: "Gemini 3.8 Flash"
  source_status: "기출"
  source_history: "131회"
  priority: 76
  priority_note: "[출제(KPC):131]"
---

## 답안 골격
```text
[세그먼테이션] ◀━━ 머리: Ⅶ 내 의견 (논리적 보호의 세그먼테이션과 물리적 배치의 페이징을 결합한 Paged Segmentation 활용)
 ┃
 ┣━ Ⅰ 개요 ───── 물리적 고정 분할의 논리적 의미 훼손 → 프로세스를 의미 단위(코드·데이터·스택)의 가변 크기로 분할
 ┣━ Ⅱ 특징 ───── 논리적 단위(가변 크기) · 보호 및 공유 용이 · 세그먼트 테이블 기반 주소 변환 · 외부 단편화 발생
 ┣━ Ⅲ 구조 ───── 논리 주소 <s, d> + 세그먼트 테이블(STBR, Limit, Base) + MMU 주소 변환기
 ┣━ Ⅳ 흐름 ───── ① 논리 주소 입력(s, d) → ② 변위 d와 Limit 비교(오버플로우 시 Trap) → ③ Base + d 물리 주소 생성
 ┣━ Ⅴ 비교 ───── 세그먼테이션(Segmentation) vs 페이징(Paging)
 ┗━ Ⅵ 실무 ───── 외부 단편화(External Fragmentation) / 메모리 압축(Compaction) 비용 / 세그먼테이션 폴트
```
- 필수 키워드: 세그먼테이션 · 가변 크기 · 세그먼트 테이블 · Base/Limit · 외부 단편화 · 세그먼테이션 폴트 · 페이징 결합
- 배점 전략: 10점 = Ⅰ 개요 → Ⅲ 세그먼테이션 주소 변환 매핑도 → Ⅴ 세그먼테이션 vs 페이징 비교표 / 25점 = Ⅰ~Ⅶ 전개, Ⅳ 주소 변환 및 한계 검사(Limit Check) 흐름과 Ⅵ Paged Segmentation 복합 아키텍처
- 기출: 131회 3교시 5번 `운영체제 메모리 관리 기법 중 페이징 기법과 세그먼테이션 기법 비교` 서술형 출제

## 한 줄 본질
- 메모리를 일률적인 크기로 자르면 코드나 함수 같은 논리적 단위의 경계가 잘려 권한 제어와 공유가 복잡해짐 → 코드, 데이터, 스택 등 프로그래머 관점의 논리적 모듈 단위(가변 크기)로 분할하여 매핑 → 메모리 보호 및 함수 공유 극대화 / 빈 공간이 조각나는 외부 단편화 발생

## 핵심 그림
```text
[ 세그먼테이션 하드웨어 주소 변환 및 Limit 보호 검사 ]

CPU 발생 논리 주소: [ 세그먼트 번호 (s) | 변위 / 오프셋 (d) ]
                             │                   │
                             v                   │
           세그먼트 테이블 (Segment Table)        │
           +----+------------+------------+      │
           | s  | 한계(Limit)| 기준(Base) |      │
           +----+------------+------------+      │
           | 0  |    1000    |   0x2000   |      │
           | 1  |    0400    |   0x5000   |      │
           +----+─────┬──────+─────┬──────+      │
                      │            │             │
                      v            │             v
           [ d < Limit 검사? ] <───┼─────────────┘
                      │            │             │
        +─────────────┴─────+      │             │
     Yes│                 No│ (Trap: Seg Fault!) │
        v                   │                    │
    [ 기준(Base) + 변위(d) 덧셈 ] <──────────────┘
                │
                v
       [ 실제 물리 메모리 주소 ]
```

## 핵심 용어
- Base(기준 주소): 세그먼트가 물리 메모리에 적재된 시작 물리 주소
- Limit(한계 크기): 해당 세그먼트의 유효한 길이로, 변위(d)가 이 크기를 넘어서면 불법 접근으로 판단하여 하드웨어 트랩(Trap) 발생
- 외부 단편화(External Fragmentation): 세그먼트들이 할당과 해제를 반복하면서 메모리 사이사이에 작은 유휴 공간들이 조각나, 총 여유 공간은 충분함에도 새 세그먼트를 적재하지 못하는 현상

## 핵심 통찰
- 세그먼테이션은 '보호와 공유'의 최강자임 → 함수 단위(예: 표준 C 라이브러리 `printf`)로 세그먼트를 정의하면 해당 세그먼트에 '읽기+실행' 권한만 딱 부여하여 여러 프로세스가 물리 메모리 1개만 안전하게 공유할 수 있음
- 순수 세그먼테이션이 사장된 이유는 '외부 단편화의 악몽' 때문임 → 프로세스가 떴다 죽을 때마다 물리 메모리에 구멍이 숭숭 뚫려 주기적으로 전체 메모리를 복사해 밀어붙이는 압축(Compaction)을 해야 하며, 이는 엄청난 CPU 정지를 유발함
- 현대 x86-64 아키텍처는 세그먼테이션을 사실상 우회함 → 64비트 롱 모드(Long Mode)에서는 CS, DS, ES, SS 세그먼트 레지스터의 Base를 강제로 0으로 고정(Flat Memory Model)하고, 메모리 관리를 전적으로 '페이징'에 위임함

## 이웃 토픽과 구분
- 세그먼테이션 vs 페이징: 세그먼테이션 = 논리적 의미 단위, 가변 크기, 외부 단편화 발생, 보호·공유 우수 / 페이징 = 물리적 프레임 단위, 고정 크기(4KB), 내부 단편화 발생, 메모리 활용 극대화

## 문제·원인·대책
- 사례: 131회 기출 및 임베디드 OS 환경에서 세그먼트 반복 동적 로딩 중 외부 단편화로 인한 프로세스 기동 불가 장애
| 문제 | 원인 | 대책 | 효과 |
|---|---|---|---|
| 메모리 잔여 용량이 50MB인데 10MB 크기 세그먼트 할당 실패 | 세그먼트 해제 후 자투리 공간들이 불연속적으로 쪼개짐(외부 단편화) | 최초 적합(First-Fit) 대신 버디 시스템(Buddy System) 또는 슬랩 할당자 적용 | 단편화 최소화 및 할당 속도 개선 |
| 프로그램 비정상 포인터 연산 시 `Segmentation Fault (core dumped)` 종료 | 변위(d)가 세그먼트 테이블의 Limit 필드를 초과하거나 권한 위반 | 커널 MMU 트랩 발생 및 시그널(SIGSEGV) 처리 핸들러 디버깅 연계 | 다른 프로세스 메모리 영역 침범 원천 차단 |

## 이렇게 출제된다
- 제131회 3교시 5번: "운영체제 메모리 관리 기법 중 페이징 기법과 세그먼테이션 기법의 개념을 설명하고, 두 기법을 비교하시오." → 요구 포인트: Ⅰ 세그먼테이션의 개념 및 필요성 + Ⅲ 주소 변환 메커니즘과 세그먼트 테이블 구조도 + Ⅴ 페이징과의 핵심 비교표 및 단편화 이슈

## 내 의견
- [Paged Segmentation 복합 아키텍처의 현대적 이해와 활용] 논리적 모듈화(세그먼트)의 장점과 물리적 할당 효율(페이징)의 장점을 결합한 페이징된 세그먼테이션 구조는 OS 설계의 정수임 → 나라면: 애플리케이션 아키텍처 관점에서 프로세스 주소 공간을 가상 세그먼트(Text, Data, Heap, Stack)로 분할하여 권한을 격리하되, 하부 메모리 할당은 4KB 페이징 단위로 처리되도록 커널 설정을 유지하여 외부 단편화 없는 완전한 메모리 보호 실현

## 찾아볼 것
- x86-64 CPU의 FS 및 GS 세그먼트 레지스터가 TLS(Thread Local Storage)에 활용되는 구조
