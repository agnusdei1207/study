---
title: "IPC(Inter Process Communication)"
author: "Gemini 3.8 Flash"
date: "2026-09-24T21:00:00+09:00"
tags:
  - "notes-computer-system"
extra:
  model: "GPT-6"

---

## 지식 로드맵 내 현재 위치

컴퓨터 시스템 및 네트워크 → 핵심 개념 → IPC

## 30초 인출

- 본질: 운영체제의 메모리 보호 메커니즘으로 인해 다른 프로세스의 가상 주소 공간을 직접 읽고 쓸 수 없는 구조적 장벽
- 메커니즘: OS 커널이 중계하는 통신 채널(파이프, 큐, 소켓)이나 공통 물리 메모리 페이지를 매핑(공유 메모리)하여 데이터 교환 및 동기화 지원

## 핵심 용어

- 공유 메모리(Shared Memory): 두 개 이상의 프로세스가 동일한 물리 메모리 영역을 각자의 가상 주소 공간에 매핑하여 커널 개입 없이 최고 속도로 데이터를 공유하는 IPC 기법
- 유닉스 도메인 소켓(UDS: Unix Domain Socket): 네트워크 프로토콜 스택(IP/TCP 체크섬, 라우팅)을 거치지 않고 OS 커널 내부 메모리 버퍼에서 직접 프로세스 간 양방향 바이트 스트림을 전송하는 초고속 소켓
- 익명 파이프(Anonymous Pipe): 부모-자식 프로세스 간 단방향 통신만 지원하는 임시 커널 버퍼 채널(명령줄 파이프 `|` 의 구현체)

---

## 1교시 예상문제 (10점)

> IPC의 개념과 핵심 구조 또는 동작을 설명하시오. (예상)

---

## 1교시 10점 답안

### 정의·목적

- 정의: IPC은/는 OS 커널이 중계하는 통신 채널(파이프, 큐, 소켓)이나 공통 물리 메모리 페이지를 매핑(공유 메모리)하여 데이터 교환 및 동기화 지원 방식이다.
- 목적: 안전한 협업 프로세스 구현에 기여한다.

### 핵심 구조와 작동

```text
  [ 1. 메시지 전달 모델 (Message Passing: Pipe, Queue, Socket) ]
  Process A (User) ──(1. 커널 복사)──> [ 커널 버퍼 / 큐 ] ──(2. 유저 복사)──> Process B (User)
  * 단점: 데이터 1건당 2회 복사(User -> Kernel -> User) 및 시스템 콜 오버헤드

  [ 2. 공유 메모리 모델 (Shared Memory: shmget / mmap) ]
  +--------------------+                               +--------------------+
  | Process A 가상 메모리 |                               | Process B 가상 메모리 |
  | [ A의 가상 주소 ] ─┼──────────────┐ ┌─────────────┼─> [ B의 가상 주소 ] |
  +--------------------+              │ │             +--------------------+
                                      v v
                      +───────────────────────────────+
                      |   동일한 물리 RAM 메모리 프레임 |
                      |   (커널 복사 없는 Zero-copy)    |
                      +───────────────────────────────+
  * 장점: CPU 최고 속도 직접 읽기/쓰기 / 주의: 세마포어로 동시 접근 동기화 필수!
```

---

## 2~4교시 예상문제 (25점)

> IPC의 구조와 동작을 설명하고, 주요 비교 또는 적용 시 문제와 대응책을 제시하시오. (예상)

---

## 2~4교시 25점 답안

### Ⅰ. 핵심 구조와 작동

```text
  [ 1. 메시지 전달 모델 (Message Passing: Pipe, Queue, Socket) ]
  Process A (User) ──(1. 커널 복사)──> [ 커널 버퍼 / 큐 ] ──(2. 유저 복사)──> Process B (User)
  * 단점: 데이터 1건당 2회 복사(User -> Kernel -> User) 및 시스템 콜 오버헤드

  [ 2. 공유 메모리 모델 (Shared Memory: shmget / mmap) ]
  +--------------------+                               +--------------------+
  | Process A 가상 메모리 |                               | Process B 가상 메모리 |
  | [ A의 가상 주소 ] ─┼──────────────┐ ┌─────────────┼─> [ B의 가상 주소 ] |
  +--------------------+              │ │             +--------------------+
                                      v v
                      +───────────────────────────────+
                      |   동일한 물리 RAM 메모리 프레임 |
                      |   (커널 복사 없는 Zero-copy)    |
                      +───────────────────────────────+
  * 장점: CPU 최고 속도 직접 읽기/쓰기 / 주의: 세마포어로 동시 접근 동기화 필수!
```

### Ⅱ. 핵심 특성

- IPC 성능의 핵심은 '메모리 복사(Memory Copy) 횟수'임 → 파이프, 메시지 큐, 소켓은 유저 영역에서 커널 버퍼로 한 번, 커널에서 상대 유저 영역으로 또 한 번 총 2번의 복사가 발생하지만, 공유 메모리는 복사가 0회(Zero-copy)이므로 대용량 데이터 전송에 압도적으로 유리
- 그러나 공유 메모리는 OS가 동기화를 전혀 해주지 않으므로, 개발자가 뮤텍스나 세마포어로 상호배제를 직접 구현하지 않으면 즉시 데이터 오염 발생
- 동일 호스트 내 컨테이너나 마이크로서비스 간 통신 시 일반 TCP 루프백(127.0.0.1) 소켓을 쓰면 불필요한 TCP 패킷화 및 체크섬 연산으로 CPU를 낭비하므로 유닉스 도메인 소켓(UDS)을 써야 함

### Ⅲ. 관련 개념과 구분

- 메시지 전달(Message Passing) vs 공유 메모리(Shared Memory): 메시지 전달 = 소량 데이터 및 동기화가 자동 보장되는 안전한 통신(복사 비용 큼) / 공유 메모리 = 대용량 초고속 통신(복사 비용 0, 별도 동기화 기제 필수)

### Ⅳ. 적용 문제와 대응

- 사례: 136회 기출 고빈도 트레이딩 시스템 및 AI 비전 파이프라인에서 프로세스 간 통신 지연 발생
| 문제 | 원인 | 대책 | 효과 |
|---|---|---|---|
| 고화질 카메라 영상 프레임을 파이썬 추론 프로세스로 전송 시 FPS 급락 | 표준 소켓 통신 사용으로 인한 초당 수 GB 메모리 복사 및 직렬화 오버헤드 | 공유 메모리(`shm_open`) 및 Apache Arrow 플라즈마 스토어 도입 | 제로 카피 데이터 공유로 전송 지연 90% 이상 제거 |
| 동일 노드 내 Envoy 프록시와 웹 서비스 간 통신 시 CPU 사용률 과다 | TCP 로컬 루프백 소켓의 불필요한 TCP/IP 헤더 패킷 처리 | 유닉스 도메인 소켓(UDS: `.sock` 파일) 인터페이스 전환 | 커널 네트워크 스택 우회로 지연시간 반감 및 처리량 배가 |

### Ⅴ. 기술사적 제언

| 문제 | 해결 방안 |
|---|---|
| 적용 환경에서 발생하는 핵심 제약 | 기존 대책을 적용하고 핵심 운영 지표를 확인해 개선한다. |

## 출제 이력과 검증 출처

- 제136회 4교시 2번: "프로세스간 통신을 위해 사용되는 IPC(InterProcess Communication)에 대하여 다음을 설명하시오." → 요구 포인트: Ⅰ 개념 및 필요성 + Ⅲ 메시지 전달 모델과 공유 메모리 모델의 비교도 + Ⅴ 대표 기법 5종(파이프, 메시지큐, 공유메모리, 세마포어, 소켓) 특징 및 선택 기준
