---
title: "OSI 7계층(OSI 7 Layer)"
author: "Gemini 3.8 Flash"
date: "2026-09-24T21:00:00+09:00"
tags:
  - "notes-network"
extra:
  model: "GPT-6"

---

## 지식 로드맵 내 현재 위치

컴퓨터 시스템 및 네트워크 → 핵심 개념 → OSI 7계층

## 30초 인출

- 본질: 서로 다른 컴퓨터 제조사의 독자적 통신 규격으로 인한 장비 간 연동 불가능
- 메커니즘: 통신 기능을 물리 신호부터 사용자 응용까지 7단계 모듈로 분할하고 표준 인터페이스를 규격화

## 핵심 용어

- PDU(Protocol Data Unit): 각 계층에서 데이터와 해당 계층의 제어 헤더(PCI)를 합친 전송 단위 (Bit, Frame, Packet, Segment, Data)
- 캡슐화(Encapsulation): 상위 계층의 데이터를 하위 계층의 페이로드(Payload)로 담고, 앞뒤에 제어 정보(Header/Trailer)를 덧붙여 내려보내는 과정
- SAP(Service Access Point): 상위 계층이 하위 계층이 제공하는 통신 서비스를 호출하기 위해 접근하는 논리적 인터페이스 접점 (포트 번호, IP 프로토콜 번호 등)

---

## 1교시 예상문제 (10점)

> OSI 7계층의 개념과 핵심 구조 또는 동작을 설명하시오. (예상)

---

## 1교시 10점 답안

### Ⅰ. 정의·목적

- 정의: OSI 7계층은/는 통신 기능을 물리 신호부터 사용자 응용까지 7단계 모듈로 분할하고 표준 인터페이스를 규격화 방식이다.
- 목적: 이기종 통신 호환성과 체계적 장애 진단 체계 확보에 기여한다.

### Ⅱ. 핵심 구조와 작동

```text
[ OSI 7계층 구조 및 계층별 PDU·장비 매핑 ]

 계층 (Layer)         주요 역할                     PDU        대표 장비/프로토콜
+-------------------+----------------------------+----------+--------------------+
| 7. 응용 (App)     | 사용자 인터페이스, 네트워크 서비스 | Data     | HTTP, DNS, SMTP    |
| 6. 표현 (Pres)    | 데이터 암호화, 압축, 코드 변환     | Data     | TLS, JPEG, ASCII   |
| 5. 세션 (Session) | 대화 제어, 동기점 관리, 세션 복구  | Data     | RPC, NetBIOS       |
+-------------------+----------------------------+----------+--------------------+
| 4. 전송 (Trans)   | 종단 간(E2E) 신뢰성·흐름·혼잡제어 | Segment  | TCP, UDP, L4스위치 |
| 3. 네트워크 (Net)  | 논리 주소(IP), 최적 경로 라우팅   | Packet   | IP, 라우터, L3스위치|
| 2. 데이터링크(Data)| 물리 주소(MAC), 프레이밍, 오류검출 | Frame    | 이더넷, L2스위치    |
| 1. 물리 (Physical)| 비트 스트림의 전기·광·무선 전송   | Bit      | 허브, 리피터, 케이블|
+-------------------+----------------------------+----------+--------------------+
```

---

## 2~4교시 예상문제 (25점)

> OSI 7계층의 구조와 동작을 설명하고, 주요 비교 또는 적용 시 문제와 대응책을 제시하시오. (예상)

---

## 2~4교시 25점 답안

### Ⅰ. 핵심 구조와 작동

```text
[ OSI 7계층 구조 및 계층별 PDU·장비 매핑 ]

 계층 (Layer)         주요 역할                     PDU        대표 장비/프로토콜
+-------------------+----------------------------+----------+--------------------+
| 7. 응용 (App)     | 사용자 인터페이스, 네트워크 서비스 | Data     | HTTP, DNS, SMTP    |
| 6. 표현 (Pres)    | 데이터 암호화, 압축, 코드 변환     | Data     | TLS, JPEG, ASCII   |
| 5. 세션 (Session) | 대화 제어, 동기점 관리, 세션 복구  | Data     | RPC, NetBIOS       |
+-------------------+----------------------------+----------+--------------------+
| 4. 전송 (Trans)   | 종단 간(E2E) 신뢰성·흐름·혼잡제어 | Segment  | TCP, UDP, L4스위치 |
| 3. 네트워크 (Net)  | 논리 주소(IP), 최적 경로 라우팅   | Packet   | IP, 라우터, L3스위치|
| 2. 데이터링크(Data)| 물리 주소(MAC), 프레이밍, 오류검출 | Frame    | 이더넷, L2스위치    |
| 1. 물리 (Physical)| 비트 스트림의 전기·광·무선 전송   | Bit      | 허브, 리피터, 케이블|
+-------------------+----------------------------+----------+--------------------+
```

### Ⅱ. 핵심 특성

- OSI 7계층은 실무에서 프로토콜을 그대로 코딩해 쓰는 "구현 모델"이 아니라, 통신 장애 발생 시 문제를 L1~L7 중 어디로 좁힐지 판단하는 "개념적 참조 프레임워크"
- 실제 인터넷 시장을 지배한 TCP/IP는 세션·표현 계층을 응용 계층에 합쳐 4개 계층으로 간소화함으로써 빠른 상용 보급과 구현 단순성 달성
- 계층화의 대가는 "헤더 오버헤드"와 "중복 처리"임 → L2의 CRC, L3의 IP 체크섬, L4의 TCP 체크섬이 각각 중복 검증을 수행하는 비효율 내포

### Ⅲ. 관련 개념과 구분

- OSI 7계층 vs TCP/IP: OSI 모델은 엄격한 인터페이스와 계층 책임을 정의한 이론적 표준 / TCP/IP는 IETF RFC를 기반으로 시장에서 검증된 실용 프로토콜 스택

### Ⅳ. 적용 문제와 대응

- 적용 상황: 웹 서비스 접속 장애 트러블슈팅
| 문제 | 원인 | 대책 | 효과 |
|---|---|---|---|
| 특정 서버 간 통신 두절 시 원인 파악 지연 | 무작위 패킷 덤프 분석으로 트러블슈팅 범위 발산 | Bottom-Up 계층별 점검(L1 링크 불빛 → L2 ARP → L3 Ping → L4 Telnet/Port) | 장애 원인 계층을 5분 이내 신속 특정 |
| 작은 패킷 전송 시 유효 대역폭 낭비 | L2(18B)+L3(20B)+L4(20B) = 최소 58B 고정 헤더 부착 | 헤더 압축(ROHC) 또는 점보 프레임(9000B) 적용 | 전체 전송 대역폭 중 실제 데이터 페이로드 비율 향상 |

### Ⅴ. 기술사적 제언

| 문제 | 해결 방안 |
|---|---|
| 적용 환경에서 발생하는 핵심 제약 | 기존 대책을 적용하고 핵심 운영 지표를 확인해 개선한다. |

## 출제 이력과 검증 출처

- 제124회 1교시: "OSI 7계층 참조 모델의 계층별 기능 및 데이터 전송 단위(PDU), TCP/IP 모델과의 비교를 설명하시오." → 요구 포인트: Ⅲ 매핑 표 + Ⅳ 캡슐화 흐름 + Ⅴ 비교

## 찾아볼 것
- 리눅스 커널의 네트워크 스택 내부 동작(Socket Buffer `sk_buff` 구조체와 계층 통과 과정)
