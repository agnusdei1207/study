---
title: "IBN(Intent-Based Networking)"
author: "Gemini 3.8 Flash"
date: "2026-09-20T00:30:00+09:00"
tags:
  - "notes-network"
sidebar:
  badge:
    text: "기출 · 79%"
extra:
  model: "Gemini 3.8 Flash"
  source_status: "기출"
  source_history: "135회"
  priority: 79
  priority_note: "[출제:135]"
---

## 답안 골격
```text
[IBN] ◀━━ 머리: Ⅶ 내 의견 (선언형 GitOps 파이프라인과 실시간 스트리밍 텔레메트리 결합)
 ┃
 ┣━ Ⅰ 개요 ───── 수작업 CLI 설정의 휴먼 에러·지연 → 비즈니스 의도(Intent) 기반 자율 네트워크
 ┣━ Ⅱ 특징 ───── 선언형(Declarative) 정책, 자동 변환·배포, 폐루프(Closed-loop) 검증, 자가 치유(Self-Healing)
 ┣━ Ⅲ 구조 ───── 번역(Translation) 계층 + 활성화(Activation) 계층 + 보증(Assurance) 계층
 ┣━ Ⅳ 흐름 ───── ① 사용자 비즈니스 의도 입력 → ② 정책 모델 변환 및 사전 검증 → ③ 자동 프로비저닝 → ④ 텔레메트리 보증
 ┣━ Ⅴ 비교 ───── 레거시 CLI vs SDN vs IBN (명령형 수작업 vs 중앙 집중 제어 vs 의도 기반 자율 폐루프)
 ┗━ Ⅵ 실무 ───── 이기종 멀티벤더 번역 드라이버 파편화 / AI 추론 판단 불확실성 / 레거시 장비 연동
```
- 필수 키워드: 비즈니스 의도(Intent) · 선언형 정책 · 폐루프(Closed-loop) · 번역/활성화/보증 · 스트리밍 텔레메트리 · 자가 치유
- 배점 전략: 10점 = Ⅰ → Ⅲ IBN 3단계 폐루프 아키텍처 다이어그램 → Ⅴ SDN vs IBN 비교표 / 25점 = Ⅰ~Ⅶ, 135회 1교시 4번 기출의 3대 핵심 빌딩 블록 및 수학적 형식 검증 상세
- 기출: 135회 1교시 4번 `IBN(Intent-Based Networking)` → Ⅰ~Ⅴ

## 한 줄 본질
- 수천 대 장비에 엔지니어가 수작업으로 CLI를 치는 명령형(How) 설정의 휴먼 에러와 운영 병목 → 관리자가 원하는 비즈니스 목적(What)만 입력하면 시스템이 이를 프로토콜 설정으로 자동 변환해 배포하고 상태를 지속 감시하여 자가 치유 → 설정 오류 제로화와 민첩성 확보 / 의도 번역 엔진의 복잡성과 멀티벤더 하드웨어 API 파편화

## 핵심 그림
```text
[ IBN 3단계 폐루프(Closed-Loop) 아키텍처 ]

  [ 비즈니스 의도 (Intent) ] "보안팀 격리 및 ERP 트래픽 지연 10ms 이하 유지"
             |
             v
  +-------------------------------------------------------------------+
  | 1. 번역 (Translation) : 자연어/의도를 정형화된 정책 모델로 변환  |
  |    (수학적 무결성 검증, 기존 네트워크 정책과의 충돌 검사)        |
  +-------------------------------------------------------------------+
             |
             v
  +-------------------------------------------------------------------+
  | 2. 활성화 (Activation) : 하위 장비별 프로토콜 설정 자동 생성·주입  |
  |    (NETCONF/YANG, OpenFlow, Ansible 통해 물리/가상 스위치 배포)   |
  +-------------------------------------------------------------------+
             |
             v
  +-------------------------------------------------------------------+
  | 3. 보증 (Assurance) : 실시간 스트리밍 텔레메트리 수집 및 검증     |
  |    (의도와 실제 상태 대조 -> 불일치 시 자동 롤백 or 자가 치유)     |
  +-------------------------------------------------------------------+
             |                                                  ^
             +---------------- 폐루프 피드백 (Closed-loop) -----+
```

## 핵심 용어
- 선언형 네트워킹(Declarative Networking): 구체적 실행 절차(How)를 일일이 지시하는 대신, 달성해야 하는 최종 목표 상태(What)만 선언하는 방식
- 폐루프 보증(Closed-loop Assurance): 네트워크 상태를 실시간 감시하여 최초 선언된 의도와 실제 상태 사이에 괴리가 생기면 사람의 개입 없이 스스로 정책을 재조정하는 자율 제어 루프
- 수학적 형식 검증(Formal Verification): 정책을 실제 스위치에 푸시하기 전에 그래프 이론과 기호 논리학을 이용해 경로 단절, 보안 루프, 정책 충돌이 발생하지 않음을 사전 입증하는 기법

## 핵심 통찰
- SDN이 "네트워크 제어권을 중앙 집중화(중앙 컨트롤러)한 인프라 구조"라면, IBN은 그 컨트롤러 위에 "인공지능과 비즈니스 의도를 얹은 상위 두뇌 시스템"
- 단순 자동화 스크립트(Ansible 등)는 명령을 한 번 실행하고 끝나는 "오픈 루프"이지만, IBN은 텔레메트리를 통해 의도가 지켜지는지 24시간 감시하는 "폐루프"
- IBN의 핵심 병목은 배포가 아니라 "보증(Assurance)"에 있음 → 수만 개 포트에서 쏟아지는 초당 기가바이트 단위의 상태 로그에서 이상 징후를 머신러닝으로 실시간 필터링해야 함

## 이웃 토픽과 구분
- SDN vs IBN: SDN은 제어 평면과 데이터 평면의 물리/논리적 분리에 초점 / IBN은 비즈니스 목표를 네트워크 정책으로 자동 번역하고 상태를 자율 보증하는 폐루프 소프트웨어 계층에 초점

## 문제·원인·대책
- 적용 상황: 대규모 엔터프라이즈 클라우드 망 정책 변경 중 보안 침해 사고
| 문제 | 원인 | 대책 | 효과 |
|---|---|---|---|
| 신규 방화벽 규칙 적용 시 기존 통신망 블랙아웃 | 정책 간의 상충(Conflict) 및 경로 루프 사전 감지 실패 | 수학적 모델 기반 형식 검증(Formal Verification) 엔진 도입 | 장비 주입 전 정책 충돌을 시뮬레이션으로 100% 사전 차단 |
| 수작업 설정 오류로 인한 보안 격리 실패 | 엔지니어 CLI 오타 및 장비별 구문 차이로 휴먼 에러 발생 | IBN 선언형 템플릿 및 YANG 모델 기반 자동 프로비저닝 | 사람의 직접 장비 접속을 배제하여 설정 무결성 확보 |

## 이렇게 출제된다
- 제135회 1교시 4번: "IBN(Intent-Based Networking)" → 요구 포인트: IBN의 개념, 3대 핵심 구성요소(번역, 활성화, 보증), 폐루프 동작 메커니즘, 기존 SDN과의 차이점

## 내 의견
- [멀티벤더 환경에서의 IBN 벤더 락인 경계] 시스코(DNA Center) 등 특정 벤더의 독점 솔루션에 의존할 경우 이기종 장비 통합이 불가능하고 라이선스 비용 폭증 → 나라면: 오픈소스 기반 OpenConfig 표준 데이터 모델(YANG)을 채택하고, 정책 버전 관리를 Git으로 통제하는 GitOps 파이프라인과 연동하여 특정 벤더 종속이 없는 개방형 IBN 생태계 구축

## 찾아볼 것
- IETF RFC 9315(Intent-Based Networking - Concepts and Definitions) 표준 규격
