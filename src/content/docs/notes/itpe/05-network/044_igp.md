---
title: "IGP(Interior Gateway Protocol)"
author: "Gemini 3.8 Flash"
date: "2026-09-20T00:35:00+09:00"
tags:
  - "notes-network"
sidebar:
  badge:
    text: "기출 · 75%"
extra:
  model: "Gemini 3.8 Flash"
  source_status: "기출"
  source_history: "137회"
  priority: 75
  priority_note: "[출제:137]"
---

## 답안 골격
```text
[IGP] ◀━━ 머리: Ⅶ 내 의견 (망 규모와 트래픽 특성에 따른 링크상태 OSPF/IS-IS 표준 채택)
 ┃
 ┣━ Ⅰ 개요 ───── 단일 자율 시스템(AS) 내부의 최적 경로 학습 및 포워딩을 위한 라우팅 프로토콜
 ┣━ Ⅱ 특징 ───── 고속 수렴(Fast Convergence), 최단 경로(SPF) 계산, 동일 비용 다중 경로(ECMP), 계층 분할
 ┣━ Ⅲ 구조 ───── 2대 주류 알고리즘: 거리벡터(Distance Vector: RIP) + 링크상태(Link State: OSPF, IS-IS)
 ┣━ Ⅳ 흐름 ───── ① 인접 라우터 Hello 감지 → ② 링크 상태(LSA) 또는 홉수 교환 → ③ 최단 경로 트리 연산
 ┣━ Ⅴ 비교 ───── 거리벡터(RIP) vs 고급 거리벡터(EIGRP) vs 링크상태(OSPF/IS-IS)
 ┗━ Ⅵ 실무 ───── 링크 플래핑 시 SPF 재연산 부하 / 대규모망 계층 분할 실패 시 수렴 지연
```
- 필수 키워드: 내부 게이트웨이 프로토콜 · 자율 시스템(AS) · 거리벡터 · 링크상태 · 다익스트라(SPF) · ECMP
- 배점 전략: 10점 = Ⅰ → Ⅲ IGP 분류 체계 트리 → Ⅴ IGP 알고리즘별 비교표 / 25점 = Ⅰ~Ⅶ, 137회 1교시 1번 기출 IGP vs EGP 상호 연계 서술
- 기출: 137회 1교시 1번 `동적 라우팅프로토콜인 IGP(Interior Gateway Protocol)와 EGP(Exterior Gateway Protocol)를 설명하시오.` → Ⅰ~Ⅴ

## 한 줄 본질
- 관리자가 수천 개 내부 경로를 수작업으로 입력하는 정적 라우팅의 유지보수 불가능성 → 단일 AS 내부에서 라우터들이 링크 상태와 홉 수를 자동으로 주고받아 최단 경로를 실시간 계산 → 회선 장애 시 자동 우회 수렴 달성 / 토폴로지 변경 시 전 라우터가 계산을 다시 수행해야 하는 CPU 부하 발생

## 핵심 그림
```text
[ IGP (내부 게이트웨이 프로토콜) 분류 체계 ]

                      +-------------------------------+
                      |   IGP (AS 내부 라우팅 프로토콜)|
                      +---------------+---------------+
                                      |
         +----------------------------+----------------------------+
         |                                                         |
         v                                                         v
 [ 1. 거리벡터 (Distance Vector) ]               [ 2. 링크상태 (Link State) ]
 - 알고리즘: Bellman-Ford                        - 알고리즘: Dijkstra SPF
 - 특징: 홉수 기반, 이웃 주장 신뢰              - 특징: 전체 토폴로지 지도 공유
 - 프로토콜:                                     - 프로토콜:
   * RIPv1 / RIPv2 (최대 15홉)                     * OSPF (Area 계층 분할, 사실상 표준)
   * EIGRP (DUAL 알고리즘, 복합 메트릭)            * IS-IS (통신사 백본, 대규모 적합)
```

## 핵심 용어
- 링크 상태(Link State): 라우터가 자신에게 직접 연결된 인터페이스의 IP, 서브넷 마스크, 대역폭, 가동 상태를 패킷(LSA)으로 만들어 망 전체에 뿌리는 정보
- 다익스트라 최단 경로(SPF): 그래프 이론에 기반하여 출발지 노드로부터 망 내 모든 목적지 노드까지의 가중치 합이 최소가 되는 최단 경로 트리를 구하는 알고리즘
- ECMP(Equal-Cost Multi-Path): 목적지까지 가는 비용이 동일한 최적 경로가 여러 개 존재할 때, 트래픽을 두 회선으로 분산시켜 로드밸런싱하는 기술

## 핵심 통찰
- IGP는 "단일 조직의 울타리 안"에서 돌기 때문에 복잡한 정치적 정책(Policy)보다는 "어느 선이 가장 빠르고 안 끊어지는가(성능)"가 유일한 판단 기준
- 거리벡터는 이웃 라우터의 "결과표"만 전해 들으므로 루프(Loop)가 생길 위험이 있으나, 링크상태는 모든 라우터가 "원시 지도"를 똑같이 들고 있어 구조적으로 루프 프리(Loop-free)
- 클라우드 대규모 데이터센터(IP Fabric)에서는 과거 복잡한 OSPF 대신 BGP를 AS 내부 IGP처럼 활용하는 "BGP in the Data Center(RFC 7938)" 아키텍처가 최신 트렌드로 급부상

## 이웃 토픽과 구분
- IGP vs EGP: IGP는 단일 AS 내부에서 최단 경로로 패킷을 고속 전달 / EGP는 독립된 AS 경계에서 국가나 사업자 간 정책 계약에 따라 트래픽을 통제

## 문제·원인·대책
- 적용 상황: 대규모 엔터프라이즈 사옥 간 라우터 링크 플래핑 장애
| 문제 | 원인 | 대책 | 효과 |
|---|---|---|---|
| 회선 불안정 시 전 라우터 CPU 100% 포화 | OSPF 링크 상태 변경 시마다 즉각적인 SPF 알고리즘 재수행 | SPF 스로틀링(SPF Throttling) 타이머 및 LSA 감쇠 적용 | 연속된 연산을 일정 시간 지연시켜 CPU 과부하 방지 |
| 회선 장애 감지 시간 지연(수십 초) | IGP 기본 Hello/Dead 타이머의 긴 주기(10초/40초) | BFD(Bidirectional Forwarding Detection) 연동 | 50ms 이내에 회선 단절을 감지해 즉각 백업 경로 절체 |

## 이렇게 출제된다
- 제137회 1교시 1번: "동적 라우팅프로토콜인 IGP(Interior Gateway Protocol)와 EGP(Exterior Gateway Protocol)를 설명하시오." → 요구 포인트: IGP의 역할, 거리벡터와 링크상태의 비교, OSPF/RIP 특징 서술

## 내 의견
- [데이터센터 패브릭에서의 IGP 단순화] 복잡한 Area 분할과 LSA 타입이 난무하는 OSPF는 현대의 3티어 스파인-리프(Spine-Leaf) 클라우드 데이터센터에 오버엔지니어링 → 나라면: 데이터센터 내부 패브릭은 BGP(eBGP per-hop)로 통일하여 설정의 일관성과 대규모 ECMP 확장을 달성하고, 전통적인 사옥 캠퍼스 유선망에만 OSPF를 국한 적용하는 이원화 라우팅 설계 채택

## 찾아볼 것
- RFC 7938(Use of BGP for Routing in Large-Scale Data Centers) 아키텍처
