---
title: "QoS(Quality of Service)"
author: "Gemini 3.8 Flash"
date: "2026-09-20T00:30:00+09:00"
tags:
  - "notes-network"
sidebar:
  badge:
    text: "기출 · 76%"
extra:
  model: "Gemini 3.8 Flash"
  source_status: "기출"
  source_history: "125회"
  priority: 76
  priority_note: "[출제(KPC):125]"
---

## 답안 골격
```text
[QoS] ◀━━ 머리: Ⅶ 내 의견 (E2E 네트워크 슬라이싱 및 능동 큐 관리(AQM)를 통한 실시간 서비스 품질 보장)
 ┃
 ┣━ Ⅰ 개요 ───── Best Effort 망의 자원 고갈·품질 저하 → 트래픽 유형별 서비스 수준 차등 보장
 ┣━ Ⅱ 특징 ───── 4대 핵심 지표(대역폭·지연·지터·손실률), 트래픽 제어, SLA 준수, 자원 배분 최적화
 ┣━ Ⅲ 구조 ───── 분류/마킹(Classification/Marking) + 조절(Policing/Shaping) + 혼잡관리(Queuing) + 혼잡회피(WRED)
 ┣━ Ⅳ 흐름 ───── ① 패킷 유입 및 헤더 분류 → ② 속도 측정 및 초과 트래픽 제어 → ③ 큐 분기 및 가중 스케줄링 출력
 ┣━ Ⅴ 비교 ───── 트래픽 폴리싱 vs 트래픽 셰이핑 (초과 패킷 즉시 폐기 vs 버퍼 지연 후 평활화)
 ┗━ Ⅵ 실무 ───── 버퍼블로트(Bufferbloat) 현상 / 이종 망 간 QoS 태그 유실 / 오버 프로비저닝과의 비용 절충
```
- 필수 키워드: 4대 파라미터 · 트래픽 분류/마킹 · 폴리싱/셰이핑 · 큐잉(PQ/CQ/WFQ) · WRED · SLA
- 배점 전략: 10점 = Ⅰ → Ⅲ QoS 4단계 메커니즘 파이프라인 도식 → Ⅴ 폴리싱 vs 셰이핑 비교표 / 25점 = Ⅰ~Ⅶ, 125회 기출 4대 지표(대역폭, 지연, 지터, 손실)와 요소 기술 상세
- 기출: 125회 2교시: "네트워크 QoS(Quality of Service)의 정의, 4대 성능 지표 및 이를 보장하기 위한 트래픽 제어 기술을 설명하시오." → Ⅰ~Ⅵ

## 한 줄 본질
- 모든 패킷을 동일하게 선착순 처리하는 Best Effort 망의 한계로 실시간 음성/화상 트래픽이 끊기는 문제 → 패킷의 중요도를 판별하여 대역폭·지연·지터·손실률의 4대 지표를 서비스별로 차등 배분 → 미션 크리티컬 업무의 안정성 확보 / 네트워크 장비의 패킷 분류 및 다단계 큐 스케줄링 프로세싱 부담 증가

## 핵심 그림
```text
[ 네트워크 QoS 패킷 처리 4단계 파이프라인 ]

 (패킷 유입)
     |
     v
 +---------------------------------------------------------------+
 | 1. 분류 및 마킹 (Classification & Marking)                    |
 |    - ACL, CoS(L2, 3비트), DSCP(L3, 6비트) 헤더에 중요도 표기   |
 +-------------------------------+-------------------------------+
                                 |
                                 v
 +---------------------------------------------------------------+
 | 2. 트래픽 컨디셔닝 (Traffic Conditioning)                     |
 |    - 폴리싱 (Policing) : 계약 속도 초과 패킷 즉시 드롭       |
 |    - 셰이핑 (Shaping)  : 초과 패킷을 버퍼에 담아 매끄럽게 지연 |
 +-------------------------------+-------------------------------+
                                 |
                                 v
 +---------------------------------------------------------------+
 | 3. 혼잡 관리 및 스케줄링 (Congestion Management / Queuing)    |
 |    - FIFO, PQ(우선순위), CQ(커스텀), WFQ(가중 공평 큐잉)      |
 +-------------------------------+-------------------------------+
                                 |
                                 v
 +---------------------------------------------------------------+
 | 4. 혼잡 회피 (Congestion Avoidance)                           |
 |    - RED / WRED : 버퍼가 넘치기 전 TCP 세션을 확률적으로 드롭 |
 +-------------------------------+-------------------------------+
                                 |
                                 v (물리 링크 출력)
```

## 핵심 용어
- QoS 4대 파라미터: 대역폭(Bandwidth, 초당 전송량), 지연(Delay, 전달 소요시간), 지터(Jitter, 지연 시간의 불규칙한 분산/변동), 패킷 손실률(Packet Loss)
- 트래픽 셰이핑(Traffic Shaping): 토큰 버킷(Token Bucket) 메커니즘을 이용해 트래픽의 급격한 피크(Burst)를 버퍼에 잠시 보관했다가 정해진 속도로 완만하게 밀어내는 기술
- WRED(Weighted Random Early Detection): 라우터 버퍼가 100% 차서 패킷이 일괄 드롭(Tail Drop)되어 전 TCP 세션이 동시 재전송에 빠지는 현상을 막기 위해, 큐가 찰 때 중요도가 낮은 패킷부터 미리 확률적으로 골라 버리는 기술

## 핵심 통찰
- QoS는 마법처럼 대역폭을 늘려주는 기술이 아니라, "부족한 자원 속에서 누구를 먼저 살리고 누구를 희생시킬 것인가"를 결정하는 정책 도구
- 음성(VoIP) 트래픽은 약간의 손실(1% 미만)은 견디지만 지터(변동)에는 쥐약이므로 최우선 큐(LLQ)에 배정하고, 데이터 전송은 지연은 상관없으나 손실이 없어야 하므로 버퍼를 크게 배정
- 하드웨어 대역폭 가격이 폭락하면서 복잡한 QoS 정책을 거는 것보다 단순히 회선 대역폭을 10배 늘리는 "오버 프로비저닝(Over-provisioning)"이 더 경제적이라는 현실적 반론 공존

## 이웃 토픽과 구분
- 트래픽 폴리싱 vs 셰이핑: 폴리싱은 초과 트래픽을 가차 없이 폐기하여 버퍼 지연이 없음 / 셰이핑은 초과 트래픽을 메모리 큐에 대기시켜 지연을 유발하되 패킷 손실을 방지

## 문제·원인·대책
- 적용 상황: 화상회의 도중 화면 깨짐 및 음성 로봇 소리 왜곡 발생
| 문제 | 원인 | 대책 | 효과 |
|---|---|---|---|
| 패킷 도착 간격의 극심한 편차(지터) 발생 | 일반 웹 다운로드 트래픽과의 큐 경합 | Low Latency Queuing(LLQ) 엄격 우선순위 큐 적용 | 음성/화상 패킷을 최우선 스케줄링해 지터 5ms 이하 유지 |
| 라우터 버퍼 포화로 전체 TCP 동시 재전송 (Global Synchronization) | 단순 꼬리 끊기(Tail Drop)로 모든 세션 패킷 동시 손실 | WRED(Weighted RED) 조기 폐기 알고리즘 가동 | 개별 세션의 윈도우를 시간차를 두고 감소시켜 전송률 유지 |

## 이렇게 출제된다
- 제125회 2교시: "네트워크 QoS의 4대 성능 지표와 이를 보장하기 위한 4단계 메커니즘을 설명하시오." → 요구 포인트: 4대 지표(대역폭, 지연, 지터, 손실) + 분류/마킹, 컨디셔닝, 큐잉, 혼잡회피 도식화

## 내 의견
- [네트워크 가상화 슬라이싱으로의 QoS 패러다임 전환] 단순 라우터 큐잉 수준의 레거시 QoS는 대규모 5G 및 멀티클라우드 환경에서 E2E 보장에 한계 노출 → 나라면: 3GPP 표준의 네트워크 슬라이싱(Network Slicing) 기술을 결합하여, 무선망부터 백본 및 클라우드 코어까지 독립된 가상 네트워크 파이프라인을 할당하고 전용 자원을 격리하는 종단 간 SLA 보장 체계 구축

## 찾아볼 것
- 토큰 버킷(Token Bucket)과 리키 버킷(Leaky Bucket) 알고리즘의 동작 특성 비교
