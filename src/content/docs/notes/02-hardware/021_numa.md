---
sidebar:
  order: 21
  label: "021. NUMA 비균등 메모리 접근 (Non-Uniform Memory Access)"
  badge:
    text: "미출 · 50%"
    variant: note
title: "비균일 기억장치 접근: NUMA 아키텍처와 최적화 (Non-Uniform Memory Access)"
date: "2026-09-07T09:34:00+09:00"
tags:
  - "notes-hardware"
weight: 21
extra:
  question_no: "021"
  source_status: "미출"
  source_history: ""
  priority: 50
  priority_note: "메모리 지역성과 다중 소켓 확장"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **비균등 메모리 접근(Non-Uniform Memory Access, NUMA)**: 다중 프로세서 시스템에서 프로세서 코어와 메모리의 물리적 위치(소켓/노드)에 따라 메모리 접근 지연시간과 대역폭이 상이한 분산 공유 메모리 구조.
- **NUMA 노드(NUMA Node)**: CPU 코어 그룹, 사설 캐시, 통합 메모리 제어기(IMC), 로컬 DRAM 채널이 결합된 독립적인 물리적 컴퓨팅 단위.
- **로컬 메모리(Local Memory)**: 해당 프로세서 코어와 동일한 NUMA 노드에 직접 장착되어 인터커넥트 경유 없이 최단 지연시간으로 접근 가능한 메모리.

</details>

- 정의/개념: 노드 거리에 따라 지연이 다른 **분산 공유 메모리** 구조
- 배경/필요성: 단일 공유 버스 기반 UMA 구조에서 코어 수 증가 시 발생하는 **메모리 버스 대역폭 포화 및 확장성 한계**

#### 한줄 요약
- NUMA는 메모리를 소켓별로 분산 배치하여 대역폭을 확장하되, 로컬과 원격 접근 간의 지연시간 비대칭성을 소프트웨어와 하드웨어로 최적화한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **원격 메모리(Remote Memory)**: 타 프로세서 소켓(노드)에 연결되어 점대점 인터커넥트(UPI/Infinity Fabric)를 경유해야 접근 가능한 상대 노드의 메모리.
- **캐시 일관성 NUMA(Cache-Coherent NUMA, ccNUMA)**: 하드웨어 디렉터리 기반 일관성 프로토콜을 통해 분산된 NUMA 노드 간에도 단일 공유 메모리 주소 공간과 데이터 일치성을 보장하는 구조.
- **메모리 지역성(Memory Locality)**: 실행 중인 스레드와 해당 스레드가 참조하는 메모리 페이지를 동일한 NUMA 노드에 인접 배치하여 원격 트랜잭션을 최소화하는 성질.

</details>

- 노드별 **IMC**·DRAM 채널로 총 메모리 대역폭 확장
- **원격 메모리** 접근은 인터커넥트 경유로 지연 증가
- **ccNUMA** 기반 분산 메모리의 단일 주소 공간 제공

#### 한줄 요약
- NUMA는 노드별 독립 메모리 채널로 대역폭을 확장하며, 스레드와 데이터의 지역성(Locality) 최적화가 시스템 성능을 좌우한다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **노드 간 인터커넥트(Inter-Node Interconnect)**: Intel UPI(Ultra Path Interconnect), AMD Infinity Fabric 등 NUMA 소켓 간에 패킷화된 데이터와 캐시 일관성 메시지를 고속 전송하는 점대점 링크.
- **통합 메모리 제어기(Integrated Memory Controller, IMC)**: 프로세서 다이에 내장되어 로컬 DDR 채널 스케줄링 및 원격 인터커넥트 요청 중재를 전담하는 유닛.
- **NUMA 인지 운영체제(NUMA-Aware OS)**: 스레드 스케줄링 및 가상 메모리 페이지 할당 시 NUMA 토폴로지를 인식하여 로컬 노드 우선 배치를 강제하는 OS 커널.

</details>

```text
[NUMA 다중 소켓 아키텍처]
  │
  ├─ [NUMA 노드] (독립 연산 단위)
  │     ├─ [코어·캐시] (스레드 실행 및 L1~L3)
  │     └─ [로컬 메모리] (전용 채널 로컬 DRAM)
  │
  ├─ [통합 메모리 제어기] (로컬·원격 요청 중재)
  │
  ├─ [점대점 인터커넥트] (소켓 간 고속 링크·일관성 패킷)
  │
  └─ [NUMA 인지 OS] (스레드 친화도·페이지 배치 통제)
```

선의 의미: NUMA 노드 내부 구조와 소켓 간 인터커넥트 및 OS 제어 계층을 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| NUMA 노드 | 코어·캐시·로컬 DRAM의 **연산 경계** 제공 |
| 통합 메모리 제어기 | 로컬 요청 처리와 **메모리 채널** 스케줄링 |
| 점대점 인터커넥트 | 노드 간 데이터·**일관성 메시지** 전송 |
| NUMA 인지 OS | **스레드 친화도**와 페이지 배치 통제 |

#### 한줄 요약
- 통합 메모리 제어기가 각 노드 안으로 들어와 모든 코어가 함께 쓰던 공용 메모리 경로를 대신하므로, 노드 경계를 넘는 접근만 점대점 인터커넥트가 떠맡고 어느 노드에 무엇을 놓을지의 판단은 NUMA 인지 OS로 넘어간다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **First-Touch 정책(First-Touch Policy)**: 가상 메모리 페이지 할당 시 메모리를 최초로 읽거나 쓴 스레드가 속한 로컬 NUMA 노드에 물리 프레임을 우선 배치하는 기본 OS 정책.
- **페이지 마이그레이션(Page Migration)**: 특정 페이지에 대한 원격 노드의 접근 빈도가 로컬 접근보다 높아질 때 해당 물리 페이지를 원격 노드의 메모리로 동적 이전하는 최적화 기법.

</details>

```text
[코어 메모리 접근 경로] (진행 ①→④, 홈 노드 판정 결과에 따라 로컬 직접 인출과 원격 인터커넥트 왕복으로 분기, 데이터는 접근 경로 그대로 코어에 반환)
  │
  ├─ [코어·캐시] (① 물리 주소 PA 기준 메모리 접근 요청 발행)
  │
  ├─ [주소 디코더] (② 홈 노드(Home Node) 소유 판정으로 로컬·원격 분기)
  │
  ├─ [통합 메모리 제어기 (로컬 IMC)] (③ 로컬 판정 시 전용 채널 로컬 DRAM 즉시 인출)
  │
  ├─ [점대점 인터커넥트 (UPI)] (③ 원격 판정 시 원격 노드로 패킷 발행)
  │
  └─ [원격 IMC] (④ 원격 DRAM 데이터 인출 후 코어로 반환, 접근 편중 시 First-Touch 배치를 AutoNUMA 페이지 마이그레이션으로 재조정)
```

분기 결과: **로컬 노드** 판정 시 수십 나노초 내 즉시 완료되나, **원격 노드** 판정 시 인터커넥트 패킷 왕복으로 2~3배의 지연시간 발생

#### 한줄 요약
- 홈 노드 판정 후 로컬이면 직접 읽고, 원격이면 인터커넥트를 경유하여 인출 후 코어로 반환한다.

## Ⅴ. 종류 및 비교

| 메모리 구조 | NUMA | UMA |
|:---|:---|:---|
| 적용 기준 | 대규모 **다중 소켓** 서버 | 단일 소켓의 단순한 메모리 접근 |
| 핵심 특징 | 소켓별 **독립 채널**과 비균등 지연 | 단일 **공유 버스**와 균등 지연 |
| 한계 | **NUMA 인식** 배치 필요 | 코어 증가 시 **대역폭 포화** |

#### 한줄 요약
- 단일 소켓의 균일성에는 UMA가 적합하고, 대규모 다중 소켓 서버의 대역폭 확장에는 NUMA가 표준이다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **CPU 친화도(CPU Affinity)**: 프로세스 또는 스레드가 지정된 특정 CPU 코어나 NUMA 노드에서만 실행되도록 스케줄러를 고정하는 설정.
- **인터리브 메모리 정책(Interleave Policy)**: 메모리 페이지를 모든 NUMA 노드에 라운드로빈 방식으로 균등 분산 할당하여 특정 노드의 메모리 대역폭 포화를 방지하는 기법.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 단일 스레드 초기화로 특정 노드에 메모리 편중 | **인터리브 할당**(numactl --interleave) 적용 | 노드 간 메모리 용량 및 채널 대역폭 균등 활용 |
| 스레드 이주로 인한 원격 메모리 접근 지연 증가 | **CPU 친화도**(numactl --cpubind) 고정 | 로컬 메모리 적중률 향상 및 접근 지연 단축 |
| 대규모 공유 버퍼 풀의 원격 트랜잭션 집중 | **NUMA 인식 버퍼 풀** 및 인스턴스 분할 | 인터커넥트 병목 완화 및 트랜잭션 처리량 극대화 |

#### 한줄 요약
- NUMA 실무 최적화는 인터리브 할당을 통한 대역폭 분산과 CPU 친화도 바인딩을 통한 로컬 메모리 적중률 극대화가 핵심이다.

## Ⅶ. 결론

- 현대 다중 소켓 서버 및 고성능 컴퓨팅의 표준 구조(ccNUMA)로 정착
- 최근에는 단일 서버 내 다중 소켓을 넘어 **CXL 기반 메모리 풀링 및 가속기(GPU/NPU) 이종 NUMA 토폴로지**로 확장 진화 추세

#### 한줄 요약
- NUMA는 하드웨어 대역폭 확장의 핵심 기반이며, OS 및 애플리케이션의 지역성 인식 바인딩이 성능을 완성한다.
