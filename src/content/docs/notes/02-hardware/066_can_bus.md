---
sidebar:
  order: 66
  label: "066. CAN 버스 (Controller Area Network)"
  badge:
    text: "기출 · 50%"
    variant: note
title: "CAN 버스 (Controller Area Network)"
date: "2026-09-07T09:45:00+09:00"
tags:
  - "notes-hardware"
weight: 66
extra:
  question_no: "066"
  source_status: "기출"
  source_history: "129회"
  priority: 50
  priority_note: "차량용 네트워크의 핵심, 비파괴 중재와 결함 격리 메커니즘"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **CAN(Controller Area Network, ISO 11898)**: 차량 및 산업용 분산 제어 환경에서 마이크로컨트롤러(ECU) 간에 호스트 컴퓨터 없이 2선식 차동 버스를 통해 고신뢰성 통신을 지원하는 표준 직렬 통신 네트워크.
- **메시지 식별자(CAN ID)**: 노드의 물리적 하드웨어 주소가 아닌 전송 데이터의 의미(엔진 RPM, 차속 등)와 우선순위를 나타내는 11비트(표준) 또는 29비트(확장) 헤더.

</details>

- 정의/개념: **메시지 식별자(CAN ID)** 기반의 비파괴 비트 중재와 하드웨어 결함 격리(Bus-Off)를 지원하는 2선식 차동 직렬 통신 **CAN 버스 아키텍처**
- 배경/필요성: 차량 내 전자제어장치(ECU) 급증에 따른 배선 하네스의 중량·원가 폭증 및 **가혹한 전자기 노이즈(EMI) 환경에서의 통신 신뢰성 한계**

#### 한줄 요약
- CAN은 노드 주소 대신 메시지 ID 우선순위로 버스를 중재하고 차동 꼬임선으로 노이즈를 상쇄하는 차량용 표준 네트워크 기술이다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **비파괴 비트 단위 중재(Non-Destructive Bitwise Arbitration)**: 복수 노드가 동시 송신할 때 우성(Dominant 0) 비트를 전송한 노드가 버스를 계속 점유하고 열성(Recessive 1) 노드는 즉시 수신으로 전환되어 데이터 충돌 손실이 없는 메커니즘.
- **버스 오프(Bus-Off)**: 전송 에러 카운터(TEC)가 255를 초과한 결함 노드를 버스 선로에서 물리적으로 강제 격리하여 네트워크 전체 마비를 차단하는 안전 기능.

</details>

- 방송 및 하드웨어 필터링: 모든 노드가 버스 프레임을 수신하되, 수신 필터(Acceptance Filter)를 통해 관심 ID만 인터럽트 수신
- 우선순위 중재: 비파괴 비트 중재로 승자 프레임을 유지하고 패자는 재시도
- 자동 결함 격리: 전송 에러 카운터 기반 3단계 전이를 통해 결함 노드를 **버스 오프(Bus-Off)**로 자동 분리

#### 한줄 요약
- 비파괴 중재로 충돌 지연을 없애고, 에러 카운터 기반 버스 오프로 결함 노드를 자동 격리하여 신뢰성을 확보한다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **CAN 컨트롤러(CAN Controller)**: 프레임 패킷화, 비트 스터핑, CRC 검증, 비트 중재 및 에러 카운팅을 하드웨어로 수행하는 디지털 로직.
- **CAN 트랜시버(CAN Transceiver)**: 디지털 로직 레벨(TxD/RxD 0/1) 신호를 CAN_H, CAN_L 차동 전압 레벨로 상호 변환하는 아날로그 물리 계층(PHY) 칩.
- **차동 꼬임선(Twisted Pair)**: 두 선의 전압 차로 동상 잡음 영향을 줄이는 물리 계층 배선.

</details>

```text
[CAN 분산 네트워크 아키텍처]
  │
  ├─ [ECU 노드 계층] (호스트 및 인터페이스)
  │     ├─ [호스트 MCU] (애플리케이션 및 센서 제어)
  │     ├─ [CAN 컨트롤러] (MAC 계층, 비트 중재/CRC)
  │     └─ [CAN 트랜시버] (PHY 계층, 로직/차동 변환)
  │
  └─ [2선식 차동 버스 매체] (물리 전송선)
        ├─ [차동 신호선] (CAN_H, CAN_L 꼬임선)
        ├─ [종단 저항] (양단 120Ω 신호 반사 방지)
        └─ [신호 전압 규격] (우성 차동 2.0V, 열성 0V)
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 호스트 MCU | 센싱 처리와 송신 메시지 큐 관리 |
| **CAN 컨트롤러** | 중재·CRC와 TEC·REC 오류 관리 |
| **CAN 트랜시버** | 로직과 **CAN**_H·CAN_L 전압 변환 |
| **차동 꼬임선** | 동상 잡음 억제와 멀티드롭 연결 |
| 120Ω 종단 저항 | 버스 양 끝의 신호 반사 억제 |

#### 한줄 요약
- CAN 컨트롤러가 호스트 MCU 소프트웨어를 대신해 중재·CRC·오류 계수를 하드웨어로 처리하고, 트랜시버가 로직 레벨과 차동 전압 사이에 끼어들어 단선 기준 신호가 감당하지 못하던 잡음을 두 선의 전압 차로 상쇄하므로 노드 증설이 배선 신설이 아니라 버스 분기로 끝난다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **ACK 슬롯(Acknowledge Slot)**: 송신 노드가 열성(1)을 보내는 1비트 구간 동안 정상 수신한 모든 노드가 우성(0)을 덮어써 수신 성공을 알리는 물리 핸드셰이크 필드.
- **비트 스터핑(Bit Stuffing)**: 동일 극성의 비트가 5개 연속되면 역극성 비트 1개를 하드웨어로 강제 삽입하여 동기화 클록 엣지를 유지하는 물리 계층 규칙.

</details>

```text
[CAN 비트 중재·프레임 전송 경로] (진행 ①→⑤, ID 비트 비교로 중재 승리·패배로 분기, ACK 실패 시 TEC 증가·재전송으로 귀결)
  │
  ├─ [송신 노드 CAN 컨트롤러] (① 버스 유휴 감지 시 복수 노드가 SOF 우성 비트 동시 송출, CAN ID MSB부터 1비트 단위 송출 개시)
  │
  ├─ [CAN 트랜시버] (② RxD 핀으로 버스 실제 전압 레벨 동시 피드백 감시, 열성 1 송출 중 우성 0 감지 시 중재 패배 판정)
  │
  ├─ [중재 승리 노드] (③④ 송신 지속 확정 후 데이터 페이로드·CRC 시퀀스를 충돌 손실 없이 연속 송출, 5비트 연속 동일 극성마다 비트 스터핑 삽입으로 동기화 유지)
  │
  ├─ [중재 패배 노드] (③④ 송신 구동 즉각 중단, 수신 모드로 전환 후 차기 유휴 시 재전송)
  │
  └─ [전 수신 노드·TEC 카운터] (⑤ CRC 검증 후 ACK 슬롯의 열성 구간을 우성 0으로 덮어써 수신 성공 응답, ACK 실패·전송 오류 시 TEC 증가)
```

분기 결과: 식별자 숫자가 낮을수록(**우성** 0이 많을수록) 중재에서 승리하여 지연 없이 전송되며, 패배한 노드는 손실 없이 즉시 수신으로 전환됨

#### 한줄 요약
- 우성 비트가 열성을 덮는 물리 특성만으로 중재를 끝내 충돌 재전송 비용을 없애는 대신, 식별자가 큰 노드의 전송 시점은 구조적으로 뒤로 밀리는 것을 감수한다.

## Ⅴ. 종류 및 비교

| 대상 구분 | Classical CAN 2.0A/B | CAN FD (Flexible Data-rate) | LIN (Local Interconnect) | 차량용 이더넷 (Automotive) |
|:---|:---|:---|:---|:---|
| 적용 기준 | 레거시 차량 파워트레인 및 범용 ECU 간 기본 제어 시 | ADAS 센서 및 섀시 제어 대용량(최대 64B) 고속 전송 시 | 도어/미러/시트 등 저속 단순 편의 장치 초저비용 배선 연결 시 | 자율주행 라이다/카메라 대용량 영상 및 중앙 백본망 구축 시 |
| 핵심 특징 | 2선식 차동 버스, 비파괴 비트 중재, 최대 1 Mbps, 최대 8바이트 페이로드, 높은 하드웨어 신뢰성 | 데이터 구간 가변 전송률(최대 5~8 Mbps), 최대 64바이트 페이로드, Classical CAN과의 하위 호환성 | 1선식 UART 기반 단일 마스터-다중 슬레이브 버스, 최대 20 kbps, 8바이트 페이로드, 초저비용 와이어링 | 100 Mbps~10 Gbps 고속 스위칭 스타 토폴로지, 1500바이트 대용량 패킷, SOME/IP 및 DoIP 지원 |
| 한계 | 대역폭(1 Mbps) 및 페이로드(8B) 제약으로 현대 자율주행 대용량 센서 데이터 수용 한계 | 트랜시버 전파 지연 및 버스 토폴로지 반사파 한계로 10 Mbps 초과 확장 난항 | 20 kbps 극저속 대역폭으로 실시간 제어 적용 불가 및 마스터 폴링 병목 | 고가의 전용 PHY/스위치 하드웨어 비용 및 2선 멀티드롭 버스 대비 배선 복잡도 상승 |

#### 한줄 요약
- 기본 제어에는 CAN 2.0, 대용량 실시간 제어에는 CAN FD, 단순 편의 장치에는 **LIN**, 대용량 센서/백본에는 차량용 이더넷이 사용된다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **SecOC(Secure Onboard Communication)**: AUTOSAR 표준 보안 프로토콜로, CAN 메시지에 메시지 인증 코드(MAC)와 신선도 값(Freshness Value)을 부착하여 스푸핑 및 재전송 공격을 차단하는 기술.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 결함 노드의 에러 프레임 폭주 | **에러 카운터 기반 Bus-Off 자동 격리** | 불량 노드가 버스에 미치는 영향 제한 |
| 종단 저항 미부착에 따른 신호 반사파 | **양 끝단 120Ω 종단 저항 실장** | 반사 억제로 신호 무결성 개선 |
| 브로드캐스트 특성에 따른 스푸핑 위협 | **AUTOSAR SecOC MAC 인증** 적용 | 패킷 위변조 및 재생 공격 차단 |

#### 한줄 요약
- 실무에서는 Bus-Off로 결함을 격리하고, 120Ω 종단 저항으로 반사파를 막으며, **SecOC**로 보안 위변조를 차단한다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **차세대 캔 통신(CAN eXtra Long, CAN XL)**: 최대 10~20Mbps 전송 속도와 2048바이트 페이로드를 지원하여 CAN FD와 차량용 이더넷 사이를 잇는 차세대 차량 네트워크 표준.
- **영역 기반 아키텍처(Zonal Architecture)**: 차량의 물리적 위치(영역)별로 존 컨트롤러를 배치하고 중앙 초고속 컴퓨터와 이더넷 백본으로 연결하는 차세대 전장 구조.
- **소프트웨어 정의 차량(Software-Defined Vehicle, SDV)**: 하드웨어보다 소프트웨어를 중심으로 차량 기능의 지속적 업데이트와 확장이 이루어지는 미래형 자동차 아키텍처.

</details>

- 클래식 CAN 기반 분산 제어의 성숙과 함께, 대용량 실시간 센서 처리를 위한 CAN FD 및 차세대 CAN XL로의 확장과 SDV 시대 차량용 이더넷 백본 및 영역 기반 아키텍처로의 계층적 융합 추세.
- 버스 양단 120Ω 종단 저항 정합을 통한 반사파 억제와 더불어, 차량 내 브로드캐스트 스푸핑을 방어하는 AUTOSAR SecOC 암호 인증 체계 확보 필요.

#### 한줄 요약
- CAN FD/XL 및 차량용 이더넷의 계층적 통합 네트워크를 수립하고 종단 임피던스 정합과 SecOC 암호 인증으로 전장 신뢰성과 보안성을 동시 확보해야 한다.
