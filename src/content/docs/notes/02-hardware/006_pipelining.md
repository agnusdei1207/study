---
sidebar:
  order: 6
  label: "006. 파이프라이닝 기본 구조 5단계 (Pipelining)"
  badge:
    text: "기출 · 50%"
    variant: note
title: "명령어 파이프라이닝: 5단계 구조와 성능 모델 (Instruction Pipelining)"
date: "2026-09-07T09:28:00+09:00"
tags:
  - "notes-hardware"
weight: 6
extra:
  question_no: "006"
  source_status: "기출"
  source_history: "122회"
  priority: 50
  priority_note: "단계 중첩과 처리량 설계"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **파이프라이닝(Pipelining)**: 명령어 처리 과정을 복수의 하위 단계(IF-ID-EX-MEM-WB)로 세분화하고, 서로 다른 명령어를 매 클록 사이클마다 병렬로 중첩 실행하여 시스템의 전체 처리량을 극대화하는 하드웨어 설계 기법.
- **처리량(Throughput)**: 단위 시간당 중앙처리장치가 처리를 완료하여 파이프라인을 빠져나가는 총 명령어 수.
- **지연시간(Latency)**: 단일 명령어가 파이프라인의 첫 단계인 인출부터 마지막 단계인 결과 기록까지 전체 경로를 통과하는 데 소요되는 총 시간.

</details>

- 정의/개념: 단일 명령어의 처리 주기를 복수의 독립 단계(IF-ID-EX-MEM-WB)로 세분화하고 연속된 명령어를 매 사이클마다 중첩 실행하여 단위 시간당 **처리량**을 극대화하는 프로세서 실행 구조
- 배경/필요성: 단일 사이클 및 비파이프라인 구조에서는 한 명령어 실행 중 나머지 하드웨어 유닛이 유휴(Idle) 상태로 방치되어 자원 낭비가 발생하므로 단계 경계에 **파이프라인 레지스터**를 배치하여 동시 가동성 확보

#### 한줄 요약
- 파이프라이닝은 단일 명령어의 지연시간을 단축하는 것이 아니라, 여러 명령어를 중첩 실행하여 시스템 전체의 처리량(Throughput)을 극대화하는 기법이다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **클록당 명령어 수(Instructions Per Cycle, IPC)**: CPU가 단일 클록 사이클당 평균적으로 완료하는 명령어 수(이상적 단일 파이프라인에서 IPC=1).
- **최장 단계 지연(Critical Path Delay)**: 파이프라인 단계 중 조합 논리 회로의 전파 지연이 가장 긴 단계의 소요 시간으로, 프로세서의 최대 동작 클록 주파수를 결정.
- **파이프라인 스톨(Pipeline Stall)**: 해저드 발생 시 특정 단계의 전진을 멈추고 버블(NOP)을 삽입하여 데이터 정합성을 유지하는 하드웨어 제어 동작.

</details>

- 처리량 극대화: 5단계 중첩 실행을 통해 이상적인 환경에서 **클록당 명령어 수(IPC)**를 1에 근접하도록 향상
- 클록 주기 결정: 분할된 파이프라인 단계 중 **최장 단계 지연**에 의해 전체 프로세서 클록 주파수가 제약되므로 단계별 균등 분할 필수
- 해저드 페널티: 데이터·제어·구조 충돌 발생 시 **파이프라인 스톨** 및 플러시가 수반되어 유효 명령어 완료율 저하

#### 한줄 요약
- 파이프라인 단수를 늘릴수록 클록 주파수는 올라가지만, 해저드 발생 시 치러야 하는 스톨 및 플러시 손실도 함께 증가한다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **명령어 인출(Instruction Fetch, IF)**: PC가 가리키는 주소의 명령어를 메모리/캐시에서 읽어와 IF/ID 레지스터에 적재하고 PC를 갱신하는 단계.
- **명령어 해독(Instruction Decode, ID)**: 명령어의 Opcode와 피연산자를 해독하고 레지스터 파일에서 피연산자 값을 인출하는 단계.
- **실행(Execute, EX)**: ALU를 통해 산술·논리 연산 수행, 메모리 유효 주소 계산, 분기 조건 판정을 수행하는 단계.
- **메모리 접근(Memory Access, MEM)**: 데이터 메모리(D-Cache)에 접근하여 로드 데이터를 읽거나 스토어 데이터를 기록하는 단계.
- **결과 기록(Write Back, WB)**: ALU 연산 결과 또는 메모리 로드 데이터를 최종 목적 레지스터 파일에 기록하는 단계.
- **파이프라인 레지스터(Pipeline Register)**: 인접한 단계 경계(IF/ID, ID/EX, EX/MEM, MEM/WB)에 위치하여 데이터와 제어 신호를 매 클록 동기화하는 래치 레지스터.

</details>

```text
[5단계 RISC 파이프라인]
  │
  ├─ [1. 명령어 인출 (IF)] (PC·명령어 캐시 읽기)
  │     └─ [IF/ID 레지스터] (명령어·PC 동기화)
  │
  ├─ [2. 명령어 해독 (ID)] (Opcode 해석·피연산자 인출)
  │     └─ [ID/EX 레지스터] (제어신호·피연산자 래치)
  │
  ├─ [3. 연산 실행 (EX)] (ALU 연산·유효주소 계산)
  │     └─ [EX/MEM 레지스터] (연산결과·분기판정 래치)
  │
  ├─ [4. 메모리 접근 (MEM)] (데이터 캐시 입출력)
  │     └─ [MEM/WB 레지스터] (로드데이터·결과값 래치)
  │
  └─ [5. 결과 기록 (WB)] (목적 레지스터 최종 저장)
```

선의 의미: 파이프라인 5단계 실행 유닛과 단계별 동기화 레지스터의 계층 구조를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 명령어 인출(IF) 유닛 | PC 기반 명령어 메모리/캐시 읽기 및 차기 PC 자동 갱신 담당 |
| 명령어 해독(ID) 유닛 | Opcode 해석, 제어 신호 생성 및 레지스터 파일 **피연산자 인출** 수행 |
| 실행(EX) 유닛 | ALU 기반 산술·논리 연산 처리 및 분기 조건 유효 주소 계산 담당 |
| 메모리 접근(MEM) 유닛 | 로드/스토어 명령어 수행을 위한 데이터 캐시 메모리 입출력 제어 |
| 결과 기록(WB) 유닛 | 최종 연산 결과 또는 로드 데이터를 목적 범용 레지스터에 영구 보존 |
| 파이프라인 레지스터 | 각 단계 사이에 위치하여 중간 결과와 제어 신호를 동기화 래치 |

#### 한줄 요약
- 5단계 파이프라인은 IF-ID-EX-MEM-WB의 5개 실행 유닛과 그 경계를 분리하는 파이프라인 레지스터로 구성되어 매 사이클 병렬 실행을 유지한다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **정상 상태(Steady State)**: 파이프라인 5개 단계가 모두 유효한 명령어로 채워져 매 클록마다 WB 단계에서 1개의 명령어가 지속 완결되는 상태.
- **파이프라인 플러시(Pipeline Flush)**: 분기 오예측 또는 인터럽트 발생 시 파이프라인 내부에 이미 진입한 후속 명령어들을 무효화(NOP)하는 제어 동작.

</details>

```text
[파이프라인 진행 제어 경로] (진행 ①→④, 의존성 해소·정정 주소 재인출 후 정상 전진으로 WB 단계 완료 배출)
  │
  ├─ [명령어 인출 (IF)] (① 새 명령어 진입, 해저드 감지 유닛이 데이터 의존성(RAW)·제어 분기 여부 매 클록 검사)
  │
  ├─ [IF/ID 레지스터] (② 로드-사용 해저드 감지 시 갱신·PC 증가 동결, ID/EX 단계에 NOP 버블 주입)
  │
  ├─ [연산 실행 (EX)] (③ 분기 오예측 확정 시 IF·ID 단계의 투기 명령어 플러시 후 정정된 주소 재인출)
  │
  └─ [결과 기록 (WB)] (④ 장애 없는 정상 전진 시 매 클록 1개 명령어 완결 배출)
```

분기 결과: **정상 상태**에서는 매 클록 1개 명령어가 완료되나, 파이프라인 스톨은 파이프라인에 버블을 주입하고, **파이프라인 플러시**는 진입된 다수의 명령어를 일괄 폐기한다

#### 한줄 요약
- 파이프라인은 정상 상태에서 매 클록 1명령어를 완료하지만, 해저드 발생 시 스톨이나 플러시 제어를 통해 실행 정합성을 복구한다.

## Ⅴ. 종류 및 비교

| 프로세서 실행 구조 | 5단계 파이프라인 구조 | 순차 비파이프라인 구조 |
|:---|:---|:---|
| 적용 기준 | **명령어 처리량 극대화** 및 범용 컴퓨팅 | **최소 하드웨어 면적** 및 단순 순차 제어 |
| 핵심 특징 | 단계 분할을 통한 **명령어 중첩 실행** | 1명령어 완료 후 차기 명령어 인출 |
| 한계 | 해저드 발생 시 **파이프라인 스톨** 및 면적 증가 | 기능 유닛 유휴 발생 및 낮은 처리량 (CPI >> 1) |

#### 한줄 요약
- 처리량이 중요하면 파이프라인, 단순 제어가 중요하면 순차 구조를 선택한다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **포워딩(Forwarding)**: 선행 연산 결과를 레지스터 기록(WB) 전에 ALU 출력에서 후속 명령어의 EX 입력으로 직결하는 하드웨어 우회 경로.
- **분기 예측(Branch Prediction)**: 조건부 분기의 방향과 목표 주소를 사전 추측하여 파이프라인 단절을 방지하는 기술.
- **단계 분할(Stage Splitting)**: 임계 지연이 큰 단계를 하위 단계로 재분할하여 프로세서 클록 주파수를 높이는 설계 기법.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 데이터 의존성(RAW)으로 인한 파이프라인 스톨 발생 | **포워딩 회로** 구축 및 컴파일러 명령어 재배치 | 데이터 대기 사이클 제거 및 IPC 저하 방어 |
| 조건부 분기 판정 지연으로 인한 인출 중단 및 버블 유입 | 2비트 포화 카운터 기반 **동적 분기 예측기** 적용 | 제어 해저드에 의한 플러시 페널티 최소화 |
| 특정 단계(MEM/ALU) 지연으로 인한 클록 주파수 저하 | 긴 단계를 2단계 이상으로 **단계 분할** 수행 | 임계 경로 단축 및 최대 동작 클록 주파수 향상 |

#### 한줄 요약
- 파이프라인 실무 설계는 포워딩과 분기 예측을 통해 스톨을 방어하고, 단계 분할과 깊이 최적화를 통해 클록 주파수와 페널티의 균형을 잡는다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **슈퍼스칼라(Superscalar)**: 프로세서 내부에 복수의 실행 파이프라인을 병렬 배치하여 사이클당 2개 이상의 명령어를 동시 발행·실행하는 아키텍처.
- **비순서 실행(Out-of-Order Execution, OoO)**: 데이터 의존성이 없는 후속 명령어를 선행 명령어의 대기 시간 동안 먼저 실행하여 파이프라인 유휴를 방지하는 동적 스케줄링 기법.

</details>

- 현대 프로세서는 다중 명령어 동시 발행 구조인 **슈퍼스칼라(Superscalar)** 및 **비순서 실행(Out-of-Order Execution, OoO)** 엔진과 파이프라인을 결합하여 명령어 수준 병렬성을 극대화하는 방향으로 진화 추세.
- 파이프라인 심화에 따른 래치 오버헤드와 분기 오예측 플러시 페널티를 방어하기 위해 전력 밀도와 유효 IPC를 정량 평가한 최적 파이프라인 깊이 도출 필요.

#### 한줄 요약
- 파이프라이닝은 슈퍼스칼라 및 비순서 실행과 결합하여 진화하고 있으며, 클록 주파수 증대와 분기 플러시 페널티 간의 최적 파이프라인 단수 절충이 핵심이다.
