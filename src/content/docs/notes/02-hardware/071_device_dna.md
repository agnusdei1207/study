---
sidebar:
  order: 71
  label: "071. 디바이스 DNA (Device DNA)"
  badge:
    text: "기출 · 50%"
    variant: note
title: "디바이스 DNA (Device DNA)"
date: "2026-09-07T09:45:00+09:00"
tags:
  - "notes-hardware"
weight: 71
extra:
  question_no: "071"
  source_status: "기출"
  source_history: "123회"
  priority: 50
  priority_note: "기기 고유 특성 프로파일링과 FDS 위험 기반 인증의 핵심"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **디바이스 DNA(Device DNA / Device Fingerprint)**: 단말기의 하드웨어 렌더링 특성, OS 환경, 브라우저 엔진 설정, 네트워크 파라미터 등 수십 가지 복합 특성값을 수집·결합하여 단말기를 고유하게 식별하는 기기 프로파일링 기술.
- **위험 기반 인증(Risk-Based Authentication, RBA)**: 접속한 단말의 디바이스 DNA 신뢰도와 거래 위험 점수를 실시간 평가하여, 이상 징후 감지 시에만 2차 인증(MFA)을 요구하는 적응형 보안 체계.

</details>

- 정의/개념: 단말의 하드웨어·소프트웨어 다차원 특성을 비식별 수집하여 기기 고유 프로파일을 생성하고 **위험 기반 인증(RBA)**을 수행하는 **디바이스 DNA 기술**
- 배경/필요성: 쿠키 삭제, 시크릿 모드, 앱 재설치 등으로 인한 **기존 정적 식별자(Cookie, IMEI) 무력화 한계 및 비대면 금융 사기 위험**

#### 한줄 요약
- 디바이스 DNA는 쿠키나 고정 식별자 없이 수십 가지 단말 환경 특성을 결합하여 기기를 식별하고 FDS 위험 점수를 산출한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **다차원 특성 결합(Multi-Factor Fingerprinting)**: 화면 해상도, CPU 코어 수, Canvas 렌더링 해시, WebGL 셰이더 결과, 설치 폰트 목록, 타임존 등을 결합하는 방식.
- **이상금융거래탐지시스템(Fraud Detection System, FDS)**: 디바이스 DNA와 금융 거래 패턴을 머신러닝으로 실시간 분석하여 사기 거래를 자동 차단하는 보안 시스템.

</details>

- 비침습적 무설치 수집: 별도 보안 프로그램 설치 없이 웹 표준 자바스크립트 스크립트만으로 다차원 특성 수집
- 쿠키 독립적 식별: 브라우저 쿠키 삭제나 시크릿 모드 접속 시에도 하드웨어/엔진 특성 기반 확률적 고유 식별 수행
- 적응형 보안 연계: 단말의 위험도(Risk Score)에 따라 정상 사용자는 통과시키고, 이상 접속 시에만 단계별 추가 인증(Step-up Auth) 요구

#### 한줄 요약
- 무설치 스크립트로 하드웨어 렌더링 특성을 수집해 쿠키 없이도 기기를 식별하며, FDS와 연계해 단계별 인증을 수행한다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **Canvas 핑거프린팅(Canvas Fingerprinting)**: 브라우저 백그라운드에서 보이지 않는 2D 그래픽/텍스트를 렌더링할 때 GPU와 그래픽 드라이버의 서브픽셀 앤티에일리어싱 차이를 해시화하여 기기를 구별하는 기법.
- **WebGL 핑거프린팅**: GPU 모델명, 벤더 정보 및 3D 셰이더 연산 결과의 부동소수점 미세 오차를 추출하는 기법.

</details>

```text
[디바이스 DNA 및 RBA 인증 아키텍처]
  │
  ├─ [단말 특성 수집 계층] (비침습 스크립트)
  │     ├─ [그래픽 렌더링 특성] (Canvas 2D, WebGL 오차)
  │     ├─ [하드웨어/시스템] (CPU 코어, 화면 해상도)
  │     └─ [브라우저/환경] (오디오 지문, 폰트, 타임존)
  │
  ├─ [특성 정제 및 프로파일 계층] (서버 엔진)
  │     ├─ [특성 정규화 엔진] (가중치 퍼지 해시 생성)
  │     └─ [기기 프로파일러] (기등록 기기 DB 대조)
  │
  └─ [위험 평가 및 정책 계층] (적응형 인증)
        ├─ [위험 점수 엔진] (접속 맥락 및 IP 평판 결합)
        └─ [적응형 정책기] (저위험 통과, 고위험 MFA/차단)
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 클라이언트 스크립트 | Canvas·WebGL 등 환경 특성 수집 |
| 특성 정규화 엔진 | 변화 완충과 퍼지 해시 생성 |
| 기기 프로파일러 | 유사도 기반 기기 매칭 |
| 위험 점수 엔진 | 접속·거래 맥락의 이상 탐지 |
| 적응형 정책기 | 승인·추가 인증·차단의 **RBA** 집행 |

#### 한줄 요약
- 단말에 식별자를 심어 두는 대신 접속할 때마다 환경 특성을 다시 모아 지문을 계산하므로 지우면 사라지는 저장물이 없어지고, 그 대가로 값이 조금씩 흔들리는 문제는 정규화기의 퍼지 해시와 프로파일러의 유사도 매칭이 떠맡는다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **퍼지 해시(Fuzzy Hash)**: 입력값의 일부분이 미세하게 변경되어도 전체 해시가 완전히 달라지는 암호학적 해시와 달리, 유사도를 백분율로 산출할 수 있는 유사성 비교 해시 알고리즘.
- **Step-up 인증(Step-up Authentication)**: 평상시에는 추가 인증 없이 간편하게 서비스를 제공하다가 고위험 이상 거래 감지 시에만 OTP, 생체 인증 등 상위 보안 인증을 동적으로 요구하는 방식.

</details>

```text
[디바이스 DNA 지문 수집·위험 기반 인증 경로] (진행 ①→⑤, 지문 유사도 90% 판정과 위험 점수 등급에 따라 즉시 승인·Step-up 인증·거래 보류로 분기)
  │
  ├─ [클라이언트 수집 스크립트] (① 로그인·송금 요청 시 Canvas 2D·WebGL 셰이더 비가시 렌더링으로 50여 개 환경 특성 즉시 추출)
  │
  ├─ [특성 정규화 엔진] (② FDS 수집 서버 전송 파라미터를 가중치 기반 퍼지 해시(Fuzzy Hash)로 정규화)
  │
  ├─ [기기 프로파일러] (③ 기등록 단말 프로파일 DB 대조로 유사도(Similarity) 산출, 90% 이상은 정상 기기 확정·미만은 변조 탐지 플래그)
  │
  ├─ [위험 점수 엔진] (④ 접속 IP 평판·접속 국가·이상 송금액을 결합한 0~100점 Risk Score 실시간 산출)
  │
  └─ [적응형 정책기 (RBA)] (⑤ 저위험(Risk<30) 무자각 즉시 승인, 중위험 FIDO2/OTP Step-up 인증 요구, 고위험 거래 강제 보류·관제 통보)
```

분기 결과: 지문 유사도와 거래 맥락을 결합하여 정상 사용자는 통과시키고 이상 징후 시 2차 인증이나 차단을 가동함

#### 한줄 요약
- 지문 일치만으로 판정하면 정상 사용자의 환경 변경까지 막게 되므로, 거래 맥락을 더한 위험 점수로 인증 강도를 차등화해 차단 정확도와 사용자 마찰을 맞바꾼다.

## Ⅴ. 종류 및 비교

| 대상 구분 | Device DNA (소프트웨어 핑거프린트) | PUF (하드웨어 반도체 지문) | 정적 하드웨어 식별자 (MAC / IMEI) | FIDO2 (생체 기반 공개키 인증) |
|:---|:---|:---|:---|:---|
| 적용 기준 | 무설치 웹 환경에서 쿠키 없이 금융 FDS 및 이상 거래 탐지 시 | 물리적 실리콘 칩 복제 방지 및 제로 스토리지 하드웨어 보안 구축 시 | 폐쇄형 내부 네트워크의 고정 단말 장비 관리 및 접근 통제 시 | 생체 정보 기반 강력한 사용자 본인 인증 및 패스워드리스 로그인 시 |
| 핵심 특징 | Canvas/WebGL 렌더링 오차 등 50여 개 환경 특성 수집, 확률적 퍼지 매칭, 무설치 백그라운드 구동 | 반도체 나노 편차 활용, 온디맨드 키 생성 및 제로 스토리지, 물리적 비복제성 보장 | MAC/IMEI 하드웨어 고유 번호 직결, 단순 문자열 일치 판정, 연산 오버헤드 전무 | 공개키 암호화 및 보안 칩(TPM/TEE) 내 개인키 서명, 생체 인식 연계, 강력한 사용자 본인 인증 |
| 한계 | 브라우저 업데이트 시 핑거프린트 드리프트 발생 및 안티디텍트 도구의 지문 위조 위험 | 하드웨어 칩 설계 단계 내장 필수 및 환경 변화에 따른 ECC 헬퍼 데이터 오버헤드 | 소프트웨어 에뮬레이션 및 스푸핑 위조에 극히 취약하며 개인정보보호 규제 강화 | 단말 등록 및 사용자 생체 인식 터치 등 명시적 사용자 개입 필수 |

#### 한줄 요약
- 무설치 이상 거래 탐지에는 Device DNA가, 물리 반도체 복제 방지에는 PUF가, 패스워드리스 사용자 인증에는 **FIDO2**가 사용된다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **핑거프린트 드리프트(Fingerprint Drift)**: 브라우저 자동 업데이트나 그래픽 드라이버 패치로 인해 정상 기기의 식별 파라미터가 시간이 지남에 따라 서서히 바뀌는 현상.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 브라우저 자동 업데이트로 인한 **핑거프린트 드리프트** 발생 | 가중치 기반 **퍼지 매칭** 및 점진적 프로파일 자동 갱신 | 정상 사용자 오차단 방지 및 편의성 유지 |
| 안티디텍트 브라우저를 통한 지문 위조/변조 | HTTP 헤더와 JS 수집값 간 불일치 **교차 검증** 및 행동 패턴 결합 | 사기 도구 및 자동화 스크립트 탐지율 95% 달성 |
| 과도한 단말 정보 수집에 따른 프라이버시 규제 | 비식별 단방향 **솔트 해시** 암호화 및 원본 파라미터 미보존 | 개인정보보호법 및 글로벌 컴플라이언스 준수 |

#### 한줄 요약
- 실무에서는 퍼지 매칭으로 드리프트를 완충하고, 교차 검증으로 지문 위조를 차단하며, 단방향 해시로 프라이버시 규제를 준수한다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **행동 바이오메트릭스(Behavioral Biometrics)**: 마우스 커서 이동 궤적, 키보드 타건 속도, 터치 압력 등 사용자의 고유한 신체적 조작 패턴을 분석하여 본인을 식별하는 기술.
- **안티디텍트 브라우저(Anti-detect Browser)**: Canvas, WebGL, 브라우저 헤더 등의 하드웨어 지문 파라미터를 인위적으로 조작하여 사기 탐지 시스템을 우회하는 공격 도구.

</details>

- 무설치 웹 환경에서 FDS 및 위험 기반 인증(RBA)의 핵심 수단으로 안착되었으며, 안티디텍트 브라우저의 지문 변조에 대응하기 위해 Canvas/WebGL 정밀 분석과 AI 행동 바이오메트릭스를 결합한 복합 무자각 인증으로의 진화 추세.
- 브라우저 업데이트에 따른 핑거프린트 드리프트 완충 퍼지 매칭 로직과 개인정보보호를 위한 단방향 솔트 해시 비식별화 체계의 상호 조화 확보 필요.

#### 한줄 요약
- 다차원 핑거프린팅과 AI 행동 바이오메트릭스를 융합하고 드리프트 완충 및 비식별화 거버넌스를 수립하여 탐지 정확도와 프라이버시를 동시 달성해야 한다.
