---
sidebar:
  order: 76
  label: "076. 버스 대역폭과 전송률 계산 (Bus Bandwidth)"
  badge:
    text: "미출 · 50%"
    variant: note
title: "버스 대역폭과 전송률 계산 (Bus Bandwidth)"
date: "2026-09-07T09:45:00+09:00"
tags:
  - "notes-hardware"
weight: 76
extra:
  question_no: "076"
  source_status: "미출"
  source_history: ""
  priority: 50
  priority_note: "단위·방향별 상한과 실측 비교"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **버스 대역폭(Bus Bandwidth)**: 컴퓨터 시스템의 프로세서, 메모리, 주변장치 간 물리 인터페이스를 통해 단위 시간(초)당 전송할 수 있는 최대 데이터 처리 용량(GB/s 또는 Gbps).
- **실효 처리량(Effective Throughput)**: 라인 코딩, 패킷 헤더, 흐름 제어 및 오류 정정 오버헤드를 차감하고 실제 애플리케이션 계층에 도달하는 순수 유효 페이로드(Payload) 전송 속도.

</details>

- 정의/개념: 물리 신호 전송률, 버스 폭, 라인 코딩 및 변조 방식을 결합하여 물리 상한과 실효 처리량을 정량화하는 **버스 대역폭** 계산 체계
- 배경/필요성: 하드웨어 공칭 물리 전송률(GT/s)과 프로토콜 오버헤드로 인한 **실효 처리량(Effective Throughput)** 간의 성능 괴리 및 I/O 병목 한계

#### 한줄 요약
- 버스 대역폭은 물리 신호 전송률, 레인 수, 라인 코딩 효율에 의해 물리 상한이 결정되며, 프로토콜 오버헤드를 차감하여 실효 처리량을 산출한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **전송률(Transfer Rate, GT/s)**: 직렬 물리 신호선에서 초당 전송되는 기가 단위의 신호 전이 횟수(Giga-Transfers per second).
- **라인 코딩(Line Coding)**: 직류 균형(DC Balance)과 클록 복원(Clock Recovery)을 위해 원시 비트열을 변환하는 인코딩(8b/10b, 128b/130b, FLIT).
- **PAM4(4-Level Pulse Amplitude Modulation)**: 한 클록 단위 구간(UI)에 4단계 전압 준위를 인가하여 클록당 2비트의 데이터를 전송하는 고속 변조 기술(PCIe 6.0/7.0).

</details>

- 레인 수($N$) 및 변조 방식 비례: 직렬 차동 링크의 레인 수와 신호 변조(**PAM4**, NRZ)에 비례하여 대역폭 증대
- 라인 코딩 오버헤드 차감: 8b/10b, 128b/130b, FLIT 패킷에 따른 물리 계층 효율 반영 필수
- 프로토콜 오버헤드 반영: TLP 헤더, 시퀀스 번호, LCRC, 크레딧 흐름 제어로 인해 실제 페이로드 처리량 감소

#### 한줄 요약
- 물리 신호 **전송률**에 변조 방식, 레인 수, 라인 코딩 및 패킷 오버헤드를 단계별로 반영해야 실제 유효 대역폭이 도출된다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **순방향 오류 정정(Forward Error Correction, FEC)**: PAM4의 높은 잡음 취약성을 보정하기 위해 수신단에서 재전송 없이 비트 오류를 실시간 복구하는 하드웨어 블록.

</details>

```text
[직렬 버스 대역폭 프로토콜 계층]
  │
  ├─ [응용 및 DMA 계층] (순수 유효 페이로드 생성)
  ├─ [트랜잭션 계층] (TLP 패킷 캡슐화 및 ECRC)
  ├─ [데이터 링크 계층] (LCRC 부착 및 FLIT 흐름 제어)
  │
  └─ [물리 계층 (PHY)] (신호 전송 및 변조)
        ├─ [PCS 부계층] (128b/130b 코딩 및 FEC 엔진)
        └─ [PMA 부계층] (SerDes 직렬화 및 차동 레인)
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 소속 및 위치 | 핵심 엔지니어링 책임 | 주요 특징 |
|:---|:---|:---|:---|
| 응용 DMA 엔진 | 호스트/디바이스 | 시스템 메모리와 버스 간의 데이터 블록 이동을 지시하는 원천 | 순수 페이로드 |
| 트랜잭션 계층 | 프로토콜 스택 | 주소 라우팅 헤더 및 ECRC를 부착하여 TLP 패킷 캡슐화 | 트랜잭션 제어 |
| 데이터 링크 계층 | 프로토콜 스택 | 패킷 시퀀스 번호, LCRC 부착 및 FLIT 기반 흐름 제어 | 무손실 재전송 |
| PCS 하위 계층 | 물리 계층 디지털단 | **라인 코딩**, 바운더리 동기화 및 하드웨어 **FEC** 에러 복구 | 코딩 효율 결정 |
| PMA 하위 계층 | 물리 계층 아날로그단 | 병렬 데이터를 고속 직렬 신호로 변환(SerDes)하고 차동 핀 구동 | NRZ / PAM4 변조 |

#### 한줄 요약
- 버스 대역폭은 응용 DMA, 트랜잭션 패킷화, 데이터 링크 FLIT, PCS 라인 코딩 및 PMA SerDes 물리 레인으로 결정된다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **최대 페이로드 크기(Max Payload Size, MPS)**: 단일 TLP 패킷에 실을 수 있는 데이터 페이로드의 최대 바이트 수(128B~512B)로, 클수록 헤더 오버헤드가 줄어듦.

</details>

```text
[버스 실효 대역폭 산출 경로] (진행 ①→⑤, MPS 설정값 판정에 따라 실효 대역폭 82% 수준과 95% 달성으로 분기, 실측 검증으로 확정)
  │
  ├─ [인터페이스 사양] (① 세대별 물리 전송률(GT/s) 및 레인 수(N) 특정)
  │
  ├─ [물리 계층 (PCS·PMA)] (② NRZ/PAM4 변조·라인 코딩 효율(η) 판정 후 물리 단방향 이론 대역폭 = (전송률 × N × η) / 8 GB/s 산출)
  │
  ├─ [프로토콜 스택 (TLP·데이터 링크)] (③ TLP 헤더 + LCRC + 시퀀스 번호 패킷 오버헤드 차감)
  │
  ├─ [MPS (Max Payload Size) 설정] (④ 작은 MPS(128B)는 헤더 오버헤드 증가로 실효 대역폭 82% 수준, 대형 MPS(512B)는 극소화로 95% 달성)
  │
  └─ [벤치마크 툴 (FIO/CudaMemcpy)] (⑤ 실측 대역폭 기반 최종 검증)
```

분기 결과: 대형 **MPS** 설정 시 헤더 오버헤드가 최소화되어 물리 이론치의 95% 이상 **실효 처리량** 달성

#### 한줄 요약
- 라인 코딩 손실은 링크 세대가 정해지는 순간 함께 확정되는 고정 차감인 반면 패킷 오버헤드는 MPS 설정으로 줄일 수 있는 가변 차감이므로, 실효 처리량을 끌어올릴 여지는 후자 쪽에만 남는다.

## Ⅴ. 종류 및 비교

| 버스 인터페이스 표준 | PCIe 4.0 (x16) | PCIe 5.0 (x16) | PCIe 6.0 (x16) | DDR5-6400 (듀얼 채널) |
|:---|:---|:---|:---|:---|
| 물리 전송률 및 변조 | 16.0 GT/s (NRZ) | 32.0 GT/s (NRZ) | **64.0 GT/s (PAM4)** | 6400 MT/s (DDR 병렬) |
| 라인 코딩 방식 | 128b/130b (98.46%) | 128b/130b (98.46%) | 242B/256B FLIT + **FEC** | 무인코딩 (순수 병렬) |
| 단방향 물리 대역폭 | 31.51 GB/s | 63.02 GB/s | 121.0 GB/s | 51.2 GB/s (채널당) |
| 양방향 총 대역폭 | 63.02 GB/s | 126.0 GB/s | 242.0 GB/s | 102.4 GB/s (합산) |
| 주요 적용 분야 | 일반 고속 NVMe SSD | 최신 AI 가속기 | 차세대 AI/CXL 스위치 | 서버 메인 시스템 메모리 |

#### 한줄 요약
- PCIe는 세대마다 전송률이 2배씩 증가하며, PCIe 6.0부터는 PAM4와 FLIT 구조를 채택하여 단방향 121GB/s의 초고대역폭을 제공한다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **크레딧 기반 흐름 제어(Credit-Based Flow Control)**: 수신단 버퍼에 여유 공간(크레딧)이 있을 때만 송신단이 패킷을 전송하여 버퍼 오버플로를 0화하는 PCIe 메커니즘.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 작은 I/O 요청 시 프로토콜 헤더 오버헤드로 실효 처리량 급감 | **MPS 512B** 상향 및 I/O **버퍼 병합** | 패킷 헤더 비율 축소 및 실효 대역폭 90% 이상 확보 |
| 단위 및 방향성 혼동에 따른 사이징 오산 | **단위 체계** 명시 및 **전이중 대역폭** 분리 설계 | I/O 인프라 과소/과대 투자 리스크 원천 방지 |
| 고속 PAM4 신호 전송 시 높은 비트 에러율(BER)로 인한 패킷 손실 | 저지연 하드웨어 **FEC** 및 **FLIT 아키텍처** 결합 | 신호 재전송 지연 방지 및 무결성 대역폭 유지 |

#### 한줄 요약
- 실무에서는 MPS 최적화로 헤더를 줄이고, 정확한 단위 체계로 사이징하며, 하드웨어 FEC로 PAM4 무결성을 사수한다.

## Ⅶ. 결론

- 고속 인터커넥트(PCIe 5.0/6.0, CXL, NVLink) 설계 시 물리 전송률(GT/s), 라인 코딩(128b/130b, FLIT), MPS(**Max Payload Size**) 및 FEC 오버헤드를 반영한 실효 대역폭 산정 체계 정착
- 최근 **PAM4** 변조 및 광학 인터커넥트 융합을 통한 Tbps급 대역폭 확장으로 진화

#### 한줄 요약
- 버스 대역폭 계산은 물리 계층 전송 한계와 상위 프로토콜 오버헤드를 종합 고려하여 실효 용량을 확보하는 것이 핵심이다.
