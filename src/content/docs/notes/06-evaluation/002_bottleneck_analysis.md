---
sidebar:
  order: 2
  label: "002. 병목 분석 (Bottleneck Analysis)"
  badge:
    text: "미출 · 50%"
    variant: note
title: "시스템 성능 제약 요인 식별 및 최적화 : 병목 분석 (USE 및 RED 방법론)"
date: "2026-09-07T14:00:00+09:00"
tags:
  - "notes-evaluation"
weight: 2
extra:
  question_no: "002"
  source_status: "미출"
  source_history: ""
  priority: 50
  priority_note: "성능 튜닝 및 진단의 핵심 방법론, 병목(Bottleneck) 및 임계 경로(Critical Path) 식별, Brendan Gregg의 USE 방법론(Utilization, Saturation, Errors), Tom Wilkie의 RED 방법론(Rate, Errors, Duration), 대기열 이론(M/M/1 Queue) 기반 포화점 분석, 병목 이동(Bottleneck Shift) 검증"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **병목 분석(Bottleneck Analysis / Performance Diagnostics)**: 소프트웨어 시스템 및 IT 인프라 전 구간에서 트래픽 증가 시 처리량(Throughput)을 제약하고 응답 지연(Latency)을 급증시키는 가장 취약한 단일 제약 자원(Resource) 또는 소프트웨어 임계 경로(Critical Path)를 정량적으로 식별하고 근본 원인을 규명하는 성능 엔지니어링 기법.
- **자원 포화에 따른 대기열 폭증 및 병목 이동 결함(Queue Saturation & Bottleneck Shift Defect)**: 특정 자원(예: DB Connection Pool)의 용량이 한계에 도달했을 때 요청이 대기열(Queue)에 적체되어 응답시간이 비선형적으로 폭증하는 현상과, 해당 병목을 튜닝한 직후 차순위 자원(예: CPU 또는 Disk I/O)으로 병목 지점이 연쇄 전이되는 현상을 사전에 예측하지 못하는 구조적 결함.
- **임계 경로(Critical Path)**: 요청 처리 과정에서 병렬로 수행되는 구간 가운데 가장 오래 걸려 전체 응답시간을 결정하는 호출 경로로, 이 경로 위의 구간을 줄이지 않으면 다른 구간을 아무리 튜닝해도 응답시간이 줄지 않는다.

</details>

- 정의/개념: 처리량을 제한하는 **임계 경로**를 식별하는 성능 진단 기법
- 배경/필요성: 시스템 트래픽 급증 시 정확한 제약 자원(Resource)과 소프트웨어 임계 경로(Critical Path)를 과학적으로 식별하지 않은 채 진행하는 무분별한 하드웨어 증설은 데이터베이스 락(Lock) 경합이나 슬로우 쿼리 등의 근본 원인을 해소하지 못하고 막대한 클라우드 인프라 비용만 낭비하는 구조적 한계가 발생함에 따라, Brendan Gregg의 USE 방법론(자원 관점: Utilization, Saturation, Errors)과 Tom Wilkie의 RED 방법론(서비스 관점: Rate, Errors, Duration) 및 APM 분산 추적(Distributed Tracing)을 융합하는 **병목 분석** 체계를 도입하여 인프라 자원 포화와 서비스 지연 구간의 정밀 국소화, 튜닝 후 병목 이동(Bottleneck Shift) 사전 예측 및 최소 투자 대비 시스템 처리량(Throughput) 향상을 달성할 필요

#### 한줄 요약
- 무분별한 증설은 병목이 아닌 자원에 비용을 쓰는 것이므로, 진단에 드는 관측 비용은 증설 한 번의 낭비보다 항상 싸다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- 병목 진단 2대 핵심 방법론:
  - **USE 방법론 (Utilization, Saturation, Errors / Brendan Gregg)**: 모든 하드웨어 자원(CPU, 메모리, 디스크, 네트워크)의 사용률, 큐 적체 포화도, 에러 발생 여부를 점검하는 인프라 중심 분석.
  - **RED 방법론 (Rate, Errors, Duration / Tom Wilkie)**: 모든 마이크로서비스 엔드포인트의 초당 요청률(Rate), 실패 요청 비율(Errors), 처리 소요 시간(Duration)을 측정하는 서비스 중심 분석.

</details>

- 자원 **USE**와 서비스 RED를 시계열로 교차 진단
- 분산 추적으로 **임계 경로**의 락·슬로우 SQL 격리
- 단일 요인 변경 후 **병목 이동** 여부를 동일 부하로 검증

#### 한줄 요약
- 자원 관점과 서비스 관점을 따로 보면 각각은 정상인데 합치면 병목인 지점이 드러나며, 단일 요인 튜닝은 사이클 비용을 내고 인과관계를 산다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **베이스라인(Baseline)**: 튜닝 전에 동일 부하 프로파일로 측정해 둔 기준 성능 수치로, 이후 변경의 효과를 같은 조건에서 비교하는 대조군이 된다.
- **분산 추적(Distributed Tracing)**: 요청마다 트레이스 ID를 부여해 마이크로서비스 경계를 넘는 호출을 스팬(Span) 단위로 이어 붙여, 전체 응답시간 중 어느 서비스·구간이 얼마를 소비했는지 국소화하는 관측 기법.
- **플레임 그래프(Flame Graph)**: 샘플링한 스택 트레이스를 함수별로 누적해 폭이 CPU 점유 시간에 비례하도록 그린 그림으로, 코드 수준에서 어느 함수가 핫스폿인지 한눈에 지목한다.

</details>

```text
[병목 분석 체계]
├── [기준 부하]
│   └── 재현 가능한 성능 베이스라인
├── [인프라 관측]
│   └── USE 방법론 (자원 사용률·포화도·에러)
├── [서비스 관측]
│   └── RED 방법론 및 APM 분산 추적
└── [코드 프로파일링]
    └── 플레임 그래프 및 슬로우 쿼리 격리
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 기준 부하 | 재현 가능한 **베이스라인** 설정 |
| 인프라 관측 | 자원별 **USE** 지표 수집 |
| 서비스 관측 | API별 **RED**·**분산 추적** 수집 |
| 코드 프로파일링 | **플레임 그래프**·슬로우 쿼리로 함수·락 핫스폿 격리 |

#### 한줄 요약
- USE는 자원 경계에서 RED는 API 경계에서 같은 부하를 보므로 두 관측이 어긋나는 지점이 곧 병목 후보이며, 코드 프로파일링이 그 아래에 끼어들어 계층 지표만으로는 좁혀지지 않는 함수·락·쿼리 단위 지목을 대신해 재현 없는 추정 튜닝의 재작업을 없앤다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- 병목 분석 및 제거 5단계 반복 사이클:
  1. 부하 시험 환경 및 성능 기준선(Baseline) 확립
  2. USE 방법론을 통한 인프라 자원 포화도(Saturation) 스캔
  3. RED 방법론 및 APM 분산 추적을 통한 서비스 지연 구간 국소화
  4. 단일 병목 원인 가설 수립 및 코드/인프라 튜닝 단행
  5. 동일 부하 환경에서 재시험 수행 및 병목 이동(Bottleneck Shift) 검증

</details>

```text
기준 부하
   |
   v
1. USE·RED 관측
   |
   v
2. 임계 경로 격리
   |
   v
3. 단일 요인 변경
   |
   v
4. 동일 부하 재시험
   +-- [새 병목] 1단계로 반복
   `-- [목표 충족] 결과 확정
```

분기 결과: 동일 부하 재시험에서 [목표 충족]이면 결과를 확정하고, [새 병목]이면 차순위 자원으로 이동한 병목을 1단계부터 다시 관측하므로, 병목 이동 한 번마다 관측·격리·변경·재시험 사이클 비용이 통째로 반복된다.

#### 한줄 요약
- 한 번에 한 요인만 바꾸면 사이클 수가 늘지만, 여러 요인을 동시에 손대면 재시험 결과가 병목 이동인지 개선 실패인지 구분되지 않아 원인 재탐색 비용이 더 커진다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- 성능 분석 3대 진단 프레임워크 비교:
  - USE 방법론: 자원 중심 (Utilization, Saturation, Errors).
  - RED 방법론: 서비스 중심 (Rate, Errors, Duration).
  - Google SRE 4대 황금 신호: 사용자 및 시스템 통합 (Latency, Traffic, Errors, Saturation).

</details>

| 비교 항목 | USE 방법론 (Brendan Gregg) | RED 방법론 (Tom Wilkie) | 4대 황금 신호 (Google SRE) |
|:---|:---|:---|:---|
| 진단 대상 | 하드웨어·OS 물리 자원 | 마이크로서비스 API | 분산 서비스 전체 |
| 핵심 지표 | 가동률·포화도·에러 | 요청률·에러율·지연 | 지연·트래픽·에러·포화도 |
| 분석 관점 | 자원 공급자 관점 | 서비스 소비자 관점 | 시스템·사용자 통합 관점 |
| 주요 활용 | 인프라 병목 식별 | 호출 지연 분석 | SLO 모니터링 |
| 한계 | 코드·쿼리 추적 불가 | 자원 고갈 원인 미제공 | 커널 프로파일 부족 |

#### 한줄 요약
- USE와 RED는 같은 병목을 공급자와 소비자 양쪽에서 보는 상보 관계이고 황금 신호는 그 둘을 SLO 하나로 묶은 축약이므로, 셋 중 하나를 고르는 문제가 아니라 어느 층에서 먼저 볼지의 순서 문제다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **마이크로 버스트(Micro-burst)**: 수 ms 단위로 순간 도착률이 평균의 수 배로 치솟아 큐를 넘치게 하지만 초 단위 평균 지표에서는 사라지는 부하 패턴으로, 평균 사용률이 낮아도 순간 포화와 드롭을 일으킨다.
- **런큐(Run Queue)**: CPU 코어마다 실행 가능 상태로 스케줄링을 기다리는 스레드를 줄 세운 커널 자료구조로, 그 길이가 코어 수를 넘는 시간이 CPU 포화(Saturation)를 직접 나타낸다.
- **eBPF(extended Berkeley Packet Filter)**: 커널을 수정하지 않고 검증된 소형 프로그램을 커널 이벤트(스케줄러·시스템 콜·네트워크)에 붙여 실행하는 기술로, 런큐 지연 같은 지표를 마이크로초 해상도로 낮은 오버헤드에 수집한다.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 평균값이 순간 **마이크로 버스트** 은폐 | **eBPF**로 고해상도 **런큐** 측정 | 순간 포화 식별 |
| 튜닝 후 차순위 자원이 새 병목으로 전환 | 동일 부하로 **병목 이동** 재시험 | 연쇄 병목 조기 식별 |
| 동시 변경으로 개선 원인 규명 불가 | 단일 요인 변경 후 재검증 | 튜닝 인과관계 보존 |

#### 한줄 요약
- 세 대책의 공통 비용은 시간(고해상도 계측 부담, 재시험 회차, 변경 한 번씩)이고 사는 것은 판단의 확실성이므로, 시간을 아끼려 동시에 바꾸면 그 절약분이 원인 재탐색으로 되돌아온다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **OpenTelemetry**: 분산 시스템 전반의 트레이스, 메트릭, 로그를 통합 수집하여 서비스 간 지연과 병목을 추적할 수 있도록 지원하는 클라우드 네이티브 관찰성 표준 프레임워크.
- **RCA (Root Cause Analysis, 근본 원인 분석)**: 장애나 성능 저하를 유발한 표면적 증상 뒤에 숨어 있는 가장 핵심적인 설계 결함, 자원 경합 또는 코드 결함을 논리적으로 규명하는 진단 기법.

</details>

- 경험적 추정을 탈피하여 시스템 인프라와 마이크로서비스 전 구간의 제약 요인을 수학적·실측 기반으로 규명하는 시스템 성능 진단 및 최적화(Bottleneck Analysis / USE & RED Methodologies / Critical Path Profiling)의 핵심 엔지니어링 표준으로 확고히 자리 잡았으며, eBPF 커널 프로파일링 및 분산 트레이싱(OpenTelemetry) 기반 자동 근본원인 분석(RCA)으로 진화하는 가운데, 실무 성능 진단 및 튜닝 수행 시에는 인프라 USE 포화도와 서비스 RED 메트릭의 시계열 상관분석, 가설 기반의 단일 요인 튜닝(One Factor at a Time)을 통한 인과관계 보존, 동일 부하 조건 재시험을 통한 차순위 자원으로의 병목 이동(Bottleneck Shift) 전수 검증을 결합하여 시스템 처리 용량과 안정성을 확보해야 한다.

#### 한줄 요약
- 병목은 제거되는 것이 아니라 이동하는 것이므로, 튜닝 종료 조건은 병목의 해소 비용과 인프라 증설 비용 간의 트레이드오프를 평가하여 체계적으로 판단해야 한다.
