---
sidebar:
  order: 5
  label: "005. 암달의 법칙 - 병렬화 한계 (Amdahl's Law)"
  badge:
    text: "기출 · 50%"
    variant: note
title: "병렬 컴퓨팅 가속 한계 및 확장성 모델링 : 암달의 법칙 (Amdahl's Law vs Gustafson's Law)"
date: "2026-09-07T14:00:00+09:00"
tags:
  - "notes-evaluation"
weight: 5
extra:
  question_no: "005"
  source_status: "기출"
  source_history: "132회"
  priority: 50
  priority_note: "132회 기출, 병렬 컴퓨팅 가속비(Speedup)와 확장성 한계, Gene Amdahl의 암달의 법칙(Amdahl's Law, Strong Scaling: S(N)=1/((1-p)+p/N)), 순차 실행 구간(Serial Fraction 1-p)에 의한 이론적 최대 가속 상한(1/(1-p)), 존 구스타프슨의 법칙(Gustafson's Law, Weak Scaling: S(N)=N-(1-p)(N-1)), 멀티코어 및 GPU 가속기 사이징"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **암달의 법칙(Amdahl's Law / Gene Amdahl 1967)**: 고정된 크기의 워크로드(Workload)를 처리할 때, 병렬 처리 프로세서(Core/Node)의 개수를 아무리 무한대($N \to \infty$)로 증설하더라도 전체 시스템의 이론적 최대 가속비(Speedup)는 프로그램 내에 존재하는 순차 실행 구간의 비율($1-p$, Serial Fraction)의 역수($\frac{1}{1-p}$)를 절대 초과할 수 없다는 컴퓨터 아키텍처 수리 법칙.
- **순차 병목에 의한 프로세서 과잉 투자 결함(Diminishing Returns & Amdahl's Wall Defect)**: 소프트웨어 내부의 동기화 락(Lock)이나 단일 스레드 I/O 작업과 같은 순차 실행 구간을 개선하지 않은 채 하드웨어 CPU 코어만 무분별하게 64코어, 128코어로 증설할 경우, 가속비가 급격히 포화되어 수억 원의 인프라 투자 비용 대비 성능 향상이 5% 미만에 그치는 구조적 결함.

</details>

- 정의/개념: 멀티코어 및 분산 컴퓨팅 환경에서 프로세서 증설에 따른 성능 가속비를 정량 예측하기 위해, 고정 문제 크기에서 순차 실행 비율($1-p$)의 역수로 성능 상한을 도출하는 **암달의 법칙(강한 확장성)**과 문제 규모 확장을 전제로 가속을 모델링하는 **구스타프슨의 법칙(약한 확장성)**을 가정 조건에 따라 비교하는 **병렬 성능 평가 모델**
- 배경/필요성: 순차 실행 구간(1-p)을 개선하지 않은 채 무분별하게 프로세서만 증설할 경우 가속비가 한계에 수렴하는 **성능 병목 포화 및 하드웨어 과잉 투자 비효율 발생**

#### 한줄 요약
- 암달의 법칙은 고정 작업 크기에서 순차 실행 비율(1-p)이 전체 병렬 가속 상한(1/(1-p))을 결정함을 규명한다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **암달의 법칙 3대 핵심 수리 변수**:
  - **병렬화 가능 비율 ($p$, Parallel Fraction)**: 전체 프로그램 실행 시간 중 다중 코어로 완벽히 분할 가능한 작업 비율 ($0 \le p \le 1$).
  - **순차 실행 비율 ($1-p$, Serial Fraction)**: 데이터 의존성, 초기화, 락 동기화로 인해 단일 코어에서만 실행되어야 하는 비율.
  - **투입 프로세서 수 ($N$, Number of Processors)**: 병렬 연산에 동시 투입되는 CPU 코어 또는 컴퓨팅 노드 개수.

</details>

- 코어 증가에도 순차 시간이 남는 **강한 확장성** 포화
- 무한 코어에서 $1/(1-p)$로 제한되는 **가속 상한**
- 코어 증설보다 순차 구간 축소가 높이는 **병렬 효율**

#### 한줄 요약
- 강한 확장성 포화, 이론적 가속 상한($\frac{1}{1-p}$), 직렬 구간 단축의 최고 ROI를 제공한다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **암달의 법칙 가속비 공식 유도 과정**:
  1. 단일 프로세서 실행 시간: $T(1) = T_{serial} + T_{parallel} = T_1(1-p) + T_1 p$.
  2. $N$개 프로세서 실행 시간: $T(N) = T_1(1-p) + \frac{T_1 p}{N}$.
  3. 가속비(Speedup) 정의: $S(N) = \frac{T(1)}{T(N)} = \frac{T_1}{T_1(1-p) + \frac{T_1 p}{N}} = \frac{1}{(1-p) + \frac{p}{N}}$.

</details>

```text
[암달의 법칙 가속 모델]
├── [순차 실행 구간 (1-p)]
│   ├── 단일 코어 독점 실행
│   └── 이론적 가속 상한 결정 (1/(1-p))
└── [병렬 실행 구간 (p)]
    ├── N개 프로세서 분할 연산
    └── 코어 수 증가에 따른 처리 시간 단축 (p/N)
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| **기준 실행** | 단일 코어의 기준 시간 측정 |
| **순차 실행부** | 코어 증가와 무관한 순차 시간 유지 |
| **병렬 실행부** | $N$개 코어에 병렬 작업 분할 |

#### 한줄 요약
- 코어 증설이 줄여 주는 것은 병렬 실행부뿐이고 순차 실행부는 기준 실행 시간 그대로 남으므로, 증설한 코어는 순차 구간이 지나가는 동안 유휴로 대기하는 비용을 그대로 치른다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **암달의 법칙 기반 멀티코어 인프라 최적 사이징 5단계 프로세스**:
  1. 단일 코어 환경에서 소프트웨어 실행 프로파일링($T_1$) 수행
  2. 함수별 호출 시간을 분석하여 순차 비율($1-p$)과 병렬 비율($p$) 정밀 분리
  3. 코어 수($N=2, 4, 8, 16, 32, 64$)에 따른 이론 가속비($S(N)$) 및 효율($E(N)$) 계산
  4. 병렬 효율이 70% 이하로 급감하는 최적 코어 수(Inflection Point) 도출
  5. 순차 구간(DB Lock, 동기 I/O)을 비동기/Lock-free로 리팩토링하여 가속 상한 확장

</details>

```text
[단일 코어 실행]
        │
        ▼
1. 실행 구간 측정
        │
        ▼
2. 병렬 비율 산출
        │
        ▼
3. 코어별 가속비 비교
        │
        ▼
4. 코어 수·순차 구간 조정
        │
        ▼
[실측 가속비 검증]
```

**동작 원리**

1. **실행 구간 측정**: 프로파일링으로 순차·병렬 실행 시간 분리
2. **병렬 비율 산출**: 전체 시간에서 $p$와 $1-p$ 비율 계산
3. **코어별 가속비 비교**: $S(N)$과 병렬 효율로 증설 한계 판정
4. **코어 수·순차 구간 조정**: 코어 증설과 순차 구간 개선안 선택

#### 한줄 요약
- 실행 구간 측정과 가속비 비교로 코어 수를 조정한다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **병렬 컴퓨팅 2대 가속 법칙 비교**:
  - 암달의 법칙 (Amdahl's Law): 고정 작업 크기 (Strong Scaling) $\rightarrow$ 순차 비율에 의한 가속 상한 수렴 ($S(N) = \frac{1}{(1-p) + p/N}$).
  - 구스타프슨의 법칙 (Gustafson's Law): 가변 작업 크기 (Weak Scaling) $\rightarrow$ 코어 수에 비례한 선형 가속 ($S(N) = N - (1-p)(N-1)$).

</details>

| 병렬 가속 법칙 | 암달의 법칙 | 구스타프슨의 법칙 |
|:---|:---|:---|
| 적용 기준 | 고정 작업의 응답시간 단축 | 프로세서와 함께 작업 규모 확대 |
| 핵심 특징 | **강한 확장성**에서 순차 비율로 상한 결정 | **약한 확장성**에서 작업량에 비례한 가속 |
| 한계 | 통신·동기화 비용 미반영 | 고정 작업의 응답시간 개선 판단 제약 |

#### 한줄 요약
- 암달의 법칙은 고정 작업의 가속 상한을 규명하고, 구스타프슨의 법칙은 대규모 분산 컴퓨팅의 선형 확장을 입증한다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **USL(Universal Scalability Law)**: 암달의 법칙에 자원 경합(Contention)과 캐시 일관성(Coherency) 오버헤드를 반영하여 프로세서 증설 시 성능이 저하되는 음의 확장성 구간까지 모델링하는 법칙이다.
- **MVCC(Multi-Version Concurrency Control)**: 데이터 변경 시 이전 버전을 보존하여 읽기 작업과 쓰기 작업이 서로 락을 걸지 않고 병렬로 수행되도록 돕는 동시성 제어 기법이다.
- **CAS(Compare-And-Swap)**: 하드웨어 수준의 원자적 연산을 활용하여 뮤텍스 락 없이 공유 변수를 안전하게 갱신하는 락프리(Lock-Free) 동기화 메커니즘이다.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 순차 비율 추정으로 **가속비 오차** 확대 | 프로파일러로 **Lock·I/O 대기** 실측 | 코어 수 산정 오차 감소 |
| 코어 증가로 **캐시 일관성 경합** 확대 | **USL**로 통신·경합 비용 보정 | 실제 확장성 상한 예측 |
| Row Lock 대기로 **순차 구간** 증가 | **MVCC·CAS**로 공유 상태 경합 축소 | 병렬화 가능 비율 증가 |

#### 한줄 요약
- 프로파일러로 실측하여 오차를 줄이고, USL로 캐시 경합을 방어하며, Lock-free 기법으로 순차 구간을 축소한다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **넌블로킹 알고리즘(Non-blocking Algorithm)**: 특정 스레드가 중단되더라도 다른 스레드의 진행이 영구적으로 차단되지 않도록 보장하는 락프리/웨이트프리 동기화 알고리즘이다.
- **강한 확장성(Strong Scaling)**: 문제의 크기를 고정하고 프로세서 수를 늘릴 때 실행 시간이 단축되는 정도를 측정하는 확장성 모델이다.
- **약한 확장성(Weak Scaling)**: 프로세서 수 증가에 비례하여 문제의 크기도 함께 늘려 프로세서당 작업량을 일정하게 유지하는 확장성 모델이다.

</details>

- 고정된 작업 규모에서 병렬 하드웨어 증설의 이론적 한계와 비용 효율성을 수리적으로 규명하여 최적의 멀티코어 인프라 투자 결정을 가능케 하는 **병렬 컴퓨팅 가속 한계 및 확장성 모델링(Amdahl's Law vs Gustafson's Law / Strong Scaling / Serial Fraction)**의 핵심 아키텍처 법칙으로 확고히 자리 잡았으며, GPU 대규모 병렬 AI 연산 및 멀티코어 분산 시스템 설계의 가속비 판정 기준으로 널리 활용되는 가운데, 실무 시스템 사이징 및 병렬화 최적화 시에는 **프로파일러를 통한 순차 실행 구간($1-p$)의 엄밀한 실측, MVCC 및 Non-blocking 알고리즘 적용을 통한 직렬 임계구역 최소화, 고정 작업 응답 단축(암달의 법칙)과 대규모 데이터 확장(구스타프슨의 법칙)의 워크로드별 상호보완적 적용**을 결합하여 병렬 컴퓨팅의 투자 대비 효율(ROI)을 체계적으로 달성해야 한다.

#### 한줄 요약
- 순차 실행 비율 개선과 워크로드별 스케일링 법칙 적용을 통해 병렬 시스템의 가속 효율을 높이고 최적 자원을 배분해야 한다.
