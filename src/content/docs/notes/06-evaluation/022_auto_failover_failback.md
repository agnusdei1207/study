---
sidebar:
  order: 22
  label: "022. 자동 페일오버•페일백 (Auto Failover Failback)"
  badge:
    text: "기출 · 50%"
    variant: note
title: "무중단 장애 극복 및 안전한 원상 복구 : 자동 페일오버 및 페일백 (Auto Failover & Failback)"
date: "2026-09-07T14:00:00+09:00"
tags:
  - "notes-evaluation"
weight: 22
extra:
  question_no: "022"
  source_status: "기출"
  source_history: "137회"
  priority: 50
  priority_note: "137회 기출, 자동 장애 조치(Auto Failover: 감지, 펜싱, 승격, 라우팅 전환, RTO 단축) 및 안전한 원상 복귀(Failback: LSN 차분 역동기화, 대기 노드 편입, 점진적 통제 전환), 플래핑(Flapping) 방지 유예 타이머(Hysteresis Timer), 데이터 역전(Data Divergence) 방어"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **자동 페일오버(Auto Failover)**: 주 노드 장애를 감지해 대기 노드를 자동 승격하는 장애 복구 절차.
- **페일백(Failback)**: 복구 노드를 재동기화한 뒤 주 역할을 돌려주는 원상 복구 절차.
- **복구 시간 목표(Recovery Time Objective, RTO)**: 장애 후 서비스 복구까지 허용하는 최대 시간.

</details>

- 정의/개념: 장애 노드를 격리하고 대기 노드를 승격한 뒤 복구 노드를 재편입하는 **고가용성 전환 체계**
- 배경/필요성: 운영 인프라 장애 발생 시 사람의 수동 개입에 의존할 경우 장애 인지와 판단 지연으로 인해 복구 소요 시간(RTO)이 수십 분 이상 급증하거나, 일시적인 네트워크 지터(Jitter)로 인해 주-대기 노드 역할이 초 단위로 반복 전환되는 플래핑(Flapping) 현상 및 복구 노드의 성급한 원상 복귀로 인한 데이터 역전(Data Divergence) 결함이 발생하는 구조적 한계가 발생함에 따라, 헬스체크 쿼럼 감시, STONITH 노드 펜싱, 가상 IP(VIP) 플로팅 및 LSN(Log Sequence Number) 차분 역동기화를 결합하는 자동 페일오버 및 통제 페일백 체계를 도입하여 **장애 발생 시 5초 이내의 무인 자동 복구(Auto Failover), 히스테리시스 타이머 기반의 플래핑 억제 및 안정적인 데이터 정합성을 보증하는 안전한 원상 복귀(Controlled Failback)**를 달성할 필요

#### 한줄 요약
- 장애 시 대기 노드로 자동 전환하고, 복구 후 데이터 정합성을 확인해 원래 구성으로 돌아간다.

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- **히스테리시스(Hysteresis)**: 연속 실패와 복구 안정 시간을 요구해 잦은 역할 전환을 억제하는 제어 방식.
- **플래핑(Flapping)**: 일시적 상태 변화로 노드 역할이 반복 전환되는 현상.
- **로그 시퀀스 번호(Log Sequence Number, LSN)**: 로그 순서를 식별해 노드 간 적용 위치를 비교하는 번호.

</details>

- 장애 전환은 자동화하고 원상 복귀는 승인하는 **비대칭 운영 정책**
- 연속 실패·복구 안정 시간으로 역할 **플래핑** 억제
- 복구 노드의 대기 모드 편입과 **LSN** 동기화로 데이터 역전 방지

#### 한줄 요약
- 장애 전환은 빠르게 자동화하되 원상 복귀는 최신 데이터 확인 후 통제한다.

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- **쿼럼(Quorum)**: 과반 동의로 유효한 클러스터 파티션을 정하는 합의 기준.
- **STONITH(Shoot The Other Node In The Head)**: 장애 노드의 전원이나 저장소 접근을 차단하는 펜싱 방식.
- **가상 IP(Virtual IP, VIP)**: 활성 노드가 인계받아 서비스 접점을 유지하는 논리 주소.

</details>

```text
[고가용성 클러스터]
├── [클러스터 제어기]
│   ├── [헬스체크·쿼럼] 상태 감시 및 판정
│   └── [펜싱 장치] STONITH 전원 격리
├── [서비스 접점]
│   └── [VIP] 활성 노드 트래픽 인계
└── [데이터 계층]
    ├── [주 노드] 트랜잭션 처리
    └── [대기 노드] 실시간 데이터 복제
```

- 선의 의미: 계층 구조 및 상하위 포함 관계를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| 클러스터 제어기 | **헬스체크·쿼럼** 기반 역할 전환 결정 |
| 펜싱 장치 | 구 주 노드의 전원·저장소 접근 **차단** |
| VIP | 활성 노드로 서비스 **접점 인계** |
| 주·대기 노드 | 트랜잭션 처리와 **복제 상태** 보유 |

#### 한줄 요약
- 제어기가 두 노드의 역할을 정하고, 펜싱·VIP·복제 계층이 안전한 전환을 지원한다.

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **연결 드레이닝(Connection Draining)**: 기존 요청을 완료한 뒤 신규 연결만 다른 노드로 보내는 전환 방식.

</details>

```text
장애 노드        클러스터 제어기        대기·복구 노드
    |                    |                    |
    |------------------->| 장애 신호          |
    |                    | 1. 장애 판정       |
    |<-------------------| 2. 노드 펜싱       |
    |                    |------------------->|
    |                    | 3. 대기 노드 승격  |
    |                    |<-------------------|
    |                    | 4. 복구 노드 동기화|
    |                    |------------------->|
    |                    | 5. 역할 원상 복귀  |
    |<----------------------------------------| 서비스 재개
```

**동작 원리**

1. **장애 판정**: 연속 헬스체크 실패와 쿼럼으로 장애 확정
2. **노드 펜싱**: 구 주 노드의 쓰기 권한 차단
3. **대기 노드 승격**: 대기 노드 승격과 VIP 인계
4. **복구 노드 동기화**: LSN 차이를 재생해 최신 상태 일치
5. **역할 원상 복귀**: 연결 드레이닝 후 승인 기반 역할 전환

#### 한줄 요약
- 장애를 확정해 대기 노드로 전환하고, 복구 노드를 최신화한 뒤 승인받아 역할을 되돌린다.

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- **복구 시점 목표(Recovery Point Objective, RPO)**: 장애 시 허용하는 최대 데이터 손실 시점.

</details>

| 전환 절차 | 자동 페일오버 | 통제 페일백 |
|:---|:---|:---|
| 적용 기준 | 주 노드 장애 확정 시 | 복구 노드 동기화 완료 시 |
| 핵심 특징 | **RTO** 단축을 위한 자동 승격 | **RPO** 보존을 위한 승인 전환 |
| 한계 | 오탐 시 **플래핑** 위험 | 역동기화 누락 시 **데이터 역전** 위험 |

#### 한줄 요약
- 장애 전환은 복구 속도가, 원상 복귀는 데이터 정합성이 우선이다.

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **연결 유효성 검사(Connection Validation)**: 데이터베이스 커넥션 풀(HikariCP 등)에서 커넥션을 대여하기 직전에 `SELECT 1` 등의 테스트 쿼리를 실행하여 실제 활성 마스터 노드에 정상 연결되어 있는지 검증하는 기능이다.
- **히스테리시스 타이머(Hysteresis Timer)**: 일시적인 네트워크 지연으로 인한 페일오버 오탐을 방지하기 위해, 상태 변경 감지 후 즉시 동작하지 않고 사전에 정의된 안정화 시간 동안 관측을 지속하는 지연 제어 장치이다.
- **데이터 역전(Data Divergence)**: 구 마스터 노드가 장애 직전에 로컬에만 기록하고 복제되지 않은 잔여 트랜잭션을 가진 상태에서, 신규 마스터가 다른 트랜잭션을 처리한 후 구 마스터가 복구되어 충돌을 일으키는 데이터 불일치 현상이다.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 일시 지연을 장애로 오판해 **플래핑** 발생 | 연속 실패·복구 안정 시간 기반 **히스테리시스** 적용 | 불필요한 역할 전환 억제 |
| 구 주 노드 재기동으로 **데이터 역전** 발생 | 대기 모드 편입 후 **LSN** 일치 검증 | 최신 트랜잭션 보존 |
| 연결 풀이 구 주 노드를 계속 참조 | **연결 유효성 검사**와 재연결 설정 | 활성 노드로 세션 전환 |

#### 한줄 요약
- 전환 오탐, 데이터 역전, 오래된 연결을 각각 유예 시간, 동기화, 재연결로 통제한다.

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **파트로니(Patroni)**: etcd나 Consul과 같은 분산 합의 저장소를 기반으로 PostgreSQL의 고가용성 클러스터와 자동 페일오버/페일백을 템플릿화하여 제공하는 오픈소스 오케스트레이션 도구이다.
- **LSN(Log Sequence Number)**: 데이터베이스의 트랜잭션 로그 레코드마다 고유하게 부여되는 단조 증가 64비트 정수 식별자로, 복구 노드와 마스터 간의 데이터 동기화 완료 시점을 정밀하게 대조하는 기준이다.
- **통제된 페일백(Controlled Failback)**: 자동 승격과 달리, 복구된 노드의 하드웨어 상태와 데이터 정합성을 관리자가 최종 승인한 후 서비스 트래픽 영향이 적은 시간대에 역할을 교체하는 운영 방식이다.

</details>

- 장애 발생 시에는 사람의 개입 없는 자동 절체로 서비스 중단 시간을 최소화하고 정상화 후에는 철저한 데이터 동기화 검증을 거쳐 안전하게 원상 복구하는 **무중단 장애 극복 및 안전한 원상 복구(Auto Failover & Controlled Failback / Hysteresis & LSN Sync)의 핵심 고가용성 운영 표준**으로 확고히 자리 잡았으며, 오케스트레이터(Kubernetes/Patroni) 및 서비스 메시 자동 라우팅으로 진화하는 가운데, 실무 절체/복귀 시스템 구축 시에는 **오탐에 의한 플래핑을 방어하기 위한 연속 실패 판정 및 히스테리시스 유예 타이머 설정, 구 주 노드 재기동 시 데이터 역전을 방지하기 위한 대기 모드 우선 편입 및 LSN 일치 검증, 커넥션 풀의 자동 연결 유효성 검사(Connection Validation) 적용**을 결합하여 무중단 복원력과 데이터 정합성을 체계적으로 완성해야 한다.

#### 한줄 요약
- 자동 페일오버와 통제된 페일백을 통해 장애 시 RTO를 대폭 단축하고 복구 시 데이터 정합성을 체계적으로 보증해야 한다.
