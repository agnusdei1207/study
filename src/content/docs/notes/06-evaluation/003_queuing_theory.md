---
sidebar:
  order: 3
  label: "003. 큐잉 이론 - M/M/1·M/M/c (Queuing Theory)"
  badge:
    text: "기출 · 50%"
    variant: note
title: "대기행렬 기반 시스템 용량 산정 및 성능 모델링 : 큐잉 이론 (M/M/1 및 M/M/c 모델)"
date: "2026-09-15T09:17:00+09:00"
tags:
  - "notes-evaluation"
weight: 3
extra:
  question_no: "003"
  source_status: "기출"
  source_history: "131회"
  priority: 50
  priority_note: "131회 기출, 큐잉 이론(Queuing Theory / 대기행렬 모델링), 켄달 표기법(Kendall's Notation A/S/c/K/N/D), 단일 서버 M/M/1 모델 수식 유도(이용률 ρ=λ/μ, 평균 체류시간 W=1/(μ-λ), 대기열 길이 L_q=ρ²/(1-ρ)), 다중 서버 M/M/c 모델 및 얼랑 C(Erlang-C) 공식, 안정 조건(Stability Condition ρ < 1) 및 비선형 대기 폭증 방어"
---

## Ⅰ. 개요

<details><summary>용어 설명</summary>

- **큐잉 이론(Queuing Theory / 대기행렬 이론)**: 무작위(Random)로 유입되는 서비스 요청의 도착률(Arrival Rate, $\lambda$)과 서버의 처리 서비스율(Service Rate, $\mu$)을 확률 과정(Poisson/Exponential Distribution)으로 수학적 모델링하여, 시스템 내 대기열 길이($L_q$)와 평균 체류 시간($W$)을 정량적으로 산출하고 최적의 서버 용량을 산정하는 수리 성능 분석 기법.
- **자원 이용률 100% 접근에 따른 비선형 대기열 폭증 결함(Non-linear Hockey-Stick Latency Defect)**: 서버의 CPU 이용률($\rho$)이 80%~90%를 초과할 때, 대기시간($W_q$)이 선형적으로 증가하는 것이 아니라 분모의 $(1-\rho)$ 수식에 의해 지수적/비선형적으로 폭증(하키스틱 곡선)하여 시스템이 순식간에 응답 불가(Hang) 상태에 빠지는 수학적 결함.

</details>

- 정의/개념: 도착률과 서비스율의 확률 분포로 대기열($L_q$)·체류시간($W$)을 산출해 목표 SLA를 만족하는 최소 서버 수($c$)를 정하는 **큐잉 이론** 기반 수리적 용량 사이징 체계
- 배경/필요성: 트래픽 유입 시 서버 이용률($\rho$)이 80%~90%를 초과할 때 대기시간이 $(1-\rho)$ 분모 수식에 의해 지수적으로 폭증하는 하키스틱 지연을 고려하지 않고 경험적 증설을 수행하면 버스트 부하 시 연쇄 타임아웃 붕괴를 초래하므로, 켄달 표기법과 M/M/c 모델 기반으로 안정 조건($\rho < 1$)과 체류시간을 정량 산출해 비선형 대기 폭증을 방어할 필요

#### 한줄 요약
- 증설 한 대의 비용은 선형이지만 대기시간은 $(1-\rho)$ 분모로 비선형이므로, 이용률 80% 부근의 마지막 몇 %를 비우는 데 쓰는 서버가 가장 효과적인 지연 보험

## Ⅱ. 특징

<details><summary>용어 설명</summary>

- 큐잉 이론 3대 핵심 모델링 변수:
  - **도착률 ($\lambda$, Arrival Rate)**: 단위 시간당 시스템으로 유입되는 평균 요청 건수 (Poisson 과정).
  - **서비스율 ($\mu$, Service Rate)**: 단일 서버가 단위 시간당 완전히 처리할 수 있는 평균 요청 건수 (Exponential 분포).
  - **서버 이용률 ($\rho$, Traffic Intensity / Utilization)**: 단일 서버 모델에서 $\rho = \frac{\lambda}{\mu}$, $c$개 서버 모델에서 $\rho = \frac{\lambda}{c\mu}$.
- **얼랑 C 공식(Erlang-C Formula)**: M/M/c 모델에서 도착한 요청이 모든 서버가 사용 중이어서 대기해야 할 확률을 $\lambda$·$\mu$·$c$로 구하는 식으로, 이 대기 확률에 평균 대기시간을 곱해 공용 큐의 기대 지연을 산출한다.

</details>

- **도착률**이 **서비스율**과 서버 수의 곱에 가까워질수록 발산하는 비선형 대기시간
- **서버 이용률** $\rho<1$ 인 안정 조건에서만 정상 상태 해가 존재
- 공용 큐의 대기 확률을 구하는 **Erlang-C**

#### 한줄 요약
- 세 변수 중 설계자가 통제할 수 있는 것은 $c$뿐이므로, 도착률 예측 오차는 서버 수의 여유로 흡수하는 것이 기본 원칙

## Ⅲ. 구조 및 구성요소

<details><summary>용어 설명</summary>

- 켄달 표기법 (Kendall's Notation: $A / S / c / K / N / D$):
  - $A$: 도착 시간 간격 분포 ($M$: Markovian/Poisson, $D$: Deterministic, $G$: General).
  - $S$: 서비스 시간 분포 ($M$: Exponential, $D$: Deterministic, $G$: General).
  - $c$: 병렬 서비스 처리 서버 대수 ($1, 2, \dots, \infty$).
  - $K$: 시스템 최대 수용 용량 (생략 시 $\infty$).
  - $N$: 소스 모집단 크기 (생략 시 $\infty$).
  - $D$: 대기열 처리 규칙 (FIFO, LIFO, Priority, 생략 시 FIFO).

</details>

```text
[켄달 표기법 (A/S/c/K/N/D)]
  │
  ├─ [확률 분포 (A / S)]
  │   ├─ 도착 시간 간격 분포 (A)
  │   └─ 서비스 시간 처리 분포 (S)
  │
  ├─ [시스템 용량 (c / K)]
  │   ├─ 병렬 서비스 서버 대수 (c)
  │   └─ 시스템 최대 수용 한계 (K)
  │
  └─ [제약 및 정책 (N / D)]
      ├─ 요청 소스 모집단 크기 (N)
      └─ 대기열 스케줄링 규칙 (D)
```

- 선의 의미: 상하 인터페이스 연결(`│`) 및 켄달 표기 파라미터 소속 관계(`├─`, `└─`)를 나타낸다.

| 구성요소 | 책임 |
|:---|:---|
| A | 도착 시간 간격 분포 정의 |
| S | 서비스 시간 분포 정의 |
| c | 병렬 서버 수 정의 |
| K | 시스템 최대 수용량 정의 |
| N | 요청 소스 모집단 크기 정의 |
| D | FIFO·LIFO·Priority 대기 규칙 정의 |

#### 한줄 요약
- 켄달 표기법은 여섯 자리로 대기 시스템을 기술하되 $K$·$N$을 생략하면 무한 수용을 가정하므로, 누락 시 산정치가 낙관적으로 왜곡

## Ⅳ. 흐름도

<details><summary>용어 설명</summary>

- **안정 조건(Stability Condition)**: 시스템 이용률 $\rho = \frac{\lambda}{c\mu} < 1$ 조건을 만족해야 대기열이 무한 발산하지 않고 정상 상태 확률 분포가 수렴하는 수학적 한계.
- **안전 여유율(Headroom)**: 포아송 분포 가정의 오차와 일시적 버스트 트래픽, N+1 장애 복구를 감당하기 위해 이론상 최소 서버 수 위에 가산하는 예비 용량.

</details>

```text
[대기행렬 용량 산정 경로] (진행 ①→④, 파라미터 계측 및 필요 용량 도출)
  │
  ├─ [워크로드 프로파일러] (① 도착률 λ 및 서비스율 μ 실측)
  │
  ├─ [수리 모델링 엔진] (② 켄달 표기법 기반 M/M/c 모델링)
  │
  ├─ [안정성·지연 검증기] (③ 가동률 ρ<1 안정 조건 및 목표 W 달성 여부 판정)
  │
  └─ [용량 확정 모듈] (④ 버스트·N+1 여유율 20% 반영 및 최소 인스턴스 c 확정)
```

분기 결과: 가동률 ρ<1 안정 조건 충족 시 최소 서버 대수 산출하나, 목표 체류시간 W 초과 시 비선형 지연 방어를 위한 인스턴스 추가 증설 비용 발생

#### 한줄 요약
- 안정 조건만 만족하는 7대는 가동률 95%에서 체류시간이 폭증하고 한 대를 더한 8대는 12.5ms로 안정되므로, 갈래는 포화 직전 구간의 급증에서 결정

## Ⅴ. 종류 및 비교

<details><summary>용어 설명</summary>

- 큐잉 아키텍처 3대 토폴로지 비교:
  - 단일 큐-단일 서버 (M/M/1): 직렬 처리 구조.
  - 단일 큐-다중 서버 (M/M/c): 글로벌 로드밸런싱 풀 구조.
  - 다중 큐-다중 서버 ($c \times M/M/1$): 파티셔닝/샤딩 분할 구조.

</details>

| 비교 항목 | M/M/1 (단일 큐 - 단일 서버) | M/M/c (단일 큐 - 다중 병렬 서버) | $c \times$ M/M/1 (개별 큐 - 개별 서버) |
|:---|:---|:---|:---|
| 큐 구조 | 1개 큐 $\rightarrow$ 1대 서버 | 1개 공용 큐 $\rightarrow$ $c$대 병렬 서버 | $c$개 개별 큐 $\rightarrow$ $c$대 개별 서버 |
| 적용 사례 | Node.js 단일 스레드, DB Mutex | WAS 로드밸런서, 쿠버네티스 서비스| Kafka 파티션별 컨슈머, 샤딩 DB |
| 대기 시간 ($W_q$)| 가장 김 ($W_q = \frac{\rho}{\mu(1-\rho)}$)| 가장 짧음 (얼랑 C 분산 효과) | 중간 (큐 간 부하 불균형 시 지연)|
| 서버 유휴 낭비 | 없음 (단일 자원) | 최소화 (놀고 있는 서버가 즉시 처리)| 큼 (특정 큐는 꽉 차고 타 큐는 놂)|
| 장애 영향도 (SPOF)| 치명적 (서버 다운 시 서비스 마비) | 격리 (1대 장애 시 $c-1$대로 가동) | 부분적 (해당 파티션만 서비스 중단)|

#### 한줄 요약
- 공용 큐는 대기시간과 유휴를 최소화하지만 단일 장애점이 될 수 있고, 개별 큐는 격리를 얻는 대신 불균형 지연이 발생하므로 요구 조건에 맞게 선택

## Ⅵ. 실무 고려사항 및 대책

<details><summary>용어 설명</summary>

- **폴라첵-킨친 공식(Pollaczek-Khinchine Formula)**: 도착은 포아송이지만 서비스 시간이 일반 분포인 M/G/1 큐의 평균 대기시간을 $W_q = \frac{\lambda(\sigma^2 + 1/\mu^2)}{2(1-\rho)}$로 구하는 식으로, 서비스 시간 분산 $\sigma^2$이 커질수록 같은 이용률에서도 대기가 길어짐을 정량화한다.
- **HPA(Horizontal Pod Autoscaler)**: 쿠버네티스에서 CPU 이용률 등 메트릭이 목표치를 넘으면 파드 복제 수를 자동으로 늘리고 내려가면 줄이는 컨트롤러로, 큐잉 모델의 서버 수 $c$를 부하에 따라 동적으로 조절한다.
- **최소 연결(Least Connections)**: 로드밸런서가 현재 활성 연결 수가 가장 적은 서버로 새 요청을 보내는 분배 알고리즘으로, 라운드로빈과 달리 처리가 느린 노드에 요청이 적체되는 것을 자동으로 완화한다.

</details>

| 문제 | 대책 | 효과 |
|:---|:---|:---|
| 롱테일(Pareto) 서비스 시간에서 M/M/1 예측치 대비 실제 대기시간 폭증 | **폴라첵-킨친 공식**으로 서비스 시간 분산($\sigma^2$) 반영 | 비정규 트래픽에서 대기시간 예측 오차 축소 |
| 가동률 90% 이상 운영 시 버스트에 하키스틱 지연·연쇄 타임아웃 | 목표 이용률 70~75% 상한과 **HPA** 임계치 연동 | 비선형 대기 폭증 구간 진입 차단 |
| 느린 노드로 라우팅된 요청 적체로 클러스터 불균형 | 라운드로빈 대신 **최소 연결** 동적 분배 | 노드 간 처리 편차 제거 |

#### 한줄 요약
- 세 대책은 모델 가정(지수 분포·평균 이용률·균등 분배)이 깨지는 지점을 실측으로 보정하는 기법

## Ⅶ. 결론

<details><summary>용어 설명</summary>

- **얼랑 C(Erlang-C)**: 모든 서버가 점유 중일 때 도착한 고객이 대기열에서 기다리는 확률을 구하는 수식으로, 콜센터 상담원 및 WAS 스레드 풀 사이징의 표준 모델이다.
- **카프카(Kafka)**: 고성능 분산 이벤트 스트리밍 플랫폼으로, 파티션 단위의 개별 큐 구조를 통해 대규모 데이터 버퍼링과 비동기 처리를 지원한다.
- **래빗MQ(RabbitMQ)**: AMQP 표준 기반의 메시지 브로커로, 단일 큐 및 라우팅 키 기반의 유연한 메시지 분배를 통해 서비스 간 비동기 결합도를 낮춘다.

</details>

- 불확실한 트래픽 환경에서 대기열 지연과 서버 처리 역학을 확률 모델로 규명하는 큐잉 이론은 오토스케일링과 메시지 버퍼 설계를 뒷받침하는 **수리 성능 모델링**의 핵심 표준으로 정착
- 실무 엔지니어는 롱테일 지연 방어를 위해 **폴라첵-킨친 공식**을 연계 적용하고, 비선형 대기 폭증을 회피하기 위한 가동률 70% 상한 제한과 공용 큐 기반 M/M/c 로드밸런싱 풀 구성을 확립

#### 한줄 요약
- 서버 수 산정의 기준은 안정 조건 통과가 아니라 목표 응답시간과 여유율이며, 가동률 70% 상한 설정은 비선형 폭증을 방어하는 핵심 원칙
